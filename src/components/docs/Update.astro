---
interface Props { label: string; tags?: string[] }
const { label, tags = [] } = Astro.props;
---
<article class="whale-update">
  <header><time>{label}</time>{tags.map((tag) => <span>{tag}</span>)}</header>
  <div><slot /></div>
</article>
