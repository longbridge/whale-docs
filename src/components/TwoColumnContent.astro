<div class="whale-doc-columns">
  <aside class="right-sidebar-container print:hidden">
    <div class="right-sidebar"><slot name="right-sidebar" /></div>
  </aside>
  <div class="main-pane"><slot /></div>
</div>

<style>
  .whale-doc-columns { min-width: 0; }
  .main-pane { min-width: 0; isolation: isolate; }
  @media (min-width: 72rem) {
    .whale-doc-columns { display: flex; }
    .right-sidebar-container {
      order: 2;
      position: relative;
      width: var(--whale-toc-width);
      flex: none;
    }
    .right-sidebar {
      position: fixed;
      inset-block: var(--sl-nav-height) 0;
      width: var(--whale-toc-width);
      overflow-y: auto;
      scrollbar-width: none;
      background: var(--nb-card);
    }
    .main-pane { order: 1; width: calc(100% - var(--whale-toc-width)); }
  }
</style>
