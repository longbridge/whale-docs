---
import WhaleAppCarousel from "../../../components/WhaleAppCarousel";

interface Props {
	marketLabel?: string;
	watchlistLabel?: string;
	assetsLabel?: string;
	newsLabel?: string;
	ariaLabel?: string;
	previousLabel?: string;
	nextLabel?: string;
}
---

<WhaleAppCarousel client:load {...Astro.props} />
