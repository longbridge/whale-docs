---
import { DocumentAccordion } from "@components/DocumentAccordion";
interface Props { title: string; open?: boolean }
const { title, open } = Astro.props;
---
<DocumentAccordion client:load title={title} open={open}><slot /></DocumentAccordion>
