---
title: "虚拟资产账户头寸"
description: "dataset: va_account_balances"
openapi: "post /v1/datasets/va_account_balances"
---

获取 **虚拟资产账户头寸** 数据集。接口：`POST /v1/datasets/va_account_balances`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
