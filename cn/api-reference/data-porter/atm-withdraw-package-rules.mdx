---
title: "自动化出金打包配置表"
description: "dataset: atm_withdraw_package_rules"
openapi: "post /v1/datasets/atm_withdraw_package_rules"
---

获取 **自动化出金打包配置表** 数据集。接口：`POST /v1/datasets/atm_withdraw_package_rules`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
