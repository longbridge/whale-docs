---
title: "渠道账户调拨记录"
description: "dataset: account_fund_transfers"
openapi: "post /v1/datasets/account_fund_transfers"
---

获取 **渠道账户调拨记录** 数据集。接口：`POST /v1/datasets/account_fund_transfers`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
