---
title: "兑换池变动表"
description: "dataset: atm_exchange_pool_limit_changes"
openapi: "post /v1/datasets/atm_exchange_pool_limit_changes"
---

获取 **兑换池变动表** 数据集。接口：`POST /v1/datasets/atm_exchange_pool_limit_changes`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
