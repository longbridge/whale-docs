---
title: "申购订单同步列表"
description: "dataset: ipo_sync_records"
openapi: "post /v1/datasets/ipo_sync_records"
---

获取 **申购订单同步列表** 数据集。接口：`POST /v1/datasets/ipo_sync_records`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
