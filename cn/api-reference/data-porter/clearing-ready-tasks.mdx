---
title: "清算 - 清算前准备任务"
description: "dataset: clearing_ready_tasks"
openapi: "post /v1/datasets/clearing_ready_tasks"
---

获取 **清算 - 清算前准备任务** 数据集。接口：`POST /v1/datasets/clearing_ready_tasks`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
