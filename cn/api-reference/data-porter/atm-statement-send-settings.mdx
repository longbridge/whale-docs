---
title: "结单发送管理"
description: "dataset: atm_statement_send_settings"
openapi: "post /v1/datasets/atm_statement_send_settings"
---

获取 **结单发送管理** 数据集。接口：`POST /v1/datasets/atm_statement_send_settings`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
