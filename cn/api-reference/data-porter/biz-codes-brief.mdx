---
title: "业务码查询 (非 LB 使用)"
description: "dataset: biz_codes_brief"
openapi: "post /v1/datasets/biz_codes_brief"
---

获取 **业务码查询 (非 LB 使用)** 数据集。接口：`POST /v1/datasets/biz_codes_brief`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
