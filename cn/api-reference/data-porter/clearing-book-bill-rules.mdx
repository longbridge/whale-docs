---
title: "自动打标规则"
description: "dataset: clearing_book_bill_rules"
openapi: "post /v1/datasets/clearing_book_bill_rules"
---

获取 **自动打标规则** 数据集。接口：`POST /v1/datasets/clearing_book_bill_rules`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
