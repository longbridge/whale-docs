---
title: "结单"
description: "dataset: atm_statement_histories"
openapi: "post /v1/datasets/atm_statement_histories"
---

获取 **结单** 数据集。接口：`POST /v1/datasets/atm_statement_histories`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
