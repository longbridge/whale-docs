---
title: "清算交收 - 持仓流水同步"
description: "dataset: clearing_equity_holding_change_realtimes"
openapi: "post /v1/datasets/clearing_equity_holding_change_realtimes"
---

获取 **清算交收 - 持仓流水同步** 数据集。接口：`POST /v1/datasets/clearing_equity_holding_change_realtimes`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
