---
title: "转入转出 SI 指令列表"
description: "dataset: atm_settlement_instructions"
openapi: "post /v1/datasets/atm_settlement_instructions"
---

获取 **转入转出 SI 指令列表** 数据集。接口：`POST /v1/datasets/atm_settlement_instructions`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
