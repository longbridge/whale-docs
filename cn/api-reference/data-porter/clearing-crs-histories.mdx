---
title: "历年收益数据"
description: "dataset: clearing_crs_histories"
openapi: "post /v1/datasets/clearing_crs_histories"
---

获取 **历年收益数据** 数据集。接口：`POST /v1/datasets/clearing_crs_histories`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
