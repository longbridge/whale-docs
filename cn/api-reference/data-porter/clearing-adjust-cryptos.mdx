---
title: "手工调账 - 虚拟资产"
description: "dataset: clearing_adjust_cryptos"
openapi: "post /v1/datasets/clearing_adjust_cryptos"
---

获取 **手工调账 - 虚拟资产** 数据集。接口：`POST /v1/datasets/clearing_adjust_cryptos`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
