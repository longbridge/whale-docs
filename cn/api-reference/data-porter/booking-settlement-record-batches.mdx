---
title: "交收批次"
description: "dataset: booking_settlement_record_batches"
openapi: "post /v1/datasets/booking_settlement_record_batches"
---

获取 **交收批次** 数据集。接口：`POST /v1/datasets/booking_settlement_record_batches`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
