---
title: "租户支持的仓位方向查询 (临时写死配置)"
description: "dataset: account_position_side_options"
openapi: "post /v1/datasets/account_position_side_options"
---

获取 **租户支持的仓位方向查询 (临时写死配置)** 数据集。接口：`POST /v1/datasets/account_position_side_options`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
