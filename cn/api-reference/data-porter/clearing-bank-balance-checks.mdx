---
title: "银行余额对账"
description: "dataset: clearing_bank_balance_checks"
openapi: "post /v1/datasets/clearing_bank_balance_checks"
---

获取 **银行余额对账** 数据集。接口：`POST /v1/datasets/clearing_bank_balance_checks`。Playground 顶部 Base URL 下拉可切换测试（默认）与生产环境。

### 请求示例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
