---
title: "租户支持的仓位方向查询 (临时写死配置)"
description: "dataset: account_position_side_options"
openapi: "post /v1/datasets/account_position_side_options"
---

Query the **租户支持的仓位方向查询 (临时写死配置)** dataset. Endpoint: `POST /v1/datasets/account_position_side_options`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
