---
title: "转仓邮件发送记录"
description: "dataset: atm_email_records"
openapi: "post /v1/datasets/atm_email_records"
---

Query the **转仓邮件发送记录** dataset. Endpoint: `POST /v1/datasets/atm_email_records`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
