---
title: "区域列表配置"
description: "dataset: atm_bank_regions"
openapi: "post /v1/datasets/atm_bank_regions"
---

Query the **区域列表配置** dataset. Endpoint: `POST /v1/datasets/atm_bank_regions`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
