---
title: "结单发送管理"
description: "dataset: atm_statement_send_settings"
openapi: "post /v1/datasets/atm_statement_send_settings"
---

Query the **结单发送管理** dataset. Endpoint: `POST /v1/datasets/atm_statement_send_settings`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
