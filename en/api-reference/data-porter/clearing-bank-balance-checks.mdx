---
title: "银行余额对账"
description: "dataset: clearing_bank_balance_checks"
openapi: "post /v1/datasets/clearing_bank_balance_checks"
---

Query the **银行余额对账** dataset. Endpoint: `POST /v1/datasets/clearing_bank_balance_checks`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
