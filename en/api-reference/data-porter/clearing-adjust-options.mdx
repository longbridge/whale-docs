---
title: "手工调账 - 期权"
description: "dataset: clearing_adjust_options"
openapi: "post /v1/datasets/clearing_adjust_options"
---

Query the **手工调账 - 期权** dataset. Endpoint: `POST /v1/datasets/clearing_adjust_options`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
