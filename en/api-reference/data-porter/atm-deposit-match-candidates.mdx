---
title: "加款匹配 - 匹配用户入账"
description: "dataset: atm_deposit_match_candidates"
openapi: "post /v1/datasets/atm_deposit_match_candidates"
---

Query the **加款匹配 - 匹配用户入账** dataset. Endpoint: `POST /v1/datasets/atm_deposit_match_candidates`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
