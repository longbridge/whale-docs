---
title: "风险等级配置池 (Sass)"
description: "dataset: fund_risk_levels"
openapi: "post /v1/datasets/fund_risk_levels"
---

Query the **风险等级配置池 (Sass)** dataset. Endpoint: `POST /v1/datasets/fund_risk_levels`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
