---
title: "汇兑池配置"
description: "dataset: atm_exchange_pool_limits"
openapi: "post /v1/datasets/atm_exchange_pool_limits"
---

Query the **汇兑池配置** dataset. Endpoint: `POST /v1/datasets/atm_exchange_pool_limits`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
