---
title: "待收费合约下的成交记录"
description: "dataset: booking_calculated_contract_order_histories"
openapi: "post /v1/datasets/booking_calculated_contract_order_histories"
---

Query the **待收费合约下的成交记录** dataset. Endpoint: `POST /v1/datasets/booking_calculated_contract_order_histories`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
