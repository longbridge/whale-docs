---
title: "日限额变动表"
description: "dataset: atm_bank_daily_limit_changes"
openapi: "post /v1/datasets/atm_bank_daily_limit_changes"
---

Query the **日限额变动表** dataset. Endpoint: `POST /v1/datasets/atm_bank_daily_limit_changes`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
