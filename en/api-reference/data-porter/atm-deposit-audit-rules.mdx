---
title: "风控入金规则"
description: "dataset: atm_deposit_audit_rules"
openapi: "post /v1/datasets/atm_deposit_audit_rules"
---

Query the **风控入金规则** dataset. Endpoint: `POST /v1/datasets/atm_deposit_audit_rules`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
