---
title: "转入转出 SI 指令列表"
description: "dataset: atm_settlement_instructions"
openapi: "post /v1/datasets/atm_settlement_instructions"
---

Query the **转入转出 SI 指令列表** dataset. Endpoint: `POST /v1/datasets/atm_settlement_instructions`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
