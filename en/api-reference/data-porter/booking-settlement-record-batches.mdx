---
title: "交收批次"
description: "dataset: booking_settlement_record_batches"
openapi: "post /v1/datasets/booking_settlement_record_batches"
---

Query the **交收批次** dataset. Endpoint: `POST /v1/datasets/booking_settlement_record_batches`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
