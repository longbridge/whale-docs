---
title: "自动化出金打包配置表"
description: "dataset: atm_withdraw_package_rules"
openapi: "post /v1/datasets/atm_withdraw_package_rules"
---

Query the **自动化出金打包配置表** dataset. Endpoint: `POST /v1/datasets/atm_withdraw_package_rules`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
