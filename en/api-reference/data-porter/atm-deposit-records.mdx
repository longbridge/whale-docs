---
title: "用户加款明细"
description: "dataset: atm_deposit_records"
openapi: "post /v1/datasets/atm_deposit_records"
---

Query the **用户加款明细** dataset. Endpoint: `POST /v1/datasets/atm_deposit_records`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
