---
title: "清算 - 清算前准备任务"
description: "dataset: clearing_ready_tasks"
openapi: "post /v1/datasets/clearing_ready_tasks"
---

Query the **清算 - 清算前准备任务** dataset. Endpoint: `POST /v1/datasets/clearing_ready_tasks`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
