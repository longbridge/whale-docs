---
title: "手工调账 - 基金"
description: "dataset: clearing_adjust_funds"
openapi: "post /v1/datasets/clearing_adjust_funds"
---

Query the **手工调账 - 基金** dataset. Endpoint: `POST /v1/datasets/clearing_adjust_funds`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
