---
title: "申购订单同步列表"
description: "dataset: ipo_sync_records"
openapi: "post /v1/datasets/ipo_sync_records"
---

Query the **申购订单同步列表** dataset. Endpoint: `POST /v1/datasets/ipo_sync_records`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
