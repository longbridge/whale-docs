---
title: "备注预设置"
description: "dataset: atm_remark_templates"
openapi: "post /v1/datasets/atm_remark_templates"
---

Query the **备注预设置** dataset. Endpoint: `POST /v1/datasets/atm_remark_templates`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
