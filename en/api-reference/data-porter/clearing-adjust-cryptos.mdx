---
title: "手工调账 - 虚拟资产"
description: "dataset: clearing_adjust_cryptos"
openapi: "post /v1/datasets/clearing_adjust_cryptos"
---

Query the **手工调账 - 虚拟资产** dataset. Endpoint: `POST /v1/datasets/clearing_adjust_cryptos`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
