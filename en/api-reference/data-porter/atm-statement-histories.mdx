---
title: "结单"
description: "dataset: atm_statement_histories"
openapi: "post /v1/datasets/atm_statement_histories"
---

Query the **结单** dataset. Endpoint: `POST /v1/datasets/atm_statement_histories`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
