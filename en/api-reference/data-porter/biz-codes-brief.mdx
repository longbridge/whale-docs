---
title: "业务码查询 (非 LB 使用)"
description: "dataset: biz_codes_brief"
openapi: "post /v1/datasets/biz_codes_brief"
---

Query the **业务码查询 (非 LB 使用)** dataset. Endpoint: `POST /v1/datasets/biz_codes_brief`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
