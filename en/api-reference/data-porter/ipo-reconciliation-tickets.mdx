---
title: "核对审批"
description: "dataset: ipo_reconciliation_tickets"
openapi: "post /v1/datasets/ipo_reconciliation_tickets"
---

Query the **核对审批** dataset. Endpoint: `POST /v1/datasets/ipo_reconciliation_tickets`. The base-URL dropdown in the playground lets you switch between Test (default) and Production.

### Request Example

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
