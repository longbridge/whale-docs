---
title: BrokerAPI
description: 面向 Broker 服务端集成与自研管理后台的机构级 API
---

BrokerAPI 是面向 Broker 的机构级 API，可用于 Server-to-Server 系统集成，也可为 Broker 自研管理后台提供数据与 SaaS 柜台能力。它以 **Broker** 为授权主体，一份凭证覆盖机构获批范围内的能力。

无论用于系统集成还是自研管理后台，实际 API 请求都应由 Broker 控制的服务端发起。Access Key 等机构凭证不得存放在浏览器代码或 Broker App 中；自研管理后台界面应通过 Broker Server 调用 BrokerAPI。

## 一览 {/* at-a-glance */}

|              |                                                        |
| ------------ | ------------------------------------------------------ |
| 调用方       | Broker Server、Broker 自研管理后台的服务端             |
| 授权主体     | Broker（**机构级授权 / Broker-scoped Authorization**） |
| 私有数据范围 | 获批的机构范围                                         |
| 形态         | HTTPS API；支持 Server-to-Server 集成与自研管理后台    |

## API 路径分类 {/* api-families */}

BrokerAPI 包含两类来源的能力：专门设计的 Server-to-Server 接口，以及从 Whale Console 现有业务流程中整理开放的接口。你可以通过请求路径了解接口的设计方式：

| 路径特征                | 定位                                                                                       | 常见能力                         |
| ----------------------- | ------------------------------------------------------------------------------------------ | -------------------------------- |
| 包含 `/whaleapi/`       | 最初为 Whale 租户的服务端与 Whale 对接而设计，已在生产环境长期使用，并持续服务实际对接。   | 直接的 Server-to-Server 查询和业务操作 |
| 包含 `/datasets/`       | 从 Whale Console 已有能力中整理开放，大部分基于通用配置的 Dataset 框架。                   | 列表查询、筛选、分页和可选导出   |
| 其他路径                | Whale Console 单项功能使用的独立接口，适用于不适合 Dataset 模型的业务。                    | 状态修改、流程操作和详情查询     |

这一分类说明接口的设计来源，不代表新旧或推荐优先级。不同类型的 API 彼此互补，请根据所需数据和业务流程选择具体接口。

## 不适用于 {/* not-for */}

- Broker App 使用 WhaleCoreSDK 完全自行实现证券功能 UI —— 请使用 [WhaleCoreSDK 与 TradingAPI](/zh-cn/trading-api/overview)
- 由开发者构建第三方应用 —— 请使用 [OpenAPI](https://open.longportapp.com)
- 嵌入移动端交易 UI —— 请使用 [WhaleAppSDK](/zh-cn/whalesdk/whaleapp/overview)

## 环境 {/* environments */}

| 环境 | 域名                           |
| ---- | ------------------------------ |
| 测试 | `https://b-api.longbridge.xyz` |
| 生产 | `https://b-api.lbkrs.com`      |

## 开始使用 {/* get-started */}

1. [快速开始](/zh-cn/broker-api/get-started/quickstart) —— 发起第一个请求
2. [鉴权](/zh-cn/broker-api/get-started/authentication) —— 必需请求头与凭证
3. [请求头](/zh-cn/broker-api/get-started/headers) —— 内容类型、语言与请求元数据
4. 在侧边栏按业务域浏览 API 参考
