---
title: 运维参考
description: 错误模型、限流、Changelog 与弃用政策
---

在生产环境对接 BrokerAPI 的运维参考。

<Warning>
以下内容**待契约确认**，暂不定义具体行为 —— 本页用于跟踪这些事项，避免被默认假设。请勿推断未在此记录的行为。
</Warning>

## 错误模型 —— 待定

统一错误目录（通用 HTTP 错误之外的业务错误码）正在与 API 团队确认中。

## 限流与配额 —— 待定

正式的限流规则、配额档位与限流行为待确认。

## 重试、超时与幂等性 —— 待定

幂等键支持与推荐的重试 / 超时策略待确认。

## Changelog 与弃用政策 —— 待定

按产品维护的 Changelog 和固定弃用窗口将在此发布。破坏性变更一定伴随弃用通知，不会以静默修改现有页面的方式发布。
