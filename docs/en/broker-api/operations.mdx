---
title: Operations
description: Error model, rate limits, changelog, and deprecation policy
---

Operational reference for running against BrokerAPI in production.

<Warning>
The items below are **pending contract confirmation** and intentionally left unspecified — this page tracks them so they are not silently assumed. Do not infer behavior that is not documented here.
</Warning>

## Error model — TBD {/* error-model-tbd */}

A unified error catalog (business error codes beyond generic HTTP errors) is being confirmed with the API team.

## Rate limits & quotas — TBD {/* rate-limits-quotas-tbd */}

Formal rate limits, quota tiers, and throttling behavior are pending confirmation.

## Retries, timeouts & idempotency — TBD {/* retries-timeouts-idempotency-tbd */}

Idempotency-key support and recommended retry/timeout policies are pending confirmation.

## Changelog & deprecation policy — TBD {/* changelog-deprecation-policy-tbd */}

A per-product changelog and a fixed deprecation window will be published here. Breaking changes will always ship with a deprecation notice — never as silent edits to existing pages.
