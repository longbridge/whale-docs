---
title: "租户支持的仓位方向查询 (临时写死配置)"
description: "dataset: account_position_side_options"
openapi: "post /v1/datasets/account_position_side_options"
---

取得 **租户支持的仓位方向查询 (临时写死配置)** 資料集。接口：`POST /v1/datasets/account_position_side_options`。Playground 頂部 Base URL 下拉可切換測試（預設）與生產環境。

### 請求範例

```json
{
  "body": {},
  "page": 1,
  "page_size": 20
}
```
