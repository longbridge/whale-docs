openapi: 3.1.1
info:
  title: Longport Whale Broker API
  description: Institution-grade server-side APIs for brokers using Longport Whale.
  version: 2.0.0
servers:
  - url: https://b-api.longbridge.xyz
    description: Testing
  - url: https://b-api.lbkrs.com
    description: Production
security:
  - bearerAuth: []
tags:
  - name: Service Parameter
paths:
  /clearing/upsert_clearing_ready_setting:
    post:
      operationId: upsert_clearing_ready_setting
      summary: Upsert Clearing Ready Setting
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Create or update a pre-clearing readiness check-item setting. Whether an existing record is matched by `task_code` determines create versus update.

        ### Request parameters

        - Required: `task_code` (task code), `task_name` (task name)
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `task_code` is the business-unique identifier of the check item. When editing an existing record, reuse the target record's `task_code`; it must not be changed. `process_mode` controls how the item is handled after a failure, and `is_enable` controls whether it is enabled. `audit_frontend_resource` is caller-provided audit context.

        ### Risk warning

        This operation changes the check items of the pre-clearing readiness flow and may affect end-of-day clearing execution and interception. Confirm the target record and parameters before calling.

        ### Result

        On success the check-item setting is created or updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - task_code
                - task_name
              properties:
                task_code:
                  type: string
                  description: |-
                    The corresponding field is read-only on the edit form. Its submitted value comes from
                    `task_code_val` on the selected record, not from user input.
                task_name:
                  type: string
                  description: |-
                    On submission, takes the task_code field value (display name) of the current row
                    record, matching the content of the read-only form input.
                process_mode:
                  type: string
                  description: Processing mode to apply when a pre-clearing check fails.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '0'
                      label: Tip Only
                    - value: '1'
                      label: Ticket Process
                    - value: '2'
                      label: Forced Calibration
                  example: '0'
                is_enable:
                  type: boolean
                  description: |-
                    A switch component; before submission it is converted to a boolean via
                    values.is_enable === true || values.is_enable === 'true'.
                audit_frontend_resource:
                  type: object
                  description: |-
                    A non-business field used only to display the before/after state in the audit log; the
                    front end builds it as {...currentItem, after_enable, before_enable }, containing the
                    two booleans before_enable/after_enable for the enabled state before and after the
                    change.
            example:
              task_code: ready_ibond_check
              task_name: IBOND 交易檢查
              process_mode: '1'
              is_enable: false
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/update_system_config:
    post:
      operationId: update_system_config
      summary: Update System Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Update the value of a clearing-system configuration item.

        ### Request parameters

        - Required: `config_code` (config code), `config_value` (config value)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `config_code` identifies the clearing-system configuration item to change, and `config_value` is its new value.

        ### Risk warning

        This operation changes the clearing system's runtime configuration and may affect clearing-flow behavior. Confirm the config item and value before calling.

        ### Result

        On success the configuration item is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - config_code
                - config_value
              properties:
                config_code:
                  type: number
                  description: This field is required.
                config_value:
                  type: string
                  description: This field is required.
            example:
              config_code: 1
              config_value: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/lb/bankaccount:
    put:
      operationId: atm_lb_bankaccount_edit
      summary: Edit Company Bank Account
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_fund.companybank_update
      x-lbonly: false
      description: |-
        Edit a company bank-account configuration. The request structure matches "Add company bank account", with the account details wrapped in `lb_bank_account_info`.

        ### Request parameters

        - Required: `id` (bank account ID), `lb_bank_account_info` (bank-account request data, same structure as "Add company bank account")
        - Optional: `audit_frontend_resource` (caller-provided audit-log payload)
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `id` identifies the company bank account to edit. The fields inside `lb_bank_account_info` follow the "Add company bank account" endpoint and are not repeated here.

        ### Risk warning

        This operation changes the bank-account information used for company deposits and withdrawals and may affect subsequent deposits, withdrawals, and reconciliation. Confirm the target account and parameters before calling.

        ### Result

        On success the company bank account is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - lb_bank_account_info
              properties:
                id:
                  type: string
                  description: Bank Account ID associated with this request.
                lb_bank_account_info:
                  type: object
                  description: |-
                    The field structure is identical to the request parameters of the "Add Company Bank
                    Account" endpoint and is not expanded again here.
                audit_frontend_resource:
                  type: object
                  description: Taken from the detail data prior to editing; not a business field.
            example:
              id: '1'
              lb_bank_account_info:
                nature_type: ''
                currency: USD
                account_name: ''
                region: ''
                bank_id: ''
                bank_account: ''
                bank_name: ''
                bank_region: ''
                swift_code: ''
                bank_addr: ''
                simple_name: ''
                deposit_types: []
                withdraw_types: []
                version: 1
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    post:
      operationId: atm_lb_bankaccount_add
      summary: Add Company Bank Account
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_fund.companybank_update
      x-lbonly: false
      description: |-
        Create a company bank account, used to configure the bank-account information for company deposits and withdrawals.

        ### Request parameters

        - Required: `nature_type` (account nature), `currency`, `account_name`, `region`, `bank_id`, `bank_account`, `bank_name`, `bank_region`, `swift_code`, `bank_addr`, `simple_name`, `deposit_types` (deposit methods), `withdraw_types` (withdrawal methods), `version`
        - Optional: `bank_code`, `connect_customer_id` (routing / Connect Customer ID), `net_customer_id`, `intermediary_swift_code`, `support_exchange` (whether FX is supported), `remark`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `deposit_types` and `withdraw_types` are arrays; drop any item with an empty `name`/`id` before submitting. `version` is fixed to `1` for backward compatibility.

        ### Risk warning

        This operation adds a bank account used for company deposits and withdrawals and may affect subsequent deposits, withdrawals, and reconciliation. Confirm the account information before calling.

        ### Result

        On success the company bank account is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - nature_type
                - currency
                - account_name
                - region
                - bank_id
                - bank_account
                - bank_name
                - bank_region
                - swift_code
                - bank_addr
                - simple_name
                - deposit_types
                - withdraw_types
                - version
              properties:
                nature_type:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                currency:
                  type: string
                  description: Currency code used for this request. This field is required.
                account_name:
                  type: string
                  description: This field is required.
                region:
                  type: string
                  description: This field is required.
                bank_id:
                  type: string
                  description: Bank ID associated with this request.
                bank_account:
                  type: string
                  description: This field is required.
                bank_code:
                  type: string
                bank_name:
                  type: string
                  description: This field is required.
                bank_region:
                  type: string
                  description: This field is required.
                swift_code:
                  type: string
                  description: This field is required.
                bank_addr:
                  type: string
                  description: This field is required.
                connect_customer_id:
                  type: string
                  description: Connect Customer ID associated with this request.
                net_customer_id:
                  type: string
                  description: Net Customer ID associated with this request.
                simple_name:
                  type: string
                  description: This field is required.
                intermediary_swift_code:
                  type: string
                deposit_types:
                  type: array
                  description: Array elements {name, id}; items with empty name/id are filtered out before submission.
                  items:
                    type: string
                withdraw_types:
                  type: array
                  description: Array elements {name, id, support_bank}; items with empty name/id are filtered out before submission.
                  items:
                    type: string
                support_exchange:
                  type: string
                  description: Specifies Support Exchange.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                remark:
                  type: string
                version:
                  type: number
                  description: Always pass 1, for compatibility with the legacy interface.
            example:
              nature_type: ''
              currency: USD
              account_name: ''
              region: ''
              bank_id: ''
              bank_account: ''
              bank_code: ''
              bank_name: ''
              bank_region: ''
              swift_code: ''
              bank_addr: ''
              connect_customer_id: ''
              net_customer_id: ''
              simple_name: ''
              intermediary_swift_code: ''
              deposit_types: []
              withdraw_types: []
              support_exchange: 'yes'
              remark: ''
              version: 1
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    get:
      operationId: atm_lb_bankaccount_detail
      summary: Company Bank Account Detail
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_fund.companybank_query
      x-lbonly: false
      description: |-
        Retrieve the full configuration of a single company bank account by `id`.

        ### Request parameters

        - Required: `id` (company bank account id), passed as a query parameter.
        - See each field's definition for its format, value range, and business meaning.

        ### Returns

        On success `data` contains the account's full configuration; the fields match the request fields of "Add / Edit company bank account". See the response definition below for field meanings.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: Company bank account id, supplied as a query parameter.
      responses:
        '200':
          description: The request was processed successfully. `data` contains the full configuration of the company bank account.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Full configuration of the company bank account.
                    properties:
                      nature_type:
                        type: string
                        description: 'Account nature. Common values: `client` (client account) and `house` (house account).'
                      account_type_id:
                        type: string
                        description: Account type id.
                      lb_company_id:
                        type: string
                        description: Owning company id.
                      account_name:
                        type: string
                        description: Account name.
                      bank_account:
                        type: string
                        description: Bank account number.
                      bank_name:
                        type: string
                        description: Bank name.
                      bank_id:
                        type: integer
                        description: Bank id identifying the bank associated with this account.
                      bank_region:
                        type: string
                        description: Region where the bank is located.
                      region:
                        type: string
                        description: Country or region the account belongs to.
                      swift_code:
                        type: string
                        description: Swift code.
                      bank_addr:
                        type: string
                        description: Bank address.
                      simple_name:
                        type: string
                        description: Abbreviation.
                      deposit_types:
                        type: array
                        description: List of deposit methods. Each item describes one deposit method.
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: Deposit method id.
                            name:
                              type: string
                              description: Deposit method name.
                            support_bank:
                              type: integer
                              description: 'Whether the bank channel is supported: `0` not supported, `1` supported.'
                      withdraw_types:
                        type: array
                        description: List of withdrawal methods. Each item describes one withdrawal method.
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: Withdrawal method id.
                            name:
                              type: string
                              description: Withdrawal method name.
                            support_bank:
                              type: integer
                              description: 'Whether the bank channel is supported: `0` not supported, `1` supported.'
                      remark:
                        type: string
                        description: Remark.
                      support_exchange:
                        type: integer
                        description: 'Whether currency exchange is supported: `0` not supported, `1` supported.'
                        x-enum-details:
                          - value: 0
                            label: Not Supported
                          - value: 1
                            label: Support
                      net_customer_id:
                        type: string
                        description: Net Customer ID.
                      connect_customer_id:
                        type: string
                        description: Connect Customer ID (interbank routing number).
                      uen:
                        type: string
                        description: Unique Entity Number (UEN).
                      currency:
                        type: string
                        description: Currency code.
                      intermediary_swift_code:
                        type: string
                        description: Intermediary bank Swift code.
                      bank_code:
                        type: string
                        description: Bank code (such as the ABA routing number); may be empty.
              example:
                code: 0
                message: success
                data:
                  nature_type: client
                  account_type_id: ''
                  lb_company_id: ''
                  account_name: test
                  bank_account: '1234567890'
                  bank_name: DBS Bank
                  bank_id: 8
                  bank_region: SINGAPORE
                  region: SINGAPORE
                  swift_code: DBSSSGSG
                  bank_addr: 8 Finance Street, Central, Hong Kong
                  simple_name: '11'
                  deposit_types:
                    - id: '1'
                      name: 电汇
                      support_bank: 0
                  withdraw_types:
                    - id: '1'
                      name: 电汇
                      support_bank: 1
                  remark: ''
                  support_exchange: 0
                  net_customer_id: ''
                  connect_customer_id: ''
                  uen: ''
                  currency: USD
                  intermediary_swift_code: ''
                  bank_code: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/currency/config:
    put:
      operationId: atm_currency_config_edit
      summary: Edit Currency Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.update_current_config
      x-lbonly: false
      description: |-
        Edit a currency setting, used to maintain a currency's withdrawal, FX, precision, and display configuration.

        ### Request parameters

        - Required: `id` (config ID), `currency_iso` (currency code), `currency` (currency identifier), `icon`, `logo`, `support_withdraw`, `support_exchange`, `min_withdrawal_amount`, `min_exchange_amount`, `exchange_rate_precision`, `amount_precision`, `amount_round_mode`, `standard_money` (whether it is the base currency), `abbreviation_multi_name` (localized short name), `multi_name` (localized full name), `json_config` (display and business toggles)
        - Optional: `cash_amount` (minimum deposit amount), `remarks`, `audit_frontend_resource` (caller-provided audit-log payload)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        Toggles such as `support_withdraw` and `support_exchange` are ultimately merged into `json_config`. `abbreviation_multi_name` and `multi_name` are localized JSON strings (with `zh-CN`, `zh-HK`, `en`).

        ### Risk warning

        This operation changes the currency's withdrawal, FX, and display configuration and may affect the funding services available to customers. Confirm the target currency and parameters before calling.

        ### Result

        On success the currency setting is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - currency_iso
                - currency
                - icon
                - logo
                - support_withdraw
                - support_exchange
                - min_withdrawal_amount
                - min_exchange_amount
                - exchange_rate_precision
                - amount_precision
                - amount_round_mode
                - standard_money
                - abbreviation_multi_name
                - multi_name
                - json_config
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
                currency_iso:
                  type: string
                  description: This field is required.
                currency:
                  type: string
                  description: Currency code used for this request. This field is required.
                icon:
                  type: string
                  description: This field is required.
                logo:
                  type: string
                  description: This field is required.
                cash_amount:
                  type: string
                support_withdraw:
                  type: string
                  description: Ultimately merged into json_config.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                support_exchange:
                  type: string
                  description: Ultimately merged into json_config.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                min_withdrawal_amount:
                  type: string
                  description: This field is required.
                min_exchange_amount:
                  type: string
                  description: This field is required.
                exchange_rate_precision:
                  type: number
                  description: This field is required.
                amount_precision:
                  type: string
                  description: Number of decimal places retained for monetary amounts. This field is required.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '0'
                      label: No decimal places
                    - value: '1'
                      label: One decimal place
                    - value: '2'
                      label: Two decimal places
                  example: '0'
                amount_round_mode:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                standard_money:
                  type: string
                  description: Specifies Standard Money. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                remarks:
                  type: string
                  description: Remarks saved with this operation.
                abbreviation_multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en}.
                multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en}.
                json_config:
                  type: string
                  description: JSON string {app_show, support_withdraw, support_exchange, support_deposit}.
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              id: '1'
              currency_iso: USD
              currency: USD
              icon: ''
              logo: ''
              cash_amount: '0'
              support_withdraw: 'yes'
              support_exchange: 'yes'
              min_withdrawal_amount: '0'
              min_exchange_amount: '0'
              exchange_rate_precision: 2
              amount_precision: '2'
              amount_round_mode: ''
              standard_money: 'no'
              remarks: ''
              abbreviation_multi_name: '{"zh-CN":"","zh-HK":"","en":"USD"}'
              multi_name: '{"zh-CN":"","zh-HK":"","en":"USD"}'
              json_config: '{"app_show":"yes","support_withdraw":"yes","support_exchange":"yes","support_deposit":"yes"}'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    post:
      operationId: atm_currency_config_add
      summary: Add Currency Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.update_current_config
      x-lbonly: false
      description: |-
        Create a currency setting, used to configure withdrawal, FX, precision, and display parameters for a new currency.

        ### Request parameters

        - Required: `currency_iso` (three-letter currency code), `currency` (currency identifier), `icon`, `logo`, `support_withdraw`, `support_exchange`, `min_withdrawal_amount`, `min_exchange_amount`, `exchange_rate_precision`, `amount_precision`, `amount_round_mode`, `standard_money` (whether it is the base currency), `abbreviation_multi_name` (localized short name), `multi_name` (localized full name), `json_config` (display and business toggles)
        - Optional: `cash_amount` (minimum deposit amount), `remarks`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `support_withdraw` and `support_exchange` are merged into `json_config` on submission. `abbreviation_multi_name`, `multi_name`, and `json_config` are all JSON strings. Allowed values for `amount_round_mode` come from the ref `atm_ref_amount_round_mode`.

        ### Risk warning

        This operation adds an available currency and its funding-business configuration and may affect the deposit, withdrawal, and FX capabilities visible to customers. Confirm the parameters before calling.

        ### Result

        On success the currency setting is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currency_iso
                - currency
                - icon
                - logo
                - support_withdraw
                - support_exchange
                - min_withdrawal_amount
                - min_exchange_amount
                - exchange_rate_precision
                - amount_precision
                - amount_round_mode
                - standard_money
                - abbreviation_multi_name
                - multi_name
                - json_config
              properties:
                currency_iso:
                  type: string
                  description: A three-letter currency code.
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CAD
                    - MYR
                    - AMD
                    - BGN
                    - BTN
                    - BWP
                    - BZD
                    - CDF
                    - CHF
                    - DKK
                    - GHS
                    - GMD
                    - GNF
                    - HRK
                    - HUF
                    - ILS
                    - INR
                    - IQD
                    - IRR
                    - ISK
                    - LRD
                    - LYD
                    - MOP
                    - RUB
                    - UAH
                    - UGX
                    - UYI
                    - UYU
                    - UZS
                    - XAF
                    - XOF
                    - XSU
                    - YER
                    - IDR
                    - TWD
                    - THB
                    - KRW
                    - NZD
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CAD
                      label: CAD
                    - value: MYR
                      label: MYR
                    - value: AMD
                      label: AMD
                    - value: BGN
                      label: BGN
                    - value: BTN
                      label: BTN
                    - value: BWP
                      label: BWP
                    - value: BZD
                      label: BZD
                    - value: CDF
                      label: CDF
                    - value: CHF
                      label: CHF
                    - value: DKK
                      label: DKK
                    - value: GHS
                      label: GHS
                    - value: GMD
                      label: GMD
                    - value: GNF
                      label: GNF
                    - value: HRK
                      label: HRK
                    - value: HUF
                      label: HUF
                    - value: ILS
                      label: ILS
                    - value: INR
                      label: INR
                    - value: IQD
                      label: IQD
                    - value: IRR
                      label: IRR
                    - value: ISK
                      label: ISK
                    - value: LRD
                      label: LRD
                    - value: LYD
                      label: LYD
                    - value: MOP
                      label: MOP
                    - value: RUB
                      label: RUB
                    - value: UAH
                      label: UAH
                    - value: UGX
                      label: UGX
                    - value: UYI
                      label: UYI
                    - value: UYU
                      label: UYU
                    - value: UZS
                      label: UZS
                    - value: XAF
                      label: XAF
                    - value: XOF
                      label: XOF
                    - value: XSU
                      label: XSU
                    - value: YER
                      label: YER
                    - value: IDR
                      label: IDR
                    - value: TWD
                      label: TWD
                    - value: THB
                      label: THB
                    - value: KRW
                      label: KRW
                    - value: NZD
                      label: NZD
                  example: USD
                currency:
                  type: string
                  description: Currency code used for this request. This field is required.
                icon:
                  type: string
                  description: This field is required.
                logo:
                  type: string
                  description: This field is required.
                cash_amount:
                  type: string
                  description: Required only when support_deposit=yes; otherwise left empty by the client.
                support_withdraw:
                  type: string
                  description: |-
                    Before submission it is moved from a top-level request field into json_config and no
                    longer appears separately in the final body (listed here as the pre-submission form
                    field).
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                support_exchange:
                  type: string
                  description: Same as above; ultimately merged into json_config.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                min_withdrawal_amount:
                  type: string
                  description: This field is required.
                min_exchange_amount:
                  type: string
                  description: This field is required.
                exchange_rate_precision:
                  type: number
                  description: This field is required.
                amount_precision:
                  type: string
                  description: Specifies Amount Precision. This field is required.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '0'
                      label: 0 decimal places
                    - value: '1'
                      label: 1 decimal place
                    - value: '2'
                      label: 2 decimal places
                  example: '0'
                amount_round_mode:
                  type: string
                  description: An enumerated field whose values come from ref atm_ref_amount_round_mode.
                  enum:
                    - round
                    - truncate
                  x-enum-details:
                    - value: round
                      label: Rounding Off
                    - value: truncate
                      label: Truncate Without Rounding
                  example: round
                standard_money:
                  type: string
                  description: Specifies Standard Money. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                remarks:
                  type: string
                  description: Remarks saved with this operation.
                abbreviation_multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en} assembled from the request field abbrev_name_cn/abbrev_name_hk/currency fields.
                multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en}, assembled from the request fields name_cn/name_hk/currency.
                json_config:
                  type: string
                  description: |-
                    A JSON string {app_show, support_withdraw, support_exchange, support_deposit},
                    assembled from the request fields of the same names.
            example:
              currency_iso: USD
              currency: USD
              icon: ''
              logo: ''
              cash_amount: '0'
              support_withdraw: 'yes'
              support_exchange: 'yes'
              min_withdrawal_amount: '0'
              min_exchange_amount: '0'
              exchange_rate_precision: 2
              amount_precision: '2'
              amount_round_mode: ''
              standard_money: 'no'
              remarks: ''
              abbreviation_multi_name: '{"zh-CN":"","zh-HK":"","en":"USD"}'
              multi_name: '{"zh-CN":"","zh-HK":"","en":"USD"}'
              json_config: '{"app_show":"yes","support_withdraw":"yes","support_exchange":"yes","support_deposit":"yes"}'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/whale/account:
    put:
      operationId: whale_account_edit
      summary: Update Whale Account
      tags:
        - Service Parameter
      x-permission-key:
        - atm.settle_chnl_bank_acct_operation
      x-lbonly: false
      description: |-
        Edit a settlement-channel bank account. The field structure matches "Add settlement-channel account", with an additional `id` to locate the target record.

        ### Request parameters

        - Required: `id` (account ID)
        - The remaining fields follow "Add settlement-channel bank account". See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `id` identifies the settlement-channel bank account to edit; the remaining fields (settlement channel, account name, bank account, region, currency, etc.) have the same meaning as in the create endpoint.

        ### Risk warning

        This operation changes the bank-account information used by a settlement channel and may affect fund settlement and transfers. Confirm the target account and parameters before calling.

        ### Result

        On success the settlement-channel bank account is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - settlement_channel
                - account_name
                - bank_account
                - region
                - bank_addr
                - currency
              properties:
                id:
                  type: string
                  description: Record ID
                settlement_channel:
                  type: string
                  description: Settlement channel
                main_accounts:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Main account name
                      type:
                        type: string
                        description: Main account type
                      account:
                        type: string
                        description: Main account number
                      scenario:
                        type: string
                        description: Business scenario
                account_name:
                  type: string
                  description: Account name
                bank_account:
                  type: string
                  description: Bank account number
                bank_id:
                  type: string
                  description: Card issuing bank
                region:
                  type: string
                  description: Country/Region
                bank_addr:
                  type: string
                  description: Bank address
                withdraw_types:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Settlement channel
                      types:
                        type: string
                        description: Withdrawal method
                remark:
                  type: string
                  description: Remark
                currency:
                  type: string
                  description: Currency
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    post:
      operationId: whale_account_add
      summary: Create Whale Account
      tags:
        - Service Parameter
      x-permission-key:
        - atm.settle_chnl_bank_acct_operation
      x-lbonly: false
      description: |-
        Create a settlement-channel bank account, used to configure the receiving/paying bank account for a settlement channel.

        ### Request parameters

        - Required: `settlement_channel`, `main_accounts` (main account), `account_name`, `bank_account`, `bank_id`, `region`, `currency`
        - Optional: `bank_addr`, `withdraw_types`, `remark`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        The account is bound to the specified settlement channel and used for subsequent fund settlement and transfers.

        ### Risk warning

        This operation adds a bank account relied on by a settlement channel and may affect fund settlement and transfers. Confirm the account information before calling.

        ### Result

        On success the settlement-channel bank account is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - settlement_channel
                - account_name
                - bank_account
                - region
                - bank_addr
                - currency
              properties:
                id:
                  type: string
                  description: Record ID
                settlement_channel:
                  type: string
                  description: Settlement channel
                main_accounts:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Main account name
                      type:
                        type: string
                        description: Main account type
                      account:
                        type: string
                        description: Main account number
                      scenario:
                        type: string
                        description: Business scenario
                account_name:
                  type: string
                  description: Account name
                bank_account:
                  type: string
                  description: Bank account number
                bank_id:
                  type: string
                  description: Card issuing bank
                region:
                  type: string
                  description: Country/Region
                bank_addr:
                  type: string
                  description: Bank address
                withdraw_types:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: Settlement channel
                      types:
                        type: string
                        description: Withdrawal method
                remark:
                  type: string
                  description: Remark
                currency:
                  type: string
                  description: Currency
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/whale/package/rule:
    post:
      operationId: whale_package_rule_add
      summary: Create Whale Package Rule
      tags:
        - Service Parameter
      x-permission-key:
        - atm.funds_alloc_config_operation
      x-lbonly: false
      description: |-
        Create a fund-transfer (allocation) rule that defines how funds are automatically transferred between a specified source and destination according to a strategy.

        ### Request parameters

        - Required: `transfer_type`, `from_type` (source type), `from_main_account`, `from_main_account_type`, `from_account_id`, `to_type` (destination type), `to_main_account`, `to_main_account_type`, `to_account_id`, `allocation_channel`, `transfer_strategy`, `package_type` (package type / frequency), `currency`, `amount`, `status`
        - Optional: `service_time`, `postscript`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        The rule defines the transfer strategy, frequency, currency, and amount from source to destination. `status` controls whether the rule is active.

        ### Risk warning

        This operation creates a rule that triggers real fund transfers; once active it may move real funds between accounts. Confirm the source, destination, amount, and strategy before calling.

        ### Result

        On success the fund-transfer rule is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - transfer_type
                - from_type
                - to_type
                - transfer_strategy
                - package_type
                - postscript
                - status
              properties:
                id:
                  type: string
                  description: Rule ID
                transfer_type:
                  type: string
                  description: Allocation type
                from_type:
                  type: string
                  description: Source channel
                from_main_account:
                  type: string
                  description: Main account (source; required when the source channel is not a bank)
                from_main_account_type:
                  type: string
                  description: Main account type (source)
                from_account_id:
                  type: string
                  description: Bank account (source; required when the source channel is a bank or the currency is not crypto)
                to_type:
                  type: string
                  description: Destination channel
                to_main_account:
                  type: string
                  description: Main account (destination; required when the destination channel is not a bank)
                to_main_account_type:
                  type: string
                  description: Main account type (destination)
                to_account_id:
                  type: string
                  description: Bank account (destination; required when the destination channel is a bank or the currency is not crypto)
                allocation_channel:
                  type: string
                  description: Allocation channel (required when the source channel is a bank)
                transfer_strategy:
                  type: string
                  description: Allocation strategy
                package_type:
                  type: string
                  description: Frequency
                currency:
                  type: string
                  description: Currency (required when the amount is not system-calculated)
                amount:
                  type: string
                  description: Amount (required when the amount is not system-calculated)
                service_time:
                  type: string
                  description: Service time (JSON-serialized object)
                postscript:
                  type: string
                  description: Transfer postscript
                status:
                  type: string
                  description: Status
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    put:
      operationId: whale_package_rule_edit
      summary: Update Whale Package Rule
      tags:
        - Service Parameter
      x-permission-key:
        - atm.funds_alloc_config_operation
      x-lbonly: false
      description: |-
        Edit a fund-transfer (allocation) rule. The field structure matches "Add fund-transfer rule", with an additional `id` to locate the target record.

        ### Request parameters

        - Required: `id` (rule ID)
        - The remaining fields follow "Add fund-transfer rule". See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `id` identifies the fund-transfer rule to edit; the remaining fields (source, destination, strategy, frequency, currency, amount, status, etc.) have the same meaning as in the create endpoint.

        ### Risk warning

        This operation changes a rule that triggers real fund transfers and may alter the direction, amount, or frequency of subsequent real fund movements. Confirm the target rule and parameters before calling.

        ### Result

        On success the fund-transfer rule is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - transfer_type
                - from_type
                - to_type
                - transfer_strategy
                - package_type
                - postscript
                - status
              properties:
                id:
                  type: string
                  description: Rule ID
                transfer_type:
                  type: string
                  description: Allocation type
                from_type:
                  type: string
                  description: Source channel
                from_main_account:
                  type: string
                  description: Main account (source; required when the source channel is not a bank)
                from_main_account_type:
                  type: string
                  description: Main account type (source)
                from_account_id:
                  type: string
                  description: Bank account (source; required when the source channel is a bank or the currency is not crypto)
                to_type:
                  type: string
                  description: Destination channel
                to_main_account:
                  type: string
                  description: Main account (destination; required when the destination channel is not a bank)
                to_main_account_type:
                  type: string
                  description: Main account type (destination)
                to_account_id:
                  type: string
                  description: Bank account (destination; required when the destination channel is a bank or the currency is not crypto)
                allocation_channel:
                  type: string
                  description: Allocation channel (required when the source channel is a bank)
                transfer_strategy:
                  type: string
                  description: Allocation strategy
                package_type:
                  type: string
                  description: Frequency
                currency:
                  type: string
                  description: Currency (required when the amount is not system-calculated)
                amount:
                  type: string
                  description: Amount (required when the amount is not system-calculated)
                service_time:
                  type: string
                  description: Service time (JSON-serialized object)
                postscript:
                  type: string
                  description: Transfer postscript
                status:
                  type: string
                  description: Status
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/add/coin/config:
    post:
      operationId: va_add_coin_config
      summary: Add Coin Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_operation
      x-lbonly: false
      description: |-
        Create a virtual-asset coin configuration to enable a coin and set its deposit/withdrawal, conversion, and limit parameters.

        ### Request parameters

        - Required: `coin` (coin code), `chain_types`, `conversion_currency`, `conversion_rate`, `multi_name` (localized full name), `icon`, `support_deposit`, `support_withdraw`, `deposit_need_micro_verification`, `quantity_precision`, `quantity_round_mode` (rounding logic)
        - Optional: `max_per_withdraw`, `min_per_withdraw`, `daily_withdraw_limit`, `weekly_withdraw_limit`, `remark`
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `multi_name` is a localized JSON string (with `zh-CN`, `zh-HK`, `en`). `quantity_round_mode` is `round` (half-up) or `truncate`. Create and edit share the same request type; omit `id` when creating.

        ### Risk warning

        This operation adds an available virtual-asset coin along with its deposit/withdrawal and limit configuration and may affect the virtual-asset funding services visible to customers. Confirm the parameters before calling.

        ### Result

        On success the virtual-asset coin configuration is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - coin
                - chain_types
                - conversion_currency
                - conversion_rate
                - multi_name
                - icon
                - support_deposit
                - support_withdraw
                - deposit_need_micro_verification
                - quantity_precision
                - quantity_round_mode
              properties:
                coin:
                  type: string
                  description: This field is required.
                chain_types:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                conversion_currency:
                  type: string
                  description: This field is required.
                conversion_rate:
                  type: string
                  description: This field is required.
                multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en}.
                icon:
                  type: string
                  description: This field is required.
                support_deposit:
                  type: string
                  description: Specifies Support Deposit. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: Enable
                    - value: 'no'
                      label: Close
                  example: 'yes'
                support_withdraw:
                  type: string
                  description: Specifies Support Withdraw. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                deposit_need_micro_verification:
                  type: string
                  description: Specifies Deposit Need Micro Verification. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: Enable
                    - value: 'no'
                      label: Close
                  example: 'yes'
                quantity_precision:
                  type: number
                  description: This field is required.
                quantity_round_mode:
                  type: string
                  description: round = round half up; truncate = truncate directly.
                  enum:
                    - round
                    - truncate
                  x-enum-details:
                    - value: round
                      label: Round
                    - value: truncate
                      label: Truncate
                  example: round
                max_per_withdraw:
                  type: string
                min_per_withdraw:
                  type: string
                daily_withdraw_limit:
                  type: string
                weekly_withdraw_limit:
                  type: string
                remark:
                  type: string
            example:
              coin: USDT
              chain_types: TRC20,ERC20
              conversion_currency: USD
              conversion_rate: '1'
              multi_name: '{"zh-CN":"","zh-HK":"","en":"USDT"}'
              icon: ''
              support_deposit: 'yes'
              support_withdraw: 'yes'
              deposit_need_micro_verification: 'no'
              quantity_precision: 6
              quantity_round_mode: truncate
              max_per_withdraw: ''
              min_per_withdraw: ''
              daily_withdraw_limit: ''
              weekly_withdraw_limit: ''
              remark: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
                        description: Tip
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/modify/coin/config:
    post:
      operationId: va_modify_coin_config
      summary: Modify Coin Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_operation
      x-lbonly: false
      description: |-
        Edit a virtual-asset coin configuration. It shares the request type with "Add coin configuration"; an `id` is additionally required when editing.

        ### Request parameters

        - Required: `id` (config ID), `coin`, `chain_types`, `conversion_currency`, `conversion_rate`, `multi_name` (localized full name), `icon`, `support_deposit`, `support_withdraw`, `deposit_need_micro_verification`, `quantity_precision`, `quantity_round_mode` (rounding logic)
        - Optional: `max_per_withdraw`, `min_per_withdraw`, `daily_withdraw_limit`, `weekly_withdraw_limit`, `remark`
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `id` identifies the coin configuration to edit. `multi_name` is a localized JSON string, and `quantity_round_mode` is `round` (half-up) or `truncate`.

        ### Risk warning

        This operation changes a coin's deposit/withdrawal, conversion, and limit configuration and may affect the virtual-asset funding services visible to customers. Confirm the target coin and parameters before calling.

        ### Result

        On success the virtual-asset coin configuration is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - coin
                - chain_types
                - conversion_currency
                - conversion_rate
                - multi_name
                - icon
                - support_deposit
                - support_withdraw
                - deposit_need_micro_verification
                - quantity_precision
                - quantity_round_mode
              properties:
                id:
                  type: number
                  description: Config ID associated with this request.
                coin:
                  type: string
                  description: This field is required.
                chain_types:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                conversion_currency:
                  type: string
                  description: This field is required.
                conversion_rate:
                  type: string
                  description: This field is required.
                multi_name:
                  type: string
                  description: A JSON string {zh-CN, zh-HK, en}.
                icon:
                  type: string
                  description: This field is required.
                support_deposit:
                  type: string
                  description: Specifies Support Deposit. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: Enable
                    - value: 'no'
                      label: Close
                  example: 'yes'
                support_withdraw:
                  type: string
                  description: Specifies Support Withdraw. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                deposit_need_micro_verification:
                  type: string
                  description: Specifies Deposit Need Micro Verification. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: Enable
                    - value: 'no'
                      label: Close
                  example: 'yes'
                quantity_precision:
                  type: number
                  description: This field is required.
                quantity_round_mode:
                  type: string
                  description: round = round half up; truncate = truncate directly.
                  enum:
                    - round
                    - truncate
                  x-enum-details:
                    - value: round
                      label: Round
                    - value: truncate
                      label: Truncate
                  example: round
                max_per_withdraw:
                  type: string
                min_per_withdraw:
                  type: string
                daily_withdraw_limit:
                  type: string
                weekly_withdraw_limit:
                  type: string
                remark:
                  type: string
            example:
              id: 1
              coin: USDT
              chain_types: TRC20,ERC20
              conversion_currency: USD
              conversion_rate: '1'
              multi_name: '{"zh-CN":"","zh-HK":"","en":"USDT"}'
              icon: ''
              support_deposit: 'yes'
              support_withdraw: 'yes'
              deposit_need_micro_verification: 'no'
              quantity_precision: 6
              quantity_round_mode: truncate
              max_per_withdraw: ''
              min_per_withdraw: ''
              daily_withdraw_limit: ''
              weekly_withdraw_limit: ''
              remark: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
                        description: Tip
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/modify/lookup:
    put:
      operationId: modify_lookup
      summary: Modify Lookup
      tags:
        - Service Parameter
      x-permission-key:
        - atm.modify_lookup
      x-lbonly: false
      description: |-
        Edit a deposit/withdrawal remark preset, used to change the text and priority of a predefined remark.

        ### Request parameters

        - Required: `id` (remark preset ID), `category` (remark type), `value_cn` (Simplified Chinese text), `value_hk` (Traditional Chinese text), `value_en` (English text), `priority`
        - Optional: `audit_frontend_resource` (caller-provided audit context)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `category` cannot be changed when editing. `value_cn`/`value_hk`/`value_en` are up to 128 characters, or up to 500 characters when `category` is `setting_remark_withdrawal_reject` or `setting_remark_deposit_reject`. `priority` ranges from `1` to `999999`.

        ### Result

        On success the remark preset is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - category
                - value_cn
                - value_hk
                - value_en
                - priority
              properties:
                id:
                  type: string
                  description: Lookup ID associated with this request.
                category:
                  type: string
                  description: Remark category. This field is read-only and cannot be modified when editing.
                  enum:
                    - setting_remark_deposit
                    - setting_remark_withdrawal
                    - setting_remark_securitiesdeposit
                    - setting_remark_securitieswithdrawal
                    - setting_remark_accountadjustment
                    - setting_remark_withdrawal_reject
                    - setting_remark_deposit_reject
                    - setting_remark_withdrawal_card_reject
                    - setting_remark_fund
                    - setting_remark_otc_derivatives
                    - setting_remark_deposit_matching
                  x-enum-details:
                    - value: setting_remark_deposit
                      label: Deposit
                    - value: setting_remark_withdrawal
                      label: Withdrawal
                    - value: setting_remark_securitiesdeposit
                      label: Securities Deposit
                    - value: setting_remark_securitieswithdrawal
                      label: Securities Withdrawal
                    - value: setting_remark_accountadjustment
                      label: Account Adjustment
                    - value: setting_remark_withdrawal_reject
                      label: Withdrawal rejection
                    - value: setting_remark_deposit_reject
                      label: Deposit rejection
                    - value: setting_remark_withdrawal_card_reject
                      label: Refuse Add Bank Card
                    - value: setting_remark_fund
                      label: Fund
                    - value: setting_remark_otc_derivatives
                      label: OTC Derivatives
                    - value: setting_remark_deposit_matching
                      label: Deposit Matching
                  example: setting_remark_deposit
                value_cn:
                  type: string
                  description: |-
                    Up to 128 characters; up to 500 characters when category is
                    setting_remark_withdrawal_reject or setting_remark_deposit_reject.
                value_hk:
                  type: string
                  description: The length limit is the same as value_cn.
                value_en:
                  type: string
                  description: The length limit is the same as value_cn.
                priority:
                  type: number
                  description: Value range 1-999999.
                audit_frontend_resource:
                  type: object
                  description: Snapshot of the record before modification, used for audit tracing; not a user-entered field.
            example:
              id: '101'
              category: setting_remark_deposit
              value_cn: 银行转账入金
              value_hk: 銀行轉賬入金
              value_en: Bank Transfer Deposit
              priority: 10
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/add/lookup:
    post:
      operationId: add_lookup
      summary: Add Lookup
      tags:
        - Service Parameter
      x-permission-key:
        - atm.add_lookup
      x-lbonly: false
      description: |-
        Create a deposit/withdrawal remark preset, used to add a reusable predefined remark.

        ### Request parameters

        - Required: `category` (remark category), `priority`, `value_cn` (Simplified Chinese text), `value_hk` (Traditional Chinese text), `value_en` (English text)
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `priority` is a positive integer in the range `1`-`999999`. `value_cn`/`value_hk`/`value_en` are the remark text for each language, provided by the caller.

        ### Result

        On success the remark preset is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - category
                - priority
                - value_cn
                - value_hk
                - value_en
              properties:
                category:
                  type: string
                  description: Remark category.
                  enum:
                    - setting_remark_deposit
                    - setting_remark_withdrawal
                    - setting_remark_securitiesdeposit
                    - setting_remark_securitieswithdrawal
                    - setting_remark_accountadjustment
                    - setting_remark_withdrawal_reject
                    - setting_remark_deposit_reject
                    - setting_remark_withdrawal_card_reject
                    - setting_remark_fund
                    - setting_remark_otc_derivatives
                    - setting_remark_deposit_matching
                  x-enum-details:
                    - value: setting_remark_deposit
                      label: Deposit
                    - value: setting_remark_withdrawal
                      label: Withdrawal
                    - value: setting_remark_securitiesdeposit
                      label: Securities Deposit
                    - value: setting_remark_securitieswithdrawal
                      label: Securities Withdrawal
                    - value: setting_remark_accountadjustment
                      label: Account Adjustment
                    - value: setting_remark_withdrawal_reject
                      label: Withdrawal rejection
                    - value: setting_remark_deposit_reject
                      label: Deposit rejection
                    - value: setting_remark_withdrawal_card_reject
                      label: Refuse Add Bank Card
                    - value: setting_remark_fund
                      label: Fund
                    - value: setting_remark_otc_derivatives
                      label: OTC Derivatives
                    - value: setting_remark_deposit_matching
                      label: Deposit Matching
                  example: setting_remark_deposit
                priority:
                  type: number
                  description: A positive integer in the range 1–999999.
                value_cn:
                  type: string
                  description: |-
                    Simplified Chinese text of the predefined remark entry, entered by the caller. Required; up to 128 characters.
                value_hk:
                  type: string
                  description: |-
                    Traditional Chinese (Hong Kong) text of the predefined remark entry, entered by the caller. Required; up to 128 characters.
                value_en:
                  type: string
                  description: |-
                    English text of the predefined remark entry, entered by the caller. Required; up to 128 characters.
            example:
              category: '1'
              priority: 1
              value_cn: 客户资料待补充
              value_hk: 客戶資料待補充
              value_en: Client information pending
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/delete/lookup:
    delete:
      operationId: delete_lookup
      summary: Delete Lookup
      tags:
        - Service Parameter
      x-permission-key:
        - atm.delete_lookup
      x-lbonly: false
      description: |-
        Delete a deposit/withdrawal remark preset.

        ### Request parameters

        - Required: `id` (remark preset ID)
        - See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        Only the `id` of the record to delete is required.

        ### Risk warning

        This operation deletes a configured remark preset; the predefined text will no longer be available afterward. Confirm the target record before calling.

        ### Result

        On success the remark preset is deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Lookup ID associated with this request.
            example:
              id: '101'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/query_support_currency:
    post:
      operationId: query_support_currency
      summary: Query Currency Pairs
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.support_currency_update
      x-lbonly: false
      description: |-
        Query the currency-pair configuration list. All filter fields are optional; an empty request body or an omitted field returns all currency-pair configurations.

        ### Request parameters

        - `created_at`: filter by creation time; returns all when omitted.
        - `from_currency`: filter by debit currency; returns all when omitted.
        - `to_currency`: filter by credit currency; returns all when omitted.
        - See each field's definition for its format, value range, and business meaning.

        ### Returns

        On success `data.support_currency_resp` contains the currency-pair configuration records grouped by source currency. See the response definition below for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                created_at:
                  type: string
                  description: |-
                    Filters by creation time. The value is a comma-joined date range `start,end`, e.g. `2026-07-01,2026-07-01`.

                    When omitted, no filtering is applied on this field.
                  example: 2026-07-01,2026-07-01
                from_currency:
                  type: string
                  description: |-
                    Filters by the debit (source) currency, given as a three-letter currency code.

                    When omitted, no filtering is applied on this field.
                  example: USD
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CAD
                    - MYR
                    - AMD
                    - BGN
                    - BTN
                    - BWP
                    - BZD
                    - CDF
                    - CHF
                    - DKK
                    - GHS
                    - GMD
                    - GNF
                    - HRK
                    - HUF
                    - ILS
                    - INR
                    - IQD
                    - IRR
                    - ISK
                    - LRD
                    - LYD
                    - MOP
                    - RUB
                    - UAH
                    - UGX
                    - UYI
                    - UYU
                    - UZS
                    - XAF
                    - XOF
                    - XSU
                    - YER
                    - IDR
                    - TWD
                    - THB
                    - KRW
                    - NZD
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CAD
                      label: CAD
                    - value: MYR
                      label: MYR
                    - value: AMD
                      label: AMD
                    - value: BGN
                      label: BGN
                    - value: BTN
                      label: BTN
                    - value: BWP
                      label: BWP
                    - value: BZD
                      label: BZD
                    - value: CDF
                      label: CDF
                    - value: CHF
                      label: CHF
                    - value: DKK
                      label: DKK
                    - value: GHS
                      label: GHS
                    - value: GMD
                      label: GMD
                    - value: GNF
                      label: GNF
                    - value: HRK
                      label: HRK
                    - value: HUF
                      label: HUF
                    - value: ILS
                      label: ILS
                    - value: INR
                      label: INR
                    - value: IQD
                      label: IQD
                    - value: IRR
                      label: IRR
                    - value: ISK
                      label: ISK
                    - value: LRD
                      label: LRD
                    - value: LYD
                      label: LYD
                    - value: MOP
                      label: MOP
                    - value: RUB
                      label: RUB
                    - value: UAH
                      label: UAH
                    - value: UGX
                      label: UGX
                    - value: UYI
                      label: UYI
                    - value: UYU
                      label: UYU
                    - value: UZS
                      label: UZS
                    - value: XAF
                      label: XAF
                    - value: XOF
                      label: XOF
                    - value: XSU
                      label: XSU
                    - value: YER
                      label: YER
                    - value: IDR
                      label: IDR
                    - value: TWD
                      label: TWD
                    - value: THB
                      label: THB
                    - value: KRW
                      label: KRW
                    - value: NZD
                      label: NZD
                to_currency:
                  type: string
                  description: |-
                    Filters by the credit (target) currency, given as a three-letter currency code.

                    When omitted, no filtering is applied on this field.
                  example: HKD
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CAD
                    - MYR
                    - AMD
                    - BGN
                    - BTN
                    - BWP
                    - BZD
                    - CDF
                    - CHF
                    - DKK
                    - GHS
                    - GMD
                    - GNF
                    - HRK
                    - HUF
                    - ILS
                    - INR
                    - IQD
                    - IRR
                    - ISK
                    - LRD
                    - LYD
                    - MOP
                    - RUB
                    - UAH
                    - UGX
                    - UYI
                    - UYU
                    - UZS
                    - XAF
                    - XOF
                    - XSU
                    - YER
                    - IDR
                    - TWD
                    - THB
                    - KRW
                    - NZD
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CAD
                      label: CAD
                    - value: MYR
                      label: MYR
                    - value: AMD
                      label: AMD
                    - value: BGN
                      label: BGN
                    - value: BTN
                      label: BTN
                    - value: BWP
                      label: BWP
                    - value: BZD
                      label: BZD
                    - value: CDF
                      label: CDF
                    - value: CHF
                      label: CHF
                    - value: DKK
                      label: DKK
                    - value: GHS
                      label: GHS
                    - value: GMD
                      label: GMD
                    - value: GNF
                      label: GNF
                    - value: HRK
                      label: HRK
                    - value: HUF
                      label: HUF
                    - value: ILS
                      label: ILS
                    - value: INR
                      label: INR
                    - value: IQD
                      label: IQD
                    - value: IRR
                      label: IRR
                    - value: ISK
                      label: ISK
                    - value: LRD
                      label: LRD
                    - value: LYD
                      label: LYD
                    - value: MOP
                      label: MOP
                    - value: RUB
                      label: RUB
                    - value: UAH
                      label: UAH
                    - value: UGX
                      label: UGX
                    - value: UYI
                      label: UYI
                    - value: UYU
                      label: UYU
                    - value: UZS
                      label: UZS
                    - value: XAF
                      label: XAF
                    - value: XOF
                      label: XOF
                    - value: XSU
                      label: XSU
                    - value: YER
                      label: YER
                    - value: IDR
                      label: IDR
                    - value: TWD
                      label: TWD
                    - value: THB
                      label: THB
                    - value: KRW
                      label: KRW
                    - value: NZD
                      label: NZD
            example:
              from_currency: USD
              to_currency: HKD
              created_at: 2026-07-01,2026-07-01
      responses:
        '200':
          description: |-
            The request was processed successfully. `data.support_currency_resp` is the list of currency-pair
            configuration records grouped by source currency.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - support_currency_resp
                    properties:
                      support_currency_resp:
                        type: array
                        description: |-
                          **Returned value:** List of currency-pair configuration records matching the query
                          conditions, grouped by source currency. Each item represents one source currency and
                          all of its target currencies.
                        items:
                          type: object
                          properties:
                            from_currency:
                              type: string
                              description: '**Returned value:** Debit (source) currency, a three-letter currency code.'
                            to_currency:
                              type: string
                              description: |-
                                **Returned value:** The target-currency configuration under this source currency,
                                provided as a JSON-encoded string. When parsed, it is an object whose `id_to_ccy`
                                field is an array of target currencies. Each array item contains:

                                - `id`: Unique identifier of this "source-target" record, used for editing or deletion.
                                - `to_ccy`: Credit (target) currency, a three-letter currency code.
                                - `default_currency`: Whether this is the default target currency for the source currency; `true` means default.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
              example:
                code: 0
                message: success
                data:
                  support_currency_resp:
                    - from_currency: USD
                      to_currency: '{"id_to_ccy":[{"id":"1001","to_ccy":"HKD","default_currency":true},{"id":"1002","to_ccy":"JPY","default_currency":false}]}'
                      created_at: '1780480167'
                    - from_currency: HKD
                      to_currency: '{"id_to_ccy":[{"id":"1003","to_ccy":"USD","default_currency":true}]}'
                      created_at: '1780480200'
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/support_currency_add:
    post:
      operationId: support_currency_add
      summary: Add Support Currency
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.support_currency_update
      x-lbonly: false
      description: |-
        Add a supported FX currency pair, configuring the exchange relationship from a source currency to one or more target currencies.

        ### Request parameters

        - Required: `from_currency` (source currency), `to_currency` (target currency)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `from_currency` is a three-letter currency code. `to_currency` supports multiple values joined by commas before submitting, so several target currencies can be added for one source currency at once.

        ### Risk warning

        This operation enables new FX currency pairs and may affect the exchange paths available to customers. Confirm the currency pair before calling.

        ### Result

        On success the FX currency pair is added; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - from_currency
                - to_currency
              properties:
                from_currency:
                  type: string
                  description: The debit (source) currency, a three-letter currency code.
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CAD
                    - MYR
                    - AMD
                    - BGN
                    - BTN
                    - BWP
                    - BZD
                    - CDF
                    - CHF
                    - DKK
                    - GHS
                    - GMD
                    - GNF
                    - HRK
                    - HUF
                    - ILS
                    - INR
                    - IQD
                    - IRR
                    - ISK
                    - LRD
                    - LYD
                    - MOP
                    - RUB
                    - UAH
                    - UGX
                    - UYI
                    - UYU
                    - UZS
                    - XAF
                    - XOF
                    - XSU
                    - YER
                    - IDR
                    - TWD
                    - THB
                    - KRW
                    - NZD
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CAD
                      label: CAD
                    - value: MYR
                      label: MYR
                    - value: AMD
                      label: AMD
                    - value: BGN
                      label: BGN
                    - value: BTN
                      label: BTN
                    - value: BWP
                      label: BWP
                    - value: BZD
                      label: BZD
                    - value: CDF
                      label: CDF
                    - value: CHF
                      label: CHF
                    - value: DKK
                      label: DKK
                    - value: GHS
                      label: GHS
                    - value: GMD
                      label: GMD
                    - value: GNF
                      label: GNF
                    - value: HRK
                      label: HRK
                    - value: HUF
                      label: HUF
                    - value: ILS
                      label: ILS
                    - value: INR
                      label: INR
                    - value: IQD
                      label: IQD
                    - value: IRR
                      label: IRR
                    - value: ISK
                      label: ISK
                    - value: LRD
                      label: LRD
                    - value: LYD
                      label: LYD
                    - value: MOP
                      label: MOP
                    - value: RUB
                      label: RUB
                    - value: UAH
                      label: UAH
                    - value: UGX
                      label: UGX
                    - value: UYI
                      label: UYI
                    - value: UYU
                      label: UYU
                    - value: UZS
                      label: UZS
                    - value: XAF
                      label: XAF
                    - value: XOF
                      label: XOF
                    - value: XSU
                      label: XSU
                    - value: YER
                      label: YER
                    - value: IDR
                      label: IDR
                    - value: TWD
                      label: TWD
                    - value: THB
                      label: THB
                    - value: KRW
                      label: KRW
                    - value: NZD
                      label: NZD
                  example: USD
                to_currency:
                  type: string
                  description: Three-letter currency codes. Select multiple values and join them with commas before submitting.
            example:
              from_currency: USD
              to_currency: HKD,JPY,SGD
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/forex/support_currency_update:
    post:
      operationId: support_currency_update_v2
      summary: Update Support Currency V2
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.support_currency_update
      x-lbonly: false
      description: |-
        Batch-edit supported FX currency-pair configurations, allowing add, modify, and delete of sub-items in one request, and setting a target currency as default.

        ### Request parameters

        - Required: `UpdateSupportCurrency` (change list), `UpdateSupportCurrency[].from_currency`, `UpdateSupportCurrency[].to_currency`, `UpdateSupportCurrency[].action`
        - Optional: `UpdateSupportCurrency[].id` (sub-item ID), `UpdateSupportCurrency[].default_currency` (whether to set as the default target), `audit_frontend_resource` (audit snapshot with the before/after target-currency lists)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `UpdateSupportCurrency` is a change list; each item's `action` specifies add, modify, or delete. Added items have no `id`; modified/deleted items must carry the corresponding sub-item `id`. `default_currency` marks a target currency as the default exchange target for that source currency.

        ### Risk warning

        This operation batch-changes FX currency pairs and the default target currency and may affect the exchange paths and default exchange behavior available to customers. Confirm the change list before calling.

        ### Result

        On success the currency-pair configurations are batch-updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - UpdateSupportCurrency
                - UpdateSupportCurrency[].from_currency
                - UpdateSupportCurrency[].to_currency
                - UpdateSupportCurrency[].action
              properties:
                UpdateSupportCurrency:
                  type: array
                  description: |-
                    Each item contains id/from_currency/to_currency/action/default_currency; newly added
                    items use action=1, items with a changed to_currency use action=2, and removed old
                    items use action=3; in one case a single item is always passed with action=2 and
                    default_currency=true to set the default target currency.
                  items:
                    type: string
                UpdateSupportCurrency[].id:
                  type: string
                  description: The ID of an existing target-currency record; empty when creating (action=1).
                UpdateSupportCurrency[].from_currency:
                  type: string
                  description: Candidate values come from the ref `tenant_currencies_for_exchange`. This field is required.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_for_exchange
                UpdateSupportCurrency[].to_currency:
                  type: string
                  description: Candidate values come from the ref `tenant_currencies_for_exchange`. This field is required.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_for_exchange
                UpdateSupportCurrency[].action:
                  type: number
                  description: Action type. See the options below.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add.
                    - value: '2'
                      label: Modify / Set as Default
                    - value: '3'
                      label: Delete.
                  example: '1'
                UpdateSupportCurrency[].default_currency:
                  type: boolean
                  description: Only passed as true in the "Set as Default" scenario.
                audit_frontend_resource:
                  type: object
                  description: Sent only in the edit scenario; contains before/after target-currency arrays, used for back-office audit trails.
            example:
              UpdateSupportCurrency:
                - id: '101'
                  from_currency: USD
                  to_currency: HKD
                  action: 2
                  default_currency: true
              audit_frontend_resource:
                after:
                  - HKD
                  - JPY
                before:
                  - HKD
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/support_currency_delete:
    post:
      operationId: support_currency_delete
      summary: Delete Support Currency
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.support_currency_update
      x-lbonly: false
      description: |-
        Delete supported FX target-currency records. One call deletes a batch of target currencies under a given source currency.

        ### Request parameters

        - Required: `ids` (list of target-currency record IDs to delete)
        - Optional: `audit_frontend_resource` (audit snapshot with the source currency and the deleted target currencies)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `ids` are taken from the `id`s of all target-currency sub-items under a source currency, allowing a batch of target currencies under that source to be deleted at once.

        ### Risk warning

        This operation removes configured FX currency pairs and may affect the exchange paths available to customers. Confirm the records to delete before calling.

        ### Result

        On success the FX currency pairs are deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  description: |-
                    Use the `id` values of all target-currency entries under the source currency to delete
                    all of them in one request.
                  items:
                    type: string
                audit_frontend_resource:
                  type: object
                  description: |-
                    Contains from_currency (source currency code) and to_currency (array of deleted target
                    currency codes); used only for back-office audit trails.
            example:
              ids:
                - '201'
                - '202'
              audit_frontend_resource:
                from_currency: USD
                to_currency:
                  - HKD
                  - JPY
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/service_strategy_delete:
    post:
      operationId: service_strategy_delete
      summary: Enable/Disable Service Strategy
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchangeserve_update
      x-lbonly: false
      description: |-
        Enable or disable an FX service strategy.

        ### Request parameters

        - Required: `id` (strategy ID), `enable_flag` (enabled state)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` is taken from the target strategy record. `enable_flag` is `true` to enable and `false` to disable.

        ### Risk warning

        This operation toggles an FX strategy's enabled state and may affect whether the FX service under that strategy takes effect for customers. Confirm the target strategy before calling.

        ### Result

        On success the strategy's enabled state is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - enable_flag
              properties:
                id:
                  type: string
                  description: Taken from the row data record.id.
                enable_flag:
                  type: boolean
                  description: false = disabled, true = enabled.
                  enum:
                    - 'false'
                    - 'true'
                  x-enum-details:
                    - value: 'false'
                      label: Disable
                    - value: 'true'
                      label: Enabled
                  example: 'false'
            example:
              id: '1'
              enable_flag: false
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/service_strategy_update:
    post:
      operationId: service_strategy_update
      summary: Update Service Strategy
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchangeserve_update
      x-lbonly: false
      description: |-
        Edit an FX service strategy. The field structure matches "Add FX strategy"; the target strategy's `id` is required when editing.

        ### Request parameters

        - Required: `id` (strategy ID)
        - The remaining fields follow "Add FX strategy". See each field's definition for its format, value range, default behavior, and business meaning.

        ### Business notes

        `id` identifies the FX strategy to edit; the remaining fields (service strategy, channel, supported currency pairs, service time, FX mode, spread-bearing method, etc.) have the same meaning as in the create endpoint.

        ### Risk warning

        This operation changes an FX strategy configuration and may affect the FX service that takes effect for customers under that strategy. Confirm the target strategy and parameters before calling.

        ### Result

        On success the FX strategy is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Strategy ID associated with this request.
                is_default:
                  type: boolean
                  description: |-
                    Boolean flag for is default.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                change_type:
                  type: string
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: All
                    - value: '1'
                      label: Large-amount exchange
                    - value: '2'
                      label: Small-amount exchange (within the pool)
                    - value: '3'
                      label: Small-amount exchange (beyond the pool)
                  example: '0'
                  description: |-
                    change_type.
                service_strategy:
                  type: string
                channel_name:
                  type: string
                  description: Required when it is not the default strategy and channels must be distinguished.
                support_currency:
                  type: array
                  description: |-
                    An array of supported currency pairs, each a `USD/HKD`-style string (assembled from each item's currency_prefix/currency_suffix in support_currencies).

                    The candidate currency pairs come from the supported currency-pair list returned by `POST /forex/query_support_currency`.
                  items:
                    type: string
                  x-value-source:
                    endpoint: POST /forex/query_support_currency
                support_currencies:
                  type: array
                  description: |-
                    The request field support_currency raw array, which is moved entirely under the
                    support_currencies key before submission.
                  items:
                    type: string
                service_time:
                  type: string
                  description: |-
                    A JSON string assembled from the segmented request fields
                    service_time.time_zones/time_rule/time_rule_0, etc. The structure is fairly complex
                    (covering time zones and time-slot rules); the inner fields are not exhaustively
                    enumerated here.
                is_combine:
                  type: boolean
                  description: |-
                    Boolean flag for is combine.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                forex_mode:
                  type: string
                  description: |-
                    forex_mode. The set of accepted values could not be confirmed from the frontend; refer to the backend definition.
                bear_wave:
                  type: string
                  description: |-
                    bear_wave. The set of accepted values could not be confirmed from the frontend; refer to the backend definition.
                currency_prefix:
                  type: string
                currency_suffix:
                  type: string
            example:
              id: '123'
              is_default: false
              change_type: ''
              service_strategy: ''
              channel_name: ''
              support_currency:
                - USD/HKD
              support_currencies:
                - currency_prefix: USD
                  currency_suffix: HKD
              service_time: '{"time_zones":"hk_trade_day","time_rule":"09:00:00-16:00:00"}'
              is_combine: false
              forex_mode: ''
              bear_wave: ''
              currency_prefix: ''
              currency_suffix: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/service_strategy_add:
    post:
      operationId: service_strategy_add
      summary: Add Service Strategy
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchangeserve_update
      x-lbonly: false
      description: |-
        Create an FX service strategy, used to define a combined rule set of exchange channels, supported currency pairs, service time, and FX mode.

        ### Request parameters

        - This endpoint has no required parameters; see the field definitions below for optional parameters.
        - Optional: `is_default` (whether it is the default strategy), `change_type`, `service_strategy`, `channel_name`, `support_currency` (assembled supported pairs), `support_currencies` (raw supported pairs), `service_time`, `is_combine`, `forex_mode` (FX mode), `bear_wave` (spread-bearing method), `currency_prefix`, `currency_suffix`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        The strategy defines a combined rule set of exchange channel, supported currency pairs, service time, and FX mode. `is_default` can mark it as the default strategy.

        ### Risk warning

        This operation creates an FX strategy and may affect the FX service available to customers. Confirm the parameters before calling.

        ### Result

        On success the FX strategy is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                is_default:
                  type: boolean
                  description: |-
                    Boolean flag for is default.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                change_type:
                  type: string
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: All
                    - value: '1'
                      label: Large-amount exchange
                    - value: '2'
                      label: Small-amount exchange (within the pool)
                    - value: '3'
                      label: Small-amount exchange (beyond the pool)
                  example: '0'
                  description: |-
                    change_type.
                service_strategy:
                  type: string
                channel_name:
                  type: string
                  description: Required when it is not the default strategy and channels must be distinguished.
                support_currency:
                  type: array
                  description: |-
                    An array of supported currency pairs, each a `USD/HKD`-style string (assembled from each item's currency_prefix/currency_suffix in support_currencies).

                    The candidate currency pairs come from the supported currency-pair list returned by `POST /forex/query_support_currency`.
                  items:
                    type: string
                  x-value-source:
                    endpoint: POST /forex/query_support_currency
                support_currencies:
                  type: array
                  description: |-
                    The request field support_currency raw array, which is moved entirely under the
                    support_currencies key before submission.
                  items:
                    type: string
                service_time:
                  type: string
                  description: |-
                    A JSON string assembled from the segmented request fields
                    service_time.time_zones/time_rule/time_rule_0, etc. The structure is fairly complex
                    (covering time zones and time-slot rules); the inner fields are not exhaustively
                    enumerated here.
                is_combine:
                  type: boolean
                  description: |-
                    Boolean flag for is combine.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                forex_mode:
                  type: string
                  description: |-
                    forex_mode. The set of accepted values could not be confirmed from the frontend; refer to the backend definition.
                bear_wave:
                  type: string
                  description: |-
                    bear_wave. The set of accepted values could not be confirmed from the frontend; refer to the backend definition.
                currency_prefix:
                  type: string
                currency_suffix:
                  type: string
            example:
              is_default: false
              change_type: ''
              service_strategy: ''
              channel_name: ''
              support_currency:
                - USD/HKD
              support_currencies:
                - currency_prefix: USD
                  currency_suffix: HKD
              service_time: '{"time_zones":"hk_trade_day","time_rule":"09:00:00-16:00:00"}'
              is_combine: false
              forex_mode: ''
              bear_wave: ''
              currency_prefix: ''
              currency_suffix: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/add/exchange/balance:
    post:
      operationId: add_exchange_balance
      summary: Add Exchange Balance
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchange_pool_config_update
      x-lbonly: false
      description: |-
        Create an FX-pool quota configuration.

        ### Request parameters

        - Required: `params` (configuration content)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `params` carries the FX-pool quota configuration content (including currency and related information); submitting it creates one FX-pool quota configuration.

        ### Risk warning

        This operation adds an FX-pool quota configuration and may affect the quota available for FX business. Confirm the configuration content before calling.

        ### Result

        On success the FX-pool quota configuration is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                params:
                  type: object
                  required:
                    - currency
                    - alarm_amount
                  properties:
                    currency:
                      type: string
                      description: Currency
                    alarm_amount:
                      type: number
                      description: Alarm threshold amount
                    remaining_amount:
                      type: number
                      description: Remaining quota adjustment
                    remark:
                      type: string
                      description: Remark
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/modify/exchange/balance:
    post:
      operationId: modify_exchange_balance
      summary: Modify Exchange Balance
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchange_pool_config_update
      x-lbonly: false
      description: |-
        Edit an FX-pool quota configuration.

        ### Request parameters

        - Required: `id` (config ID), `params` (configuration content)
        - Optional: `audit_frontend_resource` (caller-provided audit-log payload)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` is taken from the target record; `params` follows the same structure as "Create FX-pool quota configuration" (including `currency`).

        ### Risk warning

        This operation changes an FX-pool quota configuration and may affect the quota available for FX business. Confirm the target record and configuration content before calling.

        ### Result

        On success the FX-pool quota configuration is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - params
              properties:
                id:
                  type: string
                  description: Taken from the row data record.id.
                params:
                  type: object
                  description: |-
                    Same as "Add Large FX Configuration": currency (disabled/uneditable when editing),
                    alarm_amount, remaining_amount, and remark.
                audit_frontend_resource:
                  type: object
                  description: Taken from the row data record before editing; not a business field.
            example:
              id: '1'
              params:
                currency: USD
                alarm_amount: '10000'
                remaining_amount: '50000'
                remark: ''
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/delete/bank/balance:
    post:
      operationId: delete_bank_balance
      summary: Delete Exchange Balance
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchange_pool_config_update
        - lb_hk_outcashpackagerule.bankbalance_update
      x-lbonly: false
      description: |-
        Delete an FX-pool quota configuration.

        ### Request parameters

        - Required: `id` (record ID)
        - Optional: `business_type`
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` identifies the FX-pool quota configuration record to delete.

        ### Risk warning

        This operation deletes an FX-pool quota configuration and may affect the quota available for FX business. Confirm the target record before calling.

        ### Result

        On success the FX-pool quota configuration is deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Record ID associated with this request.
                business_type:
                  type: string
                  description: Identifies the business scenario to which this request applies.
            example:
              id: '123'
              business_type: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/rate_reference_batch_add:
    post:
      operationId: rate_reference_batch_add
      summary: Batch Add Rate Reference
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.rate_reference_update
      x-lbonly: false
      description: |-
        Batch-create reference-rate configurations, submitting multiple reference-rate entries at once.

        ### Request parameters

        - Required: `data` (reference-rate entry list)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `data` is the reference-rate entry list, submitted as a JSON array; each item corresponds to one reference-rate configuration.

        ### Risk warning

        This operation batch-creates reference-rate configurations, and reference rates affect the calculation of executed FX prices. Confirm the entry list before calling.

        ### Result

        On success the reference-rate configurations are batch-created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - data
              properties:
                data:
                  type: array
                  description: Submit Data as a JSON array. This field is required.
                  items:
                    type: string
            example:
              data: []
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/rate_reference_update:
    post:
      operationId: rate_reference_update
      summary: Update Rate Reference
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.rate_reference_update
      x-lbonly: false
      description: |-
        Edit a reference-rate configuration, used to change a reference rate's channel, currency pair, and spread settings.

        ### Request parameters

        - Required: `id` (reference-rate record ID), `channel_name` (FX channel), `from_currency`, `to_currency`, `spread_mode`, `bp` (spread value)
        - Optional: `audit_frontend_resource` (audit snapshot with the pre-edit original data)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        Allowed values for `channel_name` come from the ref `spot_forex_sources`; those for `from_currency` and `to_currency` come from the ref `tenant_currencies_for_exchange` and are mutually exclusive. Allowed values for `spread_mode` come from the ref `forex_spread_mode`. `bp` ranges from `0` to `100` with up to 4 decimal places.

        ### Risk warning

        This operation changes a reference rate and its spread configuration, directly affecting the calculation of executed FX prices. Confirm the target record and parameters before calling.

        ### Result

        On success the reference-rate configuration is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - channel_name
                - from_currency
                - to_currency
                - spread_mode
                - bp
              properties:
                id:
                  type: string
                  description: The ID of the reference-exchange-rate record to edit, taken from the list row data; required when editing.
                channel_name:
                  type: string
                  description: The rate-source channel code; the allowed values come from ref `spot_forex_sources`.
                from_currency:
                  type: string
                  description: A three-letter currency code; the allowed values come from the caller ref `tenant_currencies_for_exchange`.
                to_currency:
                  type: string
                  description: |-
                    Three-letter currency code; the allowed values come from the caller ref
                    `tenant_currencies_for_exchange`, and it is mutually exclusive with from_currency.
                spread_mode:
                  type: string
                  description: |-
                    Enum from the caller ref `forex_spread_mode`; in client logic, when the value is
                    RATE the markup is by percentage (%), otherwise the markup is a fixed value.
                  enum:
                    - VALUE
                    - RATE
                  x-enum-details:
                    - value: VALUE
                      label: Value
                    - value: RATE
                      label: Ratio
                  example: VALUE
                bp:
                  type: string
                  description: |-
                    Range 0-100, up to 4 decimal places (client validation regex /^\d+(\.\d{1,4})?$/);
                    when spread_mode is RATE it means a percentage markup, otherwise a fixed-value markup.
                audit_frontend_resource:
                  type: object
                  description: |-
                    A snapshot of the original record before editing (client originData/queryData),
                    used only for back-office audit trail; not a business field.
            example:
              id: '12345'
              channel_name: CIBC
              from_currency: USD
              to_currency: HKD
              spread_mode: RATE
              bp: '0.5'
              audit_frontend_resource:
                channel_name: CIBC
                from_currency: USD
                to_currency: HKD
                bp: '0.3'
                spread_mode: RATE
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/rate_reference_delete:
    post:
      operationId: rate_reference_delete
      summary: Delete Reference Exchange Rate Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.rate_reference_update
      x-lbonly: false
      description: |-
        Delete a reference-rate configuration.

        ### Request parameters

        - Required: `id` (config ID)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` identifies the reference-rate configuration record to delete.

        ### Risk warning

        This operation deletes a reference-rate configuration, and reference rates affect the calculation of executed FX prices. Confirm the target record before calling.

        ### Result

        On success the reference-rate configuration is deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/user_rate_add_batch:
    post:
      operationId: user_rate_add_batch
      summary: Batch Add User Rate
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.rate_reference_update
      x-lbonly: false
      description: |-
        Batch-create user execution spread rates, submitting multiple user-rate entries at once.

        ### Request parameters

        - Required: `data` (user-rate entry list)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `data` is the user execution spread-rate entry list, submitted as a JSON array; each item corresponds to one user-rate configuration.

        ### Risk warning

        This operation batch-creates user execution spread rates, directly affecting the executed FX prices for the relevant users. Confirm the entry list before calling.

        ### Result

        On success the user execution spread rates are batch-created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - data
              properties:
                data:
                  type: array
                  description: Submit Data as a JSON array. This field is required.
                  items:
                    type: string
            example:
              data: []
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/user_rate_update:
    post:
      operationId: user_rate_update
      summary: Update User Rate
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.fee_update
      x-lbonly: false
      description: |-
        Edit a user execution spread rate, used to change its effective time, currency pair, and spread settings.

        ### Request parameters

        - Required: `id` (config ID), `start_time`, `end_time`, `from_currency` (source currency), `to_currency` (target currency), `default_effective` (whether it is effective by default, with no time window)
        - Optional: `action_user`, `channel_name`, `is_default`, `rate_source`, `rate_use_in`, `reference_rate`, `service_strategy`, `spread_mode`, `spread_value` (spread-band configuration), `audit_frontend_resource` (caller-provided audit-log payload)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        When `default_effective` is `true` there is no time window, and `start_time`/`end_time` are fixed to `'0'`; otherwise they carry the effective range as unix seconds.

        ### Risk warning

        This operation changes a user execution spread rate, directly affecting the executed FX prices for the relevant user. Confirm the target record and parameters before calling.

        ### Result

        On success the user execution spread rate is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - end_time
                - start_time
                - from_currency
                - to_currency
                - default_effective
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
                action_user:
                  type: string
                  description: |-
                    action_user. A free-form value (not an enum); provide the actual business value.
                channel_name:
                  type: string
                  description: Removed before submission (not sent) when service_strategy=BATCH (batch strategy).
                end_time:
                  type: string
                  description: |-
                    For a custom effective period, pass the range picker's `end_time` as a Unix timestamp
                    in seconds. For the default period, pass `0`.
                start_time:
                  type: string
                  description: Same logic as end_time.
                from_currency:
                  type: string
                  description: This field is required.
                to_currency:
                  type: string
                  description: This field is required.
                is_default:
                  type: string
                  description: |-
                    is_default. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                rate_source:
                  type: string
                rate_use_in:
                  type: string
                  description: |-
                    rate_use_in. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                reference_rate:
                  type: string
                service_strategy:
                  type: string
                spread_mode:
                  type: string
                  description: |-
                    spread_mode. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                spread_value:
                  type: array
                  description: Array elements {start_value, end_value}, split from the request field range_value interval.
                  items:
                    type: string
                default_effective:
                  type: string
                  description: |-
                    This field is required.
                  enum:
                    - 'true'
                    - 'false'
                  x-enum-details:
                    - value: 'true'
                      label: Long-term (no time window)
                    - value: 'false'
                      label: Time period
                  example: 'true'
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              id: '1'
              action_user: ''
              channel_name: ''
              end_time: '0'
              start_time: '0'
              from_currency: USD
              to_currency: HKD
              is_default: 'true'
              rate_source: ''
              rate_use_in: ''
              reference_rate: ''
              service_strategy: ''
              spread_mode: ''
              spread_value:
                - start_value: '0'
                  end_value: ''
              default_effective: 'true'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/rate_delete:
    post:
      operationId: rate_delete
      summary: Delete Rate Markup
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.rate_reference_update
      x-lbonly: false
      description: |-
        Delete an execution spread (rate markup) configuration.

        ### Request parameters

        - Required: `id` (config ID)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` identifies the execution spread configuration record to delete.

        ### Risk warning

        This operation deletes an execution spread configuration and may affect the calculation of the related executed FX prices. Confirm the target record before calling.

        ### Result

        On success the execution spread configuration is deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/amount_config_add:
    post:
      operationId: amount_config_add
      summary: Add Amount Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.amount_config_update
      x-lbonly: false
      description: |-
        Create an FX large-amount limit configuration, setting the amount threshold that triggers large FX for a currency.

        ### Request parameters

        - Required: `currency`, `amount` (large-amount threshold)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        Allowed values for `currency` change with business and are not fixed; call `GET /v1/datasets/refs/tenant_currencies_for_exchange` to obtain the currently available values. `amount` is the threshold at which FX in that currency is treated as large.

        ### Risk warning

        This operation adds an FX large-amount limit configuration and may affect large-amount determination and handling in FX. Confirm the currency and threshold before calling.

        ### Result

        On success the FX large-amount limit configuration is created; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currency
                - amount
              properties:
                currency:
                  type: string
                  description: |-
                    Dropdown selection; value comes from the tenant_currencies_for_exchange dynamic enum, not fixed.

                    Call `GET /v1/datasets/refs/tenant_currencies_for_exchange` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_for_exchange
                amount:
                  type: number
                  description: Amount involved in this business operation. This field is required.
            example:
              currency: USD
              amount: '10000'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/amount_config_update:
    post:
      operationId: amount_config_update
      summary: Update Amount Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.amount_config_update
      x-lbonly: false
      description: |-
        Edit an FX large-amount limit configuration, used to change the large-amount threshold for a currency.

        ### Request parameters

        - Required: `id` (config ID), `currency`, `amount` (large-amount threshold)
        - Optional: `audit_frontend_resource` (caller-provided audit-log payload)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` is taken from the target record; `amount` is the threshold at which FX in that currency is treated as large.

        ### Risk warning

        This operation changes an FX large-amount limit configuration and may affect large-amount determination and handling in FX. Confirm the target record and parameters before calling.

        ### Result

        On success the FX large-amount limit configuration is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - currency
                - amount
              properties:
                id:
                  type: string
                  description: Taken from the row data record.id.
                currency:
                  type: string
                  description: |-
                    Currency. Options come from the data dictionary `tenant_currencies_for_exchange` (retrieve via `GET /v1/datasets/refs/tenant_currencies_for_exchange`).
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_for_exchange
                amount:
                  type: number
                  description: Amount involved in this business operation. This field is required.
                audit_frontend_resource:
                  type: object
                  description: Taken from the row data record before editing; not a business field.
            example:
              id: '1'
              currency: USD
              amount: '10000'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      msg:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /forex/amount_config_delete:
    post:
      operationId: amount_config_delete
      summary: Delete Amount Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.amount_config_update
      x-lbonly: false
      description: |-
        Delete an FX large-amount limit configuration.

        ### Request parameters

        - Required: `id` (record ID)
        - See each field's definition for its format, value range, and business meaning.

        ### Business notes

        `id` identifies the FX large-amount limit configuration record to delete.

        ### Risk warning

        This operation deletes an FX large-amount limit configuration and may affect large-amount determination and handling in FX. Confirm the target record before calling.

        ### Result

        On success the FX large-amount limit configuration is deleted; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Record ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/clearing_busin_flows/download:
    post:
      operationId: clearing_busin_flows_download
      summary: Clearing Day-End Cutoff - Day-End Market Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Export the "Clearing day-cut - end-of-day market configuration" data to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - When the export record is no longer needed, call `DELETE /v1/datasets/download/{id}` to remove it.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_busin_flows.yaml (the query capability).
                  properties:
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - busin_no
                      - flow_name
                      - markets
                      - operator
                      - operator_val
                      - updated_at
                    example: busin_no
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Clearing Day-End Cutoff - Day-End Market Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_busin_flows
        operationId: clearing_busin_flows
  /v1/datasets/clearing_busin_flows:
    post:
      operationId: clearing_busin_flows
      summary: Clearing Day-End Cutoff - Day-End Market Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Query the "Clearing day-cut - end-of-day market configuration" data. Use `filters` to combine business filter conditions and `page`/`page_size` to control pagination.

        ### Returns

        On success, a list of matching records with pagination information is returned. See each filter field's definition for its purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Clearing Day-End Cutoff - Day-End Market Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            busin_no:
                              type: string
                              description: '**Returned value:** Busin No.'
                            flow_name:
                              type: string
                              description: '**Returned value:** Flow Name.'
                            markets:
                              type: string
                              description: '**Returned value:** Markets.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                            operator_val:
                              type: string
                              description: Raw value of `operator`. The `operator` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - busin_no: '1'
                      flow_name: 欧美市场
                      markets: '{US}'
                      operator: Zhang San
                      operator_val: user@example.com
                      updated_at: '1784150120'
                  total: 5
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_busin_flows/download
        operationId: clearing_busin_flows_download
  /v1/datasets/clearing_ready_tasks/download:
    post:
      operationId: clearing_ready_tasks_download
      summary: Clearing - Pre-Clearing Preparation Task Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Export the "Clearing - pre-clearing readiness tasks" data to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - When the export record is no longer needed, call `DELETE /v1/datasets/download/{id}` to remove it.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_ready_tasks.yaml (the query capability).
                  properties:
                    task_code:
                      type: string
                      description: |-
                        Filters results by Check Items.

                        Values are dynamic. Call `GET /v1/datasets/refs/clearing_ready_tasks` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_ready_tasks
                    flow:
                      type: string
                      description: Filters results by Check Phase.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Trade Clearing
                        - value: '2'
                          label: Day End
                      example: '1'
                    process_mode:
                      type: string
                      description: Filters results by Post-Failure Treatment.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '0'
                          label: Tip Only
                        - value: '1'
                          label: Ticket Process
                        - value: '2'
                          label: Forced Calibration
                      example: '0'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - task_code
                      - task_code_val
                      - flow
                      - flow_val
                      - process_mode
                      - process_mode_val
                      - is_enable
                      - operator
                      - operator_val
                      - updated_at
                    example: task_code
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Clearing - Pre-Clearing Preparation Task Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_ready_tasks
        operationId: clearing_ready_tasks
  /v1/datasets/clearing_ready_tasks:
    post:
      operationId: clearing_ready_tasks
      summary: Clearing - Pre-Clearing Preparation Task
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Query the "Clearing - pre-clearing readiness tasks" data. Use `filters` to combine business filter conditions and `page`/`page_size` to control pagination.

        ### Returns

        On success, a list of matching records with pagination information is returned. See each filter field's definition for its purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    task_code:
                      type: string
                      description: |-
                        Filters results by Check Items.

                        Values are dynamic. Call `GET /v1/datasets/refs/clearing_ready_tasks` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_ready_tasks
                    flow:
                      type: string
                      description: Filters results by Check Phase.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Trade Clearing
                        - value: '2'
                          label: Day End
                      example: '1'
                    process_mode:
                      type: string
                      description: Filters results by Post-Failure Treatment.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '0'
                          label: Tip Only
                        - value: '1'
                          label: Ticket Process
                        - value: '2'
                          label: Forced Calibration
                      example: '0'
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters:
                task_code: ready_transaction_file_import
                flow: '1'
                process_mode: '2'
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Clearing - Pre-Clearing Preparation Task records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            task_code:
                              type: string
                              description: '**Returned value:** Check Items.'
                            task_code_val:
                              type: string
                              description: Raw value of `task_code`. The `task_code` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            flow:
                              type: string
                              description: '**Returned value:** Check Phase.'
                            flow_val:
                              type: string
                              description: Raw value of `flow`. The `flow` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            process_mode:
                              type: string
                              description: '**Returned value:** Post-Failure Treatment.'
                            process_mode_val:
                              type: string
                              description: Raw value of `process_mode`. The `process_mode` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            is_enable:
                              type: string
                              description: '**Returned value:** Indicates whether enable/disable.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                            operator_val:
                              type: string
                              description: Raw value of `operator`. The `operator` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - task_code: string
                      task_code_val: string
                      flow: string
                      flow_val: string
                      process_mode: string
                      process_mode_val: string
                      is_enable: string
                      operator: string
                      operator_val: string
                      updated_at: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_ready_tasks/download
        operationId: clearing_ready_tasks_download
  /v1/datasets/clearing_system_configs/download:
    post:
      operationId: clearing_system_configs_download
      summary: Clearing - System Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Export the "Clearing - system configuration" data to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - When the export record is no longer needed, call `DELETE /v1/datasets/download/{id}` to remove it.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_system_configs.yaml (the query capability).
                  properties:
                    remark:
                      type: string
                      description: Filters results by Configuration Items.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - config_code
                      - config_code_val
                      - enable
                      - config_type
                      - config_type_val
                      - val
                      - maker
                      - maker_val
                      - created_at
                    example: config_code
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Clearing - System Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_system_configs
        operationId: clearing_system_configs
  /v1/datasets/clearing_system_configs:
    post:
      operationId: clearing_system_configs
      summary: Clearing - System Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Query clearing-system configuration records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    remark:
                      type: string
                      description: Filters results by Configuration Items.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Clearing - System Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            config_code:
                              type: string
                              description: '**Returned value:** Configuration Items.'
                            config_code_val:
                              type: string
                              description: Raw value of `config_code`. The `config_code` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            enable:
                              type: string
                              description: '**Returned value:** Indicates whether enable/disable.'
                            config_type:
                              type: string
                              description: '**Returned value:** Type.'
                            config_type_val:
                              type: string
                              description: Raw value of `config_type`. The `config_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            val:
                              type: string
                              description: '**Returned value:** Other parameters.'
                            maker:
                              type: string
                              description: '**Returned value:** Operator.'
                            maker_val:
                              type: string
                              description: Raw value of `maker`. The `maker` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - config_code: Deposit interest switch
                      config_code_val: '1001'
                      enable: '0'
                      config_type: Switch
                      config_type_val: '0'
                      val: ''
                      maker: 木木
                      maker_val: '21206'
                      created_at: '1775631987'
                  total: 23
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_system_configs/download
        operationId: clearing_system_configs_download
  /v1/datasets/atm_withdraw_accounts/download:
    post:
      operationId: atm_withdraw_accounts_download
      summary: Company Bank Account Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_fund.companybank_query
      x-lbonly: false
      description: |-
        Export company bank-account records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_withdraw_accounts.yaml (the query capability).
                  properties:
                    nature_type:
                      type: string
                      description: Filters results by Account Type.
                      enum:
                        - client
                        - house
                      x-enum-details:
                        - value: client
                          label: Client
                        - value: house
                          label: House
                      example: client
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_account_types
                    bank_name:
                      type: string
                      description: Filters results by Bank Name.
                    bank_account:
                      type: string
                      description: Filters results by Bank Account.
                    support_exchange:
                      type: string
                      description: Filters results by Exchange Supported?.
                      enum:
                        - '0'
                        - '1'
                      x-enum-details:
                        - value: '0'
                          label: Not Supported
                        - value: '1'
                          label: Support
                      example: '0'
                    simple_name:
                      type: string
                      description: Filters results by Abbreviation.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - nature_type
                      - currency
                      - currency_val
                      - account_name
                      - bank_name
                      - bank_account
                      - region
                      - swift_code
                      - bank_addr
                      - bank_code
                      - province
                      - city
                      - postal_code
                      - simple_name
                      - custom_in
                      - custom_out
                      - support_exchange
                      - support_exchange_val
                      - updated_at
                      - created_at
                      - remark
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Company Bank Account Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_withdraw_accounts
        operationId: atm_withdraw_accounts
  /v1/datasets/atm_withdraw_accounts:
    post:
      operationId: atm_withdraw_accounts
      summary: Company Bank Account
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_fund.companybank_query
        - accounting.list_accounting_deposit
      x-lbonly: false
      description: |-
        Query company bank-account records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    nature_type:
                      type: string
                      description: Filters results by Account Type.
                      enum:
                        - client
                        - house
                      x-enum-details:
                        - value: client
                          label: Client
                        - value: house
                          label: House
                      example: client
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_account_types
                      example: ALL
                    bank_name:
                      type: string
                      description: Filters results by Bank Name.
                      example: DBS Bank
                    bank_account:
                      type: string
                      description: Filters results by Bank Account.
                    support_exchange:
                      type: string
                      description: Filters results by Exchange Supported?.
                      enum:
                        - '0'
                        - '1'
                      x-enum-details:
                        - value: '0'
                          label: Not Supported
                        - value: '1'
                          label: Support
                      example: Not Supported
                    simple_name:
                      type: string
                      description: Filters results by Abbreviation.
                      example: '11'
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Company Bank Account records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            nature_type:
                              type: string
                              description: '**Returned value:** Account Nature.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            account_name:
                              type: string
                              description: '**Returned value:** Account Name.'
                            bank_name:
                              type: string
                              description: '**Returned value:** Bank Name.'
                            bank_account:
                              type: string
                              description: '**Returned value:** Bank Account.'
                            region:
                              type: string
                              description: '**Returned value:** Country Region.'
                            swift_code:
                              type: string
                              description: '**Returned value:** Swift Code.'
                            bank_addr:
                              type: string
                              description: '**Returned value:** Bank Address.'
                            bank_code:
                              type: string
                              description: '**Returned value:** ABA routing number.'
                            province:
                              type: string
                              description: '**Returned value:** Province.'
                            city:
                              type: string
                              description: '**Returned value:** City.'
                            postal_code:
                              type: string
                              description: '**Returned value:** Postal Code.'
                            simple_name:
                              type: string
                              description: '**Returned value:** Abbreviation.'
                            custom_in:
                              type: string
                              description: '**Returned value:** Deposit Channel.'
                            custom_out:
                              type: string
                              description: '**Returned value:** Withdrawal Channel.'
                            support_exchange:
                              type: string
                              description: '**Returned value:** Exchange Supported?.'
                            support_exchange_val:
                              type: string
                              description: Raw value of `support_exchange`. The `support_exchange` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            created_at:
                              type: string
                              description: '**Returned value:** Create Date. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '1866'
                      nature_type: client
                      currency: ALL
                      currency_val: ALL
                      account_name: test
                      bank_name: DBS Bank
                      bank_account: Zhang San
                      region: SINGAPORE
                      swift_code: DBSSSGSG
                      bank_addr: 8 Finance Street, Central, Hong Kong
                      bank_code: ''
                      province: ''
                      city: ''
                      postal_code: ''
                      simple_name: '11'
                      custom_in: ''
                      custom_out: ''
                      support_exchange: Not Supported
                      support_exchange_val: '0'
                      updated_at: '1784171302'
                      created_at: '1784171302'
                      remark: ''
                  total: 122
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_withdraw_accounts/download
        operationId: atm_withdraw_accounts_download
  /v1/datasets/atm_currency_configs:
    post:
      operationId: atm_currency_configs
      summary: Currency Setup
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Query currency configuration records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    currency_iso:
                      type: string
                      description: |-
                        Filters results by Currency code.

                        **Allowed values:** `USD`, `HKD`, `SGD`, `CNY`, `AUD`, `JPY`, `GBP`, `EUR`, `CAD`, `MYR`, `AMD`, `BGN`, `BTN`, `BWP`, `BZD`, `CDF`, `CHF`, `DKK`, `GHS`, `GMD`, `GNF`, `HRK`, `HUF`, `ILS`, `INR`, `IQD`, `IRR`, `ISK`, `LRD`, `LYD`, `MOP`, `RUB`, `UAH`, `UGX`, `UYI`, `UYU`, `UZS`, `XAF`, `XOF`, `XSU`, `YER`, `IDR`, `TWD`, `THB`, `KRW`, `NZD`
                      enum:
                        - USD
                        - HKD
                        - SGD
                        - CNY
                        - AUD
                        - JPY
                        - GBP
                        - EUR
                        - CAD
                        - MYR
                        - AMD
                        - BGN
                        - BTN
                        - BWP
                        - BZD
                        - CDF
                        - CHF
                        - DKK
                        - GHS
                        - GMD
                        - GNF
                        - HRK
                        - HUF
                        - ILS
                        - INR
                        - IQD
                        - IRR
                        - ISK
                        - LRD
                        - LYD
                        - MOP
                        - RUB
                        - UAH
                        - UGX
                        - UYI
                        - UYU
                        - UZS
                        - XAF
                        - XOF
                        - XSU
                        - YER
                        - IDR
                        - TWD
                        - THB
                        - KRW
                        - NZD
                      x-enum-details:
                        - value: USD
                          label: USD
                        - value: HKD
                          label: HKD
                        - value: SGD
                          label: SGD
                        - value: CNY
                          label: CNY
                        - value: AUD
                          label: AUD
                        - value: JPY
                          label: JPY
                        - value: GBP
                          label: GBP
                        - value: EUR
                          label: EUR
                        - value: CAD
                          label: CAD
                        - value: MYR
                          label: MYR
                        - value: AMD
                          label: AMD
                        - value: BGN
                          label: BGN
                        - value: BTN
                          label: BTN
                        - value: BWP
                          label: BWP
                        - value: BZD
                          label: BZD
                        - value: CDF
                          label: CDF
                        - value: CHF
                          label: CHF
                        - value: DKK
                          label: DKK
                        - value: GHS
                          label: GHS
                        - value: GMD
                          label: GMD
                        - value: GNF
                          label: GNF
                        - value: HRK
                          label: HRK
                        - value: HUF
                          label: HUF
                        - value: ILS
                          label: ILS
                        - value: INR
                          label: INR
                        - value: IQD
                          label: IQD
                        - value: IRR
                          label: IRR
                        - value: ISK
                          label: ISK
                        - value: LRD
                          label: LRD
                        - value: LYD
                          label: LYD
                        - value: MOP
                          label: MOP
                        - value: RUB
                          label: RUB
                        - value: UAH
                          label: UAH
                        - value: UGX
                          label: UGX
                        - value: UYI
                          label: UYI
                        - value: UYU
                          label: UYU
                        - value: UZS
                          label: UZS
                        - value: XAF
                          label: XAF
                        - value: XOF
                          label: XOF
                        - value: XSU
                          label: XSU
                        - value: YER
                          label: YER
                        - value: IDR
                          label: IDR
                        - value: TWD
                          label: TWD
                        - value: THB
                          label: THB
                        - value: KRW
                          label: KRW
                        - value: NZD
                          label: NZD
                      example: AUD
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Currency Setup records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            account_channel:
                              type: string
                              description: '**Returned value:** Account Channel.'
                            currency_iso:
                              type: string
                              description: '**Returned value:** Currency code.'
                            standard_money:
                              type: string
                              description: '**Returned value:** Standard Currency.'
                            abbrev_name_cn:
                              type: string
                              description: '**Returned value:** Abbreviation In Simplified Chinese.'
                            abbrev_name_hk:
                              type: string
                              description: '**Returned value:** Abbreviation In Traditional Chinese.'
                            name_cn:
                              type: string
                              description: '**Returned value:** Full Name In Simplified Chinese.'
                            name_hk:
                              type: string
                              description: '**Returned value:** Full Name In Traditional Chinese.'
                            currency:
                              type: string
                              description: '**Returned value:** Displayed Currency.'
                            icon:
                              type: string
                              description: '**Returned value:** Icon.'
                            min_exchange_amount:
                              type: string
                              description: '**Returned value:** Minimum Exchange Amount.'
                            min_withdrawal_amount:
                              type: string
                              description: '**Returned value:** Minimum Withdrawal Amount.'
                            min_cash_amount:
                              type: string
                              description: '**Returned value:** Cash Amount.'
                            app_show:
                              type: string
                              description: '**Returned value:** Asset Display.'
                            support_withdraw:
                              type: string
                              description: '**Returned value:** Withdrawal Function.'
                            support_deposit:
                              type: string
                              description: '**Returned value:** Deposit Feature.'
                            support_exchange:
                              type: string
                              description: '**Returned value:** Exchange Feature.'
                            exchange_rate_precision:
                              type: string
                              description: '**Returned value:** Exchange Rate Precision.'
                            amount_precision:
                              type: string
                              description: '**Returned value:** Amount Precision.'
                            amount_round_mode:
                              type: string
                              description: '**Returned value:** Retrieval Logic.'
                            amount_round_mode_val:
                              type: string
                              description: Raw value of `amount_round_mode`. The `amount_round_mode` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remarks:
                              type: string
                              description: '**Returned value:** Remark.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '20'
                      account_channel: longport_test
                      currency_iso: AUD
                      standard_money: 'no'
                      abbrev_name_cn: 澳元
                      abbrev_name_hk: 澳元
                      name_cn: 澳大利亚元
                      name_hk: 澳大利亞元
                      currency: AUD
                      icon: AUD
                      min_exchange_amount: '3.00'
                      min_withdrawal_amount: '12.00'
                      min_cash_amount: '0.00'
                      app_show: 'yes'
                      support_withdraw: 'yes'
                      support_deposit: 'yes'
                      support_exchange: 'yes'
                      exchange_rate_precision: '2'
                      amount_precision: '2'
                      amount_round_mode: Rounding Off
                      amount_round_mode_val: round
                      created_at: '1655956642'
                      updated_at: '1783972827'
                      remarks: ''
                  total: 15
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/account_settlement_accounts:
    post:
      operationId: account_settlement_accounts
      summary: Settle Chnl Acct
      tags:
        - Service Parameter
      x-permission-key:
        - atm.settle_chnl_bank_acct_inquiry
      x-lbonly: false
      description: |-
        Query settlement-channel account records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    currency_type:
                      type: string
                      description: Filters results by Currency Type.
                      enum:
                        - fiat
                        - crypto
                      x-enum-details:
                        - value: fiat
                          label: Fiat
                        - value: crypto
                          label: Crypto
                      example: Fiat
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_account_types
                      example: ALL
                    bank_id:
                      type: string
                      description: |-
                        Filters results by Bank Name.

                        Values are dynamic. Call `GET /v1/datasets/refs/region_banks_with_all` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/region_banks_with_all
                    bank_account:
                      type: string
                      description: Filters results by Bank Account.
                    settlement_channel:
                      type: string
                      description: |-
                        Filters results by Settle Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/settlement_channel_for_deposit_withdraw` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/settlement_channel_for_deposit_withdraw
                      example: settlement_hashkey_hk
                    id:
                      type: string
                      description: Filters results by ID.
                      example: '1174'
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Settle Chnl Acct records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            settlement_channel:
                              type: string
                              description: '**Returned value:** Settle Channel.'
                            currency_type:
                              type: string
                              description: '**Returned value:** Currency Type.'
                            currency_type_val:
                              type: string
                              description: Raw value of `currency_type`. The `currency_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            account_name:
                              type: string
                              description: '**Returned value:** Account Name.'
                            multi_name:
                              type: string
                              description: '**Returned value:** Bank Name.'
                            bank_account:
                              type: string
                              description: '**Returned value:** Bank Account.'
                            region:
                              type: string
                              description: '**Returned value:** Country Region.'
                            swift_code:
                              type: string
                              description: '**Returned value:** Swift Code.'
                            bank_addr:
                              type: string
                              description: '**Returned value:** Bank Address.'
                            withdraw_types:
                              type: string
                              description: '**Returned value:** Withdrawal Channel.'
                            withdraw_types_val:
                              type: string
                              description: Raw value of `withdraw_types`. The `withdraw_types` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            updated_admin_id:
                              type: string
                              description: '**Returned value:** Updater.'
                            updated_admin_id_val:
                              type: string
                              description: Raw value of `updated_admin_id`. The `updated_admin_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Create Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '1174'
                      settlement_channel: settlement_hashkey_hk
                      currency_type: Fiat
                      currency_type_val: fiat
                      currency: ALL
                      currency_val: ALL
                      account_name: 法币1
                      multi_name: 041 Chong Hing Bank Limited
                      bank_account: Zhang San
                      region: Hong Kong
                      swift_code: LCHBHKHH
                      bank_addr: 8 Finance Street, Central, Hong Kong
                      withdraw_types: Account Withdrawal,Account Transfer
                      withdraw_types_val: VA_WITHDRAW,VA_TRANSFER
                      remark: ''
                      updated_admin_id: Zhang San
                      updated_admin_id_val: '21408'
                      created_at: '1780898477'
                      updated_at: '1780898477'
                  total: 6
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/account_fund_transfer_rules:
    post:
      operationId: account_fund_transfer_rules
      summary: Funds Alloc Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.funds_alloc_config_inquiry
      x-lbonly: false
      description: |-
        Query fund-transfer rule records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by ID.
                      example: '2105'
                    currency_type:
                      type: string
                      description: Filters results by Currency Type.
                      enum:
                        - fiat
                        - crypto
                      x-enum-details:
                        - value: fiat
                          label: Fiat
                        - value: crypto
                          label: Crypto
                      example: fiat
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_account_types
                      example: HKD
                    transfer_type:
                      type: string
                      description: Filters results by Allocation Type.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                        - '4'
                      x-enum-details:
                        - value: '1'
                          label: Interbank transfer
                        - value: '2'
                          label: Settlement account recharge
                        - value: '3'
                          label: Settlement account withdrawal
                        - value: '4'
                          label: Inter-settlement account transfer
                      example: Interbank transfer
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Enabled
                        - value: '2'
                          label: Disabled
                      example: Disabled
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                    updated_at:
                      type: object
                      description: Filters results by Updated Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Funds Alloc Config records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            transfer_type:
                              type: string
                              description: '**Returned value:** Allocation Type.'
                            transfer_type_val:
                              type: string
                              description: Raw value of `transfer_type`. The `transfer_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            from_type:
                              type: string
                              description: '**Returned value:** Payment Account Info - Payment Channel.'
                            from_type_val:
                              type: string
                              description: Raw value of `from_type`. The `from_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            from_main_account:
                              type: string
                              description: '**Returned value:** Payment Account Info - Main Account.'
                            from_main_account_type:
                              type: string
                              description: '**Returned value:** Payment Account Info - Main Account Type.'
                            from_bank_name:
                              type: string
                              description: '**Returned value:** Payment Account Info - Bank Name.'
                            from_account_name:
                              type: string
                              description: '**Returned value:** Payment Account Info - Bank Account Name.'
                            from_account:
                              type: string
                              description: '**Returned value:** Payment Account Info - Bank Account No..'
                            custom_from:
                              type: string
                              description: '**Returned value:** Payment Account Info.'
                            allocation_channel_val:
                              type: string
                              description: '**Returned value:** Allocation Channel.'
                            transfer_strategy:
                              type: string
                              description: '**Returned value:** Allocation Strategy.'
                            transfer_strategy_val:
                              type: string
                              description: Raw value of `transfer_strategy`. The `transfer_strategy` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_package_type:
                              type: string
                              description: '**Returned value:** Frequency.'
                            to_type:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Deposit Channel.'
                            to_type_val:
                              type: string
                              description: Raw value of `to_type`. The `to_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            to_main_account:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Main Account.'
                            to_main_account_type:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Main Account Type.'
                            to_bank_name:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Bank Name.'
                            to_account_name:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Bank Account Name.'
                            to_account:
                              type: string
                              description: '**Returned value:** Receiving Account Info - Bank Account No..'
                            custom_to:
                              type: string
                              description: '**Returned value:** Receiving Account Info.'
                            currency_type:
                              type: string
                              description: '**Returned value:** Currency Type.'
                            currency_type_val:
                              type: string
                              description: Raw value of `currency_type`. The `currency_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            currency:
                              type: string
                              description: '**Returned value:** Currency to Alloc.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            amount_show:
                              type: string
                              description: '**Returned value:** Amt to Alloc.'
                            amount_show_val:
                              type: string
                              description: Raw value of `amount_show`. The `amount_show` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            postscript:
                              type: string
                              description: '**Returned value:** Transfer Memo.'
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            updated_admin_id:
                              type: string
                              description: '**Returned value:** Updater.'
                            updated_admin_id_val:
                              type: string
                              description: Raw value of `updated_admin_id`. The `updated_admin_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_time:
                              type: string
                              description: '**Returned value:** Time. The date or time format follows the value returned by the API.'
                            created_at:
                              type: string
                              description: '**Returned value:** Time - Create Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Time - Update Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '2105'
                      transfer_type: Interbank transfer
                      transfer_type_val: '1'
                      from_type: Bank
                      from_type_val: '1'
                      from_main_account: ''
                      from_main_account_type: ''
                      from_bank_name: 020 CMB Wing Lung Bank Ltd
                      from_account_name: 永隆银证CNY
                      from_account: '60601710613'
                      custom_from: |-
                        Payment Channel:Bank

                        Bank Name:020 CMB Wing Lung Bank Ltd
                        Bank Account Name:永隆银证CNY
                        Bank Account No.:60601710613
                      allocation_channel_val: test_API_银证转账
                      transfer_strategy: Work order approval
                      transfer_strategy_val: '2'
                      custom_package_type: Daily set time 00:00
                      to_type: Bank
                      to_type_val: '1'
                      to_main_account: ''
                      to_main_account_type: ''
                      to_bank_name: 353 China Minsheng Bank Corp.,
                      to_account_name: 民生银证转账
                      to_account: sg8888888
                      custom_to: |-
                        Deposit Channel:Bank

                        Bank Name:353 China Minsheng Bank Corp.,
                        Bank Account Name:民生银证转账
                        Bank Account No.:sg8888888
                      currency_type: ''
                      currency_type_val: ''
                      currency: HKD
                      currency_val: HKD
                      amount_show: '1.00'
                      amount_show_val: '1'
                      postscript: '111'
                      status: Disabled
                      status_val: '2'
                      priority: '899'
                      updated_admin_id: Zhang San
                      updated_admin_id_val: '1'
                      custom_time: |-
                        Create Time:2026-07-16 01:04:53
                        Update Time:2026-07-16 01:04:53
                      created_at: '2026-07-16 01:04:53'
                      updated_at: '2026-07-16 01:04:53'
                  total: 80
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/va_coin_configs/download:
    post:
      operationId: va_coin_configs_download
      summary: Cryptocurrency Export
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_inquiry
      x-lbonly: false
      description: |-
        Export cryptocurrency configuration records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of va_coin_configs.yaml (the query capability).
                  properties:
                    coin:
                      type: string
                      description: Filters results by Currency Code.
                    pi_verify:
                      type: string
                      description: Filters results by Require PI Verification.
                      enum:
                        - 'yes'
                        - 'no'
                      x-enum-details:
                        - value: 'yes'
                          label: Required
                        - value: 'no'
                          label: Not Required
                      example: 'yes'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - coin
                      - chain_types
                      - chain_types_val
                      - name_zh_cn
                      - name_zh_hk
                      - name_en
                      - custom_name
                      - icon
                      - conversion_currency
                      - conversion_rate
                      - daily_withdraw_limit
                      - weekly_withdraw_limit
                      - max_per_withdraw
                      - min_per_withdraw
                      - support_withdraw
                      - custom_withdrawal_function
                      - custom_deposit_function
                      - pi_verify
                      - pi_verify_val
                      - require_tag
                      - require_tag_val
                      - deposit_need_micro_verification
                      - quantity_precision
                      - quantity_round_mode_query
                      - quantity_round_mode_query_val
                      - updater
                      - remark
                      - created_at
                      - updated_at
                      - custom_time
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Cryptocurrency Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/va_coin_configs
        operationId: va_coin_configs
  /v1/datasets/va_coin_configs:
    post:
      operationId: va_coin_configs
      summary: Cryptocurrency
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_inquiry
      x-lbonly: false
      description: |-
        Query cryptocurrency configuration records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    coin:
                      type: string
                      description: Filters results by Currency Code.
                      example: BTC
                    pi_verify:
                      type: string
                      description: Filters results by Require PI Verification.
                      enum:
                        - 'yes'
                        - 'no'
                      x-enum-details:
                        - value: 'yes'
                          label: Required
                        - value: 'no'
                          label: Not Required
                      example: Not Required
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Cryptocurrency records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            coin:
                              type: string
                              description: '**Returned value:** Currency Code.'
                            chain_types:
                              type: string
                              description: '**Returned value:** Network.'
                            chain_types_val:
                              type: string
                              description: Raw value of `chain_types`. The `chain_types` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            name_zh_cn:
                              type: string
                              description: '**Returned value:** Name-Simplified Chinese.'
                            name_zh_hk:
                              type: string
                              description: '**Returned value:** Name-Traditional Chinese.'
                            name_en:
                              type: string
                              description: '**Returned value:** Name English.'
                            custom_name:
                              type: string
                              description: '**Returned value:** Name.'
                            icon:
                              type: string
                              description: '**Returned value:** Icon.'
                            conversion_currency:
                              type: string
                              description: '**Returned value:** Equivalent Currency.'
                            conversion_rate:
                              type: string
                              description: '**Returned value:** Conversion Rate.'
                            daily_withdraw_limit:
                              type: string
                              description: '**Returned value:** Withdrawal Function-Daily Limit.'
                            weekly_withdraw_limit:
                              type: string
                              description: '**Returned value:** Withdrawal Function-Weekly Limit.'
                            max_per_withdraw:
                              type: string
                              description: '**Returned value:** Withdrawal Function-Maximum Per Trade.'
                            min_per_withdraw:
                              type: string
                              description: '**Returned value:** Withdrawal Function-Min Withdrawal Amount.'
                            support_withdraw:
                              type: string
                              description: '**Returned value:** Withdrawal Function-Support.'
                            custom_withdrawal_function:
                              type: string
                              description: '**Returned value:** Withdrawal Function.'
                            custom_deposit_function:
                              type: string
                              description: '**Returned value:** Deposit Function.'
                            pi_verify:
                              type: string
                              description: '**Returned value:** Require PI Verification.'
                            pi_verify_val:
                              type: string
                              description: Raw value of `pi_verify`. The `pi_verify` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            require_tag:
                              type: string
                              description: '**Returned value:** Tag.'
                            require_tag_val:
                              type: string
                              description: Raw value of `require_tag`. The `require_tag` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            deposit_need_micro_verification:
                              type: string
                              description: '**Returned value:** Deposit needs micro-verification..'
                            quantity_precision:
                              type: string
                              description: '**Returned value:** Quantity Precision.'
                            quantity_round_mode_query:
                              type: string
                              description: '**Returned value:** Amount fetching logic.'
                            quantity_round_mode_query_val:
                              type: string
                              description: Raw value of `quantity_round_mode_query`. The `quantity_round_mode_query` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updater:
                              type: string
                              description: '**Returned value:** Updater.'
                            remark:
                              type: string
                              description: '**Returned value:** Remarks.'
                            created_at:
                              type: string
                              description: '**Returned value:** Time-Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Time-Update Time. The date or time format follows the value returned by the API.'
                            custom_time:
                              type: string
                              description: '**Returned value:** Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '138'
                      coin: BTC
                      chain_types: BTC,Arbitrum,AvalancheC
                      chain_types_val: BTC,Arbitrum,AvalancheC
                      name_zh_cn: Bitcoin
                      name_zh_hk: Bitcoin
                      name_en: Bitcoin
                      custom_name: |-
                        Simplified: Bitcoin
                        Traditional: Bitcoin
                        English: Bitcoin
                      icon: https://assets-canary.lbkrs.com/uploads/1_40037_ml7a3lru3ogo5ulcox2_bitcoin-btc-logo.png
                      conversion_currency: USD
                      conversion_rate: '0.00000500'
                      daily_withdraw_limit: '0.00000000'
                      weekly_withdraw_limit: '0.00000000'
                      max_per_withdraw: '0.00000000'
                      min_per_withdraw: '0.10000000'
                      support_withdraw: 'yes'
                      custom_withdrawal_function: |-
                        Supported
                        Min Withdrawal Amount: 0.10000000
                      custom_deposit_function: |-
                        Supported
                        Min Deposit Quantity: 0.00020000
                      pi_verify: Not Required
                      pi_verify_val: 'no'
                      require_tag: No Require
                      require_tag_val: 'no'
                      deposit_need_micro_verification: 'yes'
                      quantity_precision: '6'
                      quantity_round_mode_query: Rounding Off
                      quantity_round_mode_query_val: round
                      updater: user@example.com
                      remark: ''
                      created_at: '2025-10-30 17:44:22'
                      updated_at: '2026-07-16 14:12:33'
                      custom_time: |-
                        Created Time: 2025-10-30 17:44:22
                        Update Time: 2026-07-16 14:12:33
                  total: 33
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/va_coin_configs/download
        operationId: va_coin_configs_download
  /v1/datasets/atm_remark_templates/download:
    post:
      operationId: atm_remark_templates_download
      summary: Remarks template Export
      tags:
        - Service Parameter
      x-permission-key:
        - atm.query_lookup
      x-lbonly: false
      description: |-
        Export remark-template records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_remark_templates.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by ID.
                    config_value_multi_lang:
                      type: string
                      description: Filters results by Remarks.
                    updated_at:
                      type: object
                      description: Filters results by Updated At.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                    lookup_category:
                      type: string
                      description: Filters results by Remarks User.
                      enum:
                        - setting_remark_deposit
                        - setting_remark_withdrawal
                        - setting_remark_securitiesdeposit
                        - setting_remark_securitieswithdrawal
                        - setting_remark_accountadjustment
                        - setting_remark_withdrawal_reject
                        - setting_remark_deposit_reject
                        - setting_remark_withdrawal_card_reject
                        - setting_remark_fund
                        - setting_remark_otc_derivatives
                        - setting_remark_deposit_matching
                      x-enum-details:
                        - value: setting_remark_deposit
                          label: Deposit
                        - value: setting_remark_withdrawal
                          label: Withdrawal
                        - value: setting_remark_securitiesdeposit
                          label: Securities Deposit
                        - value: setting_remark_securitieswithdrawal
                          label: Securities Withdrawal
                        - value: setting_remark_accountadjustment
                          label: Account Adjustment
                        - value: setting_remark_withdrawal_reject
                          label: Withdrawal rejection
                        - value: setting_remark_deposit_reject
                          label: Deposit rejection
                        - value: setting_remark_withdrawal_card_reject
                          label: Refuse Add Bank Card
                        - value: setting_remark_fund
                          label: Fund
                        - value: setting_remark_otc_derivatives
                          label: OTC Derivatives
                        - value: setting_remark_deposit_matching
                          label: Deposit Matching
                      example: setting_remark_deposit
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - lookup_category
                      - lookup_category_val
                      - value_en
                      - value_cn
                      - value_hk
                      - priority
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Remarks template Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_remark_templates
        operationId: atm_remark_templates
  /v1/datasets/atm_remark_templates:
    post:
      operationId: atm_remark_templates
      summary: Remarks template
      tags:
        - Service Parameter
      x-permission-key:
        - atm.query_lookup
      x-lbonly: false
      description: |-
        Query remark-template records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by ID.
                      example: '14625'
                    config_value_multi_lang:
                      type: string
                      description: Filters results by Remarks.
                    updated_at:
                      type: object
                      description: Filters results by Updated At.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                    lookup_category:
                      type: string
                      description: Filters results by Remarks User.
                      enum:
                        - setting_remark_deposit
                        - setting_remark_withdrawal
                        - setting_remark_securitiesdeposit
                        - setting_remark_securitieswithdrawal
                        - setting_remark_accountadjustment
                        - setting_remark_withdrawal_reject
                        - setting_remark_deposit_reject
                        - setting_remark_withdrawal_card_reject
                        - setting_remark_fund
                        - setting_remark_otc_derivatives
                        - setting_remark_deposit_matching
                      x-enum-details:
                        - value: setting_remark_deposit
                          label: Deposit
                        - value: setting_remark_withdrawal
                          label: Withdrawal
                        - value: setting_remark_securitiesdeposit
                          label: Securities Deposit
                        - value: setting_remark_securitieswithdrawal
                          label: Securities Withdrawal
                        - value: setting_remark_accountadjustment
                          label: Account Adjustment
                        - value: setting_remark_withdrawal_reject
                          label: Withdrawal rejection
                        - value: setting_remark_deposit_reject
                          label: Deposit rejection
                        - value: setting_remark_withdrawal_card_reject
                          label: Refuse Add Bank Card
                        - value: setting_remark_fund
                          label: Fund
                        - value: setting_remark_otc_derivatives
                          label: OTC Derivatives
                        - value: setting_remark_deposit_matching
                          label: Deposit Matching
                      example: Deposit Matching
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Remarks template records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            lookup_category:
                              type: string
                              description: '**Returned value:** Remarks User.'
                            lookup_category_val:
                              type: string
                              description: Raw value of `lookup_category`. The `lookup_category` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            value_en:
                              type: string
                              description: '**Returned value:** Remarks - English.'
                            value_cn:
                              type: string
                              description: '**Returned value:** Remarks - Simplified Chinese.'
                            value_hk:
                              type: string
                              description: '**Returned value:** Remarks - Traditional Chinese.'
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created At. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '14625'
                      lookup_category: Deposit Matching
                      lookup_category_val: setting_remark_deposit_matching
                      value_en: add setting_remark_deposit_matching english remark4627
                      value_cn: 新增setting_remark_deposit_matching简体备注8871
                      value_hk: 新增setting_remark_deposit_matching繁體備註9281
                      priority: '175958'
                      created_at: '2026-06-30 06:19:59'
                      updated_at: '2026-06-30 06:19:59'
                  total: 84
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_remark_templates/download
        operationId: atm_remark_templates_download
  /v1/datasets/atm_forex_service_strategy/download:
    post:
      operationId: atm_forex_service_strategy_download
      summary: Service Strategy Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchangeserve_query
      x-lbonly: false
      description: |-
        Export forex service-strategy records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_forex_service_strategy.yaml (the query capability).
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                    support_currency:
                      type: string
                      description: Filters results by Currency Pair.
                    change_type:
                      type: string
                      description: Filters results by Exchange Type.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '0'
                          label: None
                        - value: '1'
                          label: Large Amount Exchange
                        - value: '2'
                          label: Small Amount Exchange (within exchange pool)
                        - value: '3'
                          label: Small Amount Exchange (beyond exchange pool)
                      example: '0'
                    service_strategy:
                      type: string
                      description: Filters results by Exchange Strategy.
                      enum:
                        - SINGLE
                        - BATCH
                      x-enum-details:
                        - value: SINGLE
                          label: Channel exchange
                        - value: BATCH
                          label: Pool exchange
                      example: SINGLE
                    enable_flag:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - 'true'
                        - 'false'
                      x-enum-details:
                        - value: 'true'
                          label: Enable
                        - value: 'false'
                          label: Deactivate
                      example: 'true'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - service_strategy
                      - service_strategy_val
                      - is_default
                      - is_default_val
                      - enable_flag
                      - enable_flag_val
                      - support_currency
                      - service_time
                      - channel_name
                      - channel_name_val
                      - change_type
                      - change_type_val
                      - updated_at
                      - deleted_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Service Strategy Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_forex_service_strategy
        operationId: atm_forex_service_strategy
  /v1/datasets/atm_forex_service_strategy:
    post:
      operationId: atm_forex_service_strategy
      summary: Service Strategy
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchangeserve_query
      x-lbonly: false
      description: |-
        Query forex service-strategy records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                    support_currency:
                      type: string
                      description: Filters results by Currency Pair.
                      example: '["ALL/ALL"]'
                    change_type:
                      type: string
                      description: Filters results by Exchange Type.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '0'
                          label: None
                        - value: '1'
                          label: Large Amount Exchange
                        - value: '2'
                          label: Small Amount Exchange (within exchange pool)
                        - value: '3'
                          label: Small Amount Exchange (beyond exchange pool)
                      example: Small Amount Exchange (beyond exchange pool)
                    service_strategy:
                      type: string
                      description: Filters results by Exchange Strategy.
                      enum:
                        - SINGLE
                        - BATCH
                      x-enum-details:
                        - value: SINGLE
                          label: Channel exchange
                        - value: BATCH
                          label: Pool exchange
                      example: Pool exchange
                    enable_flag:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - 'true'
                        - 'false'
                      x-enum-details:
                        - value: 'true'
                          label: Enable
                        - value: 'false'
                          label: Deactivate
                      example: Enable
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Service Strategy records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            service_strategy:
                              type: string
                              description: '**Returned value:** Exchange Strategy.'
                            service_strategy_val:
                              type: string
                              description: Raw value of `service_strategy`. The `service_strategy` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            is_default:
                              type: string
                              description: '**Returned value:** Indicates whether default?.'
                            is_default_val:
                              type: string
                              description: Raw value of `is_default`. The `is_default` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            enable_flag:
                              type: string
                              description: '**Returned value:** Indicates whether status.'
                            enable_flag_val:
                              type: string
                              description: Raw value of `enable_flag`. The `enable_flag` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            support_currency:
                              type: string
                              description: '**Returned value:** Currency Pair.'
                            service_time:
                              type: string
                              description: '**Returned value:** Service Time. The date or time format follows the value returned by the API.'
                            channel_name:
                              type: string
                              description: '**Returned value:** Exchange Channel.'
                            channel_name_val:
                              type: string
                              description: Raw value of `channel_name`. The `channel_name` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            change_type:
                              type: string
                              description: '**Returned value:** Exchange Type.'
                            change_type_val:
                              type: string
                              description: Raw value of `change_type`. The `change_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                            deleted_at:
                              type: string
                              description: '**Returned value:** Deleted Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '174011'
                      service_strategy: Pool exchange
                      service_strategy_val: BATCH
                      is_default: SPECIFIC
                      is_default_val: SPECIFIC
                      enable_flag: Enable
                      enable_flag_val: 'true'
                      support_currency: '["ALL/ALL"]'
                      service_time: '{"time_zones":"hk_trade_day","time_rule":"08:00:00-15:00:00"}'
                      channel_name: ''
                      channel_name_val: ''
                      change_type: Small Amount Exchange (beyond exchange pool)
                      change_type_val: '3'
                      updated_at: '1784183794'
                      deleted_at: ''
                  total: 3
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_forex_service_strategy/download
        operationId: atm_forex_service_strategy_download
  /v1/datasets/atm_exchange_pool_limits/download:
    post:
      operationId: atm_exchange_pool_limits_download
      summary: Exchange pool quota configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchange_pool_query
      x-lbonly: false
      description: |-
        Export exchange-pool limit records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_exchange_pool_limits.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by Quota Setup ID.
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - currency
                      - currency_val
                      - alarm_amount
                      - remaining_amount
                      - created_at
                      - updated_at
                      - remark
                      - operator
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Exchange pool quota configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_exchange_pool_limits
        operationId: atm_exchange_pool_limits
  /v1/datasets/atm_exchange_pool_limits:
    post:
      operationId: atm_exchange_pool_limits
      summary: Exchange pool quota configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.exchange_pool_query
      x-lbonly: false
      description: |-
        Query exchange-pool limit records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Quota Setup ID.
                      example: '16904'
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                      example: MYR
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Exchange pool quota configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            alarm_amount:
                              type: string
                              description: '**Returned value:** Exchange Alarm Level.'
                            remaining_amount:
                              type: string
                              description: '**Returned value:** Remaining Quota.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '16904'
                      currency: MYR
                      currency_val: MYR
                      alarm_amount: '1.00'
                      remaining_amount: 11,111,111.00
                      created_at: '1765971936'
                      updated_at: '1765971936'
                      remark: ''
                      operator: user@example.com
                  total: 6
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_exchange_pool_limits/download
        operationId: atm_exchange_pool_limits_download
  /v1/datasets/atm_forex_rate_references/download:
    post:
      operationId: atm_forex_rate_references_download
      summary: Add Rate Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.fee_query
      x-lbonly: false
      description: |-
        Export forex rate-markup records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_forex_rate_references.yaml (the query capability).
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                    spread_mode:
                      type: string
                      description: Filters results by Spread Setup.
                      enum:
                        - VALUE
                        - RATE
                      x-enum-details:
                        - value: VALUE
                          label: Value
                        - value: RATE
                          label: Ratio
                      example: VALUE
                    id:
                      type: string
                      description: Filters results by Ref Spread Setup ID.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - channel_name
                      - from_currency
                      - from_currency_val
                      - to_currency
                      - to_currency_val
                      - spread_mode
                      - spread_mode_val
                      - bp
                      - created_at
                      - updated_at
                      - action_user
                      - version
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Add Rate Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_forex_rate_references
        operationId: atm_forex_rate_references
  /v1/datasets/atm_forex_rate_references:
    post:
      operationId: atm_forex_rate_references
      summary: Add Rate Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.fee_query
      x-lbonly: false
      description: |-
        Query forex rate-markup records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                      example: MANUAL_CURRENT
                    spread_mode:
                      type: string
                      description: Filters results by Spread Setup.
                      enum:
                        - VALUE
                        - RATE
                      x-enum-details:
                        - value: VALUE
                          label: Value
                        - value: RATE
                          label: Ratio
                      example: Value
                    id:
                      type: string
                      description: Filters results by Ref Spread Setup ID.
                      example: '209742'
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Add Rate Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            channel_name:
                              type: string
                              description: '**Returned value:** Exchange Channel.'
                            from_currency:
                              type: string
                              description: '**Returned value:** Debit Currency.'
                            from_currency_val:
                              type: string
                              description: Raw value of `from_currency`. The `from_currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            to_currency:
                              type: string
                              description: '**Returned value:** Credit Currency.'
                            to_currency_val:
                              type: string
                              description: Raw value of `to_currency`. The `to_currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            spread_mode:
                              type: string
                              description: '**Returned value:** Spread Setup.'
                            spread_mode_val:
                              type: string
                              description: Raw value of `spread_mode`. The `spread_mode` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            bp:
                              type: string
                              description: '**Returned value:** BP.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                            action_user:
                              type: string
                              description: '**Returned value:** Action User.'
                            version:
                              type: string
                              description: '**Returned value:** Version.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '209742'
                      channel_name: MANUAL_CURRENT
                      from_currency: AUD
                      from_currency_val: AUD
                      to_currency: HKD
                      to_currency_val: HKD
                      spread_mode: Value
                      spread_mode_val: VALUE
                      bp: '1'
                      created_at: '1784168979'
                      updated_at: '1784168979'
                      action_user: user@example.com
                      version: '2'
                  total: 10
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_forex_rate_references/download
        operationId: atm_forex_rate_references_download
  /v1/datasets/atm_forex_rate_configs/download:
    post:
      operationId: atm_forex_rate_configs_download
      summary: Exchange Rate Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.forex_ib_rates
      x-lbonly: false
      description: |-
        Export forex rate configuration records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_forex_rate_configs.yaml (the query capability).
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                    spread_mode:
                      type: string
                      description: Filters results by Spread Setup.
                      enum:
                        - VALUE
                        - RATE
                      x-enum-details:
                        - value: VALUE
                          label: Value
                        - value: RATE
                          label: Ratio
                      example: VALUE
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - service_strategy
                      - service_strategy_val
                      - channel_name
                      - channel_name_val
                      - from_currency
                      - from_currency_val
                      - to_currency
                      - to_currency_val
                      - spread_mode
                      - spread_mode_val
                      - spread_value
                      - effective_time
                      - updated_at
                      - action_user
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Exchange Rate Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_forex_rate_configs
        operationId: atm_forex_rate_configs
  /v1/datasets/atm_forex_rate_configs:
    post:
      operationId: atm_forex_rate_configs
      summary: Exchange Rate Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.forex_ib_rates
      x-lbonly: false
      description: |-
        Query forex rate configuration records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    channel_name:
                      type: string
                      description: |-
                        Filters results by Exchange Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/forex_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/forex_channel
                      example: HSB
                    spread_mode:
                      type: string
                      description: Filters results by Spread Setup.
                      enum:
                        - VALUE
                        - RATE
                      x-enum-details:
                        - value: VALUE
                          label: Value
                        - value: RATE
                          label: Ratio
                      example: Ratio
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Exchange Rate Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            service_strategy:
                              type: string
                              description: '**Returned value:** Service Policy.'
                            service_strategy_val:
                              type: string
                              description: Raw value of `service_strategy`. The `service_strategy` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            channel_name:
                              type: string
                              description: '**Returned value:** Exchange Channel.'
                            channel_name_val:
                              type: string
                              description: Raw value of `channel_name`. The `channel_name` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            from_currency:
                              type: string
                              description: '**Returned value:** Debit Currency.'
                            from_currency_val:
                              type: string
                              description: Raw value of `from_currency`. The `from_currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            to_currency:
                              type: string
                              description: '**Returned value:** Credit Currency.'
                            to_currency_val:
                              type: string
                              description: Raw value of `to_currency`. The `to_currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            spread_mode:
                              type: string
                              description: '**Returned value:** Spread Setup.'
                            spread_mode_val:
                              type: string
                              description: Raw value of `spread_mode`. The `spread_mode` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            spread_value:
                              type: string
                              description: '**Returned value:** Transaction Spread.'
                            effective_time:
                              type: string
                              description: '**Returned value:** Effective time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                            action_user:
                              type: string
                              description: '**Returned value:** Action User.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '209675'
                      service_strategy: Channel exchange
                      service_strategy_val: SINGLE
                      channel_name: HSB
                      channel_name_val: HSB
                      from_currency: HKD
                      from_currency_val: HKD
                      to_currency: USD
                      to_currency_val: USD
                      spread_mode: Ratio
                      spread_mode_val: RATE
                      spread_value: 0-10000HKD,5;
                      effective_time: Long-term effectiveness
                      updated_at: '1784169178'
                      action_user: user@example.com
                  total: 39
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_forex_rate_configs/download
        operationId: atm_forex_rate_configs_download
  /v1/datasets/atm_forex_amount_configs/download:
    post:
      operationId: atm_forex_amount_configs_download
      summary: Large Amount Setup Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.amount_config_query
      x-lbonly: false
      description: |-
        Export large-amount limit records to a file. `filters` scopes the export and `down_fields` limits the exported columns.

        ### Export modes

        - `mode: 0` — synchronous export; the download URL is returned directly on success.
        - `mode: 1` — asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export you no longer need.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_forex_amount_configs.yaml (the query capability).
                  properties:
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - currency
                      - currency_val
                      - amount
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Large Amount Setup Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_forex_amount_configs
        operationId: atm_forex_amount_configs
  /v1/datasets/atm_forex_amount_configs:
    post:
      operationId: atm_forex_amount_configs
      summary: Large Amount Setup
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_conversion.amount_config_query
      x-lbonly: false
      description: |-
        Query large-amount limit records. Combine business filters through `filters`, and use `page` and `page_size` to control pagination.

        ### Returns

        On success, returns the matching records and pagination info. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data sources.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Large Amount Setup records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            amount:
                              type: string
                              description: '**Returned value:** Large Amount Starting point.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '32026'
                      currency: SGD
                      currency_val: SGD
                      amount: 2,000.00
                      created_at: '1783972944'
                      updated_at: '1784170224'
                  total: 6
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_forex_amount_configs/download
        operationId: atm_forex_amount_configs_download
  /v1/atm/out/cash/package/rule:
    post:
      operationId: out_cash_package_rule_add
      summary: Add Out Cash Package Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_update
      x-lbonly: false
      description: |-
        Create an automatic-withdrawal rule that defines how a withdrawal bank channel handles automatic payouts for the given regions, currencies, and limits.

        ### Request parameters

        - Required: `original_limit` (limit-type of the withdrawal account), `lb_bank_channel_id` (withdrawal bank channel ID), `package_type` (withdrawal method), `batch_max_count` (max transactions per batch), `charge_type` (fee-bearing party), `single_quota_currency_amount` (per-currency single-transaction limit), `currency`, `support_region`, `support_bank_ids`, `sec_auth` (secondary verification), `priority`, `service_time`, and others.
        - Allowed currencies for `single_quota_currency_amount` come from `GET /v1/datasets/refs/tenant_currencies_by_channel`; allowed regions for `support_region` come from `GET /v1/datasets/refs/withdraw_rule_bank_regions`.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Business notes

        The rule determines whether a matched withdrawal request is processed automatically as a package, along with its limits, fees, and service-time window.

        ### Risk warning

        This creates a real automatic-withdrawal rule and may affect how customer withdrawals are auto-processed and paid out. Confirm the channel, regions, currencies, and limits before calling.

        ### Result

        On success, the automatic-withdrawal rule is created; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - original_limit
                - lb_bank_channel_id
                - package_type
                - batch_max_count
                - charge_type
                - single_quota_currency_amount
                - single_quota_currency_amount_keys
                - currency
                - support_region
                - support_bank_ids
                - sec_auth
                - priority
                - service_time
                - lb_service_time
                - version
                - action_in
              properties:
                original_limit:
                  type: string
                  description: Whether the packaging rule is restricted to the original-limit withdrawal account dimension. Editable when creating; read-only when editing.
                  enum:
                    - '0'
                    - '1'
                  x-enum-details:
                    - value: '0'
                      label: Unrestricted
                    - value: '1'
                      label: Restricted
                  example: '0'
                lb_bank_channel_id:
                  type: string
                  description: |-
                    Dropdown; the candidate list depends on original_limit and which site it is, drawing
                    from different dynamic enums: when original_limit=1 and on certain sites, values come
                    from withdraw_methods, otherwise from
                    withdraw_company_bank_channels (the edit state uses the cascade enum
                    withdraw_company_bank_channels_cascade).

                    Call `GET /v1/datasets/refs/withdraw_methods` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/withdraw_methods
                package_type:
                  type: string
                  description: Packaging type for outbound cash payments.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                    - '4'
                  x-enum-details:
                    - value: '1'
                      label: Real Time Payment Of Single Order
                    - value: '2'
                      label: Manual Review Of Timed Packing
                    - value: '3'
                      label: Manual Packing
                    - value: '4'
                      label: Batch Auto
                  example: '1'
                batch_max_count:
                  type: string
                  description: An integer in the range 0-100, converted to a string before submission.
                charge_type:
                  type: string
                  description: Fee-bearing method for the outbound cash payment, following SWIFT charge conventions.
                  enum:
                    - SHA
                    - OUR
                    - BEN
                  x-enum-details:
                    - value: SHA
                      label: SHA
                    - value: OUR
                      label: OUR
                    - value: BEN
                      label: BEN
                  example: SHA
                fee_bank_account_id:
                  type: string
                  description: |-
                    Shown only when charge_type is SHA or OUR, an enumerated value; candidates come from
                    a dynamic query of company_bank_account_config after a bank channel is selected.
                single_quota_currency_amount:
                  type: string
                  description: |-
                    An addable-row list of [{currency, amount}]; currency's values come from the
                    tenant_currencies_by_channel dynamic enumeration and amount is a number;
                    serialized to a string before submission.

                    Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                single_quota_currency_amount_keys:
                  type: string
                  description: |-
                    The value is a sequence of indices into the single_quota_currency_amount array (e.g.
                    [0,1,...]), used to indicate row order.
                currency:
                  type: string
                  description: |-
                    A comma-separated string of the deduplicated currency values from each row of
                    single_quota_currency_amount; not an independent request field.
                support_region:
                  type: string
                  description: |-
                    Multi-select; candidates come from the withdraw_rule_bank_regions dynamic enumeration,
                    joined into a comma-separated string before submission; changing it re-queries the
                    candidate banks for support_bank_ids in cascade.

                    Call `GET /v1/datasets/refs/withdraw_rule_bank_regions` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/withdraw_rule_bank_regions
                support_bank_ids:
                  type: string
                  description: |-
                    Multi-select; candidates come from the bank list returned by
                    atm_region_banks_pick_exclude_default, queried in cascade with support_region.
                sec_auth:
                  type: string
                  description: Whether securities authorization is supported.
                  enum:
                    - '0'
                    - '1'
                  x-enum-details:
                    - value: '0'
                      label: Not Supported
                    - value: '1'
                      label: Support
                  example: '0'
                priority:
                  type: number
                  description: An integer in the range 0-999.
                status:
                  type: string
                  description: 'Single-select; values: ''1'' = enabled, ''2'' = disabled.'
                  enum:
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '1'
                      label: Enable
                    - value: '2'
                      label: Disable
                  example: '1'
                remark:
                  type: string
                rule_codes:
                  type: string
                  description: |-
                    Shown only on certain sites; multi-select, values from the risk_rule_indicators_withdraw dynamic
                    enum (includes all=All; selecting All mutually excludes and clears the other options).

                    Call `GET /v1/datasets/refs/risk_rule_indicators_withdraw` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/risk_rule_indicators_withdraw
                original_limit_bank_accounts:
                  type: string
                  description: |-
                    Shown only on certain sites when original_limit='1'; multi-select accounts grouped by
                    bank, with candidates from the dynamic query atm_lb_bank_accounts_package_rule.
                service_time:
                  type: string
                  description: |-
                    A JSON string assembled from time_zones (enum: hk_trade_day/calendar_day/week) +
                    time_rule; when time_zones='week', time_rule is multiple groups of {week_interval,
                    time_interval}.
                  enum:
                    - hk_trade_day
                    - calendar_day
                    - week
                  x-enum-details:
                    - value: hk_trade_day
                      label: HK Trading Day
                    - value: calendar_day
                      label: Calendar Day
                    - value: week
                      label: Week
                  example: hk_trade_day
                lb_service_time:
                  type: string
                  description: Same structure as service_time; a separate time-window configuration, as a JSON string.
                version:
                  type: number
                  description: Always sends 2 (for legacy-endpoint compatibility); not a visible field.
                name:
                  type: string
                  description: |-
                    Not a direct request field; before submission the name is taken from the candidate
                    corresponding to lb_bank_channel_id and submitted along with it (only present when a
                    match is found).
                action_in:
                  type: number
                  description: Not a request field; always sends 1 (shared by add/edit).
                audit_frontend_resource:
                  type: object
                  description: |-
                    Not a business field; used for backend audit-log display. In the create scenario the
                    front end always sends { before_id: the source rule ID carried in from the "Copy"
                    entry (may be empty), showCopy: whether it is a copy-create }.
            example:
              original_limit: '0'
              lb_bank_channel_id: '10086'
              package_type: '1'
              batch_max_count: '50'
              charge_type: SHA
              fee_bank_account_id: Zhang San
              single_quota_currency_amount: '[{"currency":"USD","amount":10000}]'
              single_quota_currency_amount_keys:
                - 0
              currency: USD
              support_region: hk,us
              support_bank_ids: 101,102
              sec_auth: '1'
              priority: 10
              status: '1'
              remark: ''
              rule_codes:
                - all
              original_limit_bank_accounts: []
              service_time: '{"time_zones":"week","time_rule":[{"week_interval":"1-5","time_interval":"09:00:00-18:00:00"}]}'
              lb_service_time: '{"time_zones":"week","time_rule":[{"week_interval":"1-5","time_interval":"09:00:00-18:00:00"}]}'
              version: 2
              action_in: 1
              audit_frontend_resource:
                before_id:
                showCopy:
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    put:
      operationId: out_cash_package_rule_edit
      summary: Edit Out Cash Package Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_update
      x-lbonly: false
      description: |-
        Edit the configuration of an existing automatic-withdrawal rule.

        ### Request parameters

        - Required: `id` (rule ID), `config` (rule configuration).
        - Optional: `front_payload` (display configuration data), `audit_frontend_resource` (caller-provided audit context).
        - `id` locates the target rule; `config` carries the full configuration in the same shape as the create endpoint.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes a real automatic-withdrawal rule and may affect how customer withdrawals are auto-processed and paid out. Confirm the target rule and configuration before calling.

        ### Result

        On success, the automatic-withdrawal rule is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - config
              properties:
                id:
                  type: string
                  description: Rule ID associated with this request.
                config:
                  type: object
                  description: This field is required.
                front_payload:
                  type: object
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              id: '123'
              config: {}
              front_payload: {}
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    delete:
      operationId: out_cash_package_rule_delete
      summary: Delete Out Cash Package Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_update
      x-lbonly: false
      description: |-
        Delete an automatic-withdrawal rule.

        ### Request parameters

        - Required: `id` (rule ID), `action_in` (operation type).
        - `id` locates the rule to delete.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This deletes a real automatic-withdrawal rule; afterwards the affected withdrawal requests are no longer auto-processed by this rule. Confirm the target rule before calling.

        ### Result

        On success, the automatic-withdrawal rule is deleted; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - action_in
              properties:
                id:
                  type: string
                  description: Rule ID associated with this request.
                action_in:
                  type: number
                  description: This field is required.
            example:
              id: '123'
              action_in: 1
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/add/bank/balance:
    post:
      operationId: add_bank_balance
      summary: Add Daily Limit
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_update
      x-lbonly: false
      description: |-
        Create a daily-limit configuration that sets, for a given company bank name / bank account / withdrawal method combination, the daily initial withdrawal quota and the remaining-balance alarm threshold for one currency.

        ### Request parameters

        - Configuration fields are wrapped together in the `params` object.
        - Required: `params.lb_bank_name` (company bank name), `params.lb_bank_account_ids` (bank account), `params.bank_operation_types_ids` (withdrawal method), `params.currency`, `params.limit_amount` (initial quota), `params.alarm_amount` (daily remaining-balance alarm threshold).
        - Optional: `params.remark`.
        - See the individual field descriptions for each field's format, allowed values, default behavior, and business meaning.

        ### Risk warning

        This creates a real daily-limit configuration and directly affects the account's available withdrawal quota for the day. Verify the bank, account, currency, and amounts before calling.

        ### Result

        On success, the daily-limit configuration is created; `data` carries no additional business fields.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                params:
                  type: object
                  required:
                    - lb_bank_name
                    - lb_bank_account_ids
                    - bank_operation_types_ids
                    - currency
                    - limit_amount
                    - alarm_amount
                  properties:
                    lb_bank_name:
                      type: string
                      description: Bank name
                    lb_bank_account_ids:
                      type: string
                      description: Bank account
                    bank_operation_types_ids:
                      type: string
                      description: Business type
                    currency:
                      type: string
                      description: Currency
                    limit_amount:
                      type: string
                      description: Limit
                    alarm_amount:
                      type: string
                      description: Alarm amount
                    remark:
                      type: string
                      description: Remark
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/modify/bank/balance:
    post:
      operationId: modify_bank_balance
      summary: Edit Daily Limit
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.bankbalance_update
      x-lbonly: false
      description: |-
        Edit an existing daily-limit configuration. `id` locates the target record, and the fields in `params` update it; the company bank name cannot be changed while editing.

        ### Request parameters

        - Required: `id` (daily-limit configuration number).
        - Fields to update are wrapped in `params`; required: `params.lb_bank_name` (company bank name), `params.lb_bank_account_ids` (bank account), `params.bank_operation_types_ids` (withdrawal method), `params.currency`, `params.limit_amount` (initial quota), `params.alarm_amount` (daily remaining-balance alarm threshold); optional: `params.remark`.
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, default behavior, and business meaning.

        ### Risk warning

        This changes a real daily-limit configuration and directly affects the account's available withdrawal quota for the day. Verify the target record and amounts before calling.

        ### Result

        On success, the daily-limit configuration is updated; `data` carries no additional business fields.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - params
              properties:
                id:
                  type: string
                  description: Daily Limit Setup ID of the record to edit, used to locate the target record. This field is required.
                  example: '12345'
                params:
                  type: object
                  description: The set of daily limit configuration fields to update.
                  required:
                    - lb_bank_name
                    - lb_bank_account_ids
                    - bank_operation_types_ids
                    - currency
                    - limit_amount
                    - alarm_amount
                  properties:
                    lb_bank_name:
                      type: string
                      description: |-
                        Company bank name. This field cannot be changed when editing and must stay consistent with the original record. This field is required.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_bank_names` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_bank_names
                      example: BOC_HK
                    lb_bank_account_ids:
                      type: string
                      description: |-
                        Bank account, multi-select. Join multiple bank account ids with commas, for example `1001,1002`; pass `all` to apply to every account under the selected company bank name. This field is required.

                        The available values depend on the selected `lb_bank_name`.
                      example: 1001,1002
                    bank_operation_types_ids:
                      type: string
                      description: |-
                        Withdrawal method, multi-select. Join multiple method ids with commas, for example `10,20`; pass `all` to apply to every withdrawal method. This field is required.

                        The available values depend on the selected `lb_bank_name` and `lb_bank_account_ids`.
                      example: 10,20
                    currency:
                      type: string
                      description: |-
                        Currency. This field is required.

                        This dictionary is filtered by account channel. Values are dynamic; call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                      example: HKD
                    limit_amount:
                      type: string
                      description: Daily initial quota, passed as a string-encoded amount. This field is required.
                      example: '1000000'
                    alarm_amount:
                      type: string
                      description: Alarm level of the remaining daily quota, passed as a string-encoded amount. An alert is triggered when the remaining daily quota falls below this value. This field is required.
                      example: '100000'
                    remark:
                      type: string
                      description: Remark, optional. Free-text note recording additional information about this configuration.
                      example: 香港账户日限额
                audit_frontend_resource:
                  type: object
                  description: Caller-provided audit context used for operation logging, optional. Typically carries key information about the record being edited.
            example:
              id: '12345'
              params:
                lb_bank_name: BOC_HK
                lb_bank_account_ids: 1001,1002
                bank_operation_types_ids: 10,20
                currency: HKD
                limit_amount: '1000000'
                alarm_amount: '100000'
                remark: 香港账户日限额
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atmrisk/modify_balance_rule:
    post:
      operationId: modify_balance_rule
      summary: Edit Balance Blocking Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_risk_withdraw.risk_balance_rules_modify
      x-lbonly: false
      description: |-
        Edit a single balance-interception rule, updating its warning balance and interception balance.

        ### Request parameters

        - Required: `id` (rule ID), `warning_amount` (warning balance), `intercept_amount` (interception balance).
        - `id` locates the rule to edit; `warning_amount` and `intercept_amount` are non-negative integer amounts, in the currency configured on the rule itself.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes real balance-interception thresholds and may affect whether withdrawal requests are warned or intercepted. Verify the target rule and amounts before calling.

        ### Result

        On success, the rule's warning and interception balances are updated; `data` carries no additional business fields.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - warning_amount
                - intercept_amount
              properties:
                id:
                  type: string
                  description: ID of the balance blocking rule to edit, used to locate the target rule. This field is required.
                  example: '123'
                warning_amount:
                  type: integer
                  minimum: 0
                  description: Warning amount, a non-negative integer. A warning is triggered when the account balance falls below this threshold. The currency follows the rule's own configuration. This field is required.
                  example: 555
                intercept_amount:
                  type: integer
                  minimum: 0
                  description: Intercept amount, a non-negative integer. Withdrawals are intercepted when the account balance falls below this threshold. The currency follows the rule's own configuration. This field is required.
                  example: 666
            example:
              id: '123'
              warning_amount: 555
              intercept_amount: 666
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atmrisk/switch_balance_rule:
    post:
      operationId: switch_balance_rule
      summary: Switch Balance Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_risk_withdraw.risk_balance_rules_enable
      x-lbonly: false
      description: |-
        Enable or disable a balance-interception rule.

        ### Request parameters

        - Required: `id` (rule ID), `rule_switch` (rule toggle).
        - `id` locates the target rule.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes whether a real balance-interception rule is in effect and may affect whether withdrawal requests are intercepted. Confirm the target rule before calling.

        ### Result

        On success, the rule's enabled state is toggled; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - rule_switch
              properties:
                id:
                  type: string
                  description: Rule ID associated with this request.
                rule_switch:
                  type: boolean
                  description: |-
                    Boolean flag for rule switch. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
            example:
              id: '123'
              rule_switch: true
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/update/direct/withdraw/audit/config:
    post:
      operationId: update_direct_withdraw_audit_config
      summary: Update Direct Withdraw Audit Config
      tags:
        - Service Parameter
      x-permission-key:
        - atm.update_direct_withdraw_audit_config
      x-lbonly: false
      description: |-
        Toggle the configuration switch that controls whether withdrawals require manual review.

        ### Request parameters

        - Required: `config_key` (configuration key), `config_value` (configuration value).
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes the effective configuration for whether withdrawals require review, and may cause withdrawal requests to skip or enter manual review. Confirm the configuration key and value before calling.

        ### Result

        On success, the withdrawal-review switch is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - config_key
                - config_value
              properties:
                config_key:
                  type: string
                  description: This field is required.
                config_value:
                  type: string
                  description: Specifies Config Value. This field is required.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'On'
                    - value: 'no'
                      label: 'Off'
                  example: 'yes'
            example:
              config_key: direct_withdraw_need_audit
              config_value: 'yes'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/withdraw/prompt/add:
    post:
      operationId: add_withdraw_prompt
      summary: Add Withdraw Prompt
      tags:
        - Service Parameter
      x-permission-key:
        - withdrawalparameter.withdrawal_parameter_management
      x-lbonly: false
      description: |-
        Create a withdrawal prompt / parameter configuration that returns fee, arrival-time, and other prompt details to withdrawal scenarios, keyed by region, bank, currency, and time range.

        ### Request parameters

        - Required: `region_codes` (region code list), `bank_ids` (bank ID list), `currency`, `time_type`, `time_left` (range lower bound), `time_right` (range upper bound), `priority`, `rule_status`, `prompts` (prompt detail list).
        - Each `prompts` item includes the amount range, platform fee, bank fee, and arrival time.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Business notes

        When the rule matches, the configured fee and arrival-time prompts are shown to the corresponding withdrawal scenario.

        ### Risk warning

        This creates a real withdrawal-prompt configuration and directly affects the fee and arrival-time information customers see when withdrawing. Verify the regions, banks, currency, and prompt details before calling.

        ### Result

        On success, the withdrawal-prompt configuration is created; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - region_codes
                - bank_ids
                - currency
                - time_type
                - time_left
                - time_right
                - priority
                - rule_status
                - prompts
              properties:
                region_codes:
                  type: array
                  description: Submit Region Codes as a JSON array. This field is required.
                  items:
                    type: string
                bank_ids:
                  type: array
                  description: Submit Bank IDs as a JSON array. This field is required.
                  items:
                    type: string
                currency:
                  type: string
                  description: Currency code used for this request. This field is required.
                time_type:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                time_left:
                  type: string
                  description: This field is required.
                time_right:
                  type: string
                  description: This field is required.
                priority:
                  type: number
                  description: Processing priority. Interpret the value and sort order according to the allowed-value definitions. This field is required.
                rule_status:
                  type: string
                  description: |-
                    This field is required.
                  enum:
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '1'
                      label: Enabled
                    - value: '2'
                      label: Disabled
                  example: '1'
                prompts:
                  type: array
                  description: Each item contains amount_left/amount_right/plat_fee/bank_fee/arrive_time (all multilingual objects)/arrive_rule.
                  items:
                    type: string
            example:
              region_codes: []
              bank_ids: []
              currency: HKD
              time_type: ''
              time_left: ''
              time_right: ''
              priority: 1
              rule_status: ''
              prompts:
                - amount_left: ''
                  amount_right: ''
                  plat_fee:
                    en: ''
                    zh-CN: ''
                    zh-HK: ''
                  bank_fee:
                    en: ''
                    zh-CN: ''
                    zh-HK: ''
                  arrive_time:
                    en: ''
                    zh-CN: ''
                    zh-HK: ''
                  arrive_rule: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /atm/withdraw/prompt/modify:
    post:
      operationId: modify_withdraw_prompt
      summary: Modify Withdraw Prompt
      tags:
        - Service Parameter
      x-permission-key:
        - withdrawalparameter.withdrawal_parameter_management
      x-lbonly: false
      description: |-
        Edit an existing withdrawal prompt / parameter configuration. The field structure matches the create endpoint, with `id` added to locate the target record.

        ### Request parameters

        - Required: `id` (configuration ID).
        - The remaining fields (`region_codes`, `bank_ids`, `currency`, `time_type`, `time_left`, `time_right`, `priority`, `rule_status`, `prompts`) match the create endpoint; optional `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, default behavior, and business meaning.

        ### Risk warning

        This changes a real withdrawal-prompt configuration and directly affects the fee and arrival-time information customers see when withdrawing. Verify the target record and prompt details before calling.

        ### Result

        On success, the withdrawal-prompt configuration is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: ID
                region_codes:
                  type: array
                  items:
                    type: string
                  description: Region
                bank_ids:
                  type: array
                  items:
                    type: string
                  description: Bank
                currency:
                  type: string
                  description: Currency
                time_type:
                  type: string
                  description: Time type
                time_left:
                  type: string
                  description: Start time
                time_right:
                  type: string
                  description: End time
                priority:
                  type: integer
                  description: Priority
                rule_status:
                  type: string
                  description: Rule status
                prompts:
                  type: array
                  items:
                    type: object
                    properties:
                      amount_left:
                        type: string
                        description: Minimum amount
                      amount_right:
                        type: string
                        description: Maximum amount
                      plat_fee:
                        type: object
                        description: prompts[].plat_fee
                      bank_fee:
                        type: object
                        description: prompts[].bank_fee
                      arrive_time:
                        type: object
                        description: prompts[].arrive_time
                      arrive_rule:
                        type: string
                        description: Arrival rule
                audit_frontend_resource:
                  type: object
                  description: audit_frontend_resource
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/credit_amount_summary/query_v2:
    get:
      operationId: credit_amount_summary_query_v2
      summary: Query Financing Summary V2
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_credit_limit.lb_credit_limit
      x-lbonly: false
      description: |-
        Query the financing and trading-limit overview for the current company (tenant).

        ### Request parameters

        - This endpoint has no required parameters. The primary credit currency and aggregation basis are determined by the server according to the currently signed-in tenant and account permissions.

        ### Returns

        On success, `data` returns the company-level primary credit currency, total financing limit, and total trading limit together with their used, remaining, and warning thresholds, plus per-currency financing detail in `cp_finance_info_list`, and per-currency limit detail for margin financing (`normal_margin_list`) and intraday trading (`intraday_margin_list`). See the response definition below for field meanings.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The request was processed successfully. `data` contains the company's financing/trading limit summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Company financing/trading limit summary. All `cp_total_*` aggregate amounts are denominated in the `credit_currency` currency. Amount fields are integer or decimal strings.
                    properties:
                      credit_currency:
                        type: string
                        description: Credit currency, such as `HKD` or `USD`. All `cp_total_*` aggregate amounts are denominated in this currency.
                      cp_total_finance:
                        type: string
                        description: Company total financing limit (in the credit currency).
                      account_finance_total:
                        type: string
                        description: Total approved financing limit at the account level (in the credit currency).
                      cp_total_finance_used:
                        type: string
                        description: Company financing limit used (in the credit currency).
                      cp_total_finance_used_warning:
                        type: string
                        description: Alert threshold for the company's approved financing limit (in the credit currency).
                      cp_total_finance_remained:
                        type: string
                        description: Company remaining available financing limit (in the credit currency).
                      cp_total_trading_limit:
                        type: string
                        description: Company total trading limit (in the credit currency).
                      cp_total_trading_limit_reviewed:
                        type: string
                        description: Currently approved trading limit (in the credit currency).
                      cp_total_trading_limit_reviewed_warning:
                        type: string
                        description: Alert threshold for the approved trading limit (in the credit currency).
                      cp_total_trading_limit_used:
                        type: string
                        description: Trading limit used (in the credit currency).
                      cp_total_trading_limit_used_warning:
                        type: string
                        description: Alert threshold for the trading limit used (in the credit currency).
                      cp_finance_info_list:
                        type: object
                        description: Per-currency financing information. Each key is a currency code (such as `HKD` or `USD`); the value is that currency's financing summary object.
                        additionalProperties:
                          type: object
                          properties:
                            cp_finance_total:
                              type: string
                              description: Maximum / total financing limit for this currency.
                            cp_finance_used:
                              type: string
                              description: Financing limit used (occupied by clients) for this currency.
                            cp_advance_used:
                              type: string
                              description: Advance funding used for this currency.
                      normal_margin_list:
                        type: object
                        description: Per-currency limit breakdown under normal margin. Each key is a currency code; the value is that currency's detail object.
                        additionalProperties:
                          type: object
                          properties:
                            cp_finance_total:
                              type: string
                              description: Maximum credit limit for this currency.
                            cp_finance_used_warning:
                              type: string
                              description: Financing alert threshold for this currency.
                            cp_finance_used:
                              type: string
                              description: Financing limit used for this currency.
                            cp_advance_used_warning:
                              type: string
                              description: Advance funding alert threshold for this currency.
                            cp_advance_used:
                              type: string
                              description: Advance funding used for this currency.
                      intraday_margin_list:
                        type: object
                        description: Per-currency limit breakdown under intraday margin. The structure is identical to `normal_margin_list`.
                        additionalProperties:
                          type: object
                          properties:
                            cp_finance_total:
                              type: string
                              description: Maximum credit limit for this currency.
                            cp_finance_used_warning:
                              type: string
                              description: Financing alert threshold for this currency.
                            cp_finance_used:
                              type: string
                              description: Financing limit used for this currency.
                            cp_advance_used_warning:
                              type: string
                              description: Advance funding alert threshold for this currency.
                            cp_advance_used:
                              type: string
                              description: Advance funding used for this currency.
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/company_credit_cfg/reset_apply:
    post:
      operationId: company_credit_cfg_reset
      summary: Reset Company Credit Config
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Reset the company-level credit configuration, overwriting the company total credit limit, total financing limit, and related warning thresholds.

        ### Request parameters

        - Required: `credit_currency` (credit currency), `cp_credit_total` (company total credit limit), `cp_total_finance` (company total financing limit), `cp_credit_used_warning` (credit-used warning threshold), `cp_total_finance_used_warning` (financing-used warning threshold).
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes the real company-level credit configuration and directly affects available credit and financing limits. Verify the currency and all amounts before calling.

        ### Result

        On success, the company credit configuration is reset; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - credit_currency
                - cp_credit_total
                - cp_total_finance
                - cp_credit_used_warning
                - cp_total_finance_used_warning
              properties:
                credit_currency:
                  type: string
                  description: Taken from the credit currency of the record currently being edited; not editable in the dialog.
                cp_credit_total:
                  type: number
                  description: This field is required.
                cp_total_finance:
                  type: number
                  description: This field is required.
                cp_credit_used_warning:
                  type: number
                  description: This field is required.
                cp_total_finance_used_warning:
                  type: number
                  description: At most a 19-digit integer; the client performs length validation.
                audit_frontend_resource:
                  type: object
                  description: |-
                    Audit-trail field; records the original record content and $action=edit before
                    submission, used for comparison display in the approval ticket.
            example:
              credit_currency: USD
              cp_credit_total: 1000000
              cp_total_finance: 500000
              cp_credit_used_warning: 800000
              cp_total_finance_used_warning: 400000
              audit_frontend_resource:
                $action: edit
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ticket_id:
                        type: string
                        description: Ticket id
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/company_finance_cfg/reset_apply:
    post:
      operationId: company_finance_cfg_reset
      summary: Reset Company Finance Config
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Reset the company-level financing-limit configuration, overwriting the maximum financing limit and related warning thresholds for a given margin type.

        ### Request parameters

        - Required: `currency`, `cp_finance_total` (maximum financing limit), `cp_finance_used_warning` (financing-used warning threshold), `cp_advance_used_warning` (advance-used warning threshold), `margin_type` (margin type).
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - `currency` comes from the record being edited; `margin_type` identifies the margin type this configuration applies to.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes the real company-level financing-limit configuration and directly affects the available financing limit. Verify the currency, margin type, and all amounts before calling.

        ### Result

        On success, the company financing-limit configuration is reset; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currency
                - cp_finance_total
                - cp_finance_used_warning
                - cp_advance_used_warning
                - margin_type
              properties:
                currency:
                  type: string
                  description: Displayed disabled in the form; taken from the record currently being edited.
                cp_finance_total:
                  type: string
                  description: Converted to a string via toString before submission by the caller InputNumber.
                cp_finance_used_warning:
                  type: string
                  description: This field is required.
                cp_advance_used_warning:
                  type: string
                  description: This field is required.
                margin_type:
                  type: string
                  description: |-
                    Passed in via the external marginType prop; not a request field; the specific enum values
                    are not confirmed within this component.
                  enum:
                    - NormalMargin
                    - IntradayMargin
                  x-enum-details:
                    - value: NormalMargin
                      label: Margin Credit Limit
                    - value: IntradayMargin
                      label: Day Trade Credit Limit
                  example: NormalMargin
                audit_frontend_resource:
                  type: object
                  description: Records the original record content before submission, used for comparison display in the approval ticket.
            example:
              currency: USD
              cp_finance_total: '1000000'
              cp_finance_used_warning: '800000'
              cp_advance_used_warning: '200000'
              margin_type: margin
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ticket_id:
                        type: string
                        description: Ticket id
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/company_trading_limit_cfg/reset_apply:
    post:
      operationId: company_trading_limit_cfg_reset
      summary: Reset Company Trading Limit Config
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Reset the company-level trading-limit configuration, overwriting the company total trading limit and related warning thresholds.

        ### Request parameters

        - Required: `credit_currency` (credit currency), `cp_total_trading_limit` (company total trading limit), `cp_total_trading_limit_reviewed_warning` (reviewed-limit warning threshold), `cp_total_trading_limit_used_warning` (used-limit warning threshold).
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes the real company-level trading-limit configuration and directly affects the available trading limit. Verify the currency and all limits before calling.

        ### Result

        On success, the company trading-limit configuration is reset; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - credit_currency
                - cp_total_trading_limit
                - cp_total_trading_limit_reviewed_warning
                - cp_total_trading_limit_used_warning
              properties:
                credit_currency:
                  type: string
                  description: Taken from the credit_currency of the record currently being edited.
                cp_total_trading_limit:
                  type: number
                  description: This field is required.
                cp_total_trading_limit_reviewed_warning:
                  type: number
                  description: This field is required.
                cp_total_trading_limit_used_warning:
                  type: number
                  description: This field is required.
                audit_frontend_resource:
                  type: object
                  description: Records the original record content before submission, used for comparison display in the approval ticket.
            example:
              credit_currency: USD
              cp_total_trading_limit: 2000000
              cp_total_trading_limit_reviewed_warning: 1500000
              cp_total_trading_limit_used_warning: 1000000
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ticket_id:
                        type: string
                        description: Ticket id
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk_monitoring/modify_risk_monitoring_cfgs_v2:
    post:
      operationId: modify_risk_monitoring_cfgs_v2
      summary: Modify Risk Monitoring Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_margin_call.margin_call_auto_batch_forccconfig
      x-lbonly: false
      description: |-
        Modify the master switch for risk-monitoring alerts (automatic forced liquidation), enabling or disabling auto-liquidation per trade type and forced-liquidation type.

        ### Request parameters

        - Required: `trade_type` (trade type), `force_type` (forced-liquidation type), `is_enable` (whether to enable).
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes whether the real automatic forced-liquidation master switch is in effect, and may trigger or stop auto-liquidation of customer positions. Confirm the trade type and forced-liquidation type carefully before calling.

        ### Result

        On success, the auto-liquidation switch is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - trade_type
                - force_type
                - is_enable
              properties:
                trade_type:
                  type: string
                  description: |-
                    Raw transaction-type enum value. When updating a configuration, pass through the
                    value returned by the corresponding query API without translating it.
                force_type:
                  type: string
                  description: |-
                    Raw forced-liquidation-type enum value. When updating a configuration, pass through
                    the value returned by the corresponding query API without translating it.
                is_enable:
                  type: boolean
                  description: Send true when clicking "On", false when clicking "Off".
                audit_frontend_resource:
                  type: object
                  description: |-
                    An audit-trail field that records the complete record of the operated row along with
                    $action=edit before submission, for display in the approval ticket.
            example:
              trade_type: '1'
              force_type: '1'
              is_enable: true
              audit_frontend_resource:
                $action: edit
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk_monitoring/covered_opt_close_out/rules/update:
    post:
      operationId: covered_opt_close_out_rules_update
      summary: Update Covered Option Close-Out Rule
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Update the covered-option collection sub-rule for automatic forced liquidation, controlling whether covered-option close-out is enabled, whether short options are skipped, and the trigger time.

        ### Request parameters

        - Required: `is_enable_covered_opt_close_out` (whether covered-option close-out is enabled), `is_skip_short_option` (whether to skip short options).
        - Optional: `covered_opt_close_out_trigger_time` (close-out trigger time), `audit_frontend_resource` (caller-provided audit context).
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes a real automatic forced-liquidation configuration and may trigger auto-liquidation of customer option positions; the risk is very high. Confirm every switch and the trigger time carefully before calling.

        ### Result

        On success, the covered-option close-out rule is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - is_enable_covered_opt_close_out
                - is_skip_short_option
              properties:
                is_enable_covered_opt_close_out:
                  type: boolean
                  description: |-
                    Boolean flag for enable covered option close-out. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                covered_opt_close_out_trigger_time:
                  type: string
                  description: In HH:mm format; required when the rule is enabled.
                is_skip_short_option:
                  type: boolean
                  description: |-
                    Boolean flag for skip short option. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                audit_frontend_resource:
                  type: object
                  description: |-
                    Records a snapshot of the rule before modification (whether option covered close-out
                    is enabled, the trigger time, and whether selling options is skipped) before
                    submission, for before-and-after comparison in the approval ticket.
            example:
              is_enable_covered_opt_close_out: true
              covered_opt_close_out_trigger_time: '15:30'
              is_skip_short_option: false
              audit_frontend_resource:
                is_enable_covered_opt_close_out: false
                covered_opt_close_out_trigger_time: '15:00'
                is_skip_short_option: false
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk_limit/v1/account/update_exceeding_withdraw:
    post:
      operationId: account_update_exceeding_withdraw
      summary: Update Account Exceeding Withdraw Limit
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Update the excess-withdrawal interception configuration for a single securities account, setting the excess-withdrawal interception limit and enabled state for a given currency.

        ### Request parameters

        - Required: `currency`, `account_exceeding_withdraw_limit` (excess-withdrawal interception limit), `is_enable` (whether to enable), `aaid` (securities account).
        - Optional: `remark`.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This changes a real customer's excess-withdrawal interception configuration and may affect whether that account's withdrawal requests are intercepted. Verify the securities account, currency, and limit before calling.

        ### Result

        On success, the account's excess-withdrawal interception configuration is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currency
                - account_exceeding_withdraw_limit
                - is_enable
                - aaid
              properties:
                currency:
                  type: string
                  description: Currency code used for this request. This field is required.
                account_exceeding_withdraw_limit:
                  type: string
                  description: This field is required.
                is_enable:
                  type: boolean
                  description: |-
                    Boolean flag for is enable. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                aaid:
                  type: string
                  description: Specifies A/C No.. This field is required.
                remark:
                  type: string
            example:
              currency: HKD
              account_exceeding_withdraw_limit: '0'
              is_enable: true
              aaid: H00000000
              remark: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ticket_id:
                        type: string
                        description: Ticket id
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk/stock_group:
    post:
      operationId: stock_group_add
      summary: Add Risk Stock Group
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Create a stock-group configuration that gathers a set of stocks for a given application scenario.

        ### Request parameters

        - Required: `name` (stock-group name), `application` (application scenario).
        - Optional: `remark`.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Business notes

        `application` determines which risk-control or business scenarios the stock group applies to.

        ### Result

        On success, the stock group is created; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - application
              properties:
                name:
                  type: string
                  description: This field is required.
                application:
                  type: string
                  description: Application scenario for the stock group.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                    - '4'
                    - '5'
                  x-enum-details:
                    - value: '1'
                      label: Simulate Margin Call
                    - value: '2'
                      label: Report
                    - value: '3'
                      label: Margin Call
                    - value: '4'
                      label: Concentration Limit
                    - value: '5'
                      label: Option Short Selling Whitelist
                  example: '1'
                remark:
                  type: string
            example:
              name: Core Holdings
              application: elc_test
              remark: 备注说明
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    put:
      operationId: stock_group_edit
      summary: Edit Risk Stock Group
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Edit an existing stock-group configuration.

        ### Request parameters

        - Required: `id` (stock-group ID), `name` (stock-group name), `application` (application scenario).
        - Optional: `remark`, `audit_frontend_resource` (caller-provided audit context).
        - `id` comes from the stock-group record being edited.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Result

        On success, the stock group is updated; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - name
                - application
              properties:
                id:
                  type: string
                  description: Added in edit mode, taken from the edited row's record id.
                name:
                  type: string
                  description: This field is required.
                application:
                  type: string
                  description: Application scenario for the stock group.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                    - '4'
                    - '5'
                  x-enum-details:
                    - value: '1'
                      label: Simulate Margin Call
                    - value: '2'
                      label: Report
                    - value: '3'
                      label: Margin Call
                    - value: '4'
                      label: Concentration Limit
                    - value: '5'
                      label: Option Short Selling Whitelist
                  example: '1'
                remark:
                  type: string
                audit_frontend_resource:
                  type: object
                  description: In edit mode, the original record of the edited row is sent for before/after comparison on the approval ticket.
            example:
              id: '1001'
              name: Core Holdings
              application: elc_test
              remark: 备注说明
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
    delete:
      operationId: stock_group_delete
      summary: Delete Risk Stock Group
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Delete a stock group.

        ### Request parameters

        - Required: `id` (stock-group ID).
        - Optional: `audit_frontend_resource` (caller-provided audit context).
        - `id` locates the stock group to delete.
        - See the individual field descriptions for each field's format, allowed values, and business meaning.

        ### Risk warning

        This deletes a real stock-group configuration; afterwards any risk-control or business scenarios that depend on the group may be affected. Confirm the target stock group before calling.

        ### Result

        On success, the stock group is deleted; the resulting state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Stock Group ID associated with this request.
                audit_frontend_resource:
                  type: object
                  description: The full record of the deleted row, sent before submission for display on the approval ticket.
            example:
              id: '1001'
              audit_frontend_resource:
                name: Core Holdings
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk/stock-group/add-stocks:
    post:
      operationId: stock_group_add_stocks
      summary: Add Stocks to Group
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Bulk-adds securities to a specified stock group.

        ### Request parameters

        - Required: `group_id` (target stock group id) and `counter_ids` (the securities to add; multiple can be sent at once).
        - See the field descriptions for each field's format, source, and business meaning.

        ### Business notes

        Stock groups define the scope that risk-control rules apply to. This endpoint appends the securities in `counter_ids` to the group identified by `group_id`. `audit_frontend_resource` may be supplied to carry approval-display context.

        ### Risk warning

        This operation changes stock group membership and may affect risk strategies that reference the group. Confirm the target group and security list before calling.

        ### Result

        On success the securities in `counter_ids` are added to the specified group; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - group_id
                - counter_ids
              properties:
                group_id:
                  type: string
                  description: |-
                    Target stock group id that the stocks are added to.

                    Values are dynamic. Call `GET /v1/datasets/refs/stock_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/stock_groups
                  example: '12502'
                counter_ids:
                  type: array
                  items:
                    type: string
                  description: List of stock counter ids (`counter_id`) to add to the stock group; multiple values are allowed per call. Each element is a single stock's `counter_id`, consistent with the `counter_id` used by the stock group details dataset (`stock_group_details`) and the remove-stock endpoint (`stock-group/remove-stock`).
                  example:
                    - 700.HK
                    - AAPL.US
                audit_frontend_resource:
                  type: object
                  description: Caller-provided audit context for the approval workflow, such as the target stock group name (`stock_group_name`). It is used only for display in the approval process and does not affect processing. Omitting it does not affect the add operation.
            example:
              group_id: '12502'
              counter_ids:
                - 700.HK
                - AAPL.US
              audit_frontend_resource:
                stock_group_name: 裸卖空
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk/stock-group/remove-stock:
    delete:
      operationId: stock_group_remove_stock
      summary: Remove Stock from Group
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Removes a single security from a specified stock group.

        ### Request parameters

        - Required: `group_id` (target stock group id) and `counter_id` (the security to remove).
        - The request body is passed as JSON in the `DELETE` request.
        - See the field descriptions for each field's format, source, and business meaning.

        ### Risk warning

        This operation changes stock group membership and may affect risk strategies that reference the group. Confirm the target group and security before calling.

        ### Result

        On success the security identified by `counter_id` is removed from the specified group; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - group_id
                - counter_id
              properties:
                group_id:
                  type: string
                  description: |-
                    Target stock group id that the stock is removed from.

                    Values are dynamic. Call `GET /v1/datasets/refs/stock_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/stock_groups
                  example: '12502'
                counter_id:
                  type: string
                  description: Stock counter id (`counter_id`) to remove from the group. It matches the `counter_id` in the records returned by the stock group details dataset (`stock_group_details`), and is usually taken from the detail record being removed.
                  example: 700.HK
            example:
              group_id: '12502'
              counter_id: ST/US/AAPL
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/company_capital/query:
    get:
      operationId: company_capital_query
      summary: Query Company Capital
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Returns the current company-level capital configuration, including currency, shareholder funds, and the shareholder capital adjustment amount.

        ### Request parameters

        - This endpoint has no required parameters. Issue the `GET` request as the current account; the server returns the capital configuration for the caller's company.

        ### Business notes

        The capital configuration is read-only company-level information. It is modified through `POST /risk/v1/cp_capital_adjust/modify` (modify company capital).

        ### Returns

        On success the company's current capital configuration is returned in `data`. See the response definition below for field meanings.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The request was processed successfully. `data` contains the current company capital configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Current company capital configuration.
                    properties:
                      credit_currency:
                        type: string
                        description: Currency of the capital configuration, for example `USD` or `HKD`. When modifying capital this field is not editable; the currency returned here is reused as-is.
                      shareholder_fund:
                        type: string
                        description: Shareholder fund amount, denominated in `credit_currency`. Returned as a numeric string; may be `0` when not configured.
                      shareholder_amount:
                        type: string
                        description: Shareholder capital adjustment amount, denominated in `credit_currency`. Returned as a numeric string; may be `0` when not configured.
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/cp_capital_adjust/modify:
    post:
      operationId: cp_capital_adjust_modify
      summary: Modify Company Capital
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Updates the company-level capital configuration.

        ### Request parameters

        - Required: `credit_currency` (currency), `shareholder_fund` (shareholder funds), and `shareholder_amount` (shareholder capital adjustment amount).
        - All inputs are converted to strings before submission; the integer part of amount fields is at most 12–13 digits and may not be `0`.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        This endpoint directly updates the company capital configuration, which is the basis for company-level credit and risk calculations. `audit_frontend_resource` may be supplied to carry approval-display context.

        ### Risk warning

        This operation modifies the real company capital configuration and is extremely high-risk. Verify the currency, amounts, and target company before calling.

        ### Result

        On success the capital configuration is updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - credit_currency
                - shareholder_fund
                - shareholder_amount
              properties:
                credit_currency:
                  type: string
                  description: Displayed disabled in the form; taken from the record currently being edited.
                shareholder_fund:
                  type: string
                  description: The integer part is at most 12–13 digits; the client validates length and 0 is not allowed.
                shareholder_amount:
                  type: string
                  description: The integer part is at most 12–13 digits; the client validates length and 0 is not allowed.
                audit_frontend_resource:
                  type: object
                  description: Records the original record content before submission, used for comparison display in the approval ticket.
            example:
              credit_currency: USD
              shareholder_fund: '1000000'
              shareholder_amount: '500000'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk_monitoring/get_warning_val_cal_rule:
    post:
      operationId: get_warning_val_cal_rule
      summary: Get Warning Value Calculation Rule
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Returns the calculation strategy for margin-call warning values.

        ### Request parameters

        - This endpoint accepts no business parameters.

        ### Business notes

        Returns the currently effective rule for computing margin-call warning values, for use by risk monitoring.

        ### Returns

        On success the warning-value calculation strategy is returned in `data`. See the response definition below for field meanings.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: The request was processed successfully. `data` contains `margin`, `cash`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      margin:
                        description: '**Returned value:** Margin object. See the nested fields below for its structure.'
                        type: object
                        properties:
                          is_cal_in_debt:
                            type: boolean
                            description: Loan interest to be deducted
                          is_cal_nav:
                            type: boolean
                            description: Margin owed
                          is_cal_over_finance:
                            type: boolean
                            description: Financing limit exceeded
                          is_max_trade_credit:
                            type: boolean
                            description: Trading limit
                          ratio:
                            type: string
                            description: Ratio
                          over_available_finance:
                            type: string
                            description: Over-limit financing amount
                          is_auto_over_finance:
                            type: boolean
                            description: Whether over-limit financing is triggered automatically
                      cash:
                        description: '**Returned value:** Cash object. See the nested fields below for its structure.'
                        type: object
                        properties:
                          is_cal_in_debt:
                            type: boolean
                            description: Loan interest to be deducted
                          is_cal_nav:
                            type: boolean
                            description: Margin owed
                          is_max_trade_credit:
                            type: boolean
                            description: Trading limit
              example:
                code: 0
                message: success
                data:
                  margin:
                    is_cal_in_debt: false
                    is_cal_nav: false
                    is_cal_over_finance: false
                    is_max_trade_credit: false
                    ratio: string
                    over_available_finance: string
                    is_auto_over_finance: false
                  cash:
                    is_cal_in_debt: false
                    is_cal_nav: false
                    is_max_trade_credit: false
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/risk_monitoring/rule_engine/save_risk_rule:
    post:
      operationId: save_risk_rule
      summary: Save Risk Rule
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Creates or edits a risk-control engine rule.

        ### Request parameters

        - Required: `rule_name` (rule name), `group_name` (rule group), `status` (status), and `rule_detail` (rule detail list).
        - Optional: `risk_rule_id` (rule id), `aaid` (customer account id), `group_id` (customer group id), and `audit_frontend_resource` (approval-ticket display resource).
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        Both create and edit use this endpoint: omit `risk_rule_id` to create; supply `risk_rule_id` to edit that rule. A rule can be bound to a customer or customer group via `aaid` or `group_id`.

        ### Risk warning

        This operation affects the real risk-control rule engine and can change automated risk decisions and actions. Confirm the rule content and scope before calling.

        ### Result

        On success the rule is created or updated; see the `200` response definition for the returned content.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rule_name
                - group_name
                - status
                - rule_detail
              properties:
                risk_rule_id:
                  type: string
                  description: Passed when editing an existing rule; not passed when creating.
                rule_name:
                  type: string
                  description: This field is required.
                group_name:
                  type: string
                  description: Name of the risk group.
                  enum:
                    - ignore_margin_call
                    - over_financing_margin_call_ignore
                  x-enum-details:
                    - value: ignore_margin_call
                      label: MarginCall Exemption
                    - value: over_financing_margin_call_ignore
                      label: OverLimit MarginCall Exemption
                  example: ignore_margin_call
                status:
                  type: number
                  description: 'Single-select: 1 = enable, 2 = disable.'
                  enum:
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '1'
                      label: Enabled
                    - value: '2'
                      label: Disable
                  example: '1'
                aaid:
                  type: string
                  description: Submitted only when the rule type is "Customized - single account"; it is the selected client's AAID.
                group_id:
                  type: string
                  description: |-
                    Submitted only when the rule type is "Customized - client group"; values come from the
                    risk_client_groups dynamic enumeration and are not fixed.

                    Call `GET /v1/datasets/refs/risk_client_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/risk_client_groups
                rule_detail:
                  type: array
                  description: Each item contains parameter (the rule parameter name for float type, a numeric string).
                  items:
                    type: string
                audit_frontend_resource:
                  type: object
                  description: |-
                    Audit information shown in the approval ticket: operation type (add/edit), rule type
                    (global/customized), and a human-readable text comparison of the rule details before
                    and after the change.
            example:
              risk_rule_id: ''
              rule_name: Default Rule
              group_name: risk_group_1
              status: 1
              aaid: ''
              group_id: ''
              rule_detail:
                - parameter: CashAccountType
                  operator: Zhang San
                  target_value: '1'
              audit_frontend_resource:
                $action: add
                rule_type: system
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/account_credit_cfg/add_review:
    post:
      operationId: account_credit_cfg_add_review
      summary: Add Account Credit Config (Submit for Review)
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Creates a per-account (user-level) credit configuration for a specified securities account and submits it for review.

        ### Request parameters

        - Account scope: `member_id` (member id) and `aaid` (securities account) locate the target customer; both required.
        - Credit subject: `credit_type` and `credit_currency` define the scope of the credit configuration.
        - Limits and coefficients: `applicable`, `auto_credit_limit`, `credit_coefficient`, `ccf`, `credit_fps`, and related fields describe the credit limits and calculation parameters.
        - Coefficient rules: `credit_coefficient_rule_list` is an optional tiered credit-coefficient rule set; when supplied, also set `is_enable_credit_coefficient_rule` to `true`.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        This endpoint does not take effect immediately: a successful submission creates an approval ticket, and the configuration is applied only after the ticket is approved. The ticket id is returned in `data.ticket_id` of the `200` response.

        ### Risk warning

        This operation adds a real customer credit configuration that, once approved, affects the customer's available credit limit. Confirm the target customer and credit parameters before calling.

        ### Result

        On success an approval ticket is created and its id returned in `data.ticket_id`; the configuration takes effect only after the ticket is approved.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - member_id
                - aaid
                - credit_type
                - credit_currency
                - applicable
                - auto_credit_limit
                - ccf
                - auto_credit
                - credit_coefficient
              properties:
                member_id:
                  type: string
                  description: Member ID of the target client, used to locate the client being credited. This field is required.
                  example: '101456715'
                aaid:
                  type: string
                  description: A/C No. of the target client. This field is required.
                  example: H00000000
                credit_type:
                  type: string
                  description: |-
                    Credit type. The value comes from the credit-type data dictionary. This field is required.

                    Example: `1` denotes Margin Credit Limit.
                  example: '1'
                credit_currency:
                  type: string
                  description: Currency of the credit limit. This field is required.
                  example: HKD
                applicable:
                  type: integer
                  description: Maximum credit amount that can be submitted from the app; must be a multiple of `1000`. This field is required.
                  example: 1000000
                auto_credit_limit:
                  type: integer
                  description: Maximum limit for system auto-approval; must be a multiple of `1000`. This field is required.
                  example: 1000000
                net_asset_ratio:
                  type: string
                  description: Declared net-asset-value multiple, passed as a string. Required only when `credit_type` is `1` (Margin Credit Limit); omit for other credit types.
                  example: '1'
                credit_fps:
                  type: number
                  description: Account fundamental analysis factor (FPS), ranging from `0` to `1`. Required only when `credit_type` is `1` (Margin Credit Limit).
                  example: 0.3
                ccf:
                  type: number
                  description: Credit conversion factor (CCF), passed as a decimal. It is entered as a percentage (`0`–`100`) and must be divided by `100` before submission; for example, submit `1` for `100%`. This field is required.
                  example: 1
                auto_credit:
                  type: boolean
                  description: 'Whether auto-credit is enabled: `true` to enable, `false` to disable. This field is required.'
                  example: true
                credit_coefficient:
                  type: number
                  description: Default credit multiplier coefficient. This field is required.
                  example: 1
                is_enable_credit_coefficient_rule:
                  type: boolean
                  description: Whether tiered credit-coefficient rules are enabled. Set to `true` when `credit_coefficient_rule_list` is non-empty.
                  example: true
                credit_coefficient_rule_list:
                  type: array
                  description: Array of tiered credit-coefficient rules. Each item maps a value range of a given metric (such as client net assets) to a credit coefficient. Omit or pass an empty array when no tiered rules are configured.
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                        description: Metric type matched by the rule; the value comes from the credit-coefficient-rule-type dictionary, for example `0` for client net assets.
                        example: '0'
                      coefficient:
                        type: string
                        description: Credit coefficient applied when this range is matched.
                        example: '1.5'
                      left_operator:
                        type: string
                        description: Comparison operator for the lower bound; use the "unlimited" marker when the bound is open.
                        example: '0'
                      right_operator:
                        type: string
                        description: Comparison operator for the upper bound; use the "unlimited" marker when the bound is open.
                        example: '1'
                      left_value:
                        type: string
                        description: Value of the lower bound; may be empty when the lower bound is unlimited.
                        example: '0'
                      right_value:
                        type: string
                        description: Value of the upper bound; may be empty when the upper bound is unlimited.
                        example: '1000000'
            example:
              member_id: '101456715'
              aaid: H00000000
              credit_type: '1'
              credit_currency: HKD
              applicable: 1000000
              auto_credit_limit: 1000000
              net_asset_ratio: '1'
              credit_fps: 0.3
              ccf: 1
              auto_credit: true
              credit_coefficient: 1
              is_enable_credit_coefficient_rule: true
              credit_coefficient_rule_list:
                - type: '0'
                  coefficient: '1.5'
                  left_operator: '0'
                  right_operator: '1'
                  left_value: '0'
                  right_value: '1000000'
      responses:
        '200':
          description: The request was processed successfully. An approval ticket is created, and `data.ticket_id` contains the generated ticket ID.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Result of the review submission.
                    properties:
                      ticket_id:
                        type: string
                        description: ID of the approval ticket created after submission; use it to track the approval progress.
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/global_credit_cfg/modify_review:
    post:
      operationId: global_credit_cfg_modify_review
      summary: Modify Global Credit Config (Submit for Review)
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Modifies a global (tenant-level) credit configuration and submits it for review.

        ### Request parameters

        - Target record: `id` identifies the global credit configuration to modify; required.
        - Credit subject: `credit_type` and `credit_currency`.
        - Limits and coefficients: `applicable`, `auto_credit_limit`, `credit_coefficient`, `ccf`, `credit_fps`, and related fields describe the credit limits and calculation parameters.
        - Coefficient rules: `credit_coefficient_rule_list` is an optional tiered credit-coefficient rule set; when supplied, also set `is_enable_credit_coefficient_rule` to `true`.
        - Audit context: `audit_frontend_resource`, supplied by the caller, records audit information for this change.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        A global credit configuration applies to the entire tenant rather than an individual customer, so the request body has no account-level fields such as `member_id` or `aaid`; only `id` identifies the record. This endpoint does not take effect immediately: a successful submission creates an approval ticket, and the change is applied only after approval.

        ### Risk warning

        This operation modifies a tenant-level credit configuration that, once approved, affects credit calculations for the whole tenant. Confirm the target record and credit parameters before calling.

        ### Result

        On success an approval ticket is created and its id returned in `data.ticket_id`; the change takes effect only after the ticket is approved.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - credit_type
                - credit_currency
                - applicable
                - auto_credit_limit
                - ccf
                - auto_credit
                - credit_coefficient
              properties:
                id:
                  type: string
                  description: ID of the global credit configuration record to modify. This field is required.
                  example: '12345'
                credit_type:
                  type: string
                  description: |-
                    Credit type. The value comes from the credit-type data dictionary. This field is required.

                    Example: `1` denotes Margin Credit Limit.
                  example: '1'
                credit_currency:
                  type: string
                  description: Currency of the credit limit. This field is required.
                  example: HKD
                applicable:
                  type: integer
                  description: Maximum credit amount that can be submitted from the app; must be a multiple of `1000`. This field is required.
                  example: 1000000
                auto_credit_limit:
                  type: integer
                  description: Maximum limit for system auto-approval; must be a multiple of `1000`. This field is required.
                  example: 1000000
                net_asset_ratio:
                  type: string
                  description: Declared net-asset-value multiple, passed as a string. Required only when `credit_type` is `1` (Margin Credit Limit); omit for other credit types.
                  example: '1'
                credit_fps:
                  type: number
                  description: Account fundamental analysis factor (FPS), ranging from `0` to `1`. Required only when `credit_type` is `1` (Margin Credit Limit).
                  example: 0.3
                ccf:
                  type: number
                  description: Credit conversion factor (CCF), passed as a decimal. It is entered as a percentage (`0`–`100`) and must be divided by `100` before submission; for example, submit `1` for `100%`. This field is required.
                  example: 1
                auto_credit:
                  type: boolean
                  description: 'Whether auto-credit is enabled: `true` to enable, `false` to disable. This field is required.'
                  example: true
                credit_coefficient:
                  type: number
                  description: Default credit multiplier coefficient. This field is required.
                  example: 1
                is_enable_credit_coefficient_rule:
                  type: boolean
                  description: Whether tiered credit-coefficient rules are enabled. Set to `true` when `credit_coefficient_rule_list` is non-empty.
                  example: true
                credit_coefficient_rule_list:
                  type: array
                  description: Array of tiered credit-coefficient rules. Each item maps a value range of a given metric (such as client net assets) to a credit coefficient. Omit or pass an empty array when no tiered rules are configured.
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                        description: Metric type matched by the rule; the value comes from the credit-coefficient-rule-type dictionary, for example `0` for client net assets.
                        example: '0'
                      coefficient:
                        type: string
                        description: Credit coefficient applied when this range is matched.
                        example: '1.5'
                      left_operator:
                        type: string
                        description: Comparison operator for the lower bound; use the "unlimited" marker when the bound is open.
                        example: '0'
                      right_operator:
                        type: string
                        description: Comparison operator for the upper bound; use the "unlimited" marker when the bound is open.
                        example: '1'
                      left_value:
                        type: string
                        description: Value of the lower bound; may be empty when the lower bound is unlimited.
                        example: '0'
                      right_value:
                        type: string
                        description: Value of the upper bound; may be empty when the upper bound is unlimited.
                        example: '1000000'
                audit_frontend_resource:
                  type: object
                  description: Caller-provided audit context recorded for this modification (for example, a snapshot of the record before the change).
            example:
              id: '12345'
              credit_type: '1'
              credit_currency: HKD
              applicable: 1000000
              auto_credit_limit: 1000000
              net_asset_ratio: '1'
              credit_fps: 0.3
              ccf: 1
              auto_credit: true
              credit_coefficient: 1
              is_enable_credit_coefficient_rule: true
              credit_coefficient_rule_list:
                - type: '0'
                  coefficient: '1.5'
                  left_operator: '0'
                  right_operator: '1'
                  left_value: '0'
                  right_value: '1000000'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. An approval ticket is created, and `data.ticket_id` contains the generated ticket ID.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Result of the review submission.
                    properties:
                      ticket_id:
                        type: string
                        description: ID of the approval ticket created after submission; use it to track the approval progress.
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /risk/v1/account_credit_cfg/delete:
    post:
      operationId: account_credit_cfg_delete
      summary: Delete Account Credit Config
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_credit_limit.credit_limit_para_update
      x-lbonly: false
      description: |-
        Deletes a per-account credit parameter configuration for a specified customer.

        ### Request parameters

        - Required: `aaid` (securities account), `member_id` (member id), and `credit_type` (credit type).
        - The three fields together locate the customer credit configuration to delete.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Risk warning

        This operation deletes a real customer credit configuration; afterward the customer's configuration for that credit type no longer applies. Confirm the target customer and credit type before calling.

        ### Result

        On success the specified customer credit configuration is deleted; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - aaid
                - member_id
                - credit_type
              properties:
                aaid:
                  type: string
                  description: Specifies A/C No.. This field is required.
                member_id:
                  type: string
                  description: Member ID associated with this request.
                credit_type:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
            example:
              aaid: H00000000
              member_id: '101456715'
              credit_type: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/atm_withdraw_package_rules/download:
    post:
      operationId: atm_withdraw_package_rules_download
      summary: Withdrawal Rules Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_query
      x-lbonly: false
      description: |-
        Exports the "automatic withdrawal rules" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_withdraw_package_rules.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by Withdrawal Rule ID.
                    lb_bank_channel_id:
                      type: string
                      description: |-
                        Filters results by Withdraw Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_bank_channels` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_bank_channels
                    package_type:
                      type: string
                      description: Filters results by Withdrawal Policy.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                        - '4'
                      x-enum-details:
                        - value: '1'
                          label: Real Time Payment Of Single Order
                        - value: '2'
                          label: Manual Review Of Timed Packing
                        - value: '3'
                          label: Manual Packing
                        - value: '4'
                          label: Batch Auto
                      example: '1'
                    support_region:
                      type: string
                      description: |-
                        Filters results by Supported Bank Types.

                        Values are dynamic. Call `GET /v1/datasets/refs/cash_notice_bank_region` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/cash_notice_bank_region
                    support_bank_ids:
                      type: string
                      description: |-
                        Filters results by Supported Bank Name.

                        Values are dynamic. Call `GET /v1/datasets/refs/cash_notice_region_bank_name` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/cash_notice_region_bank_name
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Enabled
                        - value: '2'
                          label: Disabled
                      example: '1'
                    audit_status:
                      type: string
                      description: Filters results by Audit Status.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '0'
                          label: Init
                        - value: '1'
                          label: New pending review
                        - value: '2'
                          label: Waiting Edit Review
                        - value: '3'
                          label: Delete pending approval
                      example: '0'
                    original_limit:
                      type: string
                      description: Filters results by Original Limit.
                      enum:
                        - '0'
                        - '1'
                      x-enum-details:
                        - value: '0'
                          label: Unrestricted
                        - value: '1'
                          label: Restricted
                      example: '0'
                    rule_codes:
                      type: string
                      description: |-
                        Filters results by Hit Rules.

                        Values are dynamic. Call `GET /v1/datasets/refs/risk_rule_indicators_withdraw` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/risk_rule_indicators_withdraw
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - original_limit
                      - original_limit_val
                      - rule_codes
                      - rule_codes_val
                      - custom_lb_bank_channel_id
                      - custom_original_limit_accounts
                      - custom_original_limit_accounts_val
                      - fee_bank_account_id
                      - fee_bank_account_id_val
                      - package_type
                      - package_type_val
                      - batch_max_count
                      - charge_type
                      - custom_single_quota_currency_amount
                      - custom_service_time
                      - custom_lb_service_time
                      - support_region
                      - support_region_val
                      - support_bank_ids
                      - support_bank_ids_val
                      - sec_auth_show
                      - sec_auth_show_val
                      - priority
                      - status
                      - status_val
                      - audit_status
                      - audit_status_val
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Withdrawal Rules Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_withdraw_package_rules
        operationId: atm_withdraw_package_rules
  /v1/datasets/atm_withdraw_package_rules:
    post:
      operationId: atm_withdraw_package_rules
      summary: Withdrawal Rules
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.outcashpackagerule_query
      x-lbonly: false
      description: |-
        Queries the "automatic withdrawal rules" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Withdrawal Rule ID.
                      example: '10000006675'
                    lb_bank_channel_id:
                      type: string
                      description: |-
                        Filters results by Withdraw Channel.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_bank_channels` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_bank_channels
                    package_type:
                      type: string
                      description: Filters results by Withdrawal Policy.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                        - '4'
                      x-enum-details:
                        - value: '1'
                          label: Real Time Payment Of Single Order
                        - value: '2'
                          label: Manual Review Of Timed Packing
                        - value: '3'
                          label: Manual Packing
                        - value: '4'
                          label: Batch Auto
                      example: Real Time Payment Of Single Order
                    support_region:
                      type: string
                      description: |-
                        Filters results by Supported Bank Types.

                        Values are dynamic. Call `GET /v1/datasets/refs/cash_notice_bank_region` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/cash_notice_bank_region
                    support_bank_ids:
                      type: string
                      description: |-
                        Filters results by Supported Bank Name.

                        Values are dynamic. Call `GET /v1/datasets/refs/cash_notice_region_bank_name` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/cash_notice_region_bank_name
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Enabled
                        - value: '2'
                          label: Disabled
                      example: Enabled
                    audit_status:
                      type: string
                      description: Filters results by Audit Status.
                      enum:
                        - '0'
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '0'
                          label: Init
                        - value: '1'
                          label: New pending review
                        - value: '2'
                          label: Waiting Edit Review
                        - value: '3'
                          label: Delete pending approval
                      example: Init
                    original_limit:
                      type: string
                      description: Filters results by Original Limit.
                      enum:
                        - '0'
                        - '1'
                      x-enum-details:
                        - value: '0'
                          label: Unrestricted
                        - value: '1'
                          label: Restricted
                      example: Unrestricted
                    rule_codes:
                      type: string
                      description: |-
                        Filters results by Hit Rules.

                        Values are dynamic. Call `GET /v1/datasets/refs/risk_rule_indicators_withdraw` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/risk_rule_indicators_withdraw
                      example: All
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Withdrawal Rules records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            original_limit:
                              type: string
                              description: '**Returned value:** Original Limit.'
                            original_limit_val:
                              type: string
                              description: Raw value of `original_limit`. The `original_limit` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            rule_codes:
                              type: string
                              description: '**Returned value:** Hit Rules.'
                            rule_codes_val:
                              type: string
                              description: Raw value of `rule_codes`. The `rule_codes` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_lb_bank_channel_id:
                              type: string
                              description: '**Returned value:** Withdraw Channel.'
                            custom_original_limit_accounts:
                              type: string
                              description: '**Returned value:** Withdrawal Account.'
                            custom_original_limit_accounts_val:
                              type: string
                              description: Raw value of `custom_original_limit_accounts`. The `custom_original_limit_accounts` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            fee_bank_account_id:
                              type: string
                              description: '**Returned value:** Service fee account.'
                            fee_bank_account_id_val:
                              type: string
                              description: Raw value of `fee_bank_account_id`. The `fee_bank_account_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            package_type:
                              type: string
                              description: '**Returned value:** Withdrawal Policy.'
                            package_type_val:
                              type: string
                              description: Raw value of `package_type`. The `package_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            batch_max_count:
                              type: string
                              description: '**Returned value:** Batch Maximum Number.'
                            charge_type:
                              type: string
                              description: '**Returned value:** Charge Type.'
                            custom_single_quota_currency_amount:
                              type: string
                              description: '**Returned value:** Supported Currencies & Single Limit.'
                            custom_service_time:
                              type: string
                              description: '**Returned value:** Bank Service Counter. The date or time format follows the value returned by the API.'
                            custom_lb_service_time:
                              type: string
                              description: '**Returned value:** Company Service Period. The date or time format follows the value returned by the API.'
                            support_region:
                              type: string
                              description: '**Returned value:** Bank Region.'
                            support_region_val:
                              type: string
                              description: Raw value of `support_region`. The `support_region` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            support_bank_ids:
                              type: string
                              description: '**Returned value:** Supported Bank List Name.'
                            support_bank_ids_val:
                              type: string
                              description: Raw value of `support_bank_ids`. The `support_bank_ids` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            sec_auth_show:
                              type: string
                              description: '**Returned value:** Bank Securities Supported?.'
                            sec_auth_show_val:
                              type: string
                              description: Raw value of `sec_auth_show`. The `sec_auth_show` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            audit_status:
                              type: string
                              description: '**Returned value:** Audit Status.'
                            audit_status_val:
                              type: string
                              description: Raw value of `audit_status`. The `audit_status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '10000006675'
                      original_limit: Unrestricted
                      original_limit_val: '0'
                      rule_codes: All
                      rule_codes_val: all
                      custom_lb_bank_channel_id: DBS016DBS_API_原路退回
                      custom_original_limit_accounts: |-
                        Bank Name:DBS
                        Bank Account：0725865530
                        Abbreviation：DBS016DBS
                        Swift Code：DBSSSGSGXXX
                      custom_original_limit_accounts_val: '119'
                      fee_bank_account_id: Zhang San
                      fee_bank_account_id_val: Zhang San
                      package_type: Real Time Payment Of Single Order
                      package_type_val: '1'
                      batch_max_count: '100'
                      charge_type: SHA
                      custom_single_quota_currency_amount: 'Currency: HKD Amount: 1,111,111.00'
                      custom_service_time: HK Trading Day:00:00:00-13:00:00
                      custom_lb_service_time: HK Trading Day:00:00:00-23:00:00
                      support_region: ''
                      support_region_val: ''
                      support_bank_ids: ''
                      support_bank_ids_val: ''
                      sec_auth_show: 'NO'
                      sec_auth_show_val: '0'
                      priority: '998'
                      status: Enabled
                      status_val: '1'
                      audit_status: Init
                      audit_status_val: '0'
                      created_at: '1784172062'
                      updated_at: '1784172177'
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_withdraw_package_rules/download
        operationId: atm_withdraw_package_rules_download
  /v1/datasets/atm_bank_daily_limits/download:
    post:
      operationId: atm_bank_daily_limits_download
      summary: Daily limits Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.bankbalance_query
      x-lbonly: false
      description: |-
        Exports the "daily limits" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_bank_daily_limits.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by Daily Limit Setup ID.
                    lb_bank_name:
                      type: string
                      description: |-
                        Filters results by Bank Account Name.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_bank_names` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_bank_names
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - last_bill_date
                      - lb_bank_name
                      - bank_accounts
                      - bank_operation_types_names
                      - currency
                      - limit_amount
                      - alarm_amount
                      - remaining_amount
                      - created_at
                      - updated_at
                      - remark
                      - operator
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Daily limits Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_bank_daily_limits
        operationId: atm_bank_daily_limits
  /v1/datasets/atm_bank_daily_limits:
    post:
      operationId: atm_bank_daily_limits
      summary: Daily limits
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.bankbalance_query
      x-lbonly: false
      description: |-
        Queries the "daily limits" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Daily Limit Setup ID.
                    lb_bank_name:
                      type: string
                      description: |-
                        Filters results by Bank Account Name.

                        Values are dynamic. Call `GET /v1/datasets/refs/company_bank_names` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/company_bank_names
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Daily limits records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** Records matching the query conditions.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            last_bill_date:
                              type: string
                              description: '**Returned value:** Cycle Base Date. The date or time format follows the value returned by the API.'
                            lb_bank_name:
                              type: string
                              description: '**Returned value:** Bank Account Name.'
                            bank_accounts:
                              type: string
                              description: '**Returned value:** A/C No..'
                            bank_operation_types_names:
                              type: string
                              description: '**Returned value:** Withdraw Method.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            limit_amount:
                              type: string
                              description: '**Returned value:** Initial Quota.'
                            alarm_amount:
                              type: string
                              description: '**Returned value:** Alarm Level Of Remaining Daily Quota.'
                            remaining_amount:
                              type: string
                              description: '**Returned value:** Remaining Daily Quota.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      last_bill_date: string
                      lb_bank_name: string
                      bank_accounts: string
                      bank_operation_types_names: string
                      currency: string
                      limit_amount: string
                      alarm_amount: string
                      remaining_amount: string
                      created_at: string
                      updated_at: string
                      remark: string
                      operator: string
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_bank_daily_limits/download
        operationId: atm_bank_daily_limits_download
  /v1/datasets/atm_bank_daily_limit_changes/download:
    post:
      operationId: atm_bank_daily_limit_changes_download
      summary: Daily Limit Changes Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.bankbalancechange_query
      x-lbonly: false
      description: |-
        Exports the "daily limit changes" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_bank_daily_limit_changes.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by Daily Limit Change ID.
                    change_type:
                      type: string
                      description: Filters results by Business Type.
                      enum:
                        - plus
                        - minus
                        - withdraw
                        - withdraw_failed
                      x-enum-details:
                        - value: plus
                          label: Manual Increase
                        - value: minus
                          label: Manual Reduce
                        - value: withdraw
                          label: Amount In Withdrawal Process
                        - value: withdraw_failed
                          label: Client Failed To Withdraw
                      example: plus
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - bank_balance_id
                      - bill_date
                      - change_type
                      - change_type_val
                      - lb_bank_name
                      - bank_accounts
                      - bank_operation_types_names
                      - currency
                      - order_no
                      - before_occurred
                      - occurred_amount
                      - after_occurred
                      - occurred_from
                      - occurred_from_val
                      - created_at
                      - updated_at
                      - remark
                      - operator
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Daily Limit Changes Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_bank_daily_limit_changes
        operationId: atm_bank_daily_limit_changes
  /v1/datasets/atm_bank_daily_limit_changes:
    post:
      operationId: atm_bank_daily_limit_changes
      summary: Daily Limit Changes
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_outcashpackagerule.bankbalancechange_query
      x-lbonly: false
      description: |-
        Queries the "daily limit changes" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Daily Limit Change ID.
                      example: '264031'
                    change_type:
                      type: string
                      description: Filters results by Business Type.
                      enum:
                        - plus
                        - minus
                        - withdraw
                        - withdraw_failed
                      x-enum-details:
                        - value: plus
                          label: Manual Increase
                        - value: minus
                          label: Manual Reduce
                        - value: withdraw
                          label: Amount In Withdrawal Process
                        - value: withdraw_failed
                          label: Client Failed To Withdraw
                      example: Manual Reduce
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                      example: USD
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Daily Limit Changes records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            bank_balance_id:
                              type: string
                              description: '**Returned value:** Daily Limit Setup ID.'
                            bill_date:
                              type: string
                              description: '**Returned value:** Cycle Base Date. The date or time format follows the value returned by the API.'
                            change_type:
                              type: string
                              description: '**Returned value:** Business Type.'
                            change_type_val:
                              type: string
                              description: Raw value of `change_type`. The `change_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            lb_bank_name:
                              type: string
                              description: '**Returned value:** Bank Account Name.'
                            bank_accounts:
                              type: string
                              description: '**Returned value:** A/C No..'
                            bank_operation_types_names:
                              type: string
                              description: '**Returned value:** Withdraw Method.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            order_no:
                              type: string
                              description: '**Returned value:** Order ID.'
                            before_occurred:
                              type: string
                              description: '**Returned value:** Initial Remaining Daily Quota.'
                            occurred_amount:
                              type: string
                              description: '**Returned value:** Amount Incurred.'
                            after_occurred:
                              type: string
                              description: '**Returned value:** Remaining Daily Quota.'
                            occurred_from:
                              type: string
                              description: '**Returned value:** Automatically By The System?.'
                            occurred_from_val:
                              type: string
                              description: Raw value of `occurred_from`. The `occurred_from` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '264031'
                      bank_balance_id: '17992'
                      bill_date: 2026-07-16
                      change_type: Manual Reduce
                      change_type_val: minus
                      lb_bank_name: 353-测试银行
                      bank_accounts: Zhang San
                      bank_operation_types_names: 网银_FPS
                      currency: USD
                      order_no: 3f74fd07-951a-438f-9cb7-77075161701f
                      before_occurred: 2,000.00
                      occurred_amount: -1,982.69
                      after_occurred: '17.31'
                      occurred_from: Manual
                      occurred_from_val: manual
                      created_at: '1784141366'
                      updated_at: '1784141366'
                      remark: ''
                      operator: user@example.com
                  total: 909
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_bank_daily_limit_changes/download
        operationId: atm_bank_daily_limit_changes_download
  /v1/datasets/atm_balance_rules/download:
    post:
      operationId: atm_balance_rules_download
      summary: Balance Blocking Rules Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_risk_withdraw.risk_balance_rules_export
      x-lbonly: false
      description: |-
        Exports the "balance interception" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of atm_balance_rules.yaml (the query capability).
                  properties:
                    biz_type:
                      type: string
                      description: Filters results by Business Type.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: withdraw
                        - value: '2'
                          label: forex
                      example: '1'
                    rule_status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: 'on'
                        - value: '2'
                          label: 'off'
                      example: '1'
                    bank_code:
                      type: string
                      description: Filters results by Bank Code.
                    bank_account:
                      type: string
                      description: Filters results by Account No.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - bank_code
                      - bank_account
                      - biz_type
                      - biz_type_val
                      - rule_status
                      - rule_status_val
                      - currency
                      - warning_amount
                      - intercept_amount
                      - updated_at
                      - modify_operator
                    example: bank_code
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Balance Blocking Rules Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/atm_balance_rules
        operationId: atm_balance_rules
  /v1/datasets/atm_balance_rules:
    post:
      operationId: atm_balance_rules
      summary: Balance Blocking Rules
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_risk_withdraw.risk_balance_rules_query
      x-lbonly: false
      description: |-
        Queries the "balance interception" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    biz_type:
                      type: string
                      description: Filters results by Business Type.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: withdraw
                        - value: '2'
                          label: forex
                      example: Forex
                    rule_status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: 'on'
                        - value: '2'
                          label: 'off'
                      example: 'Off'
                    bank_code:
                      type: string
                      description: Filters results by Bank Code.
                      example: '072'
                    bank_account:
                      type: string
                      description: Filters results by Account No.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Balance Blocking Rules records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            bank_code:
                              type: string
                              description: '**Returned value:** Bank Code.'
                            bank_account:
                              type: string
                              description: '**Returned value:** Account No.'
                            biz_type:
                              type: string
                              description: '**Returned value:** Business Type.'
                            biz_type_val:
                              type: string
                              description: Raw value of `biz_type`. The `biz_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            rule_status:
                              type: string
                              description: '**Returned value:** Status.'
                            rule_status_val:
                              type: string
                              description: Raw value of `rule_status`. The `rule_status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            warning_amount:
                              type: string
                              description: '**Returned value:** Warning Amount.'
                            intercept_amount:
                              type: string
                              description: '**Returned value:** Intercept Amount.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Modified At. The date or time format follows the value returned by the API.'
                            modify_operator:
                              type: string
                              description: '**Returned value:** Processed By.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - bank_code: '072'
                      bank_account: Zhang San
                      biz_type: Forex
                      biz_type_val: '2'
                      rule_status: 'Off'
                      rule_status_val: '2'
                      currency: USD
                      warning_amount: '555'
                      intercept_amount: '666'
                      updated_at: '1769571199'
                      modify_operator: Zhang San
                  total: 7
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/atm_balance_rules/download
        operationId: atm_balance_rules_download
  /v1/datasets/atm_withdraw_prompts:
    post:
      operationId: atm_withdraw_prompts
      summary: Withdrawal Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - withdrawalparameter.withdrawal_parameter_query
      x-lbonly: false
      description: |-
        Queries the "withdrawal parameter configuration" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Id.
                    rule_status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - '1'
                        - '2'
                      x-enum-details:
                        - value: '1'
                          label: Enabled
                        - value: '2'
                          label: Disabled
                      example: '1'
                    rule_name:
                      type: string
                      description: Filters results by Rule Name.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Withdrawal Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            region_codes:
                              type: string
                              description: '**Returned value:** Bank Region.'
                            region_codes_val:
                              type: string
                              description: Raw value of `region_codes`. The `region_codes` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            bank_ids:
                              type: string
                              description: '**Returned value:** Bank Name.'
                            bank_ids_val:
                              type: string
                              description: Raw value of `bank_ids`. The `bank_ids` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_time:
                              type: string
                              description: '**Returned value:** Submission Deadline. The date or time format follows the value returned by the API.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            rule_status:
                              type: string
                              description: '**Returned value:** Status.'
                            rule_status_val:
                              type: string
                              description: Raw value of `rule_status`. The `rule_status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created At. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            modify_operator:
                              type: string
                              description: '**Returned value:** Modify Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      region_codes: string
                      region_codes_val: string
                      bank_ids: string
                      bank_ids_val: string
                      custom_time: string
                      currency: string
                      priority: string
                      rule_status: string
                      rule_status_val: string
                      created_at: string
                      updated_at: string
                      modify_operator: string
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/risk_monitoring_configs/download:
    post:
      operationId: risk_monitoring_configs_download
      summary: Auto Close Position Toggle Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_margin_call.margin_call_auto_batch_forccconfig
      x-lbonly: false
      description: |-
        Exports the "auto liquidation switch" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of risk_monitoring_configs.yaml (the query capability).
                  properties:
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - trade_type
                      - trade_type_val
                      - force_type
                      - force_type_val
                      - status
                      - status_val
                      - op_user
                      - updated_at
                    example: trade_type
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Auto Close Position Toggle Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/risk_monitoring_configs
        operationId: risk_monitoring_configs
  /v1/datasets/risk_monitoring_configs:
    post:
      operationId: risk_monitoring_configs
      summary: Auto Close Position Toggle
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_margin_call.margin_call_auto_batch_forccconfig
      x-lbonly: false
      description: |-
        Queries the "auto liquidation switch" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Auto Close Position Toggle records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            trade_type:
                              type: string
                              description: '**Returned value:** Trade Type.'
                            trade_type_val:
                              type: string
                              description: Raw value of `trade_type`. The `trade_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            force_type:
                              type: string
                              description: '**Returned value:** Forceselling Type.'
                            force_type_val:
                              type: string
                              description: Raw value of `force_type`. The `force_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            op_user:
                              type: string
                              description: '**Returned value:** Processed By.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - trade_type: Margin Trade
                      trade_type_val: Normal
                      force_type: Manual Forceselling (User)
                      force_type_val: Manual
                      status: Opend
                      status_val: 'true'
                      op_user: user@example.com
                      updated_at: '1774596600'
                  total: 4
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/risk_monitoring_configs/download
        operationId: risk_monitoring_configs_download
  /v1/datasets/account_exceeding_withdraw_limit_configs/download:
    post:
      operationId: account_exceeding_withdraw_limit_configs_download
      summary: Client Fast Repayment Tier Limit Settings Export
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.withdrawal_limit_query
      x-lbonly: false
      description: |-
        Exports the "account-level instant-repayment limit settings" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of account_exceeding_withdraw_limit_configs.yaml (the query capability).
                  properties:
                    aaid:
                      type: string
                      description: Filters results by Client.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - origin_id
                      - real_name
                      - is_apply
                      - is_apply_val
                      - currency
                      - quota
                      - last_update_user
                      - updated_at
                    example: origin_id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Client Fast Repayment Tier Limit Settings Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/account_exceeding_withdraw_limit_configs
        operationId: account_exceeding_withdraw_limit_configs
  /v1/datasets/account_exceeding_withdraw_limit_configs:
    post:
      operationId: account_exceeding_withdraw_limit_configs
      summary: Client Fast Repayment Tier Limit Settings
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.withdrawal_limit_query
      x-lbonly: false
      description: |-
        Queries the "account-level instant-repayment limit settings" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    aaid:
                      type: string
                      description: Filters results by Client.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Client Fast Repayment Tier Limit Settings records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            origin_id:
                              type: string
                              description: '**Returned value:** A/C No.'
                            real_name:
                              type: string
                              description: '**Returned value:** Client Name.'
                            is_apply:
                              type: string
                              description: '**Returned value:** Indicates whether status.'
                            is_apply_val:
                              type: string
                              description: Raw value of `is_apply`. The `is_apply` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            quota:
                              type: string
                              description: '**Returned value:** Quota Limit.'
                            last_update_user:
                              type: string
                              description: '**Returned value:** Operator.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Operating Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - origin_id: H00000000
                      real_name: Zhang San
                      is_apply: Effective
                      is_apply_val: 'true'
                      currency: HKD
                      quota: 1,000.00
                      last_update_user: user@example.com
                      updated_at: '1782182679'
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/account_exceeding_withdraw_limit_configs/download
        operationId: account_exceeding_withdraw_limit_configs_download
  /v1/datasets/instrument_stock_groups/download:
    post:
      operationId: instrument_stock_groups_download
      summary: Stock Group Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.stock_group_list_export
      x-lbonly: false
      description: |-
        Exports the "stock group configuration" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of instrument_stock_groups.yaml (the query capability).
                  properties:
                    name:
                      type: string
                      description: Filters results by Stock Group Name.
                    id:
                      type: string
                      description: |-
                        Filters results by Stock Group.

                        Values are dynamic. Call `GET /v1/datasets/refs/stock_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/stock_groups
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - name
                      - application
                      - application_val
                      - stock_count
                      - remark
                      - updated_at
                      - last_operator
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Stock Group Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/instrument_stock_groups
        operationId: instrument_stock_groups
  /v1/datasets/instrument_stock_groups:
    post:
      operationId: instrument_stock_groups
      summary: Stock Group Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.stock_group_list_query
      x-lbonly: false
      description: |-
        Queries the "stock group configuration" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    name:
                      type: string
                      description: Filters results by Stock Group Name.
                      example: 裸卖空
                    id:
                      type: string
                      description: |-
                        Filters results by Stock Group.

                        Values are dynamic. Call `GET /v1/datasets/refs/stock_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/stock_groups
                      example: '12502'
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Stock Group Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            name:
                              type: string
                              description: '**Returned value:** Stock Group Name.'
                            application:
                              type: string
                              description: '**Returned value:** Application Scenarios.'
                            application_val:
                              type: string
                              description: Raw value of `application`. The `application` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            stock_count:
                              type: string
                              description: '**Returned value:** Stock Count.'
                            remark:
                              type: string
                              description: '**Returned value:** Remarks.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            last_operator:
                              type: string
                              description: '**Returned value:** Last Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '12502'
                      name: 裸卖空
                      application: Option Short Selling Whitelist
                      application_val: '5'
                      stock_count: '1'
                      remark: ''
                      updated_at: '1784014380'
                      last_operator: user@example.com
                  total: 38
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/instrument_stock_groups/download
        operationId: instrument_stock_groups_download
  /v1/datasets/stock_group_details/download:
    post:
      operationId: stock_group_details_download
      summary: Stock Group Details Export
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.stock_group_details_export
      x-lbonly: false
      description: |-
        Exports the "stock group details" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of stock_group_details.yaml (the query capability).
                  properties:
                    counter_id:
                      type: string
                mode:
                  type: integer
                  default: 0
                  description: Defaults to 0 (sync) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Supports csv/xlsx; defaults to csv.
                  enum:
                    - csv
                    - xlsx
                  example: csv
                file_name:
                  type: string
                down_fields:
                  type: array
                  description: When omitted, all fields are exported; allowed values are listed under items.enum (the same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - counter_id
                      - counter_id_val
                      - stock_name
                      - market
                      - market_val
                      - stock_count
                      - updated_at
                      - operator
                    example: counter_id
                ticket_content:
                  type: string
                  description: Shown to the approver when the template has a download-approval ticket configured.
                ticket_preview_url:
                  type: string
                hide:
                  type: integer
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: Export result. In sync mode the download URL is returned directly; in async mode a file_download_id is returned for subsequent polling.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: 0 means success; a non-zero value is a business error code (data is usually empty in that case). The HTTP status is always 200 — do not rely on the HTTP status to judge success or failure.
                    enum:
                      - 0
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: Returned in sync mode (mode:0); empty in async mode.
                      file_download_id:
                        type: string
                        description: 'Returned in async mode (mode:1); it is the {id} in GET/DELETE /v1/datasets/download/{id}: GET obtains the final download URL, DELETE deletes the export record.'
                      ticket_id:
                        type: string
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400': &id001
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        '401': &id002
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
          description: The user is not authenticated, or the login session has expired.
        '403': &id003
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        '408': &id004
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-dataset-parent:
        method: GET
        path: /v1/datasets/stock_group_details
        operationId: stock_group_details
  /v1/datasets/stock_group_details:
    post:
      operationId: stock_group_details
      summary: Stock Group Details
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.stock_group_details_query
      x-lbonly: false
      description: |-
        Queries the "stock group details" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    counter_id:
                      type: string
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: 0 means success; a non-zero value is a business error code (data is usually empty in that case). The HTTP status is always 200 — do not rely on the HTTP status to judge success or failure.
                    enum:
                      - 0
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            counter_id:
                              type: string
                            counter_id_val:
                              type: string
                              description: 'Raw value behind field counter_id: counter_id returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.'
                            stock_name:
                              type: string
                            market:
                              type: string
                            market_val:
                              type: string
                              description: 'Raw value behind field market: market returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.'
                            stock_count:
                              type: string
                            updated_at:
                              type: string
                            operator:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - counter_id: string
                      counter_id_val: string
                      stock_name: string
                      market: string
                      market_val: string
                      stock_count: string
                      updated_at: string
                      operator: string
                  total: 1
        '400': *id001
        '401': *id002
        '403': *id003
        '408': *id004
      x-dataset-download:
        method: POST
        path: /v1/datasets/stock_group_details/download
        operationId: stock_group_details_download
  /v1/datasets/risk_rules/download:
    post:
      operationId: risk_rules_download
      summary: Risk Rules Management Export
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.risk_rules_manager_export
      x-lbonly: false
      description: |-
        Exports the "risk rule management" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of risk_rules.yaml (the query capability).
                  properties:
                    rule_name:
                      type: string
                      description: Filters results by Rule Name.
                    group_name:
                      type: string
                      description: Filters results by Risk Control Condition.
                      enum:
                        - ignore_margin_call
                        - over_financing_margin_call_ignore
                      x-enum-details:
                        - value: ignore_margin_call
                          label: MarginCall Exemption
                        - value: over_financing_margin_call_ignore
                          label: OverLimit MarginCall Exemption
                      example: ignore_margin_call
                    rule_type:
                      type: string
                      description: Filters results by Rule Type.
                      enum:
                        - global
                        - personalise
                      x-enum-details:
                        - value: global
                          label: Global
                        - value: personalise
                          label: Personalise
                      example: global
                    aaid:
                      type: string
                      description: Filters results by Client.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - group_name
                      - group_name_val
                      - rule_type
                      - rule_type_val
                      - rule_name
                      - status
                      - status_val
                      - updated_at
                      - opt_user
                      - aaid
                      - aaid_val
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Risk Rules Management Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/risk_rules
        operationId: risk_rules
  /v1/datasets/risk_rules:
    post:
      operationId: risk_rules
      summary: Risk Rules Management
      tags:
        - Service Parameter
      x-permission-key:
        - risk_config.risk_rules_manager_query
      x-lbonly: false
      description: |-
        Queries the "risk rule management" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    rule_name:
                      type: string
                      description: Filters results by Rule Name.
                      example: test 20260204
                    group_name:
                      type: string
                      description: Filters results by Risk Control Condition.
                      enum:
                        - ignore_margin_call
                        - over_financing_margin_call_ignore
                      x-enum-details:
                        - value: ignore_margin_call
                          label: MarginCall Exemption
                        - value: over_financing_margin_call_ignore
                          label: OverLimit MarginCall Exemption
                      example: MarginCall Exemption
                    rule_type:
                      type: string
                      description: Filters results by Rule Type.
                      enum:
                        - global
                        - personalise
                      x-enum-details:
                        - value: global
                          label: Global
                        - value: personalise
                          label: Personalise
                      example: Personalise
                    aaid:
                      type: string
                      description: Filters results by Client.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Risk Rules Management records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            group_name:
                              type: string
                              description: '**Returned value:** Risk Control Condition.'
                            group_name_val:
                              type: string
                              description: Raw value of `group_name`. The `group_name` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            rule_type:
                              type: string
                              description: '**Returned value:** Rule Type.'
                            rule_type_val:
                              type: string
                              description: Raw value of `rule_type`. The `rule_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            rule_name:
                              type: string
                              description: '**Returned value:** Rule Name.'
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Date. The date or time format follows the value returned by the API.'
                            opt_user:
                              type: string
                              description: '**Returned value:** Operator.'
                            aaid:
                              type: string
                              description: '**Returned value:** A/C No..'
                            aaid_val:
                              type: string
                              description: Raw value of `aaid`. The `aaid` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '643'
                      group_name: MarginCall Exemption
                      group_name_val: ignore_margin_call
                      rule_type: Personalise
                      rule_type_val: personalise
                      rule_name: test 20260204
                      status: Disable
                      status_val: '2'
                      updated_at: '1770172159'
                      opt_user: user@example.com
                      aaid: H00000000
                      aaid_val: '10000000'
                  total: 152
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/risk_rules/download
        operationId: risk_rules_download
  /v1/datasets/risk_credit_configs/download:
    post:
      operationId: risk_credit_configs_download
      summary: Credit Line Parameters Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_credit_limit.credit_limit_para_qurey
      x-lbonly: false
      description: |-
        Exports the "credit parameters" dataset to a file.

        ### Request parameters

        - No required parameters: `filters` sets the export scope and `down_fields` limits the exported columns.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when no longer needed.

        ### Returns

        On success returns either the download URL or a `file_download_id`, depending on the selected mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of risk_credit_configs.yaml (the query capability).
                  properties:
                    member_id:
                      type: string
                      description: Filters results by Member ID.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - member_id
                      - member_id_val
                      - member_real_name
                      - aaid
                      - aaid_val
                      - credit_type
                      - credit_type_val
                      - account_channel
                      - credit_currency
                      - credit_setting_type
                      - credit_setting_type_val
                      - auto_credit
                      - auto_credit_val
                      - auto_credit_limit
                      - credit_coefficient
                      - credit_fps
                      - ccf
                      - applicable
                      - net_asset_ratio
                    example: member_id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Credit Line Parameters Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/risk_credit_configs
        operationId: risk_credit_configs
  /v1/datasets/risk_credit_configs:
    post:
      operationId: risk_credit_configs
      summary: Credit Line Parameters
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_credit_limit.credit_limit_para_qurey
      x-lbonly: false
      description: |-
        Queries the "credit parameters" dataset. Combine business filters via `filters` and control pagination with `page` and `page_size`.

        ### Returns

        On success returns the matching record list with pagination info. See the field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    member_id:
                      type: string
                      description: Filters results by Member ID.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Credit Line Parameters records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            member_id:
                              type: string
                              description: '**Returned value:** Member ID.'
                            member_id_val:
                              type: string
                              description: Raw value of `member_id`. The `member_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            member_real_name:
                              type: string
                              description: '**Returned value:** Name.'
                            aaid:
                              type: string
                              description: '**Returned value:** A/C No.'
                            aaid_val:
                              type: string
                              description: Raw value of `aaid`. The `aaid` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            credit_type:
                              type: string
                              description: '**Returned value:** Credit Limit Type.'
                            credit_type_val:
                              type: string
                              description: Raw value of `credit_type`. The `credit_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            account_channel:
                              type: string
                              description: '**Returned value:** Channel.'
                            credit_currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            credit_setting_type:
                              type: string
                              description: '**Returned value:** Global/ Individual.'
                            credit_setting_type_val:
                              type: string
                              description: Raw value of `credit_setting_type`. The `credit_setting_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            auto_credit:
                              type: string
                              description: '**Returned value:** Status.'
                            auto_credit_val:
                              type: string
                              description: Raw value of `auto_credit`. The `auto_credit` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            auto_credit_limit:
                              type: string
                              description: '**Returned value:** Max System Auto Approval Limit.'
                            credit_coefficient:
                              type: string
                              description: '**Returned value:** Credit Limit Multiplier Coefficient.'
                            credit_fps:
                              type: string
                              description: '**Returned value:** Credit FPS.'
                            ccf:
                              type: string
                              description: '**Returned value:** CCF(%).'
                            applicable:
                              type: string
                              description: '**Returned value:** APP Max Credit Limit Submission Amt.'
                            net_asset_ratio:
                              type: string
                              description: '**Returned value:** Declared NAV Multiples.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - member_id: ''
                      member_id_val: ''
                      member_real_name: Zhang San
                      aaid: ''
                      aaid_val: ''
                      credit_type: Margin Credit Limit
                      credit_type_val: '1'
                      account_channel: longport_test
                      credit_currency: HKD
                      credit_setting_type: Global
                      credit_setting_type_val: '0'
                      auto_credit: 'On'
                      auto_credit_val: 'true'
                      auto_credit_limit: 1,000,000.00
                      credit_coefficient: '1.00'
                      credit_fps: '0.30'
                      ccf: '100.00'
                      applicable: 1,000,000.00
                      net_asset_ratio: '1'
                  total: 14789
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/risk_credit_configs/download
        operationId: risk_credit_configs_download
  /corporate_action/sub_position_view/create:
    post:
      operationId: sub_position_view_create
      summary: Create Sub Account View
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_update
      x-lbonly: false
      description: |-
        Creates a sub-position view that organizes sub-position scopes into groups.

        ### Request parameters

        - Required: `view_name` (view name), `usage_scene` (usage scenario), `groups` (group list), `groups[].group_name` (group name), and `groups[].sub_position_scope` (sub-position scope).
        - Optional: `remark`.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        A sub-position view is scoped by `usage_scene` and may contain multiple groups, each defining its own sub-position scope.

        ### Risk warning

        This operation creates a real sub-position view configuration that may be referenced by downstream business such as corporate actions. Confirm the view name, scenario, and group scopes before calling.

        ### Result

        On success the sub-position view is created; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - view_name
                - usage_scene
                - groups
                - groups[].group_name
                - groups[].sub_position_scope
              properties:
                view_name:
                  type: string
                  description: This field is required.
                usage_scene:
                  type: number
                  description: Required. Single-select usage scenario for the sub-account view.
                  enum:
                    - 1
                    - 2
                    - 3
                  x-enum-details:
                    - value: 1
                      label: Rounding Adjustment
                    - value: 2
                      label: Refund
                    - value: 3
                      label: After-event Adjustment
                  example: 1
                remark:
                  type: string
                groups:
                  type: array
                  description: A list of groups, multiple can be added; each item contains a group name and a sub-account scope.
                  items:
                    type: string
                groups[].group_name:
                  type: string
                  description: Group name, required; group names must not repeat within a single submission.
                groups[].sub_position_scope:
                  type: string
                  description: Required. Multi-select scope of settlement sub-accounts assigned to the group. Each value must be an existing sub-account number in the system.
            example:
              view_name: 尾差调整视图A
              usage_scene: 1
              remark: 用于日终尾差调整
              groups:
                - group_name: 分组1
                  sub_position_scope:
                    - '1001'
                    - '1002'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /corporate_action/sub_position_view/del:
    post:
      operationId: sub_position_view_del
      summary: Delete Sub Account View
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_update
      x-lbonly: false
      description: |-
        Deletes a specified sub-position view.

        ### Request parameters

        - Required: `id` (sub-position view id).
        - Optional: `audit_frontend_resource` (approval-ticket display resource).
        - See the field descriptions for each field's format, range, and business meaning.

        ### Risk warning

        This operation deletes a real sub-position view configuration; afterward business that references the view can no longer use it. Confirm the target view before calling.

        ### Result

        On success the specified sub-position view is deleted; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Sub Account View ID associated with this request.
                audit_frontend_resource:
                  type: object
                  description: |-
                    Used to display the original view information and the operation action marker in the
                    approval ticket. It is not a required business field.
            example:
              id: '10086'
              audit_frontend_resource:
                view_name: Main Position
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /corporate_action/sub_position_view/detail:
    get:
      operationId: sub_position_view_detail
      summary: Sub Account View Detail
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_query
      x-lbonly: false
      description: |-
        Returns the full configuration of a single sub-position view by `id`.

        ### Request parameters

        - Required: `id` (sub-position view id), passed as a query parameter.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Returns

        On success the view's full configuration is returned in `data`, including the base info `view` and the group list `groups`. See the response definition below for field meanings.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: Sub-account view id, supplied as a query parameter.
      responses:
        '200':
          description: The request was processed successfully. `data` contains the full configuration of the sub-account view.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Full configuration of the sub-account view, including the view basic information `view` and the group list `groups`.
                    properties:
                      view:
                        type: object
                        description: Basic information of the sub-account view.
                        properties:
                          id:
                            type: string
                            description: Sub-account view id.
                          view_name:
                            type: string
                            description: View name.
                          usage_scene:
                            type: integer
                            description: Usage scenario of the sub-account view.
                            enum:
                              - 1
                              - 2
                              - 3
                            x-enum-details:
                              - value: 1
                                label: Rounding Adjustment
                              - value: 2
                                label: Refund
                              - value: 3
                                label: After-event Adjustment
                          remark:
                            type: string
                            description: Remark.
                      groups:
                        type: array
                        description: Group list; each item contains a group name and a sub-account scope.
                        items:
                          type: object
                          properties:
                            group_name:
                              type: string
                              description: Group name.
                            sub_position_scope:
                              type: array
                              description: Scope of settlement sub-accounts assigned to the group, a list of sub-account numbers. Each must be an existing sub-account number in the system.
                              items:
                                type: string
              example:
                code: 0
                message: success
                data:
                  view:
                    id: '1091'
                    view_name: 尾差调整视图A
                    usage_scene: 1
                    remark: 用于日终尾差调整
                  groups:
                    - group_name: 分组1
                      sub_position_scope:
                        - '1001'
                        - '1002'
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /corporate_action/sub_position_view/disable:
    post:
      operationId: sub_position_view_disable
      summary: Disable Sub Account View
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_update
      x-lbonly: false
      description: |-
        Disables a specified sub-position view.

        ### Request parameters

        - Required: `id` (sub-position view id).
        - Optional: `audit_frontend_resource` (caller-supplied audit context).
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        Once disabled, the view no longer applies to business that references it, but its configuration record is retained.

        ### Risk warning

        This operation changes the enabled status of a real sub-position view and may affect business that references it. Confirm the target view before calling.

        ### Result

        On success the specified sub-position view is disabled; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: |-
                    ID of the list record. The action is hidden when the record is disabled. `status_val`
                    is populated from a dynamic backend enum and includes at least `0` (disabled) and `1`
                    (enabled).
                audit_frontend_resource:
                  type: object
                  description: Not a business field; used only for audit-log display, containing the view name and the operation-behavior text.
            example:
              id: '10086'
              audit_frontend_resource:
                view_name: 尾差调整视图A
                $behavior: 关闭
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/add/withdraw/audit/rule:
    post:
      operationId: withdraw_audit_rule_add
      summary: Add Withdraw Audit Rule
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_approval_operation
      x-lbonly: false
      description: |-
        Creates a virtual-asset withdrawal (crypto) audit rule.

        ### Request parameters

        - Required: `rule_name` (rule name), `priority`, `upstream_broker` (channel), `account_nos` (applicable accounts), `broker_allocate_states` (broker fund-allocation states), `coin_quantity_list` (coin and quantity-range list), `delay_time` (audit delay), `audit_type` (audit rule), and `status`.
        - Optional: `equivalent_usd` (USD-equivalent amount range) and `remark`.
        - Set `account_nos` to `all` for all accounts, or a comma-separated list of securities accounts; `delay_time` is in minutes, range `1`-`999`.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        When a withdrawal request matches the rule, `audit_type` and `delay_time` determine the audit method and delay.

        ### Risk warning

        This operation creates a real withdrawal audit rule that directly affects the release and review flow for virtual-asset withdrawals. Confirm the rule conditions and status before calling.

        ### Result

        On success the withdrawal audit rule is created; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rule_name
                - priority
                - upstream_broker
                - account_nos
                - broker_allocate_states
                - coin_quantity_list
                - delay_time
                - audit_type
                - status
              properties:
                rule_name:
                  type: string
                  description: This field is required.
                priority:
                  type: number
                  description: Processing priority. Interpret the value and sort order according to the allowed-value definitions. This field is required.
                upstream_broker:
                  type: string
                  description: |-
                    Distinguishes the upstream party for the deposit account; the only client option is currently HASHKEY.

                    **Allowed values:** `HASHKEY`
                  enum:
                    - HASHKEY
                  x-enum-details:
                    - value: HASHKEY
                      label: HASHKEY
                  example: HASHKEY
                account_nos:
                  type: string
                  description: |-
                    For "All Accounts" pass all; for "Specified Accounts" pass multiple securities account
                    numbers separated by commas.
                broker_allocate_states:
                  type: string
                  description: Broker-allocation statuses. Select multiple values and join them with a comma `,` when submitting.
                  example: 4,5
                  enum:
                    - '4'
                    - '5'
                    - '6'
                    - '7'
                  x-enum-details:
                    - value: '4'
                      label: Success
                    - value: '5'
                      label: Failed
                    - value: '6'
                      label: Abnormal
                    - value: '7'
                      label: Rejected
                coin_quantity_list:
                  type: array
                  description: |-
                    A dynamic list where each item is { coin: currency (values come from the tenant_crypto_currencies
                    dynamic enum), quantity_min: minimum quantity, quantity_max: maximum quantity }.

                    Call `GET /v1/datasets/refs/tenant_crypto_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_crypto_currencies
                  items:
                    type: string
                equivalent_usd:
                  type: object
                  description: '{ amount_min, amount_max }, an optional numeric range.'
                delay_time:
                  type: number
                  description: In minutes; value range 1–999.
                audit_type:
                  type: string
                  description: Audit type.
                  enum:
                    - auto_pass
                    - auto_reject
                    - manual_ticket
                  x-enum-details:
                    - value: auto_pass
                      label: Auto Approved
                    - value: auto_reject
                      label: Auto Rejected
                    - value: manual_ticket
                      label: Ticket Approval
                  example: auto_pass
                status:
                  type: string
                  description: enabled - Enabled, disabled - Disabled
                remark:
                  type: string
            example:
              rule_name: HashKey自动通过规则
              priority: 10
              upstream_broker: HASHKEY
              account_nos: '****0000'
              broker_allocate_states: 4,5
              coin_quantity_list:
                - coin: BTC
                  quantity_min: '0'
                  quantity_max: '1'
              equivalent_usd:
                amount_min: '0'
                amount_max: '10000'
              delay_time: 30
              audit_type: auto_pass
              status: enabled
              remark: Customer-initiated request
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                      id:
                        type: string
                      tip:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/delete/withdraw/audit/rule:
    post:
      operationId: withdraw_audit_rule_delete
      summary: Delete Withdraw Audit Rule
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_approval_operation
      x-lbonly: false
      description: |-
        Deletes a specified virtual-asset withdrawal (crypto) audit rule.

        ### Request parameters

        - Required: `id` (rule id).
        - See the field descriptions for each field's format, range, and business meaning.

        ### Risk warning

        This operation deletes a real withdrawal audit rule; afterward the rule no longer participates in virtual-asset withdrawal review. Confirm the target rule before calling.

        ### Result

        On success the specified withdrawal audit rule is deleted; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Rule ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/modify/withdraw/audit/rule:
    post:
      operationId: withdraw_audit_rule_modify
      summary: Modify Withdraw Audit Rule
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_approval_operation
      x-lbonly: false
      description: |-
        Edits a virtual-asset withdrawal (crypto) audit rule.

        ### Request parameters

        - Required: `id` (rule id), `rule_name`, `priority`, `upstream_broker` (channel), `account_nos` (applicable accounts), `broker_allocate_states` (broker fund-allocation states), `coin_quantity_list` (coin and quantity-range list), `delay_time` (audit delay), `audit_type` (audit rule), and `status`.
        - Optional: `equivalent_usd` (USD-equivalent amount range) and `remark`.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        The field structure matches the create endpoint, with an additional `id` to locate the record being edited.

        ### Risk warning

        This operation modifies a real withdrawal audit rule that directly affects the release and review flow for virtual-asset withdrawals. Confirm the target record and rule conditions before calling.

        ### Result

        On success the specified withdrawal audit rule is updated; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - rule_name
                - priority
                - upstream_broker
                - account_nos
                - broker_allocate_states
                - coin_quantity_list
                - delay_time
                - audit_type
                - status
              properties:
                id:
                  type: string
                  description: The primary key of the rule to edit; required when editing.
                rule_name:
                  type: string
                  description: This field is required.
                priority:
                  type: number
                  description: Processing priority. Interpret the value and sort order according to the allowed-value definitions. This field is required.
                upstream_broker:
                  type: string
                  description: Distinguishes the upstream party for the deposit account; the only client option is currently HASHKEY.
                  enum:
                    - HASHKEY
                  x-enum-details:
                    - value: HASHKEY
                      label: HashKey
                  example: HASHKEY
                account_nos:
                  type: string
                  description: |-
                    The client offers "All Accounts" / "Specified Accounts": for all accounts pass all;
                    for specified accounts pass multiple securities account numbers, comma-separated.
                broker_allocate_states:
                  type: string
                  description: Broker-allocation statuses. Select multiple values and join them with a comma `,` when submitting.
                  example: 4,5
                  enum:
                    - '4'
                    - '5'
                    - '6'
                    - '7'
                  x-enum-details:
                    - value: '4'
                      label: Success
                    - value: '5'
                      label: Failed
                    - value: '6'
                      label: Abnormal
                    - value: '7'
                      label: Rejected
                coin_quantity_list:
                  type: array
                  description: |-
                    A dynamic list in which each item is { coin (currency, whose values come from the
                    tenant_crypto_currencies dynamic enum), quantity_min (minimum quantity), quantity_max (maximum
                    quantity) }.

                    Call `GET /v1/datasets/refs/tenant_crypto_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_crypto_currencies
                  items:
                    type: string
                equivalent_usd:
                  type: object
                  description: '{ amount_min: string, amount_max: string }; an optional numeric range input on the client.'
                delay_time:
                  type: number
                  description: In minutes; value range 1–999.
                audit_type:
                  type: string
                  description: Audit type.
                  enum:
                    - auto_pass
                    - auto_reject
                    - manual_ticket
                  x-enum-details:
                    - value: auto_pass
                      label: Auto Approved
                    - value: auto_reject
                      label: Auto Rejected
                    - value: manual_ticket
                      label: Ticket Approval
                  example: auto_pass
                status:
                  type: string
                  description: 'Enum values: enabled, disabled.'
                  enum:
                    - enabled
                    - disabled
                  x-enum-details:
                    - value: enabled
                      label: Enable
                    - value: disabled
                      label: Disable
                  example: enabled
                remark:
                  type: string
            example:
              id: '1001'
              rule_name: HashKey自动通过规则
              priority: 10
              upstream_broker: HASHKEY
              account_nos: '****0000'
              broker_allocate_states: 4,5
              coin_quantity_list:
                - coin: BTC
                  quantity_min: '0'
                  quantity_max: '1'
              equivalent_usd:
                amount_min: '0'
                amount_max: '10000'
              delay_time: 30
              audit_type: auto_pass
              status: enabled
              remark: Customer-initiated request
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/add/coin/withdraw/limit:
    post:
      operationId: add_coin_withdraw_limit
      summary: Add Coin Withdraw Limit
      tags:
        - Service Parameter
      x-permission-key:
        - atm.cryptocurrency_limit_operation
      x-lbonly: false
      description: |-
        Creates a daily crypto-withdrawal limit configuration.

        ### Request parameters

        - Required: `cut_time` (cycle base date), `exchange_currency` (exchange currency), `limit_amount` (initial limit), `member_ids` (customer ids), `priority`, `status`, and `time_zone`.
        - Optional: `frequency_limit`, `limit_type`, `remark`, `withdrawal_crypto` (supported withdrawal cryptocurrencies), and others.
        - Set `member_ids` to `all` for all customers, or a comma-separated list of customer ids; `time_zone` is used to compute the daily-limit cycle.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Risk warning

        This operation creates a real daily withdrawal-limit configuration that directly affects a customer's withdrawable amount. Confirm the applicable customers, currency, and limit before calling.

        ### Result

        On success the daily withdrawal-limit configuration is created; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - cut_time
                - exchange_currency
                - limit_amount
                - member_ids
                - priority
                - status
                - time_zone
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
                creator:
                  type: string
                cut_time:
                  type: string
                  description: This field is required.
                exchange_currency:
                  type: string
                  description: Specifies Exchange Currency. This field is required.
                  enum:
                    - USD
                    - CNY
                    - HKD
                  x-enum-details:
                    - value: USD
                      label: US Dollar
                    - value: CNY
                      label: Chinese Yuan
                    - value: HKD
                      label: Hong Kong Dollar
                  example: USD
                frequency_limit:
                  type: string
                limit_amount:
                  type: string
                  description: This field is required.
                limit_type:
                  type: string
                  description: Specifies Limit Type.
                  enum:
                    - daily
                    - weekly
                  x-enum-details:
                    - value: daily
                      label: Daily
                    - value: weekly
                      label: Weekly
                  example: daily
                member_ids:
                  type: string
                  description: Multiple values are comma-separated; send all for everyone, or specific clients such as 1,2,3.
                priority:
                  type: number
                  description: Processing priority. Interpret the value and sort order according to the allowed-value definitions. This field is required.
                remark:
                  type: string
                status:
                  type: string
                  description: Target state to set through this request. This field is required.
                  enum:
                    - enabled
                    - disabled
                  x-enum-details:
                    - value: enabled
                      label: Enable
                    - value: disabled
                      label: Disable
                  example: enabled
                time_zone:
                  type: string
                  description: Time zone used to calculate the daily cryptocurrency withdrawal limit. This field is required.
                  enum:
                    - ET
                    - CT
                    - HKT
                  x-enum-details:
                    - value: ET
                      label: US Eastern Time
                    - value: CT
                      label: US Central Time
                    - value: HKT
                      label: Hong Kong Time
                  example: ET
                updater:
                  type: string
                withdrawal_crypto:
                  type: string
            example:
              creator: ''
              cut_time: ''
              exchange_currency: USD
              frequency_limit: ''
              limit_amount: '0'
              limit_type: daily
              member_ids: all
              priority: 1
              remark: ''
              status: enabled
              time_zone: HKT
              withdrawal_crypto: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/delete/coin/withdraw/limit:
    post:
      operationId: delete_coin_withdraw_limit
      summary: Delete Coin Withdraw Limit
      tags:
        - Service Parameter
      x-permission-key:
        - atm.cryptocurrency_limit_operation
      x-lbonly: false
      description: |-
        Deletes a specified daily crypto-withdrawal limit configuration.

        ### Request parameters

        - Required: `id` (configuration id).
        - See the field descriptions for each field's format, range, and business meaning.

        ### Risk warning

        This operation deletes a real daily withdrawal-limit configuration; afterward it no longer applies to the relevant customers' withdrawal limits. Confirm the target configuration before calling.

        ### Result

        On success the specified daily withdrawal-limit configuration is deleted; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/atm/va/modify/coin/withdraw/limit:
    post:
      operationId: modify_coin_withdraw_limit
      summary: Modify Coin Withdraw Limit
      tags:
        - Service Parameter
      x-permission-key:
        - atm.cryptocurrency_limit_operation
      x-lbonly: false
      description: |-
        Edits a specified daily crypto-withdrawal limit configuration.

        ### Request parameters

        - Required: `id` (configuration id).
        - The remaining fields match the create endpoint; update `exchange_currency`, `limit_amount`, `member_ids`, `priority`, `status`, `time_zone`, and others as needed.
        - See the field descriptions for each field's format, range, and business meaning.

        ### Business notes

        The field structure matches the create endpoint; `id` is required when editing to locate the record being updated.

        ### Risk warning

        This operation modifies a real daily withdrawal-limit configuration that directly affects a customer's withdrawable amount. Confirm the target record, currency, and limit before calling.

        ### Result

        On success the specified daily withdrawal-limit configuration is updated; subsequent state and returned data follow the `200` response definition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: ID
                creator:
                  type: string
                  description: creator
                cut_time:
                  type: string
                  description: Cut-off time
                exchange_currency:
                  type: string
                  description: Exchange currency
                frequency_limit:
                  type: string
                  description: Frequency limit
                limit_amount:
                  type: string
                  description: Limit amount
                limit_type:
                  type: string
                  description: Limit type
                member_ids:
                  type: string
                  description: User
                priority:
                  type: integer
                  description: Priority
                remark:
                  type: string
                  description: Remark
                status:
                  type: string
                  description: Status
                time_zone:
                  type: string
                  description: Time zone
                updater:
                  type: string
                  description: updater
                withdrawal_crypto:
                  type: string
                  description: Withdrawal crypto
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/clearing/create_busin_flow:
    post:
      operationId: create_busin_flow
      summary: Create Day-End Clearing Business Flow
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_busin_step_edit
      x-lbonly: false
      description: |-
        Creates or edits an end-of-day clearing task flow, defining a named clearing process for a set of markets.

        ### Request parameters

        - Required: `flow_name` (flow name) and `markets` (markets).
        - Pass `id` (flow number) to edit an existing flow; omit it when creating a new one.
        - See the field definitions for value ranges and business meaning.

        ### Business notes

        The same endpoint handles both create and edit: omitting `id` creates a new flow, while passing an existing `id` overwrites that flow. `markets` declares the market scope covered by the flow.

        ### Risk warning

        This configuration affects the scope of the end-of-day clearing process. Confirm the flow name and market combination before calling.

        ### Result

        On success the corresponding clearing task flow is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - flow_name
                - markets
              properties:
                flow_name:
                  type: string
                  description: Name of the day-end clearing business flow. This field is required.
                  example: HK-EOD-Flow
                markets:
                  type: array
                  items:
                    type: string
                  description: List of market codes covered by this business flow. This field is required.
                  example:
                    - GTMMF
                id:
                  type: integer
                  description: |-
                    Identifier of the day-end clearing business flow. Provide it when editing an existing
                    flow to indicate which flow to update; omit it when creating a new flow.
                  example: 1001
            example:
              flow_name: HK-EOD-Flow
              markets:
                - GTMMF
      responses:
        '200':
          description: |-
            The request was processed successfully.

            The frontend call site does not read fixed business fields from this endpoint's response,
            so only the general response structure (`code`, `message`, `data`) is declared here; the
            concrete fields inside `data` are unverified.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/get_markets_calendar_init_status:
    post:
      operationId: get_markets_calendar_init_status
      summary: Get Markets Calendar Init Status
      tags:
        - Service Parameter
      x-permission-key:
        - clearing.clearing_calendar_operation
      x-lbonly: false
      description: |-
        Queries the clearing-calendar initialization status of each market for a given year, so callers can tell whether a market's calendar has been initialized for that year.

        ### Request parameters

        - Required: `year`.
        - Optional: `markets` (market list); when omitted, all markets are queried.
        - See the field definitions for format and business meaning.

        ### Returns

        On success, returns the calendar initialization status of each market for that year. See the response definition for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - year
              properties:
                year:
                  type: integer
                  description: The year to query. This field is required.
                  example: 2024
                markets:
                  type: array
                  items:
                    type: string
                  description: List of market codes to query. Omit to query all markets.
                  example:
                    - HK
                    - US
            example:
              year: 2024
              markets:
                - HK
                - US
      responses:
        '200':
          description: |-
            The request was processed successfully.

            The frontend receives this endpoint's response as `any` and does not read fixed business
            fields at the call site. Only the general response structure (`code`, `message`, `data`) is
            declared here; the concrete fields inside `data` are unverified.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            market:
                              type: string
                            is_init:
                              type: boolean
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/account_instruction_settings:
    post:
      operationId: manage_clearing_account_instruction_settings
      summary: Create / Edit / Delete Account-level Settlement Instruction Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_account_instruction_settings
      x-lbonly: false
      description: |-
        Creates, edits, and deletes account-level settlement rules through a single endpoint, distinguished by `action_in` (`1` = add, `2` = delete, `3` = modify). Unlike market-level settlement rules, this endpoint configures rules per specific client account (`aaid`).

        ### Request parameters

        - Required: `action_in` (operation type).
        - Add (`action_in=1`): submit the client account `aaid`, the rule-dimension fields (`market`, `settlement_channel`, `stage`, `biz_type`, `direction`, `position`, `instr_type`, `settle_method`), and the default settlement config (`custodian_id`, `sub_position_no`); `id` may be omitted.
        - Edit (`action_in=3`): pass the target record's `id` and submit the fields to update.
        - Delete (`action_in=2`): pass only `id`, `action_in`, and `audit_frontend_resource` (audit context).
        - See the field definitions for value ranges and business meaning.

        ### Business notes

        Account-level rules take precedence over market-level rules and let callers assign a settlement path to a specific client account.

        ### Risk warning

        This operation changes real settlement-rule configuration and affects downstream clearing and settlement routing. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
              properties:
                action_in:
                  type: string
                  description: Specifies the action to perform. This field is required.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Modify
                  example: '1'
                id:
                  type: string
                  description: ID of the account-level settlement instruction rule record. May be omitted on add; required on edit or delete to target an existing record.
                aaid:
                  type: string
                market:
                  type: string
                settlement_channel:
                  type: string
                stage:
                  type: string
                biz_type:
                  type: string
                  enum:
                    - '1'
                    - '5'
                  x-enum-details:
                    - value: '1'
                      label: Stock
                    - value: '5'
                      label: Option
                  example: '1'
                  description: |-
                    biz_type.
                direction:
                  type: string
                  enum:
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '1'
                      label: Buy
                    - value: '2'
                      label: Sell
                  example: '1'
                  description: |-
                    direction.
                position:
                  type: string
                instr_type:
                  type: string
                  enum:
                    - A
                    - T
                    - S
                    - I
                  x-enum-details:
                    - value: A
                      label: ATI
                    - value: T
                      label: STI
                    - value: S
                      label: SI
                    - value: I
                      label: ISI
                  example: A
                  description: |-
                    instr_type.
                settle_method:
                  type: string
                  enum:
                    - F
                    - D
                    - R
                  x-enum-details:
                    - value: F
                      label: FOP
                    - value: D
                      label: DVP
                    - value: R
                      label: RDP
                  example: F
                  description: |-
                    settle_method.
                custodian_id:
                  type: string
                sub_position_no:
                  type: string
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              action_in: '1'
              id: ''
              aaid: ''
              market: ''
              settlement_channel: ''
              stage: ''
              biz_type: ''
              direction: ''
              position: ''
              instr_type: ''
              settle_method: ''
              custodian_id: ''
              sub_position_no: ''
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/instruction_settings:
    post:
      operationId: manage_clearing_instruction_settings
      summary: Create / Edit / Delete Settlement Instruction Rule
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_instruction_settings
      x-lbonly: false
      description: |-
        Creates, edits, and deletes market-level settlement rules through a single endpoint, distinguished by `action_in` (`1` = add, `2` = delete, `3` = modify).

        ### Request parameters

        - Required: `action_in` (operation type).
        - Add (`action_in=1`): submit the rule-dimension fields (`market`, `settlement_channel`, `stage`, `account_type`, `biz_type`, `direction`, `position`, `instr_type`, `settle_method`) and the default settlement config (`custodian_id`, `sub_position_no`); `id` may be omitted.
        - Edit (`action_in=3`): pass the target record's `id` and submit the fields to update.
        - Delete (`action_in=2`): pass only `id`, `action_in`, and `audit_frontend_resource` (audit context).
        - See the field definitions for value ranges and business meaning.

        ### Risk warning

        This operation changes real settlement-rule configuration and affects downstream clearing and settlement routing. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
              properties:
                action_in:
                  type: string
                  description: Specifies the action to perform. This field is required.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Modify
                  example: '1'
                id:
                  type: string
                  description: ID of the settlement instruction rule record. May be omitted on add; required on edit or delete to target an existing record.
                market:
                  type: string
                settlement_channel:
                  type: string
                stage:
                  type: string
                account_type:
                  type: string
                  description: |-
                    account_type. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                biz_type:
                  type: string
                  description: |-
                    biz_type. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                direction:
                  type: string
                  description: |-
                    direction. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                position:
                  type: string
                instr_type:
                  type: string
                  enum:
                    - A
                    - T
                    - S
                    - I
                  x-enum-details:
                    - value: A
                      label: ATI
                    - value: T
                      label: STI
                    - value: S
                      label: SI
                    - value: I
                      label: ISI
                  example: A
                  description: |-
                    instr_type.
                settle_method:
                  type: string
                  enum:
                    - F
                    - D
                    - R
                  x-enum-details:
                    - value: F
                      label: FOP
                    - value: D
                      label: DVP
                    - value: R
                      label: RDP
                  example: F
                  description: |-
                    settle_method.
                custodian_id:
                  type: string
                sub_position_no:
                  type: string
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              action_in: '1'
              id: ''
              market: ''
              settlement_channel: ''
              stage: ''
              account_type: ''
              biz_type: ''
              direction: ''
              position: ''
              instr_type: ''
              settle_method: ''
              custodian_id: ''
              sub_position_no: ''
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/markets:
    post:
      operationId: manage_clearing_markets
      summary: Manage Clearing Markets
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_markets
      x-lbonly: false
      description: |-
        Creates, edits, and deletes clearing markets through a single endpoint, distinguished by `action_in` (`1` = add, `2` = delete, `3` = modify).

        ### Request parameters

        - Required: `action_in` (operation type) and `id` (market record ID; may be `0` when adding).
        - Optional: `market`, `currency`, `overnight_flag`, `custodian_id`, `sub_position_no`, `balance_back_n` (cash settlement T+n), `equity_back_n` (securities settlement T+n), `main_flag`, `otc_flag`, `use_public_stock_info`, `busin_flow_id` (end-of-day flow ID).
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation changes real clearing-market configuration and affects settlement cycles and the end-of-day clearing process. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
                - id
              properties:
                action_in:
                  type: string
                  description: 'Enum: 1=Add, 2=Delete, 3=Modify.'
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Modify
                  example: '1'
                id:
                  type: number
                  description: 0 on add (may be omitted); the existing record ID on edit/delete.
                market:
                  type: string
                  description: |-
                    Values come from the clearing_markets_all dynamic enumeration.

                    Call `GET /v1/datasets/refs/clearing_markets_all` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/clearing_markets_all
                currency:
                  type: string
                  description: Settlement currency of the market.
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CNH
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CNH
                      label: CNH
                  example: USD
                overnight_flag:
                  type: number
                  description: Whether this is an overnight market.
                  enum:
                    - '0'
                    - '1'
                  x-enum-details:
                    - value: '0'
                      label: 'No'
                    - value: '1'
                      label: 'Yes'
                  example: '0'
                custodian_id:
                  type: number
                  description: |-
                    Values come from the clearing_custodians dynamic enumeration.

                    Call `GET /v1/datasets/refs/clearing_custodians` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/clearing_custodians
                sub_position_no:
                  type: string
                  description: |-
                    Values come from the custodian_sub_positions dynamic enumeration.

                    Call `GET /v1/datasets/refs/custodian_sub_positions` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/custodian_sub_positions
                balance_back_n:
                  type: number
                  description: Number of days the balance is carried back (T+N).
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: '0'
                    - value: '1'
                      label: '1'
                    - value: '2'
                      label: '2'
                    - value: '3'
                      label: '3'
                  example: '0'
                equity_back_n:
                  type: number
                  description: Number of days equity is carried back (T+N).
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: '0'
                    - value: '1'
                      label: '1'
                    - value: '2'
                      label: '2'
                    - value: '3'
                      label: '3'
                  example: '0'
                main_flag:
                  type: number
                  description: Whether this is the base (main) market for clearing.
                  enum:
                    - '0'
                    - '1'
                  x-enum-details:
                    - value: '0'
                      label: 'No'
                    - value: '1'
                      label: 'Yes'
                  example: '0'
                otc_flag:
                  type: number
                  description: OTC day-end processing mode for the market.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '99'
                  x-enum-details:
                    - value: '0'
                      label: Normal Day-End Tasks
                    - value: '1'
                      label: Simplified Day-End Tasks For OTC
                    - value: '2'
                      label: Non-Day-End Process
                    - value: '99'
                      label: Disabled
                  example: '0'
                use_public_stock_info:
                  type: number
                  description: Whether the market depends on the public stock information library.
                  enum:
                    - '0'
                    - '1'
                  x-enum-details:
                    - value: '0'
                      label: Independent On Public Stock Information
                    - value: '1'
                      label: Dependent On Public Stock Information
                  example: '0'
                busin_flow_id:
                  type: number
                  description: Filled in when a day-end process is selected; empty otherwise.
            example:
              action_in: '1'
              id: 0
              market: US
              currency: USD
              overnight_flag: 0
              custodian_id: 1001
              sub_position_no: SUB01
              balance_back_n: 1
              equity_back_n: 2
              main_flag: 1
              otc_flag: 0
              use_public_stock_info: 1
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/custodians:
    post:
      operationId: manage_clearing_custodians
      summary: Manage Clearing Custodians
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_custodians
      x-lbonly: false
      description: |-
        Creates, edits, and deletes clearing custodians through a single endpoint, distinguished by `action_in`.

        ### Request parameters

        - Required: `action_in` (operation type).
        - Optional: `id`, `name`, `name_zh`, `name_hk`, `ccass_code`, `address`, `email`, `tel`, `fax`, `main_flag` (primary custodian), `custodian_type`, `custodian_code`, `settle_type`, `contact_person`, `company_name`, `stamp_duty_file_no`, `seat_nos`, `settlement_channel`, `trading_account_type` (sub-position derivation basis).
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        Use `id` to locate the target record when editing or deleting; omit `id` when adding.

        ### Risk warning

        This operation changes real custodian configuration and affects clearing and settlement. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
              properties:
                action_in:
                  type: string
                  description: Specifies Action In. This field is required.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Edit
                  example: '1'
                id:
                  type: string
                  description: Custodian ID associated with this request.
                name:
                  type: string
                name_zh:
                  type: string
                name_hk:
                  type: string
                ccass_code:
                  type: string
                address:
                  type: string
                email:
                  type: string
                tel:
                  type: string
                fax:
                  type: string
                main_flag:
                  type: string
                  enum:
                    - '1'
                    - '0'
                  x-enum-details:
                    - value: '1'
                      label: Yes (main service provider)
                    - value: '0'
                      label: No
                  example: '1'
                  description: |-
                    main_flag.
                custodian_type:
                  type: string
                  description: Specifies Custodian Type.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Agent custodian.
                    - value: '2'
                      label: Agent.
                    - value: '3'
                      label: Custodian
                  example: '1'
                custodian_code:
                  type: string
                settle_type:
                  type: string
                  description: |-
                    settle_type. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                contact_person:
                  type: string
                company_name:
                  type: string
                stamp_duty_file_no:
                  type: string
                  description: Specifies Stamp Duty File No..
                seat_nos:
                  type: string
                settlement_channel:
                  type: string
                trading_account_type:
                  type: string
                  description: |-
                    trading_account_type. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
            example:
              action_in: '1'
              id: ''
              name: ''
              name_zh: ''
              name_hk: ''
              ccass_code: ''
              address: ''
              email: ''
              tel: ''
              fax: ''
              main_flag: ''
              custodian_type: '1'
              custodian_code: ''
              settle_type: ''
              contact_person: ''
              company_name: ''
              stamp_duty_file_no: ''
              seat_nos: ''
              settlement_channel: ''
              trading_account_type: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/settlement_channels:
    post:
      operationId: manage_clearing_settlement_channels
      summary: Create / Edit / Delete Settlement Channel Mapping
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_settlement_channels
      x-lbonly: false
      description: |-
        Creates, edits, and deletes settlement-channel mappings through a single endpoint, distinguished by `action_in`.

        ### Request parameters

        - Required: `action_in` (operation type).
        - Optional: `id`, `custodian_id` (agent ID), `settlement_channel`, `channel_type` (trade type), `tolerance_threshold` (reconciliation tolerance), `account_mode` (seat-number filter), `verify_accounts` (seat numbers), `verified` (trade reconciliation mode), `remark`, `audit_frontend_resource` (audit context).
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        - Add (`action_in=1`): omit `id` and submit the mapping's business fields.
        - Edit (`action_in=3`): pass the target record's `id` together with the fields to update.
        - Delete (`action_in=2`): pass only the target record's `id` and `audit_frontend_resource`.

        ### Risk warning

        This operation changes real settlement-channel mappings and reconciliation configuration and affects fund reconciliation results. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
              properties:
                action_in:
                  type: string
                  description: Specifies Action In. This field is required.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Edit
                  example: '1'
                id:
                  type: string
                  description: Settlement channel mapping record ID that identifies the target of this request. Required when editing or deleting.
                custodian_id:
                  type: string
                  description: Specifies Broker ID, identifying the broker that owns the settlement channel mapping.
                settlement_channel:
                  type: string
                channel_type:
                  type: string
                  description: |-
                    channel_type. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                tolerance_threshold:
                  type: string
                  description: Channel amount tolerance threshold. Allowed range `[0, 1000]`; a default of `0` means the tolerance is disabled.
                account_mode:
                  type: string
                  enum:
                    - '0'
                    - '1'
                    - '2'
                  x-enum-details:
                    - value: '0'
                      label: None (no seat filter)
                    - value: '1'
                      label: Included seats
                    - value: '2'
                      label: Excluded seats
                  example: '0'
                  description: |-
                    account_mode.
                verify_accounts:
                  type: string
                verified:
                  type: string
                  description: |-
                    verified. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                remark:
                  type: string
                audit_frontend_resource:
                  type: string
                  description: Caller-provided audit context used for operation audit logging. The delete operation submits this field together with the request body.
            example:
              action_in: '1'
              id: ''
              custodian_id: ''
              settlement_channel: ''
              channel_type: ''
              tolerance_threshold: '0'
              account_mode: ''
              verify_accounts: ''
              verified: ''
              remark: ''
              audit_frontend_resource: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/sub_positions:
    post:
      operationId: manage_clearing_sub_positions
      summary: Create / Edit / Delete Custodian Sub-position
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_sub_positions
      x-lbonly: false
      description: |-
        Creates, edits, and deletes custodian sub-positions through a single endpoint, distinguished by `action_in`.

        ### Request parameters

        - Required: `action_in` (operation type).
        - Optional: `id`, `custodian_id`, `no` (sub-position number), `name` (sub-position name), `position_direction` (characteristic), `remark`, `audit_frontend_resource` (audit context).
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        - Add (`action_in=1`): omit `id` and submit the sub-position's business fields.
        - Edit (`action_in=3`): pass the target record's `id` together with the fields to update.
        - Delete (`action_in=2`): pass only the target record's `id` and `audit_frontend_resource`.

        ### Risk warning

        This operation changes real custodian sub-position configuration and affects settlement and position aggregation. Confirm the target record and parameters before calling.

        ### Result

        On success the add, edit, or delete completes. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
              properties:
                action_in:
                  type: string
                  description: Specifies Action In. This field is required.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Delete
                    - value: '3'
                      label: Edit
                  example: '1'
                id:
                  type: string
                  description: Sub-position record ID that identifies the target of this request. Required when editing or deleting.
                custodian_id:
                  type: string
                false:
                  type: string
                name:
                  type: string
                position_direction:
                  type: string
                remark:
                  type: string
                audit_frontend_resource:
                  type: string
                  description: Caller-provided audit context used for operation audit logging. The delete operation submits this field together with the request body.
            example:
              action_in: '1'
              id: ''
              custodian_id: ''
              false: ''
              name: ''
              position_direction: ''
              remark: ''
              audit_frontend_resource: ''
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/ipo/delete/channel:
    post:
      operationId: ipo_delete_channel
      summary: Delete IPO Channel
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.richchannel_manage
      x-lbonly: false
      description: |-
        Deletes a specified IPO funding channel.

        ### Request parameters

        - Required: `id` (channel ID).
        - Optional: `audit_frontend_resource` (audit context).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real IPO funding-channel configuration and may affect fund aggregation for subsequent IPO subscriptions. Confirm the target channel before calling.

        ### Result

        On success the IPO funding channel is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Channel ID associated with this request.
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              id: '123'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ipo/richchannel/manage:
    post:
      operationId: ipo_richchannel_manage
      summary: Manage IPO Rich Channel
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.richchannel_manage
      x-lbonly: false
      description: |-
        Creates or edits an IPO funding channel; create, edit, and the embedded financing-pool creation entry share the same request structure.

        ### Request parameters

        - Required: `name` (channel name), `typo` (supported business types), `to_fini` (whether to sync to HKEx).
        - Optional: `id` (channel ID, passed when editing), `remark`, `alias`, `audit_frontend_resource` (audit context).
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Business notes

        Omitting `id` creates a new channel; passing an existing `id` edits it. `name` cannot be changed when editing.

        ### Risk warning

        This operation changes real IPO funding-channel configuration and may affect fund handling for IPO subscriptions. Confirm the target record and parameters before calling.

        ### Result

        On success the IPO funding channel is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - typo
                - to_fini
              properties:
                id:
                  type: string
                  description: Pass the existing record id when editing; not sent when creating.
                name:
                  type: string
                  description: Up to 10 characters, letters/digits/underscores only; not editable in edit mode.
                typo:
                  type: string
                  description: |-
                    Multi-select; enum values CASH (cash) / FINANCING (financing) / ZEROCASH (zero-cash) /
                    PLACEMENT (placement); PLACEMENT is available only at certain entry points, while
                    others have only the first three.
                  enum:
                    - CASH
                    - FINANCING
                    - ZEROCASH
                    - PLACEMENT
                  x-enum-details:
                    - value: CASH
                      label: Cash
                    - value: FINANCING
                      label: Financing
                    - value: ZEROCASH
                      label: 0 Cost
                    - value: PLACEMENT
                      label: Placement
                  example: CASH
                to_fini:
                  type: string
                  description: Enum value "true"/"false" (string), indicating whether to sync to HKEX.
                  enum:
                    - 'true'
                    - 'false'
                  x-enum-details:
                    - value: 'true'
                      label: 'Yes'
                    - value: 'false'
                      label: 'No'
                  example: 'true'
                remark:
                  type: string
                alias:
                  type: string
                  description: Channel alias (a reserved field; not collected by any current entry form and may be omitted).
                audit_frontend_resource:
                  type: object
                  description: |-
                    Records a snapshot of the channel data before and after the change and the operation
                    type ($action: add/edit), used for backend audit trails.
            example:
              name: hk_channel_a
              typo:
                - CASH
                - FINANCING
              to_fini: 'true'
              remark: 香港现金及融资资金渠道
              audit_frontend_resource:
                $action: add
                isAdd: true
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      result:
                        type: boolean
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ipo/add/buypower:
    post:
      operationId: ipo_add_buypower
      summary: Create IPO Buy Power
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.buypower_mange
      x-lbonly: false
      description: |-
        Adds an IPO buying-power configuration, setting subscription buying-power parameters for a given security.

        ### Request parameters

        - Required: `counter_id` (security symbol), `type`, `hold_ipo` (whether the IPO is held), `floating_ration` (floating ratio), `borrow_enable` (whether borrowing is allowed).
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes real IPO buying-power configuration and directly affects how a client's available subscription buying power is computed. Confirm the security and parameters before calling.

        ### Result

        On success the IPO buying-power configuration is added. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - counter_id
                - type
                - hold_ipo
                - floating_ration
                - borrow_enable
              properties:
                counter_id:
                  type: string
                  description: Counter ID associated with this request.
                type:
                  type: string
                  description: |-
                    This field is required. Values come from a backend data dictionary (maintained dynamically), not a fixed enum; use the options the service actually returns.
                hold_ipo:
                  type: boolean
                  description: |-
                    Boolean flag for hold ipo. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
                floating_ration:
                  type: string
                  description: This field is required.
                borrow_enable:
                  type: boolean
                  description: |-
                    Boolean flag for borrow enable. this field is required.

                    **Boolean values:**
                    - `true` - The condition applies
                    - `false` - The condition does not apply
            example:
              counter_id: ST/US/AAPL
              type: ''
              hold_ipo: true
              floating_ration: '0'
              borrow_enable: true
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ipo/delete/buypower:
    post:
      operationId: ipo_delete_buypower
      summary: Delete IPO Buy Power
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.buypower_mange
      x-lbonly: false
      description: |-
        Deletes a specified IPO buying-power configuration.

        ### Request parameters

        - Required: `id` (configuration ID).
        - Optional: `audit_frontend_resource` (audit context).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real IPO buying-power configuration and affects the subscription buying-power calculation for the related security. Confirm the target configuration before calling.

        ### Result

        On success the IPO buying-power configuration is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Config ID associated with this request.
                audit_frontend_resource:
                  type: object
                  description: Provides audit context for this operation.
            example:
              id: '123'
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ipo/edit/buypower:
    post:
      operationId: ipo_edit_buypower
      summary: Update IPO Buy Power
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.buypower_mange
      x-lbonly: false
      description: |-
        Edits an existing IPO buying-power configuration. The field structure matches the add endpoint, with `id` additionally required to locate the target record.

        ### Request parameters

        - Required: `id` (configuration ID).
        - The remaining fields (`counter_id`, `type`, `hold_ipo`, `floating_ratio`, `borrow_enable`, etc.) match the add endpoint; `audit_frontend_resource` carries the audit context.
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes real IPO buying-power configuration and directly affects how a client's available subscription buying power is computed. Confirm the target record and parameters before calling.

        ### Result

        On success the IPO buying-power configuration is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: integer
                  description: Config ID
                counter_id:
                  type: string
                  description: Security
                type:
                  type: string
                  description: Type
                hold_ipo:
                  type: boolean
                  description: Whether holding-based IPO subscription is allowed
                floating_ratio:
                  type: string
                  description: IPO buying-power margin uplift factor
                borrow_enable:
                  type: boolean
                  description: Whether coin borrowing is allowed
                audit_frontend_resource:
                  type: object
                  description: audit_frontend_resource
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/ipo/rate/delete:
    post:
      operationId: ipo_rate_delete
      summary: Delete IPO Rate
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Deletes a specified IPO commission-rate configuration. This is a soft delete that sets the record's `state` to `0`.

        ### Request parameters

        - Required: `id` (rate configuration ID) and `state` (status; always pass `0`).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deactivates a real IPO commission-rate configuration and affects fee calculation for related IPO trades. Confirm the target configuration before calling.

        ### Result

        On success the IPO commission-rate configuration is soft-deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - state
              properties:
                id:
                  type: string
                  description: Rate Config ID associated with this request.
                state:
                  type: number
                  description: Always sent as 0 by the client (disable/delete).
            example:
              id: '123'
              state: 0
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/ipo/rate/save:
    post:
      operationId: ipo_rate_save
      summary: Save IPO Rate
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Creates or edits an IPO commission-rate configuration.

        ### Request parameters

        - Required: `name` (rate name), `currency`, `financing_rate`, `buy_fee`, `sfc_transaction_levy`, `transaction_fee`, `transaction_levy`, `commission` (broker commission), `lb_commission` (Longbridge commission), `audit_frontend_resource` (audit context).
        - Optional: `id` (rate ID, passed when editing).
        - `currency` candidate values vary by business; call `GET /v1/datasets/refs/tenant_currencies_by_channel` to obtain the currently available values.
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        Omitting `id` creates a new configuration; passing an existing `id` edits it. Rate items are expressed as percentages or amounts in the given currency; see the field definitions.

        ### Risk warning

        This operation changes real IPO commission-rate configuration and directly affects fee calculation for IPO trades. Confirm the parameters before calling.

        ### Result

        On success the IPO commission-rate configuration is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - currency
                - financing_rate
                - buy_fee
                - sfc_transaction_levy
                - transaction_fee
                - transaction_levy
                - commission
                - lb_commission
                - audit_frontend_resource
              properties:
                id:
                  type: string
                  description: Pass the existing record id when editing; not sent when creating.
                name:
                  type: string
                  description: This field is required.
                currency:
                  type: string
                  description: |-
                    Values come from the tenant_currencies_by_channel dynamic enumeration, e.g. HKD/USD.

                    Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                financing_rate:
                  type: number
                  description: A percentage value, e.g. 2.5 means 2.5%.
                buy_fee:
                  type: number
                  description: The amount denominated in the currency given by currency.
                sfc_transaction_levy:
                  type: number
                  description: A percentage value; when "More settings" is collapsed, the default rate is auto-filled and submitted.
                transaction_fee:
                  type: number
                  description: A percentage value; when "More settings" is collapsed, the default rate is auto-filled and submitted.
                transaction_levy:
                  type: number
                  description: A percentage value; when "More settings" is collapsed, the default rate is auto-filled and submitted.
                commission:
                  type: number
                  description: A percentage value; when "More settings" is collapsed, the default rate is auto-filled and submitted.
                lb_commission:
                  type: string
                  description: Fixed to the string "0.00"; not user-editable.
                audit_frontend_resource:
                  type: object
                  description: |-
                    Records a snapshot of the rate data before and after the change and the operation type
                    ($action: add/edit), used for backend audit trails; attached on every submission.
            example:
              id: '1052'
              name: hk_retail_default
              currency: HKD
              financing_rate: 2.5
              buy_fee: 100
              sfc_transaction_levy: 0.0027
              transaction_fee: 0.00565
              transaction_levy: 0.00015
              commission: 1
              lb_commission: '0.00'
              audit_frontend_resource:
                $action: edit
                isEdit: true
                id: '1052'
                name: hk_retail_default
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v2/ipo/rate/save-default:
    post:
      operationId: ipo_rate_save_default
      summary: Update IPO Default Commission
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Saves the default IPO commission rate, used as the fallback when no specific rate is assigned.

        ### Request parameters

        - Required: `sfc_transaction_levy`, `transaction_fee`, `transaction_levy`, `commission` (broker commission), `lb_commission` (Longbridge commission), `audit_frontend_resource` (audit context).
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes the real default IPO commission rate and affects fee calculation for every IPO trade without a specifically assigned rate. Confirm the parameters before calling.

        ### Result

        On success the default IPO commission rate is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - sfc_transaction_levy
                - transaction_fee
                - transaction_levy
                - commission
                - lb_commission
                - audit_frontend_resource
              properties:
                sfc_transaction_levy:
                  type: number
                  description: A percentage value, e.g. 0.0027 means 0.0027%.
                transaction_fee:
                  type: number
                  description: This field is required.
                transaction_levy:
                  type: number
                  description: This field is required.
                commission:
                  type: number
                  description: This field is required.
                lb_commission:
                  type: string
                  description: Always sends the string "0.00"; not user-editable.
                audit_frontend_resource:
                  type: object
                  description: |-
                    A snapshot of the default rate data before editing, queried before submission and used
                    for backend audit trails; attached on every submission.
            example:
              sfc_transaction_levy: 0.0027
              transaction_fee: 0.00565
              transaction_levy: 0.00015
              commission: 1
              lb_commission: '0.00'
              audit_frontend_resource:
                sfc_transaction_levy: 0.0025
                transaction_fee: 0.005
                transaction_levy: 0.0001
                commission: 0.8
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/security_lending_get_config:
    post:
      operationId: security_lending_get_config
      summary: Get Security Lending Config
      tags:
        - Service Parameter
      x-permission-key:
        - billing.short_selling_configure
      x-lbonly: false
      description: |-
        Reads the current securities-lending configuration for a market, including lending rates and settlement prices, so callers can pre-fill the payload for "set securities-lending config" (`POST /clearing/security_lending_set_config`).

        ### Request parameters

        - Required: `market`.
        - See the field definitions for format and business meaning.

        ### Returns

        On success, returns the market's `rate_configs` (lending-rate configuration list) and `price_configs` (lending settlement-price configuration list); either may be an empty array. See the response definition for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - market
              properties:
                market:
                  type: string
                  description: The market whose configuration is queried. This field is required.
                  example: US
            example:
              market: US
      responses:
        '200':
          description: On success, returns the market's security-lending rate and settlement-price configuration.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      rate_configs:
                        type: array
                        items:
                          type: object
                          properties:
                            origin:
                              type: string
                            rate_ratio:
                              type: string
                            rate_delta:
                              type: string
                            cutoff_method:
                              type: integer
                            cutoff_digits:
                              type: integer
                            min_rate:
                              type: string
                      price_configs:
                        type: array
                        items:
                          type: object
                          properties:
                            currency:
                              type: string
                            transfer_rate:
                              type: string
                            cutoff_method:
                              type: integer
                            cutoff_digits:
                              type: integer
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /clearing/security_lending_set_config:
    post:
      operationId: security_lending_set_config
      summary: Set Security Lending Config
      tags:
        - Service Parameter
      x-permission-key:
        - billing.short_selling_configure
      x-lbonly: false
      description: |-
        Sets the securities-lending configuration for a market, including lending rates and settlement prices, submitted as a full market-level overwrite.

        ### Request parameters

        - Required: `market`.
        - Optional: `rate_configs` (rate configuration list) and `price_configs` (price configuration list).
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        The submission overwrites the current configuration for the entire market. You may first call `POST /clearing/security_lending_get_config` to read the existing configuration before modifying it.

        ### Risk warning

        This operation changes real lending-rate and settlement-price configuration and directly affects billing and settlement for securities lending. Confirm the market and parameters before calling.

        ### Result

        On success the market's securities-lending configuration is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - market
              properties:
                market:
                  type: string
                  description: This field is required.
                rate_configs:
                  type: array
                  description: Submit Rate Configs as a JSON array.
                  items:
                    type: string
                price_configs:
                  type: array
                  description: Submit Price Configs as a JSON array.
                  items:
                    type: string
            example:
              market: HK
              rate_configs: []
              price_configs: []
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ob_file/del_financing_interest_config:
    post:
      operationId: del_financing_interest_config
      summary: Delete Financing Interest Config
      tags:
        - Service Parameter
      x-permission-key:
        - settlement.financing_interest_allocation_config
      x-lbonly: false
      description: |-
        Deletes a specified financing-interest revenue-sharing configuration.

        ### Request parameters

        - Required: `id` (configuration ID).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real financing-interest revenue-sharing configuration and affects financing-interest calculation and revenue sharing for the related periods. Confirm the target configuration before calling.

        ### Result

        On success the configuration is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: number
                  description: Config ID associated with this request.
            example:
              id: 123
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: On success `data` is an empty object.
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /ob_file/edit_financing_interest_config:
    post:
      operationId: edit_financing_interest_config
      summary: Edit Financing Interest Config
      tags:
        - Service Parameter
      x-permission-key:
        - settlement.financing_interest_allocation_config
      x-lbonly: false
      description: |-
        Creates or edits a financing-interest revenue-sharing configuration; both reuse the same endpoint, distinguished by `action_in` and whether `id` is supplied.

        ### Request parameters

        - Required: `action_in` (operation type), `currency`, `standard_financing_rate`, `min_interest_spread` (fallback interest spread), `year_fee_days` (annual-rate day count), `rounding_method`, `rounding_decimals`, `begin_month` (effective start month), `end_month` (effective end month).
        - Optional: `id` (configuration ID, passed when editing).
        - `currency` cannot be changed when editing an existing record; `begin_month` and `end_month` use the `YYYYMM` format.
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation changes real billing configuration and directly affects financing-interest calculation and revenue sharing. Confirm the target record and parameters before calling.

        ### Result

        On success the configuration is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action_in
                - currency
                - standard_financing_rate
                - min_interest_spread
                - year_fee_days
                - rounding_method
                - rounding_decimals
                - begin_month
                - end_month
              properties:
                id:
                  type: number
                  description: Included only when editing an existing record; not passed on creation.
                action_in:
                  type: string
                  description: 'Enum values: 1 = create, 2 = edit, 3 = delete; send 2 when a record id is present, otherwise 1.'
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Add
                    - value: '2'
                      label: Edit
                    - value: '3'
                      label: Delete
                  example: '1'
                currency:
                  type: string
                  description: A three-letter currency code. Read-only when editing an existing record.
                  enum:
                    - USD
                    - HKD
                    - SGD
                    - CNY
                    - AUD
                    - JPY
                    - GBP
                    - EUR
                    - CAD
                    - MYR
                    - AMD
                    - BGN
                    - BTN
                    - BWP
                    - BZD
                    - CDF
                    - CHF
                    - DKK
                    - GHS
                    - GMD
                    - GNF
                    - HRK
                    - HUF
                    - ILS
                    - INR
                    - IQD
                    - IRR
                    - ISK
                    - LRD
                    - LYD
                    - MOP
                    - RUB
                    - UAH
                    - UGX
                    - UYI
                    - UYU
                    - UZS
                    - XAF
                    - XOF
                    - XSU
                    - YER
                    - IDR
                    - TWD
                    - THB
                    - KRW
                    - NZD
                  x-enum-details:
                    - value: USD
                      label: USD
                    - value: HKD
                      label: HKD
                    - value: SGD
                      label: SGD
                    - value: CNY
                      label: CNY
                    - value: AUD
                      label: AUD
                    - value: JPY
                      label: JPY
                    - value: GBP
                      label: GBP
                    - value: EUR
                      label: EUR
                    - value: CAD
                      label: CAD
                    - value: MYR
                      label: MYR
                    - value: AMD
                      label: AMD
                    - value: BGN
                      label: BGN
                    - value: BTN
                      label: BTN
                    - value: BWP
                      label: BWP
                    - value: BZD
                      label: BZD
                    - value: CDF
                      label: CDF
                    - value: CHF
                      label: CHF
                    - value: DKK
                      label: DKK
                    - value: GHS
                      label: GHS
                    - value: GMD
                      label: GMD
                    - value: GNF
                      label: GNF
                    - value: HRK
                      label: HRK
                    - value: HUF
                      label: HUF
                    - value: ILS
                      label: ILS
                    - value: INR
                      label: INR
                    - value: IQD
                      label: IQD
                    - value: IRR
                      label: IRR
                    - value: ISK
                      label: ISK
                    - value: LRD
                      label: LRD
                    - value: LYD
                      label: LYD
                    - value: MOP
                      label: MOP
                    - value: RUB
                      label: RUB
                    - value: UAH
                      label: UAH
                    - value: UGX
                      label: UGX
                    - value: UYI
                      label: UYI
                    - value: UYU
                      label: UYU
                    - value: UZS
                      label: UZS
                    - value: XAF
                      label: XAF
                    - value: XOF
                      label: XOF
                    - value: XSU
                      label: XSU
                    - value: YER
                      label: YER
                    - value: IDR
                      label: IDR
                    - value: TWD
                      label: TWD
                    - value: THB
                      label: THB
                    - value: KRW
                      label: KRW
                    - value: NZD
                      label: NZD
                  example: USD
                standard_financing_rate:
                  type: string
                  description: A percentage value; the client validates the range > 0 and ≤ 9999, with at most 10 decimal places.
                min_interest_spread:
                  type: string
                  description: A percentage value; validated with the same rules as standard_financing_rate.
                year_fee_days:
                  type: number
                  description: |-
                    Enum comes from ref settlement_year_fee_days; submitted as a number.

                    **Allowed values:** `360`, `365`, `366`
                  enum:
                    - '360'
                    - '365'
                    - '366'
                  x-enum-details:
                    - value: '360'
                      label: '360'
                    - value: '365'
                      label: '365'
                    - value: '366'
                      label: '366'
                  example: '360'
                rounding_method:
                  type: number
                  description: Rounding method.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Round
                    - value: '2'
                      label: Round Up
                    - value: '3'
                      label: Round Down
                  example: '1'
                rounding_decimals:
                  type: number
                  description: Number of decimal places to retain, submitted as a number.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: '0'
                    - value: '1'
                      label: '1'
                    - value: '2'
                      label: '2'
                    - value: '3'
                      label: '3'
                  example: '0'
                begin_month:
                  type: string
                  description: Format YYYYMM, generated by splitting the caller "validity period" month-range picker.
                end_month:
                  type: string
                  description: Format YYYYMM, generated by splitting the caller "validity period" month-range picker.
            example:
              action_in: '1'
              currency: USD
              standard_financing_rate: '6.8'
              min_interest_spread: '0.5'
              year_fee_days: 360
              rounding_method: 0
              rounding_decimals: 2
              begin_month: '202601'
              end_month: '202612'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/add_booking_types:
    post:
      operationId: add_booking_types
      summary: Add Booking Type
      tags:
        - Service Parameter
      x-permission-key:
        - book.booking_types_update
      x-lbonly: false
      description: |-
        Adds a booking business type, defining a category of bookable business in the accounting system.

        ### Request parameters

        - Required: `id` (always `0` when adding), `source_id` (data source), `direction` (receipt/payment direction), `biz_code` (business code), `is_account` (whether it is booked), `account_currencies` (booking currencies).
        - Optional: `language_en`, `language_cn`, `language_hk` (business name in each language).
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation adds a real booking business-type configuration and affects downstream accounting and booking rules. Confirm the parameters before calling.

        ### Result

        On success the booking business type is added. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - source_id
                - direction
                - biz_code
                - is_account
                - account_currencies
              properties:
                id:
                  type: integer
                  description: On creation the client always passes 0 (values.id = 0 in onSubmit).
                source_id:
                  type: string
                  description: |-
                    Data source. Options come from the data dictionary `book_data_source_editable` (retrieve via `GET /v1/datasets/refs/book_data_source_editable`); the selected source determines the available `biz_code` values.
                  enum:
                    - '1'
                  x-enum-details:
                    - value: '1'
                      label: Funds
                  example: '1'
                direction:
                  type: string
                  description: |-
                    Payment direction. Only one value is supported. This field may be omitted when
                    `source_id` belongs to the configured `clientFeeList`.

                    **Allowed values:** `Payment`, `Payee` (Collecting)
                  enum:
                    - Payment
                    - Payee
                  x-enum-details:
                    - value: Payment
                      label: Payment
                    - value: Payee
                      label: Collecting
                  example: Payment
                biz_code:
                  type: string
                  description: |-
                    Business code. The available options depend on the selected data source (`source_id`): member fees, agent fees, settlement contracts, atomic vault and other sources each have their own business-code list, returned by the backend according to the chosen `source_id`; use the options the service returns.
                is_account:
                  type: string
                  description: |-
                    Whether accounting is enabled. When this field is `N`, `account_currencies` must be
                    `["ALL"]`.
                  enum:
                    - Y
                    - N
                  x-enum-details:
                    - value: Y
                      label: 'Yes'
                    - value: N
                      label: 'No'
                  example: Y
                account_currencies:
                  type: array
                  description: |-
                    Account currencies. A tree multi-select: either `["ALL"]` or a list of specific currency codes (e.g. `["USD","HKD"]`). Currency options come from the data dictionary `tenant_currencies_by_channel` (retrieve via `GET /v1/datasets/refs/tenant_currencies_by_channel`). Fixed to `["ALL"]` when `is_account=N`. Array elements are strings.
                  items:
                    type: string
                language_en:
                  type: string
                  description: |-
                    Extracted from the name.en of the option corresponding to biz_code in onSubmit and
                    appended on submission; not a directly entered request field.
                language_cn:
                  type: string
                  description: |-
                    Extracted in onSubmit from name['zh-CN'] of the option matching biz_code and appended
                    to the submission; not a directly entered request field.
                language_hk:
                  type: string
                  description: |-
                    On submit, taken from the name['zh-HK'] of the option corresponding to biz_code and
                    attached to the request; not a directly entered request field.
            example:
              id: 0
              source_id: '1'
              direction: '1'
              biz_code: TRADE_FEE
              is_account: Y
              account_currencies:
                - USD
                - HKD
              language_en: Trading Fee
              language_cn: 交易费
              language_hk: 交易費
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/edit_booking_types:
    post:
      operationId: edit_booking_types
      summary: Edit Booking Type
      tags:
        - Service Parameter
      x-permission-key:
        - book.booking_types_update
      x-lbonly: false
      description: |-
        Edits an existing booking business type.

        ### Request parameters

        - Required: `id` (business-type ID), `source_id` (data source), `biz_code` (business code), `is_account` (whether it is booked), `account_currencies` (booking currencies).
        - Optional: `direction` (receipt/payment direction), `language_en`, `language_cn`, `language_hk`, `audit_frontend_resource` (audit context).
        - When `is_account=N`, `account_currencies` is fixed to `['ALL']`.
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes a real booking business-type configuration and affects downstream accounting and booking rules. Confirm the target record and parameters before calling.

        ### Result

        On success the booking business type is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - source_id
                - biz_code
                - is_account
                - account_currencies
              properties:
                id:
                  type: string
                  description: Taken from the row data record.id.
                source_id:
                  type: string
                  description: |-
                    Single-select, enum sourced from ref book_data_source_editable.

                    Call `GET /v1/datasets/refs/book_data_source_editable` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_data_source_editable
                direction:
                  type: string
                  description: |-
                    A radio whose enum comes from the booking_types_pay reference; hidden and not
                    submitted when source_id is the client fee list.

                    **Allowed values:** `Payment`, `Payee` (Collecting)
                  enum:
                    - Payment
                    - Payee
                  x-enum-details:
                    - value: Payment
                      label: Payment
                    - value: Payee
                      label: Collecting
                  example: Payment
                biz_code:
                  type: string
                  description: |-
                    Business code. The available options depend on the selected data source (`source_id`): member fees, agent fees, settlement contracts, atomic vault and other sources each have their own business-code list, returned by the backend according to the chosen `source_id`; use the options the service returns.
                is_account:
                  type: string
                  description: A radio whose enum comes from the ref booking_types_is_account.
                  enum:
                    - Y
                    - N
                  x-enum-details:
                    - value: Y
                      label: 'Yes'
                    - value: N
                      label: 'No'
                  example: Y
                account_currencies:
                  type: array
                  description: A tree-select component; fixed to ['ALL'] when is_account=N.
                  items:
                    type: string
                language_en:
                  type: string
                  description: Taken from the name.en of the biz_code option.
                language_cn:
                  type: string
                  description: Taken from name['zh-CN'] of the biz_code option.
                language_hk:
                  type: string
                  description: Taken from name['zh-HK'] of the biz_code option.
                audit_frontend_resource:
                  type: object
                  description: Taken from the row data record before editing; not a business field.
            example:
              id: '1'
              source_id: '1'
              direction: ''
              biz_code: ''
              is_account: Y
              account_currencies:
                - ALL
              language_en: ''
              language_cn: ''
              language_hk: ''
              audit_frontend_resource: {}
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/addAccountItems:
    post:
      operationId: add_account_items
      summary: Add Account Items
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_items_edit
      x-lbonly: false
      description: |-
        Adds an accounting subject (chart-of-accounts item).

        ### Request parameters

        - Required: `feature` (subject attribute), `level` (subject level), `name` (subject name), `direct` (subject direction).
        - Optional: `pSubjectCode` (parent subject code), `outSubjectCode` (external subject code).
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes the real chart of accounts and affects downstream bookkeeping and accounting. Confirm the parameters before calling.

        ### Result

        On success the accounting subject is added. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - feature
                - level
                - name
                - direct
              properties:
                feature:
                  type: string
                  description: |-
                    Account-attribute ID. Use the `id` returned by the account-attribute query; the attribute
                    must be valid for the current organization.
                level:
                  type: string
                  description: |-
                    Account level.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Level 1
                    - value: '2'
                      label: Level 2
                    - value: '3'
                      label: Level 3
                  example: '1'
                name:
                  type: string
                  description: This field is required.
                direct:
                  type: string
                  description: |-
                    Account direction (debit/credit); the caller filters out the `key=3` (common) option.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Debit
                    - value: '2'
                      label: Credit
                    - value: '3'
                      label: Common
                  example: '1'
                pSubjectCode:
                  type: string
                  description: |-
                    When level=2, take the first-level account code; when level=3, take the second-level
                    account code; when level=1, fixed at 0.
                outSubjectCode:
                  type: string
                  description: The account code used for syncing to external systems; optional.
            example:
              feature: '10'
              level: '2'
              name: 银行存款
              direct: '1'
              pSubjectCode: '1001'
              outSubjectCode: '1001001'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/deleteAccountItems:
    delete:
      operationId: delete_account_items
      summary: Delete Account Items
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_items_edit
      x-lbonly: false
      description: |-
        Deletes a specified accounting subject.

        ### Request parameters

        - Required: `id` (subject ID).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real accounting subject and affects downstream bookkeeping and accounting. Confirm the target subject before calling.

        ### Result

        On success the accounting subject is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Account Item ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/updateAccountItems:
    post:
      operationId: update_account_items
      summary: Update Account Items
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_items_edit
      x-lbonly: false
      description: |-
        Edits an existing accounting subject.

        ### Request parameters

        - Required: `id` (subject ID), `feature` (subject attribute), `level` (subject level), `name` (subject name), `direct` (subject direction).
        - Optional: `outSubjectCode` (external subject code), `audit_frontend_resource` (audit context: pre-edit record snapshot).
        - In edit mode `feature` and `level` cannot be changed but are still submitted with the request.
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes a real accounting-subject configuration and affects downstream bookkeeping and accounting. Confirm the target record and parameters before calling.

        ### Result

        On success the accounting subject is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - feature
                - level
                - name
                - direct
              properties:
                id:
                  type: string
                  description: Account Item ID associated with this request.
                feature:
                  type: string
                  description: The account attribute ID; this field is disabled in edit mode but is still submitted with the form.
                level:
                  type: string
                  description: Account level. Read-only when editing but still submitted with the form.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Level 1
                    - value: '2'
                      label: Level 2
                    - value: '3'
                      label: Level 3
                  example: '1'
                name:
                  type: string
                  description: This field is required.
                direct:
                  type: string
                  description: Debit/credit direction.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Debit
                    - value: '2'
                      label: Credit
                    - value: '3'
                      label: Common
                  example: '1'
                outSubjectCode:
                  type: string
                  description: The account code used for syncing to external systems; optional.
                audit_frontend_resource:
                  type: object
                  description: A snapshot of the complete row data before editing, used for back-end audit comparison. It is not a business field.
            example:
              id: '10086'
              feature: '10'
              level: '2'
              name: 银行存款
              direct: '1'
              outSubjectCode: '1001001'
              audit_frontend_resource:
                id: '10086'
                name: 银行存款
                level_val: '2'
                feature_val: '10'
                direct_val: '1'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/account_entry_detail_v3:
    post:
      operationId: account_entry_detail
      summary: Entry Rule Detail
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_entries_query
      x-lbonly: false
      description: |-
        Queries the full configuration of a single accounting entry rule by `id`, used to pre-fill the payload when editing or copying via "create/edit accounting entry rule" (`POST /book/create_or_modify_account_entries_v2`).

        ### Request parameters

        - Required: `id` (entry rule ID), placed in the JSON request body.
        - See the field definitions for format and business meaning.

        ### Returns

        On success, `data` contains the full configuration of the entry rule, with fields mapping one-to-one to the create/edit endpoint's request parameters. See the response definition for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Id of the entry rule to retrieve.
            example:
              id: '10086'
      responses:
        '200':
          description: The request was processed successfully. `data` contains the full configuration of the entry rule.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Full configuration of the entry rule. Fields correspond to the request parameters of Create Or Modify Account Entries.
                    properties:
                      id:
                        type: string
                        description: Entry rule id.
                      source_id:
                        type: integer
                        description: |-
                          Entry data source. The value comes from the `book_entry_data_source` dynamic enum and determines the visibility and required status of subfields such as `payment_*` / `payee_*`.

                          Call `GET /v1/datasets/refs/book_entry_data_source` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_entry_data_source
                      payment_account_type_id:
                        type: string
                        description: |-
                          Payment account type. The value comes from the `book_entry_account_types` dynamic enum (cascading with `source_id`).

                          Call `GET /v1/datasets/refs/book_entry_account_types` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_entry_account_types
                      payment_booking_type_id:
                        type: string
                        description: |-
                          Payment booking type. The value comes from the `book_business_types_by_source` dynamic enum (cascading with `source_id`).

                          Call `GET /v1/datasets/refs/book_business_types_by_source` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_business_types_by_source
                      payment_channel_account:
                        type: string
                        description: Paying securities account, corresponding to the payer member's `aaid`/`origin_id`.
                      payment_member_id:
                        type: string
                        description: Payer member id, associated with `payment_channel_account` and used to prefill the payer selection.
                      payment_business_group:
                        type: string
                        description: |-
                          Payment business group. The value comes from the `book_business_groups` dynamic enum.

                          Call `GET /v1/datasets/refs/book_business_groups` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_business_groups
                      payee_account_type_id:
                        type: string
                        description: |-
                          Payee account type. The value comes from the `book_entry_account_types` dynamic enum (cascading with `source_id`).

                          Call `GET /v1/datasets/refs/book_entry_account_types` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_entry_account_types
                      payee_booking_type_id:
                        type: string
                        description: |-
                          Payee booking type. The value comes from the `book_business_types_by_source` dynamic enum (cascading with `source_id`).

                          Call `GET /v1/datasets/refs/book_business_types_by_source` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_business_types_by_source
                      payee_channel_account:
                        type: string
                        description: Receiving securities account, corresponding to the payee member's `aaid`/`origin_id`.
                      payee_member_id:
                        type: string
                        description: Payee member id, associated with `payee_channel_account` and used to prefill the payee selection.
                      channel:
                        type: string
                        description: Channel; populated under some data sources.
                      user_account_type:
                        type: integer
                        description: User account type.
                        enum:
                          - '0'
                          - '1'
                          - '2'
                          - '3'
                        x-enum-details:
                          - value: '0'
                            label: Not Match
                          - value: '1'
                            label: Cash
                          - value: '2'
                            label: Margin
                          - value: '3'
                            label: House
                      currency:
                        type: array
                        items:
                          type: string
                        description: |-
                          Currency, multi-value. The values come from the `tenant_currencies_by_channel` dynamic enum.

                          Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                      org_id:
                        type: integer
                        description: |-
                          Broker org. The value comes from the `book_brokers` dynamic enum.

                          Call `GET /v1/datasets/refs/book_brokers` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_brokers
                      trade_direction:
                        type: string
                        description: Trade direction.
                        enum:
                          - SELL
                          - BUY
                        x-enum-details:
                          - value: SELL
                            label: SELL
                          - value: BUY
                            label: BUY
                      nature_type:
                        type: string
                        description: Nature type. Populated only when the data source is a bank.
                        enum:
                          - client
                          - house
                        x-enum-details:
                          - value: client
                            label: Client
                          - value: house
                            label: House
                      bank_account:
                        type: string
                        description: |-
                          Bank account. The value comes from the `book_entry_bank_accounts` dynamic enum, and is populated only when the data source is a bank.

                          Call `GET /v1/datasets/refs/book_entry_bank_accounts` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_entry_bank_accounts
                      remark:
                        type: string
                        description: Remark, up to 100 characters.
                      amount_plus_or_minus:
                        type: string
                        description: Whether the amount is positive or negative.
                        enum:
                          - PLUS
                          - MINUS
                        x-enum-details:
                          - value: PLUS
                            label: Positive amount
                          - value: MINUS
                            label: Negative amount
                      name_types:
                        type: array
                        description: |-
                          Entry summary (name types) configuration; each item corresponds to one summary-concatenation rule. Sub-structure:

                          - `name_type` (`string`): `1` for fixed text, `2` for a referenced field type.
                          - `name` (`string`, optional): fixed text.
                          - `name_field_type` (`string`, optional): when using a referenced field type, an available summary-field type returned for the selected data source.
                        items:
                          type: object
                          properties:
                            name_type:
                              type: string
                              description: 'Summary type: `1` fixed text, `2` referenced field type.'
                            name:
                              type: string
                              description: Fixed text content (used when `name_type` is `1`).
                            name_field_type:
                              type: string
                              description: Referenced summary-field type (used when `name_type` is `2`).
                      debit_feature:
                        type: string
                        description: |-
                          Debit account feature. The value comes from the `book_account_features` dynamic enum (with an additional "All" option).

                          Call `GET /v1/datasets/refs/book_account_features` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_account_features
                      debit_subject_code:
                        type: array
                        items:
                          type: string
                        description: Debit account code path, ordered from the top level down to the leaf level; the last element is the debit leaf-level (level 3) account code.
                      debit_amount_type:
                        type: string
                        description: Debit amount type.
                        enum:
                          - AMOUNT_ABS
                          - AMOUNT_NEG
                          - AMOUNT_ORIGIN
                        x-enum-details:
                          - value: AMOUNT_ABS
                            label: The absolute value of the bill
                          - value: AMOUNT_NEG
                            label: The opposite of the bill amount
                          - value: AMOUNT_ORIGIN
                            label: Original bill amount
                      debit_display_broker:
                        type: string
                        description: Whether the debit side shows the broker (AE) account.
                        enum:
                          - 'yes'
                          - 'no'
                        x-enum-details:
                          - value: 'yes'
                            label: 'Yes'
                          - value: 'no'
                            label: 'No'
                      credit_feature:
                        type: string
                        description: |-
                          Credit account feature. The value comes from the `book_account_features` dynamic enum (with an additional "All" option).

                          Call `GET /v1/datasets/refs/book_account_features` to retrieve the current data dictionary.
                        x-value-source:
                          endpoint: GET /v1/datasets/refs/book_account_features
                      credit_subject_code:
                        type: array
                        items:
                          type: string
                        description: Credit account code path, ordered from the top level down to the leaf level; the last element is the credit leaf-level (level 3) account code.
                      credit_amount_type:
                        type: string
                        description: Credit amount type. Kept consistent with the debit amount type.
                        enum:
                          - AMOUNT_ABS
                          - AMOUNT_NEG
                          - AMOUNT_ORIGIN
                        x-enum-details:
                          - value: AMOUNT_ABS
                            label: The absolute value of the bill
                          - value: AMOUNT_NEG
                            label: The opposite of the bill amount
                          - value: AMOUNT_ORIGIN
                            label: Original bill amount
                      credit_display_broker:
                        type: string
                        description: Whether the credit side shows the broker (AE) account.
                        enum:
                          - 'yes'
                          - 'no'
                        x-enum-details:
                          - value: 'yes'
                            label: Show
                          - value: 'no'
                            label: Not Show
              example:
                code: 0
                message: success
                data:
                  id: '10086'
                  source_id: 1
                  payment_account_type_id: '3'
                  payment_booking_type_id: '12'
                  payment_channel_account: ''
                  payment_member_id: ''
                  payment_business_group: ''
                  payee_account_type_id: ''
                  payee_booking_type_id: ''
                  payee_channel_account: ''
                  payee_member_id: ''
                  channel: ''
                  user_account_type: 1
                  currency:
                    - USD
                    - HKD
                  org_id: 1
                  trade_direction: ''
                  nature_type: ''
                  bank_account: ''
                  remark: 手续费分录规则
                  amount_plus_or_minus: PLUS
                  name_types:
                    - name_type: '1'
                      name: 交易手续费
                      name_field_type: ''
                  debit_feature: All
                  debit_subject_code:
                    - '1'
                    - '11'
                    - '1122'
                  debit_amount_type: AMOUNT_ABS
                  debit_display_broker: 'no'
                  credit_feature: All
                  credit_subject_code:
                    - '6'
                    - '66'
                    - '6602'
                  credit_amount_type: AMOUNT_ABS
                  credit_display_broker: 'no'
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/create_or_modify_account_entries_v2:
    post:
      operationId: create_or_modify_account_entries_v2
      summary: Create Or Modify Account Entries
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_entries_edit
      x-lbonly: false
      description: |-
        Creates or edits an accounting entry rule, configuring a mapping from a business event to its debit/credit entries.

        ### Request parameters

        - Required: `id` (entry rule ID; `0` to add, or the original/copy-source ID to edit), `source_id` (data source), `name_types` (entry-summary config), `debit_feature` (debit subject attribute), `debit_subject_code` (debit leaf-level subject code), `debit_amount_type` (debit amount type), `credit_feature` (credit subject attribute), `credit_subject_code` (credit leaf-level subject code), `credit_amount_type` (credit amount type).
        - Optional: payer/payee account and business types, `channel`, `currency`, `org_id`, `trade_direction`, `nature_type`, `remark`, debit/credit broker-display flags, `audit_frontend_resource`, and more; see the field definitions.
        - Candidate values for `source_id`, `debit_feature`, and `credit_feature` vary by business; use refs such as `GET /v1/datasets/refs/book_entry_data_source` to obtain the currently available values.
        - See the field definitions for format, value ranges, and business meaning.

        ### Business notes

        A non-`0` `id` means edit; `0` means add. The debit and credit amount types are kept consistent.

        ### Risk warning

        This operation changes a real accounting entry rule and directly affects the accounting system's automatic bookkeeping results. Confirm the target record and parameters before calling.

        ### Result

        On success the accounting entry rule is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - source_id
                - name_types
                - debit_feature
                - debit_subject_code
                - debit_amount_type
                - credit_feature
                - credit_subject_code
                - credit_amount_type
              properties:
                id:
                  type: string
                  description: Pass 0 when adding; when editing, pass the original record id or the copy-source id.
                source_id:
                  type: integer
                  description: |-
                    The entry data source; values come from the book_entry_data_source dynamic
                    enum; converted to a number before submission; determines the visibility and required
                    status of subsequent subfields such as payer/payee.

                    Call `GET /v1/datasets/refs/book_entry_data_source` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_entry_data_source
                payer:
                  type: boolean
                  description: |-
                    A toggle controlling whether the payment_* series of fields are displayed/submitted;
                    mutually exclusive with payee (only one of the two can be enabled, unless the data
                    source allows both).
                payment_account_type_id:
                  type: string
                  description: |-
                    The values come from the book_entry_account_types dynamic enum (which changes in tandem
                    with source_id); whether it is required is determined by the data-source
                    configuration.

                    Call `GET /v1/datasets/refs/book_entry_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_entry_account_types
                payment_booking_type_id:
                  type: string
                  description: |-
                    Values come from the book_business_types_by_source dynamic enum (cascading with source_id); required under most data sources.

                    Call `GET /v1/datasets/refs/book_business_types_by_source` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_business_types_by_source
                payment_channel_account:
                  type: string
                  description: Takes the aaid/origin_id after selecting from the member search.
                payment_business_group:
                  type: string
                  description: |-
                    Values come from the book_business_groups dynamic enum.

                    Call `GET /v1/datasets/refs/book_business_groups` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_business_groups
                payee_account_type_id:
                  type: string
                  description: |-
                    Value comes from the book_entry_account_types dynamic enum (linked to source_id).

                    Call `GET /v1/datasets/refs/book_entry_account_types` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_entry_account_types
                payee_booking_type_id:
                  type: string
                  description: |-
                    Value comes from the book_business_types_by_source dynamic enum (linked to source_id).

                    Call `GET /v1/datasets/refs/book_business_types_by_source` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_business_types_by_source
                payee_channel_account:
                  type: string
                  description: Takes the aaid/origin_id after selecting from the member search.
                channel:
                  type: string
                  description: Text input; required for some data sources.
                user_account_type:
                  type: integer
                  description: User account type. Converted to a number before submission.
                  enum:
                    - '0'
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '0'
                      label: Not Match
                    - value: '1'
                      label: Cash
                    - value: '2'
                      label: Margin
                    - value: '3'
                      label: House
                  example: '0'
                currency:
                  type: array
                  description: |-
                    Multi-select; elements are strings, with values from the tenant_currencies_by_channel dynamic enum.

                    Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                  items:
                    type: string
                org_id:
                  type: integer
                  description: |-
                    Value comes from the book_brokers dynamic enum; converted to a number before submission.

                    Call `GET /v1/datasets/refs/book_brokers` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_brokers
                trade_direction:
                  type: string
                  description: Trade direction.
                  enum:
                    - SELL
                    - BUY
                  x-enum-details:
                    - value: SELL
                      label: SELL
                    - value: BUY
                      label: BUY
                  example: SELL
                nature_type:
                  type: string
                  description: Nature type. Applies only when the data source is a bank.
                  enum:
                    - client
                    - house
                  x-enum-details:
                    - value: client
                      label: Client
                    - value: house
                      label: House
                  example: client
                bank_account:
                  type: string
                  description: |-
                    The values come from the book_entry_bank_accounts dynamic enum; only displayed when the data source is a bank.

                    Call `GET /v1/datasets/refs/book_entry_bank_accounts` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_entry_bank_accounts
                remark:
                  type: string
                  description: A text area, up to 100 characters.
                amount_plus_or_minus:
                  type: string
                  description: Whether the amount is positive or negative.
                  enum:
                    - PLUS
                    - MINUS
                  x-enum-details:
                    - value: PLUS
                      label: Positive amount
                    - value: MINUS
                      label: Negative amount
                  example: PLUS
                name_types:
                  type: array
                  description: |-
                    Journal-entry summary list, each item corresponding to one summary-concatenation rule;
                    items with type 0 or an empty value are filtered out before submission. Sub-structure:
                    name_type (string, 1=fixed text / 2=referenced field type), name (string, optional,
                    fixed text), name_field_type (string, optional; when using a referenced field type,
                    use an available summary-field type returned for the selected data source).
                  items:
                    type: string
                debit_feature:
                  type: string
                  description: |-
                    The values come from the book_account_features dynamic enum (with an additional "All" option).

                    Call `GET /v1/datasets/refs/book_account_features` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_account_features
                debit_subject_code:
                  type: string
                  description: Debit account, selected from a tree; before submission the leaf-level account code of the selected path is taken.
                debit_amount_type:
                  type: string
                  description: Debit amount type. Selecting a value synchronizes the credit amount type accordingly.
                  enum:
                    - AMOUNT_ABS
                    - AMOUNT_NEG
                    - AMOUNT_ORIGIN
                  x-enum-details:
                    - value: AMOUNT_ABS
                      label: The absolute value of the bill
                    - value: AMOUNT_NEG
                      label: The opposite of the bill amount
                    - value: AMOUNT_ORIGIN
                      label: Original bill amount
                  example: AMOUNT_ABS
                debit_display_broker:
                  type: string
                  description: |-
                    Single-select with values yes/no; whether it is required is determined by the data
                    source configuration; defaults to no.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: 'Yes'
                    - value: 'no'
                      label: 'No'
                  example: 'yes'
                credit_feature:
                  type: string
                  description: |-
                    The values come from the book_account_features dynamic enum (with an additional "All" option).

                    Call `GET /v1/datasets/refs/book_account_features` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/book_account_features
                credit_subject_code:
                  type: string
                  description: Credit account, selected from a tree; before submission the leaf-level account code of the selected path is taken.
                credit_amount_type:
                  type: string
                  description: Credit amount type. Read-only and kept consistent with the debit amount type.
                  enum:
                    - AMOUNT_ABS
                    - AMOUNT_NEG
                    - AMOUNT_ORIGIN
                  x-enum-details:
                    - value: AMOUNT_ABS
                      label: The absolute value of the bill
                    - value: AMOUNT_NEG
                      label: The opposite of the bill amount
                    - value: AMOUNT_ORIGIN
                      label: Original bill amount
                  example: AMOUNT_ABS
                credit_display_broker:
                  type: string
                  description: Single-select; value yes/no. Whether it is required reuses the judgment of the debit's displayed broker.
                  enum:
                    - 'yes'
                    - 'no'
                  x-enum-details:
                    - value: 'yes'
                      label: Show
                    - value: 'no'
                      label: Not Show
                  example: 'yes'
                audit_frontend_resource:
                  type: object
                  description: |-
                    An audit-trail field, not a business parameter; on add it marks the add action
                    ($action=add), and on edit it carries the complete pre-edit input values and marks the
                    edit action ($action=edit).
            example:
              id: '0'
              source_id: 1
              payer: true
              payment_account_type_id: '3'
              payment_booking_type_id: '12'
              payment_channel_account: ''
              payment_business_group: ''
              payee_account_type_id: ''
              payee_booking_type_id: ''
              payee_channel_account: ''
              channel: ''
              user_account_type: 1
              currency:
                - USD
                - HKD
              org_id: 1
              trade_direction: ''
              nature_type: ''
              bank_account: ''
              remark: 手续费分录规则
              amount_plus_or_minus: PLUS
              name_types:
                - name_type: '1'
                  name: 交易手续费
              debit_feature: All
              debit_subject_code: '1122'
              debit_amount_type: AMOUNT_ABS
              debit_display_broker: 'no'
              credit_feature: All
              credit_subject_code: '6602'
              credit_amount_type: AMOUNT_ABS
              credit_display_broker: 'no'
              audit_frontend_resource:
                isNew: true
                $action: add
                $behavior: 新增分录规则
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/deleteAccountEntries:
    delete:
      operationId: delete_account_entries
      summary: Delete Account Entries
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_entries_edit
      x-lbonly: false
      description: |-
        Deletes a specified accounting entry rule.

        ### Request parameters

        - Required: `id` (entry rule ID).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real accounting entry rule and affects the accounting system's automatic bookkeeping. Confirm the target record before calling.

        ### Result

        On success the accounting entry rule is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: Entry Rule ID associated with this request.
            example:
              id: '123'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/delete_account_merge_rule:
    post:
      operationId: delete_account_merge_rule
      summary: Delete Account Merge Rule
      tags:
        - Service Parameter
      x-permission-key:
        - book.account_merge_rule_edit
      x-lbonly: false
      description: |-
        Deletes a specified accounting entry merge rule. The audit record identifies the deleted rule by `id`.

        ### Request parameters

        - Required: `id` (rule ID).
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation deletes a real entry merge rule and affects entry merging and accounting aggregation results. Confirm the target rule before calling.

        ### Result

        On success the merge rule is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: string
                  description: The primary-key id of the merge rule to delete, from the list row's record.id.
            example:
              id: '8821'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/edit_account_merge_rule_v2:
    post:
      operationId: edit_account_merge_rule_v2
      summary: Edit Account Merge Rule
      tags:
        - Service Parameter
      x-permission-key:
        - book.account_merge_rule_edit
      x-lbonly: false
      description: |-
        Creates or edits an accounting entry merge rule; add and edit reuse the same endpoint.

        ### Request parameters

        - Required: `id` (rule ID; `'0'` to create, or the real ID to edit), `business_name`, `original_currency`, `convert_currency`, `account_merge_rule_infos` (merge-mode configuration list).
        - Optional: `payee_booking_types` (credit/receipt business types), `payment_booking_types` (debit/payment business types), `audit_frontend_resource` (audit context).
        - Candidate values for `original_currency` and `convert_currency` vary by business; call `GET /v1/datasets/refs/tenant_currencies_by_channel` to obtain the currently available values.
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation changes a real entry merge rule and affects entry merging and accounting aggregation results. Confirm the target record and parameters before calling.

        ### Result

        On success the merge rule is created or updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - business_name
                - original_currency
                - convert_currency
                - account_merge_rule_infos
              properties:
                id:
                  type: string
                  description: Send '0' on create; send the real id on edit.
                business_name:
                  type: string
                  description: This field is required.
                original_currency:
                  type: string
                  description: |-
                    Values come from the tenant_currencies_by_channel dynamic enumeration and are not fixed.

                    Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                convert_currency:
                  type: string
                  description: |-
                    Values come from the tenant_currencies_by_channel dynamic enumeration and are not fixed.

                    Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this field's value.
                  x-value-source:
                    endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                payee_booking_types:
                  type: array
                  description: |-
                    Element structure {source_id: string, booking_type_id: string}; the client expands
                    multiple booking_type_id values under the same source_id into multiple rows; submitted
                    only when the "credit account" toggle is checked.
                  items:
                    type: string
                payment_booking_types:
                  type: array
                  description: |-
                    Element structure {source_id: string, booking_type_id: string}; submitted only when
                    the "Debit account" toggle is checked.
                  items:
                    type: string
                account_merge_rule_infos:
                  type: array
                  description: 'Each element''s structure: `debit_subject_codes` (string, comma-joined account codes or `ALL`); `credit_subject_codes` (string, same as above); `loan_consolidation_type` (enum: `respective` = merge debit and credit separately / `reduction` = net debit against credit); `debit_merge_rule` (enum `0`-`5`: `0` = summarize by account / `1` = show details / `2` = summarize by account and memo / `3` = summarize by account and broker / `4` = total debit minus total credit / `5` = total credit minus total debit); `debit_name` (string, custom name); `credit_merge_rule` (same enum as `debit_merge_rule`); `credit_name` (string).'
                  example: respective,reduction
                  enum:
                    - respective
                    - reduction
                  x-enum-details:
                    - value: respective
                      label: Credit consolidation
                    - value: reduction
                      label: Consolidation of loan deductions
                  items:
                    type: string
                audit_frontend_resource:
                  type: object
                  description: |-
                    Used only for audit-log diff display (the pre-edit record snapshot); not a business
                    field and usually ignored by the backend.
            example:
              id: '8821'
              business_name: 跨境资金合并入账规则
              original_currency: USD
              convert_currency: HKD
              payee_booking_types:
                - source_id: '3'
                  booking_type_id: '101'
                - source_id: '3'
                  booking_type_id: '102'
              payment_booking_types:
                - source_id: '5'
                  booking_type_id: '201'
              account_merge_rule_infos:
                - debit_subject_codes: 6001,6002
                  credit_subject_codes: ALL
                  loan_consolidation_type: respective
                  debit_merge_rule: '0'
                  debit_name: ''
                  credit_merge_rule: '1'
                  credit_name: ''
              audit_frontend_resource:
                isEdit: true
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/feature/add:
    post:
      operationId: feature_add
      summary: Add Account Attribute
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_features_modify
      x-lbonly: false
      description: |-
        Adds an account attribute configuration.

        ### Request parameters

        - Required: `name` (attribute name), `direct` (balance direction), `prefix_code` (level-1 subject code sequence).
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation adds a real account-attribute configuration and affects the chart of accounts and booking rules. Confirm the parameters before calling.

        ### Result

        On success an account attribute record is added. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - direct
                - prefix_code
              properties:
                name:
                  type: string
                  description: Account attribute name.
                  example: 手续费
                direct:
                  type: string
                  description: Balance side.
                  enum:
                    - '1'
                    - '2'
                    - '3'
                  x-enum-details:
                    - value: '1'
                      label: Debit
                    - value: '2'
                      label: Credit
                    - value: '3'
                      label: Common
                  example: '1'
                prefix_code:
                  type: string
                  description: Level-1 account code serial number.
                  example: '1001'
            example:
              name: 手续费
              direct: '1'
              prefix_code: '1001'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/feature/{id}:
    delete:
      operationId: feature_delete
      summary: Delete Feature
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_features_modify
      x-lbonly: false
      description: |-
        Deletes a specified account attribute configuration.

        ### Request parameters

        - Required: `id` (account attribute ID), supplied in the request path.
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real account-attribute configuration and affects the chart of accounts and booking rules. Confirm the target record before calling.

        ### Result

        On success the account attribute is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Feature ID
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/feature/modify:
    post:
      operationId: feature_modify
      summary: Modify Account Attribute
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_features_modify
      x-lbonly: false
      description: |-
        Modifies an existing account attribute configuration.

        ### Request parameters

        - Required: `id` (account attribute ID of the record to modify) and `record` (the updated fields), within which `name` (attribute name), `direct` (balance direction), and `prefix_code` (level-1 subject code sequence) are all required.
        - See the field definitions for format, value ranges, and business meaning.

        ### Risk warning

        This operation changes a real account-attribute configuration and affects the chart of accounts and booking rules. Confirm the target record and parameters before calling.

        ### Result

        On success the account attribute record is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - record
              properties:
                id:
                  type: string
                  description: Account attribute id of the record to update.
                  example: '123'
                record:
                  type: object
                  description: Updated Account Attribute fields.
                  required:
                    - name
                    - direct
                    - prefix_code
                  properties:
                    name:
                      type: string
                      description: Account attribute name.
                      example: 手续费
                    direct:
                      type: string
                      description: Balance side.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '1'
                          label: Debit
                        - value: '2'
                          label: Credit
                        - value: '3'
                          label: Common
                      example: '1'
                    prefix_code:
                      type: string
                      description: Level-1 account code serial number.
                      example: '1001'
            example:
              id: '123'
              record:
                name: 手续费
                direct: '1'
                prefix_code: '1001'
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/addFundTypes:
    post:
      operationId: add_fund_types
      summary: Create Fund Type
      tags:
        - Service Parameter
      x-permission-key:
        - accounting.fund_type
      x-lbonly: false
      description: |-
        Adds a fund type, with multilingual names.

        ### Request parameters

        - Required: `name` (name in the current locale), `name_cn` (Simplified Chinese name), `name_en` (English name), `name_hk` (Traditional Chinese name).
        - Optional: `remark`.
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation adds a real fund-type configuration and affects fund classification and accounting. Confirm the parameters before calling.

        ### Result

        On success the fund type is added. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - name_cn
                - name_en
                - name_hk
              properties:
                name:
                  type: string
                  description: Taken from the name request field's value for the current UI language (zh-CN/zh-HK/en).
                name_cn:
                  type: string
                  description: This field is required.
                name_en:
                  type: string
                  description: This field is required.
                name_hk:
                  type: string
                  description: This field is required.
                remark:
                  type: string
            example:
              name: 手续费
              name_cn: 手续费
              name_en: Handling Fee
              name_hk: 手續費
              remark: 用于记录各类手续费流水
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/deleteFundTypes:
    post:
      operationId: delete_fund_types
      summary: Delete Fund Type
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_account_changes_download
      x-lbonly: false
      description: |-
        Deletes a specified fund type.

        ### Request parameters

        - Required: `id` (fund type ID).
        - See the field definitions for format and business meaning.

        ### Risk warning

        This operation deletes a real fund-type configuration and affects fund classification and accounting. Confirm the target record before calling.

        ### Result

        On success the fund type is deleted. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
              properties:
                id:
                  type: number
                  description: Fund Type ID associated with this request.
            example:
              id: 123
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                      ids:
                        type: array
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /book/fund_type/modify:
    post:
      operationId: fund_type_modify
      summary: Modify Fund Type
      tags:
        - Service Parameter
      x-permission-key:
        - accounting.fund_type
      x-lbonly: false
      description: |-
        Edits an existing fund type, with multilingual names.

        ### Request parameters

        - Required: `id` (fund type ID), `name` (name in the current locale), `name_cn` (Simplified Chinese name), `name_en` (English name), `name_hk` (Traditional Chinese name).
        - Optional: `remark`.
        - See the field definitions for format, value ranges, defaults, and business meaning.

        ### Risk warning

        This operation changes a real fund-type configuration and affects fund classification and accounting. Confirm the target record and parameters before calling.

        ### Result

        On success the fund type is updated. Refer to the `200` response definition for returned data.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - name
                - name_cn
                - name_en
                - name_hk
              properties:
                id:
                  type: integer
                  description: The ID of the fund type to edit. This field is required.
                name:
                  type: string
                  description: Taken from the name request field's value for the current UI language (zh-CN/zh-HK/en).
                name_cn:
                  type: string
                  description: This field is required.
                name_en:
                  type: string
                  description: This field is required.
                name_hk:
                  type: string
                  description: This field is required.
                remark:
                  type: string
            example:
              id: 101
              name: 手续费
              name_cn: 手续费
              name_en: Handling Fee
              name_hk: 手續費
              remark: 用于记录各类手续费流水
      responses:
        '200':
          description: The request was processed successfully. The response `data` does not contain additional business fields.
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
              example:
                code: 0
                message: success
                data: {}
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
  /v1/datasets/corp_action_sub_position_views/download:
    post:
      operationId: corp_action_sub_position_views_download
      summary: Corporate Action Sub Position View Export
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_query
      x-lbonly: false
      description: |-
        Exports "Corporate action - sub-position view" data to a file. `filters` determines the export scope and `down_fields` can restrict the exported columns.

        ### Request parameters

        - This endpoint has no required parameters; see the field definitions for optional parameters.
        - `filters` sets the export filter conditions, `down_fields` selects the exported columns, and `file_name`/`ext` control the file name and format.

        ### Business notes

        - `mode: 0`: synchronous export; on success the download URL is returned directly.
        - `mode: 1`: asynchronous export; returns a `file_download_id` first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - When the export record is no longer needed, call `DELETE /v1/datasets/download/{id}` to remove it.

        ### Returns

        On success, returns a download URL or an export-task identifier. See the response definition for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of corp_action_sub_position_views.yaml (the query capability).
                  properties:
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - view_name
                      - usage_scene
                      - usage_scene_val
                      - status
                      - status_val
                      - remark
                      - created_at
                      - created_by
                      - created_by_val
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Corporate Action Sub Position View Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/corp_action_sub_position_views
        operationId: corp_action_sub_position_views
  /v1/datasets/corp_action_sub_position_views:
    post:
      operationId: corp_action_sub_position_views
      summary: Corporate Action Sub Position View
      tags:
        - Service Parameter
      x-permission-key:
        - corporate.sub_account_view_query
      x-lbonly: false
      description: |-
        Queries "Corporate action - sub-position view" data. Use `filters` to combine business filter conditions and `page`/`page_size` to control pagination.

        ### Returns

        On success, returns the matching record list and pagination info. See the field definitions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Corporate Action Sub Position View records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            view_name:
                              type: string
                              description: '**Returned value:** View Name.'
                            usage_scene:
                              type: string
                              description: '**Returned value:** Usage Scenario.'
                            usage_scene_val:
                              type: string
                              description: Raw value of `usage_scene`. The `usage_scene` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                              description: '**Returned value:** Remarks.'
                            created_at:
                              type: string
                              description: '**Returned value:** Creation Time. The date or time format follows the value returned by the API.'
                            created_by:
                              type: string
                              description: '**Returned value:** Created By.'
                            created_by_val:
                              type: string
                              description: Raw value of `created_by`. The `created_by` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '1091'
                      view_name: 多头子仓(不含空头)
                      usage_scene: After-event Adjustment
                      usage_scene_val: '3'
                      status: Enable
                      status_val: '1'
                      remark: '222'
                      created_at: '2026-05-27 15:36:06'
                      created_by: Zhang San
                      created_by_val: user@example.com
                  total: 15
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/corp_action_sub_position_views/download
        operationId: corp_action_sub_position_views_download
  /v1/datasets/va_coin_withdraw_audit_rules/download:
    post:
      operationId: va_coin_withdraw_audit_rules_download
      summary: Coin Withdrawal Review Rules Export
      tags:
        - Service Parameter
      x-permission-key:
        - atm.va_coin_withdraw_audit_rule_inquiry
      x-lbonly: false
      description: |-
        Exports "Coin withdrawal audit rules" data to a file. `filters` determines the export scope and `down_fields` can restrict the exported columns.

        ### Request parameters

        - This endpoint has no required parameters; see the field definitions for optional parameters.
        - `filters` sets the export filter conditions, `down_fields` selects the exported columns, and `file_name`/`ext` control the file name and format.

        ### Business notes

        - `mode: 0`: synchronous export; on success the download URL is returned directly.
        - `mode: 1`: asynchronous export; returns a `file_download_id` first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - When the export record is no longer needed, call `DELETE /v1/datasets/download/{id}` to remove it.

        ### Returns

        On success, returns a download URL or an export-task identifier. See the response definition for field meanings.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of va_coin_withdraw_audit_rules.yaml (the query capability).
                  properties:
                    rule_name:
                      type: string
                      description: Filters results by Rule Name.
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - enabled
                        - disabled
                      x-enum-details:
                        - value: enabled
                          label: Enabled
                        - value: disabled
                          label: Disabled
                      example: enabled
                    audit_type:
                      type: string
                      description: Filters results by Audit rules.
                      enum:
                        - auto_pass
                        - auto_reject
                        - manual_ticket
                      x-enum-details:
                        - value: auto_pass
                          label: Auto Approved
                        - value: auto_reject
                          label: Auto Rejected
                        - value: manual_ticket
                          label: Ticket Approval
                      example: auto_pass
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - rule_name
                      - upstream_broker
                      - account_nos
                      - account_nos_val
                      - broker_allocate_states
                      - broker_allocate_states_val
                      - custom_coin_quantity_ranges
                      - custom_equivalent_usd_range
                      - custom_delay_time
                      - audit_type
                      - audit_type_val
                      - priority
                      - status
                      - status_val
                      - created_at
                      - updated_at
                      - remark
                      - updater
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Coin Withdrawal Review Rules Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/va_coin_withdraw_audit_rules
        operationId: va_coin_withdraw_audit_rules
  /v1/datasets/va_coin_withdraw_audit_rules:
    post:
      operationId: va_coin_withdraw_audit_rules
      summary: Coin Withdrawal Review Rules
      tags:
        - Service Parameter
      x-permission-key:
        - atm.va_coin_withdraw_audit_rule_inquiry
      x-lbonly: false
      description: |-
        Queries "Coin withdrawal audit rules" data. Use `filters` to combine business filter conditions and `page`/`page_size` to control pagination.

        ### Returns

        On success, returns the matching record list and pagination info. See the field definitions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    rule_name:
                      type: string
                      description: Filters results by Rule Name.
                      example: 自动化测试_1784138182
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - enabled
                        - disabled
                      x-enum-details:
                        - value: enabled
                          label: Enabled
                        - value: disabled
                          label: Disabled
                      example: Enabled
                    audit_type:
                      type: string
                      description: Filters results by Audit rules.
                      enum:
                        - auto_pass
                        - auto_reject
                        - manual_ticket
                      x-enum-details:
                        - value: auto_pass
                          label: Auto Approved
                        - value: auto_reject
                          label: Auto Rejected
                        - value: manual_ticket
                          label: Ticket Approval
                      example: Auto Approved
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Coin Withdrawal Review Rules records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            rule_name:
                              type: string
                              description: '**Returned value:** Rule Name.'
                            upstream_broker:
                              type: string
                              description: '**Returned value:** Channel.'
                            account_nos:
                              type: string
                              description: '**Returned value:** A/C No..'
                            account_nos_val:
                              type: string
                              description: Raw value of `account_nos`. The `account_nos` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            broker_allocate_states:
                              type: string
                              description: '**Returned value:** Company Fund Allocation Status.'
                            broker_allocate_states_val:
                              type: string
                              description: Raw value of `broker_allocate_states`. The `broker_allocate_states` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_coin_quantity_ranges:
                              type: string
                              description: '**Returned value:** Crypto & Quantity Range.'
                            custom_equivalent_usd_range:
                              type: string
                              description: '**Returned value:** Equivalent USD Range.'
                            custom_delay_time:
                              type: string
                              description: '**Returned value:** Review Delay. The date or time format follows the value returned by the API.'
                            audit_type:
                              type: string
                              description: '**Returned value:** Audit rules.'
                            audit_type_val:
                              type: string
                              description: Raw value of `audit_type`. The `audit_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            status:
                              type: string
                              description: '**Returned value:** Status.'
                            status_val:
                              type: string
                              description: Raw value of `status`. The `status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            updater:
                              type: string
                              description: '**Returned value:** Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '3269'
                      rule_name: 自动化测试_1784138182
                      upstream_broker: HASHKEY
                      account_nos: '****0000'
                      account_nos_val: '****0000'
                      broker_allocate_states: ''
                      broker_allocate_states_val: ''
                      custom_coin_quantity_ranges: |
                        USDT 1 - 100
                      custom_equivalent_usd_range: 1 - 100
                      custom_delay_time: 0 min
                      audit_type: Auto Approved
                      audit_type_val: auto_pass
                      priority: '100'
                      status: Enabled
                      status_val: enabled
                      created_at: '2026-07-16 01:56:25'
                      updated_at: '2026-07-16 01:56:25'
                      remark: ''
                      updater: user@example.com
                  total: 18
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/va_coin_withdraw_audit_rules/download
        operationId: va_coin_withdraw_audit_rules_download
  /v1/datasets/va_coin_withdraw_limit_configs/download:
    post:
      operationId: va_coin_withdraw_limit_configs_download
      summary: Daily Coin Withdrawal Limit Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_inquiry
      x-lbonly: false
      description: |-
        Export coin withdrawal daily-limit configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of va_coin_withdraw_limit_configs.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by ID.
                    member_ids:
                      type: string
                      description: Filters results by Client ID.
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - enabled
                        - disabled
                      x-enum-details:
                        - value: enabled
                          label: Enabled
                        - value: disabled
                          label: Disabled
                      example: enabled
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - custom_member_ids
                      - withdrawal_crypto
                      - withdrawal_crypto_val
                      - custom_cut_time
                      - custom_limit_currency
                      - custom_frequency_limit
                      - priority
                      - raw_status
                      - raw_status_val
                      - created_at
                      - updated_at
                      - remark
                      - updater
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Daily Coin Withdrawal Limit Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/va_coin_withdraw_limit_configs
        operationId: va_coin_withdraw_limit_configs
  /v1/datasets/va_coin_withdraw_limit_configs:
    post:
      operationId: va_coin_withdraw_limit_configs
      summary: Daily Coin Withdrawal Limit Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - atm.crypto_currency_inquiry
      x-lbonly: false
      description: |-
        Query coin withdrawal daily-limit configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by ID.
                    member_ids:
                      type: string
                      description: Filters results by Client ID.
                    status:
                      type: string
                      description: Filters results by Status.
                      enum:
                        - enabled
                        - disabled
                      x-enum-details:
                        - value: enabled
                          label: Enabled
                        - value: disabled
                          label: Disabled
                      example: enabled
                    created_at:
                      type: object
                      description: Filters results by Created Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Daily Coin Withdrawal Limit Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            custom_member_ids:
                              type: string
                              description: '**Returned value:** Client ID.'
                            withdrawal_crypto:
                              type: string
                              description: '**Returned value:** Supported Crypto.'
                            withdrawal_crypto_val:
                              type: string
                              description: Raw value of `withdrawal_crypto`. The `withdrawal_crypto` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custom_cut_time:
                              type: string
                              description: '**Returned value:** Cycle Base Date. The date or time format follows the value returned by the API.'
                            custom_limit_currency:
                              type: string
                              description: '**Returned value:** Daily Limit (Currency).'
                            custom_frequency_limit:
                              type: string
                              description: '**Returned value:** Withdrawal Frequency Limit.'
                            priority:
                              type: string
                              description: '**Returned value:** Priority.'
                            raw_status:
                              type: string
                              description: '**Returned value:** Status.'
                            raw_status_val:
                              type: string
                              description: Raw value of `raw_status`. The `raw_status` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update Time. The date or time format follows the value returned by the API.'
                            remark:
                              type: string
                              description: '**Returned value:** Remarks.'
                            updater:
                              type: string
                              description: '**Returned value:** Operator.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      custom_member_ids: string
                      withdrawal_crypto: string
                      withdrawal_crypto_val: string
                      custom_cut_time: string
                      custom_limit_currency: string
                      custom_frequency_limit: string
                      priority: string
                      raw_status: string
                      raw_status_val: string
                      created_at: string
                      updated_at: string
                      remark: string
                      updater: string
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/va_coin_withdraw_limit_configs/download
        operationId: va_coin_withdraw_limit_configs_download
  /v1/datasets/clearing_account_instruction_settings/download:
    post:
      operationId: clearing_account_instruction_settings_download
      summary: Export account-level settlement rule configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_charging.client_rule
      x-lbonly: false
      description: |-
        Export account-level settlement instruction settings records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_account_instruction_settings.yaml (the query capability).
                  properties:
                    market:
                      type: string
                      description: Values come from the clear_markets dynamic enumeration; not a fixed set.; queryable via GET /v1/datasets/refs/clearing_markets_all
                    biz_type:
                      type: string
                      x-enum-details:
                        - value: '!'
                          label: All
                        - value: '1'
                          label: Stock
                        - value: '5'
                          label: Options
                      enum: &id005
                        - '!'
                        - '1'
                        - '5'
                    aaid:
                      type: string
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - aaid
                      - aaid_val
                      - market
                      - market_val
                      - settlement_channel
                      - settlement_channel_val
                      - stage
                      - stage_val
                      - biz_type
                      - biz_type_val
                      - direction
                      - direction_val
                      - position
                      - position_val
                      - instr_type
                      - instr_type_val
                      - settle_method
                      - settle_method_val
                      - custodian_id
                      - custodian_id_val
                      - sub_position_no
                      - updated_at
                      - updated_by
                    example: aaid
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_account_instruction_settings
        operationId: clearing_account_instruction_settings
  /v1/datasets/clearing_account_instruction_settings:
    post:
      operationId: clearing_account_instruction_settings
      summary: Clear Account Instruction Settings
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_charging.client_rule
      x-lbonly: false
      description: |-
        Query account-level settlement instruction settings records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    market:
                      type: string
                      description: Values come from the clear_markets dynamic enumeration; not a fixed set.; queryable via GET /v1/datasets/refs/clearing_markets_all
                    biz_type:
                      type: string
                      x-enum-details:
                        - value: '!'
                          label: All
                        - value: '1'
                          label: Stock
                        - value: '5'
                          label: Options
                      enum: *id005
                    aaid:
                      type: string
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            aaid:
                              type: string
                            aaid_val:
                              type: string
                              description: Raw value of `aaid`. The `aaid` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            market:
                              type: string
                            market_val:
                              type: string
                              description: Raw value of `market`. The `market` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settlement_channel:
                              type: string
                            settlement_channel_val:
                              type: string
                              description: Raw value of `settlement_channel`. The `settlement_channel` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            stage:
                              type: string
                            stage_val:
                              type: string
                              description: Raw value of `stage`. The `stage` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            biz_type:
                              type: string
                            biz_type_val:
                              type: string
                              description: Raw value of `biz_type`. The `biz_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            direction:
                              type: string
                            direction_val:
                              type: string
                              description: Raw value of `direction`. The `direction` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            position:
                              type: string
                            position_val:
                              type: string
                              description: Raw value of `position`. The `position` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            instr_type:
                              type: string
                            instr_type_val:
                              type: string
                              description: Raw value of `instr_type`. The `instr_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settle_method:
                              type: string
                            settle_method_val:
                              type: string
                              description: Raw value of `settle_method`. The `settle_method` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custodian_id:
                              type: string
                            custodian_id_val:
                              type: string
                              description: Raw value of `custodian_id`. The `custodian_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            sub_position_no:
                              type: string
                            updated_at:
                              type: string
                            updated_by:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - aaid: string
                      aaid_val: string
                      market: string
                      market_val: string
                      settlement_channel: string
                      settlement_channel_val: string
                      stage: string
                      stage_val: string
                      biz_type: string
                      biz_type_val: string
                      direction: string
                      direction_val: string
                      position: string
                      position_val: string
                      instr_type: string
                      instr_type_val: string
                      settle_method: string
                      settle_method_val: string
                      custodian_id: string
                      custodian_id_val: string
                      sub_position_no: string
                      updated_at: string
                      updated_by: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_account_instruction_settings/download
        operationId: clearing_account_instruction_settings_download
  /v1/datasets/clearing_calendars/download:
    post:
      operationId: clearing_calendars_download
      summary: Export calendar management configuration
      tags:
        - Service Parameter
      x-permission-key:
        - clearing.clearing_calendar_view
      x-lbonly: false
      description: |-
        Export calendar management configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_calendars.yaml (the query capability).
                  properties:
                    market:
                      type: string
                      description: Values are dynamically queried from clearing_markets_all (not fixed); fetch them live via GET /v1/datasets/refs/clearing_markets.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_markets
                    trade_date:
                      type: object
                      description: Range filter; both bounds are optional (omit a bound to leave that side unbounded).
                      properties:
                        gte:
                          type: string
                          format: date
                        lte:
                          type: string
                          format: date
                    type:
                      type: string
                      x-enum-details:
                        - value: '1'
                          label: Normal Day (Trade&Settlement)
                        - value: '2'
                          label: Non-Settlement Day (Trade Only)
                        - value: '3'
                          label: Non-Trading Day (Settlement Only)
                        - value: '4'
                          label: Typhoon
                        - value: '5'
                          label: Holidays
                      enum: &id006
                        - '1'
                        - '2'
                        - '3'
                        - '4'
                        - '5'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - trade_date
                      - type
                      - type_val
                    example: trade_date
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_calendars
        operationId: clearing_calendars
  /v1/datasets/clearing_calendars:
    post:
      operationId: clearing_calendars
      summary: Query calendar management configuration
      tags:
        - Service Parameter
      x-permission-key:
        - clearing.clearing_calendar_view
      x-lbonly: false
      description: |-
        Query calendar management configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    market:
                      type: string
                      description: Values are dynamically queried from clearing_markets_all (not fixed); fetch them live via GET /v1/datasets/refs/clearing_markets.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_markets
                    trade_date:
                      type: object
                      description: Range filter; both bounds are optional (omit a bound to leave that side unbounded).
                      properties:
                        gte:
                          type: string
                          format: date
                        lte:
                          type: string
                          format: date
                    type:
                      type: string
                      x-enum-details:
                        - value: '1'
                          label: Normal Day (Trade&Settlement)
                        - value: '2'
                          label: Non-Settlement Day (Trade Only)
                        - value: '3'
                          label: Non-Trading Day (Settlement Only)
                        - value: '4'
                          label: Typhoon
                        - value: '5'
                          label: Holidays
                      enum: *id006
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            trade_date:
                              type: string
                            type:
                              type: string
                            type_val:
                              type: string
                              description: Raw value behind field `type`. The `type` field returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - trade_date: string
                      type: string
                      type_val: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_calendars/download
        operationId: clearing_calendars_download
  /v1/datasets/clearing_instruction_settings/download:
    post:
      operationId: clearing_instruction_settings_download
      summary: Export settlement rule configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_instruction_settings
      x-lbonly: false
      description: |-
        Export settlement instruction settings records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_instruction_settings.yaml (the query capability).
                  properties:
                    market:
                      type: string
                      description: Values come from the clear_markets dynamic enumeration; not a fixed set.; queryable via GET /v1/datasets/refs/clearing_markets_all
                    biz_type:
                      type: string
                      x-enum-details:
                        - value: '!'
                          label: All
                        - value: '1'
                          label: Stock
                        - value: '5'
                          label: Options
                      enum: *id005
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - market
                      - market_val
                      - settlement_channel
                      - settlement_channel_val
                      - stage
                      - stage_val
                      - account_type
                      - account_type_val
                      - biz_type
                      - biz_type_val
                      - direction
                      - direction_val
                      - position
                      - position_val
                      - instr_type
                      - instr_type_val
                      - settle_method
                      - settle_method_val
                      - custodian_id
                      - custodian_id_val
                      - sub_position_no
                      - updated_at
                      - updated_by
                    example: market
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_instruction_settings
        operationId: clearing_instruction_settings
  /v1/datasets/clearing_instruction_settings:
    post:
      operationId: clearing_instruction_settings
      summary: Clear Instruction Settings
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_instruction_settings
      x-lbonly: false
      description: |-
        Query settlement instruction settings records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    market:
                      type: string
                      description: Values come from the clear_markets dynamic enumeration; not a fixed set.; queryable via GET /v1/datasets/refs/clearing_markets_all
                    biz_type:
                      type: string
                      x-enum-details:
                        - value: '!'
                          label: All
                        - value: '1'
                          label: Stock
                        - value: '5'
                          label: Options
                      enum: *id005
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            market:
                              type: string
                            market_val:
                              type: string
                              description: Raw value of `market`. The `market` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settlement_channel:
                              type: string
                            settlement_channel_val:
                              type: string
                              description: Raw value of `settlement_channel`. The `settlement_channel` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            stage:
                              type: string
                            stage_val:
                              type: string
                              description: Raw value of `stage`. The `stage` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            account_type:
                              type: string
                            account_type_val:
                              type: string
                              description: Raw value of `account_type`. The `account_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            biz_type:
                              type: string
                            biz_type_val:
                              type: string
                              description: Raw value of `biz_type`. The `biz_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            direction:
                              type: string
                            direction_val:
                              type: string
                              description: Raw value of `direction`. The `direction` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            position:
                              type: string
                            position_val:
                              type: string
                              description: Raw value of `position`. The `position` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            instr_type:
                              type: string
                            instr_type_val:
                              type: string
                              description: Raw value of `instr_type`. The `instr_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settle_method:
                              type: string
                            settle_method_val:
                              type: string
                              description: Raw value of `settle_method`. The `settle_method` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            custodian_id:
                              type: string
                            custodian_id_val:
                              type: string
                              description: Raw value of `custodian_id`. The `custodian_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            sub_position_no:
                              type: string
                            updated_at:
                              type: string
                            updated_by:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - market: string
                      market_val: string
                      settlement_channel: string
                      settlement_channel_val: string
                      stage: string
                      stage_val: string
                      account_type: string
                      account_type_val: string
                      biz_type: string
                      biz_type_val: string
                      direction: string
                      direction_val: string
                      position: string
                      position_val: string
                      instr_type: string
                      instr_type_val: string
                      settle_method: string
                      settle_method_val: string
                      custodian_id: string
                      custodian_id_val: string
                      sub_position_no: string
                      updated_at: string
                      updated_by: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_instruction_settings/download
        operationId: clearing_instruction_settings_download
  /v1/datasets/clearing_markets/download:
    post:
      operationId: clearing_markets_download
      summary: Trading Market Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_markets
      x-lbonly: false
      description: |-
        Export trading markets records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_markets.yaml (the query capability).
                  properties:
                    market:
                      type: string
                      description: |-
                        Filters results by Market.

                        Values are dynamic. Call `GET /v1/datasets/refs/clearing_markets` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_markets
                    id:
                      type: string
                      description: Filters results by Id.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - market
                      - otc_flag
                      - otc_flag_val
                      - busin_flow_name
                      - overnight_flag
                      - overnight_flag_val
                      - equity_back_n
                      - balance_back_n
                      - custodian_id
                      - custodian_id_val
                      - sub_position_no
                      - currency
                      - use_public_stock_info
                      - use_public_stock_info_val
                      - updated_at
                      - updated_by
                    example: market
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Trading Market Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_markets
        operationId: clearing_markets
  /v1/datasets/clearing_markets:
    post:
      operationId: clearing_markets
      summary: Trading Market
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_markets
      x-lbonly: false
      description: |-
        Query trading markets records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    market:
                      type: string
                      description: |-
                        Filters results by Market.

                        Values are dynamic. Call `GET /v1/datasets/refs/clearing_markets` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/clearing_markets
                      example: US
                    id:
                      type: string
                      description: Filters results by Id.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Trading Market records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            market:
                              type: string
                              description: '**Returned value:** Market.'
                            otc_flag:
                              type: string
                              description: '**Returned value:** Day-End Tasks Type.'
                            otc_flag_val:
                              type: string
                              description: Raw value of `otc_flag`. The `otc_flag` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            busin_flow_name:
                              type: string
                              description: '**Returned value:** Market Group.'
                            overnight_flag:
                              type: string
                              description: '**Returned value:** Overnight Tag.'
                            overnight_flag_val:
                              type: string
                              description: Raw value of `overnight_flag`. The `overnight_flag` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            equity_back_n:
                              type: string
                              description: '**Returned value:** Stock Settlement T+N.'
                            balance_back_n:
                              type: string
                              description: '**Returned value:** Cash Settlement T+N.'
                            custodian_id:
                              type: string
                              description: '**Returned value:** Custodian (Default).'
                            custodian_id_val:
                              type: string
                              description: Raw value of `custodian_id`. The `custodian_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            sub_position_no:
                              type: string
                              description: '**Returned value:** Sub A/C(Default).'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            use_public_stock_info:
                              type: string
                              description: '**Returned value:** Sources of Symbol Information.'
                            use_public_stock_info_val:
                              type: string
                              description: Raw value of `use_public_stock_info`. The `use_public_stock_info` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                            updated_by:
                              type: string
                              description: '**Returned value:** Updated By.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - market: US
                      otc_flag: Normal Day-End Tasks
                      otc_flag_val: '0'
                      busin_flow_name: 欧美市场
                      overnight_flag: 'Yes'
                      overnight_flag_val: '1'
                      equity_back_n: '1'
                      balance_back_n: '1'
                      custodian_id: IB
                      custodian_id_val: '2'
                      sub_position_no: U60265141
                      currency: USD
                      use_public_stock_info: Dependent On Public Stock Information
                      use_public_stock_info_val: '1'
                      updated_at: '1784010321'
                      updated_by: user@example.com
                  total: 26
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_markets/download
        operationId: clearing_markets_download
  /v1/datasets/clearing_custodians/download:
    post:
      operationId: clearing_custodians_download
      summary: Clearing - Custodian Management Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_custodians
      x-lbonly: false
      description: |-
        Export clearing custodians records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_custodians.yaml (the query capability).
                  properties:
                    name:
                      type: string
                      description: Filters results by Custodian Name.
                    ccass_code:
                      type: string
                      description: Filters results by CCASS Code.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - name
                      - custodian_type
                      - custodian_type_val
                      - settlement_channel
                      - settlement_channel_val
                      - settle_type
                      - settle_type_val
                      - trading_account_type
                      - trading_account_type_val
                      - ccass_code
                      - address
                      - email
                      - fax
                      - contact_person
                      - tel
                      - stamp_duty_file_no
                      - seat_nos
                      - company_name
                      - updated_at
                      - updated_by
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Clearing - Custodian Management Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_custodians
        operationId: clearing_custodians
  /v1/datasets/clearing_custodians:
    post:
      operationId: clearing_custodians
      summary: Clearing - Custodian Management
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_custodians
      x-lbonly: false
      description: |-
        Query clearing custodians records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    name:
                      type: string
                      description: Filters results by Custodian Name.
                      example: CLSA
                    ccass_code:
                      type: string
                      description: Filters results by CCASS Code.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Clearing - Custodian Management records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            name:
                              type: string
                              description: '**Returned value:** Name.'
                            custodian_type:
                              type: string
                              description: '**Returned value:** Type.'
                            custodian_type_val:
                              type: string
                              description: Raw value of `custodian_type`. The `custodian_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settlement_channel:
                              type: string
                              description: '**Returned value:** Settlement Channel.'
                            settlement_channel_val:
                              type: string
                              description: Raw value of `settlement_channel`. The `settlement_channel` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            settle_type:
                              type: string
                              description: '**Returned value:** Basis Of Holding Reconciliation.'
                            settle_type_val:
                              type: string
                              description: Raw value of `settle_type`. The `settle_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            trading_account_type:
                              type: string
                              description: '**Returned value:** Basis Of Sub Account.'
                            trading_account_type_val:
                              type: string
                              description: Raw value of `trading_account_type`. The `trading_account_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            ccass_code:
                              type: string
                              description: '**Returned value:** CCASS Code.'
                            address:
                              type: string
                              description: '**Returned value:** Address.'
                            email:
                              type: string
                              description: '**Returned value:** Email.'
                            fax:
                              type: string
                              description: '**Returned value:** Fax.'
                            contact_person:
                              type: string
                              description: '**Returned value:** Stamp Duty Contact Person.'
                            tel:
                              type: string
                              description: '**Returned value:** Stamp Duty Tel.'
                            stamp_duty_file_no:
                              type: string
                              description: '**Returned value:** Stamp Duty File No.'
                            seat_nos:
                              type: string
                              description: '**Returned value:** Broker No.'
                            company_name:
                              type: string
                              description: '**Returned value:** Report Company Name.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated At. The date or time format follows the value returned by the API.'
                            updated_by:
                              type: string
                              description: '**Returned value:** Updated By.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '20940'
                      name: CLSA
                      custodian_type: Custodian/Broker
                      custodian_type_val: '1'
                      settlement_channel: settlement_clsa_hk
                      settlement_channel_val: settlement_clsa_hk
                      settle_type: Settled Position
                      settle_type_val: '0'
                      trading_account_type: Trading Account
                      trading_account_type_val: '1'
                      ccass_code: ''
                      address: 8 Finance Street, Central, Hong Kong
                      email: user@example.com
                      fax: '1'
                      contact_person: ''
                      tel: ''
                      stamp_duty_file_no: ''
                      seat_nos: ''
                      company_name: ''
                      updated_at: '1776134933'
                      updated_by: user@example.com
                  total: 24
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_custodians/download
        operationId: clearing_custodians_download
  /v1/datasets/clearing_settlement_channels/download:
    post:
      operationId: clearing_settlement_channels_download
      summary: Trade Reconciliation Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_settlement_channels
      x-lbonly: false
      description: |-
        Export settlement channels records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_settlement_channels.yaml (the query capability).
                  properties:
                    custodian_id:
                      type: string
                    custodian_name:
                      type: string
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - channel_type
                      - channel_type_val
                      - tolerance_threshold
                      - account_mode
                      - account_mode_val
                      - verify_accounts
                      - verified
                      - verified_val
                      - remark
                      - updated_at
                      - updated_by
                    example: channel_type
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: Export result. In synchronous mode the download URL is returned directly; in asynchronous mode a `file_download_id` is returned for subsequent polling.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_settlement_channels
        operationId: clearing_settlement_channels
  /v1/datasets/clearing_settlement_channels:
    post:
      operationId: clearing_settlement_channels
      summary: Trade Reconciliation Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_settlement_channels
      x-lbonly: false
      description: |-
        Query settlement channels records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    custodian_id:
                      type: string
                    custodian_name:
                      type: string
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            channel_type:
                              type: string
                            channel_type_val:
                              type: string
                              description: Raw value of `channel_type`. The `channel_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            tolerance_threshold:
                              type: string
                            account_mode:
                              type: string
                            account_mode_val:
                              type: string
                              description: Raw value of `account_mode`. The `account_mode` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            verify_accounts:
                              type: string
                            verified:
                              type: string
                            verified_val:
                              type: string
                              description: Raw value of `verified`. The `verified` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                            updated_at:
                              type: string
                            updated_by:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - channel_type: string
                      channel_type_val: string
                      tolerance_threshold: string
                      account_mode: string
                      account_mode_val: string
                      verify_accounts: string
                      verified: string
                      verified_val: string
                      remark: string
                      updated_at: string
                      updated_by: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_settlement_channels/download
        operationId: clearing_settlement_channels_download
  /v1/datasets/clearing_sub_positions/download:
    post:
      operationId: clearing_sub_positions_download
      summary: Export custodian sub-position management
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_sub_positions
      x-lbonly: false
      description: |-
        Export custodian sub-positions records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_sub_positions.yaml (the query capability).
                  properties:
                    custodian_id:
                      type: string
                    custodian_name:
                      type: string
                    'no':
                      type: string
                    name:
                      type: string
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - 'no'
                      - name
                      - position_direction
                      - position_direction_val
                      - remark
                      - updated_at
                      - updated_by
                    example: 'no'
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: Export result. In synchronous mode the download URL is returned directly; in asynchronous mode a `file_download_id` is returned for subsequent polling.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_sub_positions
        operationId: clearing_sub_positions
  /v1/datasets/clearing_sub_positions:
    post:
      operationId: clearing_sub_positions
      summary: Query custodian sub-position management
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_clearing.clearing_sub_positions
      x-lbonly: false
      description: |-
        Query custodian sub-positions records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    custodian_id:
                      type: string
                    custodian_name:
                      type: string
                    'no':
                      type: string
                    name:
                      type: string
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: Paginated list result.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            'no':
                              type: string
                            name:
                              type: string
                            position_direction:
                              type: string
                            position_direction_val:
                              type: string
                              description: Raw value of `position_direction`. The `position_direction` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                            updated_at:
                              type: string
                            updated_by:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - 'no': string
                      name: string
                      position_direction: string
                      position_direction_val: string
                      remark: string
                      updated_at: string
                      updated_by: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_sub_positions/download
        operationId: clearing_sub_positions_download
  /v1/datasets/ipo_channels/download:
    post:
      operationId: ipo_channels_download
      summary: IPO Subscription Channel Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.ipo_channel
      x-lbonly: false
      description: |-
        Export IPO subscription channel configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of ipo_channels.yaml (the query capability).
                  properties:
                    name:
                      type: string
                      description: Filters results by Channel Name.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - name
                      - custom_typo
                      - fini
                      - fini_val
                      - remark
                    example: name
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the IPO Subscription Channel Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/ipo_channels
        operationId: ipo_channels
  /v1/datasets/ipo_channels:
    post:
      operationId: ipo_channels
      summary: IPO Subscription Channel Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.ipo_channel
      x-lbonly: false
      description: |-
        Query IPO subscription channel configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    name:
                      type: string
                      description: Filters results by Channel Name.
                      example: Show3
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated IPO Subscription Channel Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              description: '**Returned value:** Channel Name.'
                            custom_typo:
                              type: string
                              description: '**Returned value:** Support Types.'
                            fini:
                              type: string
                              description: '**Returned value:** Sync To HKEx.'
                            fini_val:
                              type: string
                              description: Raw value of `fini`. The `fini` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - name: Show3
                      custom_typo: ''
                      fini: 'No'
                      fini_val: '0'
                      remark: ''
                  total: 106
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/ipo_channels/download
        operationId: ipo_channels_download
  /v1/datasets/ipo_buy_powers/download:
    post:
      operationId: ipo_buy_powers_download
      summary: IPO Subscription Buying Power Configuration Export
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.buy_power
      x-lbonly: false
      description: |-
        Export IPO buying-power configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of ipo_buy_powers.yaml (the query capability).
                  properties:
                    counter_id:
                      type: string
                      description: Filters results by Counter ID.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - counter_id
                      - type
                      - type_val
                      - hold_ipo
                      - hold_ipo_val
                      - borrow_enable
                      - borrow_enable_val
                      - floating_ratio
                      - updated_at
                    example: counter_id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the IPO Subscription Buying Power Configuration Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/ipo_buy_powers
        operationId: ipo_buy_powers
  /v1/datasets/ipo_buy_powers:
    post:
      operationId: ipo_buy_powers
      summary: IPO Subscription Buying Power Configuration
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.buy_power
      x-lbonly: false
      description: |-
        Query IPO buying-power configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    counter_id:
                      type: string
                      description: Filters results by Counter ID.
                      example: ST/HK/3388
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated IPO Subscription Buying Power Configuration records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            counter_id:
                              type: string
                              description: '**Returned value:** Counter ID.'
                            type:
                              type: string
                              description: '**Returned value:** Type.'
                            type_val:
                              type: string
                              description: Raw value of `type`. The `type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            hold_ipo:
                              type: string
                              description: '**Returned value:** Support IPO Portfolio Margin.'
                            hold_ipo_val:
                              type: string
                              description: Raw value of `hold_ipo`. The `hold_ipo` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            borrow_enable:
                              type: string
                              description: '**Returned value:** Support to use other currency for IPO.'
                            borrow_enable_val:
                              type: string
                              description: Raw value of `borrow_enable`. The `borrow_enable` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            floating_ratio:
                              type: string
                              description: '**Returned value:** Margin Coefficient.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - counter_id: ST/HK/3388
                      type: Subscription
                      type_val: '0'
                      hold_ipo: 'NO'
                      hold_ipo_val: 'false'
                      borrow_enable: 'NO'
                      borrow_enable_val: 'false'
                      floating_ratio: '0.00'
                      updated_at: '1779793854'
                  total: 67
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/ipo_buy_powers/download
        operationId: ipo_buy_powers_download
  /v1/datasets/default_rate/download:
    post:
      operationId: default_rate_download
      summary: Default Rate Info Export
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Export default rate information records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of default_rate.yaml (the query capability).
                  properties:
                    name:
                      type: string
                      description: Filters results by Name.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - name
                      - currency
                      - buy_fee
                      - financing_rate
                      - rate_day
                      - sfc_transaction_levy
                      - transaction_fee
                      - transaction_levy
                      - commission
                      - created_at
                      - state
                      - state_val
                    example: name
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Default Rate Info Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/default_rate
        operationId: default_rate
  /v1/datasets/default_rate:
    post:
      operationId: default_rate
      summary: Default Rate Info
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Query default rate information records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    name:
                      type: string
                      description: Filters results by Name.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Default Rate Info records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              description: '**Returned value:** Name.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            buy_fee:
                              type: string
                              description: '**Returned value:** IPO Handling Fees.'
                            financing_rate:
                              type: string
                              description: '**Returned value:** Annual Financing Rate.'
                            rate_day:
                              type: string
                              description: '**Returned value:** Rate Day.'
                            sfc_transaction_levy:
                              type: string
                              description: '**Returned value:** SFC Transaction Levy (%).'
                            transaction_fee:
                              type: string
                              description: '**Returned value:** Transaction Fee (%).'
                            transaction_levy:
                              type: string
                              description: '**Returned value:** Transaction Levy (%).'
                            commission:
                              type: string
                              description: '**Returned value:** Commission (%).'
                            created_at:
                              type: string
                              description: '**Returned value:** CreatedAt. The date or time format follows the value returned by the API.'
                            state:
                              type: string
                              description: '**Returned value:** State.'
                            state_val:
                              type: string
                              description: Raw value of `state`. The `state` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - name: string
                      currency: string
                      buy_fee: string
                      financing_rate: string
                      rate_day: string
                      sfc_transaction_levy: string
                      transaction_fee: string
                      transaction_levy: string
                      commission: string
                      created_at: string
                      state: string
                      state_val: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/default_rate/download
        operationId: default_rate_download
  /v1/datasets/ipo_financing_rates/download:
    post:
      operationId: ipo_financing_rates_download
      summary: IPO Commission Rate List Export
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Export IPO commission rates records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of ipo_financing_rates.yaml (the query capability).
                  properties:
                    name:
                      type: string
                      description: Filters results by Name.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - name
                      - currency
                      - buy_fee
                      - financing_rate
                      - rate_day_count
                      - sfc_transaction_levy
                      - transaction_fee
                      - transaction_levy
                      - commission
                      - created_at
                      - state
                      - state_val
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the IPO Commission Rate List Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/ipo_financing_rates
        operationId: ipo_financing_rates
  /v1/datasets/ipo_financing_rates:
    post:
      operationId: ipo_financing_rates
      summary: IPO Commission Rate List
      tags:
        - Service Parameter
      x-permission-key:
        - ipo.financing_rate
      x-lbonly: false
      description: |-
        Query IPO commission rates records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    name:
                      type: string
                      description: Filters results by Name.
                      example: IPO演示费率
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated IPO Commission Rate List records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            name:
                              type: string
                              description: '**Returned value:** Billing Name.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            buy_fee:
                              type: string
                              description: '**Returned value:** IPO Handling Fee.'
                            financing_rate:
                              type: string
                              description: '**Returned value:** Annual Financing Rate.'
                            rate_day_count:
                              type: string
                              description: '**Returned value:** Rate Day.'
                            sfc_transaction_levy:
                              type: string
                              description: '**Returned value:** SFC Transaction Levy (%).'
                            transaction_fee:
                              type: string
                              description: '**Returned value:** Transaction Fee (%).'
                            transaction_levy:
                              type: string
                              description: '**Returned value:** Transaction Levy (%).'
                            commission:
                              type: string
                              description: '**Returned value:** Commission (%).'
                            created_at:
                              type: string
                              description: '**Returned value:** CreatedAt. The date or time format follows the value returned by the API.'
                            state:
                              type: string
                              description: '**Returned value:** Status.'
                            state_val:
                              type: string
                              description: Raw value of `state`. The `state` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '20759'
                      name: IPO演示费率
                      currency: HKD
                      buy_fee: '49.00'
                      financing_rate: '6.50'
                      rate_day_count: '365'
                      sfc_transaction_levy: '0.16120'
                      transaction_fee: '0.11057'
                      transaction_levy: '0.11002'
                      commission: '1.00000'
                      created_at: '1758859690'
                      state: Started
                      state_val: '1'
                  total: 85
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/ipo_financing_rates/download
        operationId: ipo_financing_rates_download
  /v1/datasets/clearing_security_lending_markets/download:
    post:
      operationId: clearing_security_lending_markets_download
      summary: System Config - Securities Lending Management Export
      tags:
        - Service Parameter
      x-permission-key:
        - billing.short_selling_configure
      x-lbonly: false
      description: |-
        Export securities lending management configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of clearing_security_lending_markets.yaml (the query capability).
                  properties:
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - market
                      - operator
                      - updated_at
                    example: market
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the System Config - Securities Lending Management Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/clearing_security_lending_markets
        operationId: clearing_security_lending_markets
  /v1/datasets/clearing_security_lending_markets:
    post:
      operationId: clearing_security_lending_markets
      summary: System Config - Securities Lending Management
      tags:
        - Service Parameter
      x-permission-key:
        - billing.short_selling_configure
      x-lbonly: false
      description: |-
        Query securities lending management configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated System Config - Securities Lending Management records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** Records matching the query conditions.'
                        items:
                          type: object
                          properties:
                            market:
                              type: string
                              description: '**Returned value:** Market.'
                            operator:
                              type: string
                              description: '**Returned value:** Operator.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - market: HK
                      operator: user@example.com
                      updated_at: '2026-06-23 19:07:42'
                  total: 2
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/clearing_security_lending_markets/download
        operationId: clearing_security_lending_markets_download
  /v1/datasets/risk_financing_interest_allocations/download:
    post:
      operationId: risk_financing_interest_allocations_download
      summary: Financing Interest Sharing Config Export
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Export margin financing interest revenue-sharing configuration records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of risk_financing_interest_allocations.yaml (the query capability).
                  properties:
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        **Allowed values:** `USD`, `HKD`, `SGD`, `CNY`, `AUD`, `JPY`, `GBP`, `EUR`, `CAD`, `MYR`, `AMD`, `BGN`, `BTN`, `BWP`, `BZD`, `CDF`, `CHF`, `DKK`, `GHS`, `GMD`, `GNF`, `HRK`, `HUF`, `ILS`, `INR`, `IQD`, `IRR`, `ISK`, `LRD`, `LYD`, `MOP`, `RUB`, `UAH`, `UGX`, `UYI`, `UYU`, `UZS`, `XAF`, `XOF`, `XSU`, `YER`, `IDR`, `TWD`, `THB`, `KRW`, `NZD`
                      enum:
                        - USD
                        - HKD
                        - SGD
                        - CNY
                        - AUD
                        - JPY
                        - GBP
                        - EUR
                        - CAD
                        - MYR
                        - AMD
                        - BGN
                        - BTN
                        - BWP
                        - BZD
                        - CDF
                        - CHF
                        - DKK
                        - GHS
                        - GMD
                        - GNF
                        - HRK
                        - HUF
                        - ILS
                        - INR
                        - IQD
                        - IRR
                        - ISK
                        - LRD
                        - LYD
                        - MOP
                        - RUB
                        - UAH
                        - UGX
                        - UYI
                        - UYU
                        - UZS
                        - XAF
                        - XOF
                        - XSU
                        - YER
                        - IDR
                        - TWD
                        - THB
                        - KRW
                        - NZD
                      x-enum-details:
                        - value: USD
                          label: USD
                        - value: HKD
                          label: HKD
                        - value: SGD
                          label: SGD
                        - value: CNY
                          label: CNY
                        - value: AUD
                          label: AUD
                        - value: JPY
                          label: JPY
                        - value: GBP
                          label: GBP
                        - value: EUR
                          label: EUR
                        - value: CAD
                          label: CAD
                        - value: MYR
                          label: MYR
                        - value: AMD
                          label: AMD
                        - value: BGN
                          label: BGN
                        - value: BTN
                          label: BTN
                        - value: BWP
                          label: BWP
                        - value: BZD
                          label: BZD
                        - value: CDF
                          label: CDF
                        - value: CHF
                          label: CHF
                        - value: DKK
                          label: DKK
                        - value: GHS
                          label: GHS
                        - value: GMD
                          label: GMD
                        - value: GNF
                          label: GNF
                        - value: HRK
                          label: HRK
                        - value: HUF
                          label: HUF
                        - value: ILS
                          label: ILS
                        - value: INR
                          label: INR
                        - value: IQD
                          label: IQD
                        - value: IRR
                          label: IRR
                        - value: ISK
                          label: ISK
                        - value: LRD
                          label: LRD
                        - value: LYD
                          label: LYD
                        - value: MOP
                          label: MOP
                        - value: RUB
                          label: RUB
                        - value: UAH
                          label: UAH
                        - value: UGX
                          label: UGX
                        - value: UYI
                          label: UYI
                        - value: UYU
                          label: UYU
                        - value: UZS
                          label: UZS
                        - value: XAF
                          label: XAF
                        - value: XOF
                          label: XOF
                        - value: XSU
                          label: XSU
                        - value: YER
                          label: YER
                        - value: IDR
                          label: IDR
                        - value: TWD
                          label: TWD
                        - value: THB
                          label: THB
                        - value: KRW
                          label: KRW
                        - value: NZD
                          label: NZD
                      example: USD
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - currency
                      - currency_val
                      - standard_financing_rate
                      - min_interest_spread
                      - year_fee_days
                      - year_fee_days_val
                      - created_at
                      - updated_at
                      - config_valid_period
                      - rounding_method
                      - rounding_method_val
                      - rounding_decimals
                      - rounding_decimals_val
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Financing Interest Sharing Config Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/risk_financing_interest_allocations
        operationId: risk_financing_interest_allocations
  /v1/datasets/risk_financing_interest_allocations:
    post:
      operationId: risk_financing_interest_allocations
      summary: Financing Interest Sharing Config
      tags:
        - Service Parameter
      x-permission-key:
        - settlement.financing_interest_allocation_config
      x-lbonly: false
      description: |-
        Query margin financing interest revenue-sharing configuration records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        **Allowed values:** `USD`, `HKD`, `SGD`, `CNY`, `AUD`, `JPY`, `GBP`, `EUR`, `CAD`, `MYR`, `AMD`, `BGN`, `BTN`, `BWP`, `BZD`, `CDF`, `CHF`, `DKK`, `GHS`, `GMD`, `GNF`, `HRK`, `HUF`, `ILS`, `INR`, `IQD`, `IRR`, `ISK`, `LRD`, `LYD`, `MOP`, `RUB`, `UAH`, `UGX`, `UYI`, `UYU`, `UZS`, `XAF`, `XOF`, `XSU`, `YER`, `IDR`, `TWD`, `THB`, `KRW`, `NZD`
                      enum:
                        - USD
                        - HKD
                        - SGD
                        - CNY
                        - AUD
                        - JPY
                        - GBP
                        - EUR
                        - CAD
                        - MYR
                        - AMD
                        - BGN
                        - BTN
                        - BWP
                        - BZD
                        - CDF
                        - CHF
                        - DKK
                        - GHS
                        - GMD
                        - GNF
                        - HRK
                        - HUF
                        - ILS
                        - INR
                        - IQD
                        - IRR
                        - ISK
                        - LRD
                        - LYD
                        - MOP
                        - RUB
                        - UAH
                        - UGX
                        - UYI
                        - UYU
                        - UZS
                        - XAF
                        - XOF
                        - XSU
                        - YER
                        - IDR
                        - TWD
                        - THB
                        - KRW
                        - NZD
                      x-enum-details:
                        - value: USD
                          label: USD
                        - value: HKD
                          label: HKD
                        - value: SGD
                          label: SGD
                        - value: CNY
                          label: CNY
                        - value: AUD
                          label: AUD
                        - value: JPY
                          label: JPY
                        - value: GBP
                          label: GBP
                        - value: EUR
                          label: EUR
                        - value: CAD
                          label: CAD
                        - value: MYR
                          label: MYR
                        - value: AMD
                          label: AMD
                        - value: BGN
                          label: BGN
                        - value: BTN
                          label: BTN
                        - value: BWP
                          label: BWP
                        - value: BZD
                          label: BZD
                        - value: CDF
                          label: CDF
                        - value: CHF
                          label: CHF
                        - value: DKK
                          label: DKK
                        - value: GHS
                          label: GHS
                        - value: GMD
                          label: GMD
                        - value: GNF
                          label: GNF
                        - value: HRK
                          label: HRK
                        - value: HUF
                          label: HUF
                        - value: ILS
                          label: ILS
                        - value: INR
                          label: INR
                        - value: IQD
                          label: IQD
                        - value: IRR
                          label: IRR
                        - value: ISK
                          label: ISK
                        - value: LRD
                          label: LRD
                        - value: LYD
                          label: LYD
                        - value: MOP
                          label: MOP
                        - value: RUB
                          label: RUB
                        - value: UAH
                          label: UAH
                        - value: UGX
                          label: UGX
                        - value: UYI
                          label: UYI
                        - value: UYU
                          label: UYU
                        - value: UZS
                          label: UZS
                        - value: XAF
                          label: XAF
                        - value: XOF
                          label: XOF
                        - value: XSU
                          label: XSU
                        - value: YER
                          label: YER
                        - value: IDR
                          label: IDR
                        - value: TWD
                          label: TWD
                        - value: THB
                          label: THB
                        - value: KRW
                          label: KRW
                        - value: NZD
                          label: NZD
                      example: AUD
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Financing Interest Sharing Config records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            standard_financing_rate:
                              type: string
                              description: '**Returned value:** Standard financing interest rate.'
                            min_interest_spread:
                              type: string
                              description: '**Returned value:** Bottom line interest margin.'
                            year_fee_days:
                              type: string
                              description: '**Returned value:** Annual fee rate days.'
                            year_fee_days_val:
                              type: string
                              description: Raw value of `year_fee_days`. The `year_fee_days` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                            config_valid_period:
                              type: string
                              description: '**Returned value:** Configuration validity period.'
                            rounding_method:
                              type: string
                              description: '**Returned value:** Extraction method.'
                            rounding_method_val:
                              type: string
                              description: Raw value of `rounding_method`. The `rounding_method` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            rounding_decimals:
                              type: string
                              description: '**Returned value:** Cut the number of digits.'
                            rounding_decimals_val:
                              type: string
                              description: Raw value of `rounding_decimals`. The `rounding_decimals` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '10199'
                      currency: AUD
                      currency_val: AUD
                      standard_financing_rate: '3.3000000000'
                      min_interest_spread: '4.5000000000'
                      year_fee_days: '360'
                      year_fee_days_val: '360'
                      created_at: '1728874931'
                      updated_at: '1728877941'
                      config_valid_period: 202404-202404
                      rounding_method: Round
                      rounding_method_val: '1'
                      rounding_decimals: '0'
                      rounding_decimals_val: '0'
                  total: 7
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/risk_financing_interest_allocations/download
        operationId: risk_financing_interest_allocations_download
  /v1/datasets/booking_booking_types/download:
    post:
      operationId: booking_booking_types_download
      summary: Business type Export
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Export business types records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_booking_types.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by No.
                    source_id:
                      type: string
                      description: |-
                        Filters results by Entry Data Source.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_data_source_filterable` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_data_source_filterable
                    code:
                      type: string
                      description: |-
                        Filters results by Business Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_type_bilateral_member_mapping` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_type_bilateral_member_mapping
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - source_id
                      - source_id_val
                      - direction
                      - direction_val
                      - code_desc
                      - is_account
                      - is_account_val
                      - account_currencies
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Business type Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_booking_types
        operationId: booking_booking_types
  /v1/datasets/booking_booking_types:
    post:
      operationId: booking_booking_types
      summary: Business type
      tags:
        - Service Parameter
      x-lbonly: false
      description: |-
        Query business types records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by No.
                      example: '124006'
                    source_id:
                      type: string
                      description: |-
                        Filters results by Entry Data Source.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_data_source_filterable` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_data_source_filterable
                      example: Payment Transaction
                    code:
                      type: string
                      description: |-
                        Filters results by Business Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_type_bilateral_member_mapping` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_type_bilateral_member_mapping
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Business type records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            source_id:
                              type: string
                              description: '**Returned value:** Entry Data Source.'
                            source_id_val:
                              type: string
                              description: Raw value of `source_id`. The `source_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            direction:
                              type: string
                              description: '**Returned value:** Payment/Collecting.'
                            direction_val:
                              type: string
                              description: Raw value of `direction`. The `direction` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            code_desc:
                              type: string
                              description: '**Returned value:** Business type.'
                            is_account:
                              type: string
                              description: '**Returned value:** Indicates whether accounting requirements.'
                            is_account_val:
                              type: string
                              description: Raw value of `is_account`. The `is_account` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            account_currencies:
                              type: string
                              description: '**Returned value:** Currency.'
                            created_at:
                              type: string
                              description: '**Returned value:** Creation time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '124006'
                      source_id: Payment Transaction
                      source_id_val: '1'
                      direction: Payment
                      direction_val: Payment
                      code_desc: '[LCDP]Deposit Cash'
                      is_account: Charge up
                      is_account_val: Y
                      account_currencies: '["ALL"]'
                      created_at: '1783296446'
                      updated_at: '1783296446'
                  total: 353
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_booking_types/download
        operationId: booking_booking_types_download
  /v1/datasets/booking_account_items/download:
    post:
      operationId: booking_account_items_download
      summary: Chart of Accounts Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_items_query
      x-lbonly: false
      description: |-
        Export accounting subjects records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_account_items.yaml (the query capability).
                  properties:
                    out_subject_code:
                      type: string
                    subject_code:
                      type: string
                    id:
                      type: string
                    feature:
                      type: string
                      description: Values come from the `pg_book_account_features` dynamic enumeration and are not fixed.; queryable via GET /v1/datasets/refs/book_account_features
                    level:
                      type: string
                      enum:
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '1'
                          label: Level 1
                        - value: '2'
                          label: Level 2
                        - value: '3'
                          label: Level 3
                      example: '1'
                    full_name:
                      type: string
                    direct:
                      type: string
                      x-enum-details:
                        - value: '1'
                          label: Debit
                        - value: '2'
                          label: Credit
                      enum: &id007
                        - '1'
                        - '2'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - account_channel
                      - feature
                      - feature_val
                      - level
                      - level_val
                      - subject_code
                      - out_subject_code
                      - full_name
                      - direct
                      - direct_val
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Chart of Accounts Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_account_items
        operationId: booking_account_items
  /v1/datasets/booking_account_items:
    post:
      operationId: booking_account_items
      summary: Chart of Accounts
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_items_query
      x-lbonly: false
      description: |-
        Query accounting subjects records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  properties:
                    out_subject_code:
                      type: string
                    subject_code:
                      type: string
                    id:
                      type: string
                    feature:
                      type: string
                      description: Values come from the `pg_book_account_features` dynamic enumeration and are not fixed.; queryable via GET /v1/datasets/refs/book_account_features
                    level:
                      type: string
                      enum:
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '1'
                          label: Level 1
                        - value: '2'
                          label: Level 2
                        - value: '3'
                          label: Level 3
                      example: '1'
                    full_name:
                      type: string
                    direct:
                      type: string
                      x-enum-details:
                        - value: '1'
                          label: Debit
                        - value: '2'
                          label: Credit
                      enum: *id007
                page:
                  type: integer
                page_size:
                  type: integer
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Chart of Accounts records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                            account_channel:
                              type: string
                            feature:
                              type: string
                            feature_val:
                              type: string
                              description: 'Raw value behind field `feature`: `feature` returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.'
                            level:
                              type: string
                            level_val:
                              type: string
                              description: 'Raw value behind field `level`: `level` returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.'
                            subject_code:
                              type: string
                            out_subject_code:
                              type: string
                            full_name:
                              type: string
                            direct:
                              type: string
                            direct_val:
                              type: string
                              description: 'Raw value behind field `direct`: `direct` returns the human-readable display value resolved via a dictionary/join mapping, while this field returns the underlying code/ID before that mapping.'
                            updated_at:
                              type: string
                      total:
                        type: integer
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      account_channel: string
                      feature: string
                      feature_val: string
                      level: string
                      level_val: string
                      subject_code: string
                      out_subject_code: string
                      full_name: string
                      direct: string
                      direct_val: string
                      updated_at: string
                  total: 1
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_account_items/download
        operationId: booking_account_items_download
  /v1/datasets/booking_account_entries/download:
    post:
      operationId: booking_account_entries_download
      summary: Rules Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_entries_query
      x-lbonly: false
      description: |-
        Export journal entry rules records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_account_entries.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by Entry Rule ID.
                    source_id:
                      type: string
                      description: |-
                        Filters results by Entry Data Source.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_data_source` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_data_source
                    payment_account_type_id:
                      type: string
                      description: |-
                        Filters results by Payment Account Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_account_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_account_type
                    payment_booking_type_id:
                      type: string
                      description: |-
                        Filters results by Payment business type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_business_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_business_type
                    payee_account_type_id:
                      type: string
                      description: |-
                        Filters results by Collecting Account Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_account_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_account_type
                    payee_booking_type_id:
                      type: string
                      description: |-
                        Filters results by Payee business type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_business_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_business_type
                    name_types:
                      type: string
                      description: Filters results by Entry Summary.
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    items_debit_query:
                      type: string
                      description: |-
                        Filters results by Items Debit Query.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_debit_items` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_debit_items
                    items_credit_query:
                      type: string
                      description: |-
                        Filters results by Items Credit Query.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_credit_items` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_credit_items
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - source_id
                      - source_id_val
                      - payment_account_type_id
                      - payment_account_type_id_val
                      - payment_booking_type_id
                      - payment_booking_type_id_val
                      - payment_channel_account
                      - payee_account_type_id
                      - payee_account_type_id_val
                      - payee_booking_type_id
                      - payee_booking_type_id_val
                      - payee_channel_account
                      - channel
                      - user_account_type
                      - user_account_type_val
                      - currency
                      - currency_val
                      - org_id
                      - org_id_val
                      - trade_direction
                      - trade_direction_val
                      - nature_type
                      - nature_type_val
                      - bank_account
                      - bank_account_val
                      - remark
                      - amount_plus_or_minus
                      - amount_plus_or_minus_val
                      - name_types
                      - sss
                      - debit_level_full_name
                      - credit_level_full_name
                      - debit_level_custom_name
                      - credit_level_custom_name
                      - debit_display_broker
                      - debit_display_broker_val
                      - credit_display_broker
                      - credit_display_broker_val
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Rules Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_account_entries
        operationId: booking_account_entries
  /v1/datasets/booking_account_entries:
    post:
      operationId: booking_account_entries
      summary: Rules
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_entries_query
      x-lbonly: false
      description: |-
        Query journal entry rules records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by Entry Rule ID.
                    source_id:
                      type: string
                      description: |-
                        Filters results by Entry Data Source.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_data_source` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_data_source
                    payment_account_type_id:
                      type: string
                      description: |-
                        Filters results by Payment Account Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_account_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_account_type
                    payment_booking_type_id:
                      type: string
                      description: |-
                        Filters results by Payment business type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_business_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_business_type
                    payee_account_type_id:
                      type: string
                      description: |-
                        Filters results by Collecting Account Type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_account_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_account_type
                    payee_booking_type_id:
                      type: string
                      description: |-
                        Filters results by Payee business type.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_business_type` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_business_type
                    name_types:
                      type: string
                      description: Filters results by Entry Summary.
                    currency:
                      type: string
                      description: |-
                        Filters results by Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies_by_channel` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies_by_channel
                    items_debit_query:
                      type: string
                      description: |-
                        Filters results by Items Debit Query.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_debit_items` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_debit_items
                    items_credit_query:
                      type: string
                      description: |-
                        Filters results by Items Credit Query.

                        Values are dynamic. Call `GET /v1/datasets/refs/book_entry_credit_items` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/book_entry_credit_items
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Rules records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            source_id:
                              type: string
                              description: '**Returned value:** Entry Data Source.'
                            source_id_val:
                              type: string
                              description: Raw value of `source_id`. The `source_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            payment_account_type_id:
                              type: string
                              description: '**Returned value:** Payment Account Type.'
                            payment_account_type_id_val:
                              type: string
                              description: Raw value of `payment_account_type_id`. The `payment_account_type_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            payment_booking_type_id:
                              type: string
                              description: '**Returned value:** Payment Business Type.'
                            payment_booking_type_id_val:
                              type: string
                              description: Raw value of `payment_booking_type_id`. The `payment_booking_type_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            payment_channel_account:
                              type: string
                              description: '**Returned value:** Paying Securities Account.'
                            payee_account_type_id:
                              type: string
                              description: '**Returned value:** Collecting Account Type.'
                            payee_account_type_id_val:
                              type: string
                              description: Raw value of `payee_account_type_id`. The `payee_account_type_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            payee_booking_type_id:
                              type: string
                              description: '**Returned value:** Collecting Business Type.'
                            payee_booking_type_id_val:
                              type: string
                              description: Raw value of `payee_booking_type_id`. The `payee_booking_type_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            payee_channel_account:
                              type: string
                              description: '**Returned value:** Receiving Securities Account.'
                            channel:
                              type: string
                              description: '**Returned value:** Channel.'
                            user_account_type:
                              type: string
                              description: '**Returned value:** User Account Type.'
                            user_account_type_val:
                              type: string
                              description: Raw value of `user_account_type`. The `user_account_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            currency:
                              type: string
                              description: '**Returned value:** Currency.'
                            currency_val:
                              type: string
                              description: Raw value of `currency`. The `currency` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            org_id:
                              type: string
                              description: '**Returned value:** Securities trader.'
                            org_id_val:
                              type: string
                              description: Raw value of `org_id`. The `org_id` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            trade_direction:
                              type: string
                              description: '**Returned value:** Trade direction.'
                            trade_direction_val:
                              type: string
                              description: Raw value of `trade_direction`. The `trade_direction` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            nature_type:
                              type: string
                              description: '**Returned value:** Nature of account.'
                            nature_type_val:
                              type: string
                              description: Raw value of `nature_type`. The `nature_type` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            bank_account:
                              type: string
                              description: '**Returned value:** Bank Account.'
                            bank_account_val:
                              type: string
                              description: Raw value of `bank_account`. The `bank_account` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            remark:
                              type: string
                              description: '**Returned value:** Remark.'
                            amount_plus_or_minus:
                              type: string
                              description: '**Returned value:** Amount plus or minus.'
                            amount_plus_or_minus_val:
                              type: string
                              description: Raw value of `amount_plus_or_minus`. The `amount_plus_or_minus` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            name_types:
                              type: string
                              description: '**Returned value:** Entry Summary.'
                            sss:
                              type: string
                              description: '**Returned value:** Entry Summary.'
                            debit_level_full_name:
                              type: string
                              description: '**Returned value:** Debit Account Name (Level 3).'
                            credit_level_full_name:
                              type: string
                              description: '**Returned value:** Credit Account Name (Level 3).'
                            debit_level_custom_name:
                              type: string
                              description: '**Returned value:** Debit Account.'
                            credit_level_custom_name:
                              type: string
                              description: '**Returned value:** Credit Account.'
                            debit_display_broker:
                              type: string
                              description: '**Returned value:** Debit AE code.'
                            debit_display_broker_val:
                              type: string
                              description: Raw value of `debit_display_broker`. The `debit_display_broker` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            credit_display_broker:
                              type: string
                              description: '**Returned value:** Credit AE code.'
                            credit_display_broker_val:
                              type: string
                              description: Raw value of `credit_display_broker`. The `credit_display_broker` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      source_id: string
                      source_id_val: string
                      payment_account_type_id: string
                      payment_account_type_id_val: string
                      payment_booking_type_id: string
                      payment_booking_type_id_val: string
                      payment_channel_account: string
                      payee_account_type_id: string
                      payee_account_type_id_val: string
                      payee_booking_type_id: string
                      payee_booking_type_id_val: string
                      payee_channel_account: string
                      channel: string
                      user_account_type: string
                      user_account_type_val: string
                      currency: string
                      currency_val: string
                      org_id: string
                      org_id_val: string
                      trade_direction: string
                      trade_direction_val: string
                      nature_type: string
                      nature_type_val: string
                      bank_account: string
                      bank_account_val: string
                      remark: string
                      amount_plus_or_minus: string
                      amount_plus_or_minus_val: string
                      name_types: string
                      sss: string
                      debit_level_full_name: string
                      credit_level_full_name: string
                      debit_level_custom_name: string
                      credit_level_custom_name: string
                      debit_display_broker: string
                      debit_display_broker_val: string
                      credit_display_broker: string
                      credit_display_broker_val: string
                      updated_at: string
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_account_entries/download
        operationId: booking_account_entries_download
  /v1/datasets/booking_account_merge_rules/download:
    post:
      operationId: booking_account_merge_rules_download
      summary: Accounting Merge Rule Export
      tags:
        - Service Parameter
      x-permission-key:
        - book.account_merge_rule_query
      x-lbonly: false
      description: |-
        Export merge rules records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_account_merge_rules.yaml (the query capability).
                  properties:
                    id:
                      type: string
                      description: Filters results by No.
                    original_currency:
                      type: string
                      description: |-
                        Filters results by Original currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies
                    convert_currency:
                      type: string
                      description: |-
                        Filters results by Convert Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies
                    business_name:
                      type: string
                      description: Filters results by Consolidated business name.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - business_name
                      - original_currency
                      - convert_currency
                      - payment_merge_booking_type
                      - payee_merge_booking_type
                      - account_merge_rule_infos
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Accounting Merge Rule Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_account_merge_rules
        operationId: booking_account_merge_rules
  /v1/datasets/booking_account_merge_rules:
    post:
      operationId: booking_account_merge_rules
      summary: Accounting Merge Rule
      tags:
        - Service Parameter
      x-permission-key:
        - book.account_merge_rule_query
      x-lbonly: false
      description: |-
        Query merge rules records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    id:
                      type: string
                      description: Filters results by No.
                    original_currency:
                      type: string
                      description: |-
                        Filters results by Original currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies
                    convert_currency:
                      type: string
                      description: |-
                        Filters results by Convert Currency.

                        Values are dynamic. Call `GET /v1/datasets/refs/tenant_currencies` to retrieve the current data dictionary, and use the returned entry's `key` as this parameter's value.
                      x-value-source:
                        endpoint: GET /v1/datasets/refs/tenant_currencies
                    business_name:
                      type: string
                      description: Filters results by Consolidated business name.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Accounting Merge Rule records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** Records matching the query conditions.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            business_name:
                              type: string
                              description: '**Returned value:** Consolidated business name.'
                            original_currency:
                              type: string
                              description: '**Returned value:** Original Currency.'
                            convert_currency:
                              type: string
                              description: '**Returned value:** Convert Currency.'
                            payment_merge_booking_type:
                              type: string
                              description: '**Returned value:** Combined Payment Business Types.'
                            payee_merge_booking_type:
                              type: string
                              description: '**Returned value:** Combined collection business type.'
                            account_merge_rule_infos:
                              type: string
                              description: '**Returned value:** Merge Mode.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Update time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      business_name: string
                      original_currency: string
                      convert_currency: string
                      payment_merge_booking_type: string
                      payee_merge_booking_type: string
                      account_merge_rule_infos: string
                      created_at: string
                      updated_at: string
                  total: 0
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_account_merge_rules/download
        operationId: booking_account_merge_rules_download
  /v1/datasets/booking_account_features/download:
    post:
      operationId: booking_account_features_download
      summary: Account Attributes Export
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_features_query
      x-lbonly: false
      description: |-
        Export accounting subject attributes records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_account_features.yaml (the query capability).
                  properties:
                    direct:
                      type: string
                      description: Filters results by Balance Side.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '1'
                          label: Debit
                        - value: '2'
                          label: Credit
                        - value: '3'
                          label: Common
                      example: '1'
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - account_channel
                      - name
                      - direct
                      - direct_val
                      - prefix_code
                      - submit_name
                      - created_at
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Account Attributes Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_account_features
        operationId: booking_account_features
  /v1/datasets/booking_account_features:
    post:
      operationId: booking_account_features
      summary: Account Attributes
      tags:
        - Service Parameter
      x-permission-key:
        - lb_hk_book.book_features_query
      x-lbonly: false
      description: |-
        Query accounting subject attributes records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    direct:
                      type: string
                      description: Filters results by Balance Side.
                      enum:
                        - '1'
                        - '2'
                        - '3'
                      x-enum-details:
                        - value: '1'
                          label: Debit
                        - value: '2'
                          label: Credit
                        - value: '3'
                          label: Common
                      example: Debit
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Account Attributes records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** List of list.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            account_channel:
                              type: string
                              description: '**Returned value:** Account Channel.'
                            name:
                              type: string
                              description: '**Returned value:** Account Attribute.'
                            direct:
                              type: string
                              description: '**Returned value:** Balance Side.'
                            direct_val:
                              type: string
                              description: Raw value of `direct`. The `direct` field contains the display value resolved through a dictionary or related record; this field contains the underlying code or ID.
                            prefix_code:
                              type: string
                              description: '**Returned value:** Account Code Serial No. (Level 1).'
                            submit_name:
                              type: string
                              description: '**Returned value:** Last edited by.'
                            created_at:
                              type: string
                              description: '**Returned value:** Created Time. The date or time format follows the value returned by the API.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '6'
                      account_channel: longport_test
                      name: Expense
                      direct: Debit
                      direct_val: '1'
                      prefix_code: '6'
                      submit_name: operator-cs
                      created_at: '1747824442'
                      updated_at: '1747824442'
                  total: 6
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_account_features/download
        operationId: booking_account_features_download
  /v1/datasets/booking_fund_types/download:
    post:
      operationId: booking_fund_types_download
      summary: Cashflow Types Export
      tags:
        - Service Parameter
      x-permission-key:
        - accounting.fund_type
      x-lbonly: false
      description: |-
        Export fund types records to a file. `filters` determines the export scope, and `down_fields` can restrict the exported columns.

        ### Export modes

        - `mode: 0`: synchronous export; the download URL is returned directly on success.
        - `mode: 1`: asynchronous export; a `file_download_id` is returned first, then call `GET /v1/datasets/download/{id}` to obtain the download URL.
        - Call `DELETE /v1/datasets/download/{id}` to remove an export record when it is no longer needed.

        ### Returns

        On success, returns the export result: the download URL in synchronous mode, or a `file_download_id` in asynchronous mode.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Same fields as the filters of booking_fund_types.yaml (the query capability).
                  properties:
                    updated_at:
                      type: object
                      description: Filters results by Updated Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                    name:
                      type: string
                      description: Filters results by Fund Type Name.
                mode:
                  type: integer
                  default: 0
                  description: Defaults to `0` (synchronous mode) when omitted.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Sync
                    - value: 1
                      label: Async
                  example: 0
                ext:
                  type: string
                  description: Download file format. Defaults to `csv` when omitted.
                  enum:
                    - csv
                    - xlsx
                  x-enum-details:
                    - value: csv
                      label: CSV
                    - value: xlsx
                      label: Excel
                  example: csv
                file_name:
                  type: string
                  description: Export file name. The server generates a name when omitted.
                down_fields:
                  type: array
                  description: |-
                    When omitted, all fields are exported; allowed values are listed under items.enum (the
                    same field list as the query endpoint's data.list.items).
                  items:
                    type: string
                    enum:
                      - id
                      - multi_name
                      - remark
                      - updated_at
                    example: id
                ticket_content:
                  type: string
                  description: Information shown to the approver when download approval is enabled for the template.
                ticket_preview_url:
                  type: string
                  description: Export approval detail URL. Used only when the export requires approval.
                hide:
                  type: integer
                  description: Whether to hide this export from the download-record list.
                  enum:
                    - 0
                    - 1
                  x-enum-details:
                    - value: 0
                      label: Not Hidden
                    - value: 1
                      label: Hidden
                  example: 0
            example:
              filters: {}
              mode: 0
              ext: csv
              file_name: ''
              remark: ''
              down_fields: []
              ticket_content: ''
              ticket_preview_url: ''
              hide: 0
      responses:
        '200':
          description: On success, returns information about the Cashflow Types Export task. Synchronous exports provide a download URL directly; asynchronous exports provide `file_download_id` for subsequent status queries.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      url:
                        type: string
                        description: 'Returned in synchronous mode (`mode: 0`); empty in asynchronous mode.'
                      file_download_id:
                        type: string
                        description: |-
                          Returned in async mode (`mode: 1`); it is the `{id}` in
                          `GET /v1/datasets/download/{id}` / `DELETE /v1/datasets/download/{id}`: GET
                          obtains the final download URL, DELETE deletes the export record.
                      ticket_id:
                        type: string
                        description: '**Returned value:** Approval ticket ID. Returned only when the operation starts an approval workflow.'
              example:
                code: 0
                message: success
                data:
                  url: https://b-api.longbridge.xyz/download/export_result.csv
                  file_download_id: ''
                  ticket_id: ''
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-parent:
        method: GET
        path: /v1/datasets/booking_fund_types
        operationId: booking_fund_types
  /v1/datasets/booking_fund_types:
    post:
      operationId: booking_fund_types
      summary: Cashflow Types
      tags:
        - Service Parameter
      x-permission-key:
        - accounting.fund_type
      x-lbonly: false
      description: |-
        Query fund types records. Combine business filters through `filters`, and control paging with `page` and `page_size`.

        ### Returns

        On success, returns the matching records together with pagination information. See the individual field descriptions for each filter's purpose, allowed values, and dynamic data source.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filters:
                  type: object
                  description: Combines business filters. Omit a filter field to disable that filter.
                  properties:
                    updated_at:
                      type: object
                      description: Filters results by Updated Time.
                      properties:
                        gte:
                          type: string
                          format: date
                          description: Inclusive lower bound of the filter range.
                        lte:
                          type: string
                          format: date
                          description: Inclusive upper bound of the filter range.
                    name:
                      type: string
                      description: Filters results by Fund Type Name.
                page:
                  type: integer
                  description: Page number to retrieve, starting from `1`.
                page_size:
                  type: integer
                  description: Number of records to return per page.
            example:
              filters: {}
              page: 1
              page_size: 20
      responses:
        '200':
          description: On success, returns paginated Cashflow Types records together with pagination metadata such as the page number, page size, and total record count.
          content:
            application/json:
              schema:
                type: object
                required:
                  - code
                  - message
                  - data
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.
                    x-enum-details:
                      - value: 0
                        label: Success
                  message:
                    type: string
                  data:
                    type: object
                    required:
                      - list
                      - total
                    properties:
                      list:
                        type: array
                        description: '**Returned value:** Records matching the query conditions.'
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: '**Returned value:** Unique identifier of the current record.'
                            multi_name:
                              type: string
                              description: '**Returned value:** Fund Type Name.'
                            remark:
                              type: string
                              description: '**Returned value:** Remarks.'
                            updated_at:
                              type: string
                              description: '**Returned value:** Updated Time. The date or time format follows the value returned by the API.'
                      total:
                        type: integer
                        description: '**Returned value:** Total number of matching records, used for pagination.'
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: '110644'
                      multi_name: '{"en":"Transfer to Haskey","zh-CN":"Transfer to Haskey","zh-HK":"Transfer to Haskey"}'
                      remark: ''
                      updated_at: '1770607837'
                  total: 144
        '400':
          description: Bad request. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '401':
          description: The user is not authenticated, or the login session has expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '403':
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
        '408':
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BrokerGatewayErrorEnvelope'
      x-dataset-download:
        method: POST
        path: /v1/datasets/booking_fund_types/download
        operationId: booking_fund_types_download
components:
  schemas:
    BrokerGatewayErrorEnvelope:
      type: object
      description: Error response returned by Broker API Gateway. `code` is the business error code, `message` contains the error details, and `data` is usually empty.
      required:
        - code
        - message
        - data
      properties:
        code:
          type: integer
          description: '`0` indicates success. Any non-zero value is a business error code (`data` is then usually empty). Common codes are listed below; a given endpoint may return a subset. Interpret together with the HTTP status.'
          enum:
            - 0
            - 401010
            - 403000
            - 3001001
            - 3001004
            - 3001006
            - 408
            - 404000
          x-enum-details:
            - value: 0
              label: Success
            - value: 401010
              label: Invalid or expired token
            - value: 403000
              label: Not authorized (see message for the specific permission)
            - value: 3001001
              label: Service busy, retry later
            - value: 3001004
              label: Filter/query format not recognized
            - value: 3001006
              label: Missing required parameter (name in message)
            - value: 408
              label: Request timeout
            - value: 404000
              label: Route not found
        message:
          type: string
        data: {}
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
