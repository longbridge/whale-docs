openapi: 3.1.1
info:
  title: Longport Whale Broker API
  description: Institution-grade server-to-server API for brokers running on Longport Whale.
  version: 2.0.0
servers:
  - url: https://b-api.longbridge.xyz
    description: Test
  - url: https://b-api.lbkrs.com
    description: Production
tags:
  - name: Account Assets
  - name: Cash Management
  - name: Stock Management
  - name: IPO System
  - name: Fund Management
  - name: VA Clearing Management
  - name: Day-End Settlement
  - name: Corporate Action
  - name: Risk Control Compliance
  - name: Reports
  - name: Broker Management
  - name: Accounting
  - name: Trading Panel
  - name: Announcement
  - name: Service Parameter
  - name: Suspicious Incident Monitoring
  - name: Campaigns
  - name: Reward Center
  - name: Misc
paths:
  /v1/whaleapi/fee/estimate:
    post:
      operationId: open_api_fee_estimate
      summary: Fee estimate
      tags:
        - Service Parameter
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                symbol:
                  type: string
                  description: Stock Symbol
                side:
                  type: object
                  properties: {}
                  description: "the operation side: Buy or Sell"
                price:
                  type: string
                  description: Price
                quantity:
                  type: string
                  description: Quantity
                currency:
                  type: string
                  description: Currency
                account_no:
                  type: string
                  description: Account Profile
              required:
                - symbol
                - price
                - quantity
                - currency
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_amount:
                        type: string
                        description: Total Fee
                      bills:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              description: Fee Code
                            name:
                              type: string
                              description: Fee Name
                            amount:
                              type: string
                              description: Fee Amount
                            dep_code:
                              type: string
                              description: Dependent Fee Code
                        description: Detail
                    required:
                      - total_amount
                      - bills
              example:
                code: 0
                message: success
                data:
                  total_amount: string
                  bills:
                    - code: string
                      name: string
                      amount: string
                      dep_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to estimate the transaction fees for a specified securities account order. By providing the stock symbol symbol, trade direction side, price price, quantity quantity, currency currency, and account number account_no, it returns the estimated total fee total_amount and a detailed fee breakdown list bills, helping users understand the cost structure before placing an order.
      x-mint:
        href: /en/api-reference/open_api_fee_estimate
        metadata:
          sidebarTitle: Fee estimate
  /v1/whaleapi/account/trade/auth_pwd:
    post:
      operationId: account_auth
      summary: Trade Password Auth
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      account:
                        type: object
                        properties:
                          aaid:
                            type: string
                            description: Aaid
                          member_id:
                            type: string
                            description: Member Id
                        required:
                          - aaid
                          - member_id
                        description: Account Info
                    required:
                      - account
                    description: Data
              example:
                code: 0
                message: success
                data:
                  account:
                    aaid: string
                    member_id: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to verify whether the trading password of a specified securities account is correct. By providing the account number account_no and the encrypted trading password encrypt_password, it returns the asset account unique identifier aaid and the user account member_id upon successful verification.
      x-mint:
        href: /en/api-reference/account_auth
        metadata:
          sidebarTitle: Trade Password Auth
  /v1/whaleapi/account/trade/reset_pwd:
    post:
      operationId: reset_trade_password
      summary: Trade Password Reset
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
                    description: Data
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to modify or reset the trading password of a specified securities account. By providing the account number account_no and the encrypted new trading password encrypt_password, it completes the update of the trading password.Unlike the trading password set interface, this interface is applicable to scenarios where the account already has a trading password but needs to be changed.
      x-mint:
        href: /en/api-reference/reset_trade_password
        metadata:
          sidebarTitle: Trade Password Reset
  /v1/whaleapi/account/trade/set_pwd:
    post:
      operationId: set_trade_password
      summary: Trade Password Set
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
                    description: Data
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to set the trading password for a specified securities account for the first time. By providing the account number account_no and the encrypted trading password encrypt_password, it completes the initial setup of the trading password.
      x-mint:
        href: /en/api-reference/set_trade_password
        metadata:
          sidebarTitle: Trade Password Set
  /v1/whaleapi/account/auth_login_pwd:
    post:
      operationId: whale_account_no_login_check
      summary: Account No. Login Password Auth
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
                password:
                  type: string
                  description: Encrypt Login Password
              required:
                - account_no
                - password
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      pass:
                        type: string
                        description: Pass or not
                    required:
                      - pass
                    description: Data
              example:
                code: 0
                message: success
                data:
                  pass: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to verify whether the login password of a specified securities account is correct. By providing the account number account_no and the encrypted login password password, it returns the verification result pass (Y for passed, N for failed).
      x-mint:
        href: /en/api-reference/whale_account_no_login_check
        metadata:
          sidebarTitle: Account No. Login Password Auth
  /v3/whaleapi/trade/execution/all:
    get:
      operationId: all_deals_whale
      summary: Trade Detail Inquiry V3
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: start_at
          in: query
          required: false
          schema:
            type: string
          description: Start time, as a Unix timestamp in seconds.
        - name: end_at
          in: query
          required: false
          schema:
            type: string
          description: End time, as a Unix timestamp in seconds.
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Profile
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page number
        - name: order_id
          in: query
          required: false
          schema:
            type: string
          description: Order ID
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      has_more:
                        type: boolean
                        description: Is there more data available
                      trades:
                        type: array
                        items:
                          type: object
                          properties:
                            order_id:
                              type: string
                              description: Order ID
                            trade_id:
                              type: string
                              description: Trade ID
                            quantity:
                              type: string
                              description: Executed Quantity
                            price:
                              type: string
                              description: Executed Price
                            trade_done_at:
                              type: string
                              description: "Executed time, as a Unix timestamp in seconds"
                            symbol:
                              type: string
                              description: Stock Symbol, in ticker.region format
                            side:
                              type: object
                              properties: {}
                              description: side
                          required:
                            - order_id
                            - trade_id
                            - quantity
                            - price
                            - trade_done_at
                            - symbol
                            - side
                        description: trades
                    required:
                      - has_more
                      - trades
              example:
                code: 0
                message: success
                data:
                  has_more: false
                  trades:
                    - order_id: string
                      trade_id: string
                      quantity: string
                      price: string
                      trade_done_at: string
                      symbol: string
                      side: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Trade Detail Inquiry V3
      x-mint:
        href: /en/api-reference/all_deals_whale
        metadata:
          sidebarTitle: Trade Detail Inquiry V3
  /v1/whaleapi/trade/withdraw_user_orders:
    post:
      operationId: batch_withdraw_user_orders_whale
      summary: Withdraw User Orders
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
                market:
                  type: string
                  description: Market
              required:
                - account_no
                - market
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_orders:
                        type: integer
                        description: Total Orders Count
                      submitted_count:
                        type: integer
                        description: Submitted Orders Count
                    required:
                      - total_orders
                      - submitted_count
              example:
                code: 0
                message: success
                data:
                  total_orders: 0
                  submitted_count: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Initiates a bulk cancel operation for all open orders under the specified account. Supports filtering by market; if no market is specified, all markets are included by default. Covers orders in states including pending submission, commissioned, and partially filled, with a maximum of 1,000 orders processed per request. Each order is cancelled independently — a failure on one order does not affect the execution of others. The final cancellation result is subject to upstream feedback.
      x-mint:
        href: /en/api-reference/batch_withdraw_user_orders_whale
        metadata:
          sidebarTitle: Withdraw User Orders
  /v1/whaleapi/trade/estimate/buy_limit:
    get:
      operationId: estimate_available_buy_limit_whale
      summary: Estimated Maximum Purchase Quantity
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: side
          in: query
          required: true
          schema:
            type: string
          description: Side
        - name: order_type
          in: query
          required: true
          schema:
            type: string
          description: Order Type
        - name: price
          in: query
          required: false
          schema:
            type: string
          description: Estimated Order Price
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: Settlement Currency
        - name: order_id
          in: query
          required: false
          schema:
            type: string
          description: Order ID
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      margin_max_qty:
                        type: string
                        description: Maximum Purchase Quantity with Financing
                      cash_max_qty:
                        type: string
                        description: Maximum Purchase Quantity with Cash
                    required:
                      - margin_max_qty
                      - cash_max_qty
              example:
                code: 0
                message: success
                data:
                  margin_max_qty: string
                  cash_max_qty: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to estimate the maximum buyable quantity for a specified stock under the current account funding conditions for a given securities account. By providing the account number account_no, stock symbol symbol, trade direction side, and order type order_type, with optional parameters including estimated order price price, settlement currency currency, and order ID order_id for the order modification scenario, it calculates and returns the maximum buyable quantity for cash accounts cash_max_qty and margin accounts margin_max_qty, taking into account available funds and margin ratios.
      x-mint:
        href: /en/api-reference/estimate_available_buy_limit_whale
        metadata:
          sidebarTitle: Estimated Maximum Purchase Quantity
  /v1/whaleapi/trade/execution/history-v2:
    get:
      operationId: history_deals_v2_whale
      summary: Historical Trade Detail Inquiry V2
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: start_at
          in: query
          required: false
          schema:
            type: string
          description: Start time, as a Unix timestamp in seconds.
        - name: end_at
          in: query
          required: false
          schema:
            type: string
          description: End time, as a Unix timestamp in seconds.
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      has_more:
                        type: boolean
                        description: Is there more data available
                      trades:
                        type: array
                        items:
                          type: object
                          properties:
                            order_id:
                              type: string
                              description: Order ID
                            trade_id:
                              type: string
                              description: Trade ID
                            quantity:
                              type: string
                              description: Executed Quantity
                            price:
                              type: string
                              description: Executed Price
                            trade_done_at:
                              type: string
                              description: "Executed time, as a Unix timestamp in seconds"
                            symbol:
                              type: string
                              description: Stock Symbol, in ticker.region format
                            side:
                              type: object
                              properties: {}
                              description: side
                          required:
                            - order_id
                            - trade_id
                            - quantity
                            - price
                            - trade_done_at
                            - symbol
                            - side
                        description: trades
                    required:
                      - has_more
                      - trades
              example:
                code: 0
                message: success
                data:
                  has_more: false
                  trades:
                    - order_id: string
                      trade_id: string
                      quantity: string
                      price: string
                      trade_done_at: string
                      symbol: string
                      side: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is an upgraded version (V2) of the historical trade detail inquiry, adding the trade direction field side to the response on top of the V1 version, providing more complete trade record information.
      x-mint:
        href: /en/api-reference/history_deals_v2_whale
        metadata:
          sidebarTitle: Historical Trade Detail Inquiry V2
  /v1/whaleapi/trade/order/history:
    get:
      operationId: history_orders_whale
      summary: Historical Order Inquiry
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: status
          in: query
          required: false
          schema:
            type: string
          description: Order Status
        - name: side
          in: query
          required: false
          schema:
            type: string
          description: Side
        - name: market
          in: query
          required: false
          schema:
            type: string
          description: Market
        - name: start_at
          in: query
          required: false
          schema:
            type: string
          description: Start time, as a Unix timestamp in seconds.
        - name: end_at
          in: query
          required: false
          schema:
            type: string
          description: End time, as a Unix timestamp in seconds.
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page number
        - name: size
          in: query
          required: false
          schema:
            type: string
          description: The Number Of Page Size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the historical order records for a specified securities account with pagination support. By providing the account number account_no and optional filter conditions such as stock symbol symbol, order status status, trade direction side, market market, and time range (start_at / end_at), combined with pagination parameters page and size, it returns the matching historical order list and a flag has_more indicating whether more data exists. The returned fields cover complete order information including order ID, order status, stock name, submitted price, submitted quantity, executed price, executed quantity, order type, time-in-force, trigger status, and settlement currency.
      x-mint:
        href: /en/api-reference/history_orders_whale
        metadata:
          sidebarTitle: Historical Order Inquiry
  /v1/whaleapi/trade/order:
    get:
      operationId: order_detail_whale
      summary: Order Details
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: order_id
          in: query
          required: true
          schema:
            type: string
          description: Order ID
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      order_id:
                        type: string
                        description: Order ID
                      status:
                        type: string
                        description: Order Status
                      stock_name:
                        type: string
                        description: Stock Name
                      quantity:
                        type: string
                        description: Submitted Quantity
                      executed_quantity:
                        type: string
                        description: Executed Quantity
                      price:
                        type: string
                        description: Submitted Price
                      executed_price:
                        type: string
                        description: Executed Price
                      submitted_at:
                        type: string
                        description: Submitted Time
                      side:
                        type: string
                        description: Side
                      symbol:
                        type: string
                        description: Stock Symbol
                      order_type:
                        type: string
                        description: Order Type
                      last_done:
                        type: string
                        description: Latest Executed Price
                      trigger_price:
                        type: string
                        description: Trigger Price
                      msg:
                        type: string
                        description: Reject Reason
                      tag:
                        type: string
                        description: Order Tag
                      time_in_force:
                        type: string
                        description: Time in Force
                      expire_date:
                        type: string
                        description: Long-term Order Expiration Time
                      updated_at:
                        type: string
                        description: "Last updated time, as a Unix timestamp in seconds"
                      trigger_at:
                        type: string
                        description: "Conditional order trigger time, as a Unix timestamp in seconds"
                      trailing_amount:
                        type: string
                        description: Order Trailing Amount
                      trailing_percent:
                        type: string
                        description: Order Trailing Percent
                      limit_offset:
                        type: string
                        description: Order Target Price Spread
                      trigger_status:
                        type: string
                        description: Conditional Order Trigger Status
                      currency:
                        type: string
                        description: Settlement Currency
                      outside_rth:
                        type: string
                        description: Outside Regular Trading Hour
                      remark:
                        type: object
                        properties: {}
                        description: Remark
                      free_status:
                        type: number
                        description: Commission-free Status
                      free_amount:
                        type: string
                        description: Commission-free Amount
                      free_currency:
                        type: string
                        description: Commission-free Currency
                      deductions_status:
                        type: number
                        description: Deduction State
                      deductions_amount:
                        type: string
                        description: Deduction Amount
                      deductions_currency:
                        type: string
                        description: Deduction Currency
                      platform_deducted_status:
                        type: number
                        description: Platform Fee Deduction State
                      platform_deducted_amount:
                        type: string
                        description: Platform Fee Deduction Amount
                      platform_deducted_currency:
                        type: string
                        description: Platform Fee Deduction Currency
                      history:
                        type: array
                        items:
                          type: object
                          properties:
                            price:
                              type: string
                              description: Filled Orders Displayed with Executed Price, Expired, Canceled, Rejected Orders Displayed with Submitted Price
                            quantity:
                              type: string
                              description: Filled Orders Displayed with Filled Quantity, Expired, Canceled, Rejected Orders Displayed with Remaining Quantity
                            status:
                              type: string
                              description: Order Status
                            msg:
                              type: string
                              description: Trade or Error Message
                            time:
                              type: string
                              description: Occurrence Time
                          required:
                            - price
                            - quantity
                            - status
                            - msg
                            - time
                        description: Order History Details
                      charge_detail:
                        type: object
                        properties:
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                code:
                                  type: number
                                  description: Fee Category Code
                                name:
                                  type: string
                                  description: Fee Category Name
                                fees:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      code:
                                        type: string
                                        description: Fee Code
                                      name:
                                        type: string
                                        description: Fee Name
                                      amount:
                                        type: string
                                        description: Unit Fee Amount
                                      current:
                                        type: string
                                        description: Fee Currency
                                    required:
                                      - code
                                      - name
                                      - amount
                                      - current
                                  description: Fee Details
                              required:
                                - code
                                - name
                            description: Trading Fee Details
                          total_amount:
                            type: string
                            description: Total Fee Amount
                          currency:
                            type: string
                            description: Settlement Currency
                        required:
                          - total_amount
                          - currency
                        description: Trading Fee
                    required:
                      - order_id
                      - status
                      - stock_name
                      - quantity
                      - executed_quantity
                      - price
                      - executed_price
                      - submitted_at
                      - side
                      - symbol
                      - order_type
                      - last_done
                      - trigger_price
                      - msg
                      - tag
                      - time_in_force
                      - expire_date
                      - updated_at
                      - trigger_at
                      - trailing_amount
                      - trailing_percent
                      - limit_offset
                      - trigger_status
                      - currency
                      - outside_rth
                      - remark
                      - free_status
                      - free_amount
                      - free_currency
                      - deductions_status
                      - deductions_amount
                      - deductions_currency
                      - platform_deducted_status
                      - platform_deducted_amount
                      - platform_deducted_currency
              example:
                code: 0
                message: success
                data:
                  order_id: string
                  status: string
                  stock_name: string
                  quantity: string
                  executed_quantity: string
                  price: string
                  executed_price: string
                  submitted_at: string
                  side: string
                  symbol: string
                  order_type: string
                  last_done: string
                  trigger_price: string
                  msg: string
                  tag: Normal
                  time_in_force: string
                  expire_date: 2023-04-02
                  updated_at: string
                  trigger_at: string
                  trailing_amount: string
                  trailing_percent: string
                  limit_offset: string
                  trigger_status: string
                  currency: string
                  outside_rth: string
                  remark: {}
                  free_status: 0
                  free_amount: string
                  free_currency: string
                  deductions_status: 0
                  deductions_amount: string
                  deductions_currency: string
                  platform_deducted_status: 0
                  platform_deducted_amount: string
                  platform_deducted_currency: string
                  history:
                    - price: "381.0000"
                      quantity: "200"
                      status: ReplacedStatus
                      msg: string
                      time: "1680856746"
                  charge_detail:
                    items:
                      - code: 1
                        name: 收费明细
                        fees:
                          - code: string
                            name: string
                            amount: string
                            current: string
                    total_amount: "0.00"
                    currency: HKD
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: his interface is used to retrieve the complete detail information of a specified order. By providing the account number account_no and order ID order_id, it returns the full order data including stock symbol, stock name, trade direction, order type, submitted price, submitted quantity, executed price, executed quantity, order status, submission time, update time, time-in-force, trigger price, trigger status, settlement currency, outside regular trading hours flag, remarks, commission-free status and amount, deduction status and amount, platform fee deduction information, order history details, and trading fee breakdown.
      x-mint:
        href: /en/api-reference/order_detail_whale
        metadata:
          sidebarTitle: Order Details
    post:
      operationId: submit_whale
      summary: Place Order
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                symbol:
                  type: string
                  description: Stock Symbol, in ticker.region format
                side:
                  type: string
                  description: Side
                order_type:
                  type: string
                  description: Order Type
                submitted_price:
                  type: string
                  description: Submitted Price
                submitted_quantity:
                  type: string
                  description: Submitted Quantity
                time_in_force:
                  type: string
                  description: Time in Force
                expire_date:
                  type: string
                  description: Long-term Order Expiration Time
                outside_rth:
                  type: string
                  description: Outside Regular Trading Hour
                trigger_price:
                  type: string
                  description: Trigger Price
                trailing_amount:
                  type: string
                  description: Trailing Amount
                trailing_percent:
                  type: string
                  description: Trailing Percent
                limit_offset:
                  type: string
                  description: Order Target Price Spread
                remark:
                  type: string
                  description: Remark
                account_no:
                  type: string
                  description: Account Number
                limit_depth_level:
                  type: number
                  description: "Specifies the bid/ask depth level. The range is -5 to 5 (with 0 in the middle); negative values are bid levels and positive values are ask levels."
                monitor_price:
                  type: string
                  description: Monitoring price
                trigger_count:
                  type: number
                  description: Number of triggers
              required:
                - symbol
                - side
                - order_type
                - submitted_quantity
                - time_in_force
                - account_no
                - trigger_count
                - monitor_price
                - limit_depth_level
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      order_id:
                        type: string
                        description: Order ID
                    required:
                      - order_id
              example:
                code: 0
                message: success
                data:
                  order_id: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to submit a stock buy or sell order for a specified securities account. By providing the account number account_no, stock symbol symbol, market market, trade direction side (buy / sell), order type order_type, quantity quantity, price price, and time-in-force time_in_force, it creates an order and returns the order ID order_id. It supports multiple order types including limit orders and market orders, as well as pre/post-market trading flags and remarks.
      x-mint:
        href: /en/api-reference/submit_whale
        metadata:
          sidebarTitle: Place Order
  /v2/whaleapi/trade/order:
    put:
      operationId: replace_whale_v2
      summary: Modify Order
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                order_id:
                  type: string
                  description: Order ID
                price:
                  type: string
                  description: Modified Order Price
                quantity:
                  type: string
                  description: Modified Order Quantity
                trigger_price:
                  type: string
                  description: Trigger Price
                trailing_amount:
                  type: string
                  description: Trailing Amount
                trailing_percent:
                  type: string
                  description: Trailing Percent
                limit_offset:
                  type: string
                  description: Order Target Price Spread
                remark:
                  type: string
                  description: Remark
                account_no:
                  type: string
                  description: Account Number
                limit_depth_level:
                  type: number
                  description: Specifies the bid/ask depth level
                monitor_price:
                  type: string
                  description: Monitoring price
                trigger_count:
                  type: number
                  description: Number of triggers
              required:
                - order_id
                - quantity
                - account_no
                - limit_depth_level
                - monitor_price
                - trigger_count
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to modify an existing unfilled or partially filled order. By providing the account number account_no, order ID order_id, and the fields to be updated (such as quantity quantity, price price, and trigger price trigger_price), it updates the order information. Modification is only supported for orders in a modifiable state (e.g., submitted or pending). Upon successful modification, the order ID remains unchanged.
      x-mint:
        href: /en/api-reference/replace_whale_v2
        metadata:
          sidebarTitle: Modify Order
    delete:
      operationId: withdraw_whale_v2
      summary: Withdraw Order
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to cancel a specified unfilled or partially filled order. By providing the account number account_no and order ID order_id, it submits a cancellation request to the exchange and changes the order status to cancelled. Cancellation is only supported for orders in a CanceledStatus or PartialWithdrawal. Once successfully cancelled, the order will no longer participate in matching.
      x-mint:
        href: /en/api-reference/withdraw_whale_v2
        metadata:
          sidebarTitle: Withdraw Order
  /v1/whaleapi/trade/execution/today:
    get:
      operationId: today_deals_whale
      summary: Today's Trade Detail Inquiry
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: order_id
          in: query
          required: false
          schema:
            type: string
          description: Order ID
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      trades:
                        type: array
                        items:
                          type: object
                          properties:
                            order_id:
                              type: string
                              description: Order ID
                            trade_id:
                              type: string
                              description: Trade ID
                            quantity:
                              type: string
                              description: Executed Quantity
                            price:
                              type: string
                              description: Executed Price
                            trade_done_at:
                              type: string
                              description: "Executed time, as a Unix timestamp in seconds"
                            symbol:
                              type: string
                              description: Stock Symbol
                          required:
                            - order_id
                            - trade_id
                            - quantity
                            - price
                            - trade_done_at
                            - symbol
                        description: trades
              example:
                code: 0
                message: success
                data:
                  trades:
                    - order_id: string
                      trade_id: string
                      quantity: string
                      price: string
                      trade_done_at: string
                      symbol: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the trade execution records for a specified securities account on the current trading day. By providing the account number account_no and optional filter conditions such as stock symbol symbol and order ID order_id, it returns all matching trade execution records for the current day. Each trade record includes order ID order_id, trade ID trade_id, executed quantity quantity, executed price price, execution time trade_done_at, and stock symbol symbol. Compared to the historical trade query interface, this interface only returns today's execution data without requiring a time range.
      x-mint:
        href: /en/api-reference/today_deals_whale
        metadata:
          sidebarTitle: Today's Trade Detail Inquiry
  /v1/whaleapi/trade/order/today:
    get:
      operationId: today_orders_whale
      summary: Today's Order Inquiry
      tags:
        - Trading Panel
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Symbol, in ticker.region format
        - name: status
          in: query
          required: false
          schema:
            type: string
          description: Order Status
        - name: side
          in: query
          required: false
          schema:
            type: string
          description: Side
        - name: market
          in: query
          required: false
          schema:
            type: string
          description: Market
        - name: account_no
          in: query
          required: false
          schema:
            type: string
          description: Account Number
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page number
        - name: size
          in: query
          required: false
          schema:
            type: string
          description: The Number Of Page Size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the order list for a specified securities account on the current trading day. By providing the account number account_no and optional filter conditions such as stock symbol symbol, order status status, trade direction side, and market market, combined with pagination parameters page and size, it returns all matching order records for the current day and a flag has_more indicating whether more data exists. Unlike the historical order query interface, this interface only returns today's order data without requiring a time range parameter, offering faster response and more real-time data.
      x-mint:
        href: /en/api-reference/today_orders_whale
        metadata:
          sidebarTitle: Today's Order Inquiry
  /v1/whaleapi/message/whale/global/push/templates:
    get:
      operationId: get_whale_push_global_templates
      summary: Global Push Templates
      description: |-
        `group_id` values:

        - `2` — Asset Movement
        - `6` — Order Status
        - `7` — Activity Related
        - `8` — Announcement
        - `9` — Account Message
        - `10` — Price Movement
        - `11` — IPO News
        - `12` — Community
        - `15` — College
        - `16` — My News
        - `17` — Today's Highlights
        - `30` — Holdings Daily
        - `31` — Market Data
        - `32` — Option Fluctuation
        - `33` — System Message
        - `34` — Dolphin Views
        - `36` — Recurring Investments
      tags:
        - Announcement
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page number (`page`).
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
          description: Page size (`page_size`).
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total:
                        type: integer
                        description: total
                      templates:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: key
                            name:
                              type: string
                              description: name
                            channel:
                              type: string
                              description: channel
                            group_id:
                              type: integer
                              description: group_id
                            locales:
                              type: array
                              items:
                                type: object
                                properties:
                                  language:
                                    type: string
                                    description: language
                                  title:
                                    type: string
                                    description: title
                                  body:
                                    type: string
                                    description: body
                                  configs:
                                    type: object
                                    properties:
                                      push_config:
                                        type: object
                                        properties:
                                          cover:
                                            type: string
                                            description: cover
                                          link:
                                            type: string
                                            description: link
                                          json:
                                            type: string
                                            description: json
                                          push_device_types:
                                            type: array
                                            items:
                                              type: string
                                            description: push_device_types
                                        required:
                                          - cover
                                          - link
                                          - json
                                          - push_device_types
                                        description: push_config
                                    required:
                                      - push_config
                                    description: configs
                                required:
                                  - language
                                  - title
                                  - body
                                  - configs
                              description: locales
                          required:
                            - key
                            - name
                            - channel
                            - group_id
                            - locales
                        description: templates
                    required:
                      - total
                      - templates
              example:
                code: 0
                message: success
                data:
                  total: 0
                  templates:
                    - key: string
                      name: string
                      channel: string
                      group_id: 0
                      locales:
                        - language: string
                          title: string
                          body: string
                          configs:
                            push_config:
                              cover: string
                              link: string
                              json: string
                              push_device_types:
                                - string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-mint:
        href: /en/api-reference/get_whale_push_global_templates
        metadata:
          sidebarTitle: Global Push Templates
  /v1/whaleapi/message/devices:
    delete:
      operationId: whale_device_logout
      description: |-
        Device Logout.

        - If only `member_id` is provided, all devices under that `member_id` are unbound.
        - If `member_id` and `device_id` are provided, only the corresponding device is unbound.
      summary: Device Logout
      tags:
        - Announcement
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-mint:
        href: /en/api-reference/whale_device_logout
        metadata:
          sidebarTitle: Device Logout
  /v1/whaleapi/reward/send_reward:
    post:
      operationId: exec_send_reward
      description: Grants cash, physical, or voucher rewards through an external interface.
      summary: Send Reward
      tags:
        - Reward Center
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                request_id:
                  type: string
                  description: request_id
                type:
                  type: string
                  description: "Reward type, for example `cash`"
                campaign_code:
                  type: string
                  description: campaign_code
                cash_value:
                  type: object
                  properties:
                    currency:
                      type: string
                      description: "Cash currency"
                    amount:
                      type: integer
                      description: "Cash amount"
                  required:
                    - currency
                    - amount
                  description: "Cash reward value"
                code:
                  type: string
                  description: "Prize code"
                remark:
                  type: string
                  description: remark
              required:
                - request_id
                - type
                - campaign_code
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ref_no:
                        type: string
                        description: ref_no
                      status:
                        type: integer
                        description: status
                    required:
                      - status
                      - ref_no
              example:
                code: 0
                message: success
                data:
                  ref_no: ceeeb96e52b2aa538c97037259c59834
                  status: 12
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-mint:
        href: /en/api-reference/exec_send_reward
  /v1/whaleapi/upload_url:
    get:
      operationId: get_open_upload_url
      summary: Get File Upload URL
      tags:
        - Misc
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: module
          in: query
          required: true
          schema:
            type: string
          description: upload biz module
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    description: url to upload file
                  key:
                    type: string
                    description: file key
              example:
                url: https://xxx.s3.ap-east-1.amazonaws.com/hkcnhkuat/2j5kdS1wx7AI6T5XnaRBof5mkNA?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Credential=AKIA43WDUGYWE42VSCWI%2F20240711%2Fap-east-1%2Fs3%2Faws4_request&X-Amz-Date=20240711T075332Z&X-Amz-Expires=900&X-Amz-SignedHeaders=host&X-Amz-Signature=fc4bc1022177a509db94acac50bfa641dec9384cfbe70d6457ce640fa0d4d805
                key: hkcnhkuat/2j5kdS1wx7AI6T5XnaRBof5mkNA
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Returns a temporary S3 remote upload URL for uploading a file directly to S3.
      x-mint:
        href: /en/api-reference/get_open_upload_url
        metadata:
          sidebarTitle: Get File Upload URL
  /v1/whaleapi/reward/reward_info:
    post:
      operationId: get_reward_info
      description: Queries reward issuance information. Supports batch lookup.
      summary: Query Reward Statuses
      tags:
        - Reward Center
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ref_nos:
                  type: array
                  items:
                    type: string
                    description: "Issuance reference number"
                  description: ref_nos
              required:
                - ref_nos
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      reward_infos:
                        type: array
                        items:
                          type: object
                          properties:
                            ref_no:
                              type: string
                              description: ref_no
                            member_id:
                              type: integer
                              description: member_id
                            status:
                              type: integer
                              description: status
                            sub_status:
                              type: integer
                              description: "Reward sub-status"
                            done_at:
                              type: integer
                              description: done_at
                          required:
                            - ref_no
                            - member_id
                            - status
                        description: reward_infos
                    required:
                      - reward_infos
              example:
                code: 0
                message: success
                data:
                  reward_infos:
                    - ref_no: ceeeb96e52b2aa538c97037259c59834
                      member_id: 52223132
                      status: 0
                      sub_status: 0
                      done_at: 1773281716
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-mint:
        href: /en/api-reference/get_reward_info
  /v1/whaleapi/search/stock:
    post:
      operationId: search_stock
      summary: Search Stock
      tags:
        - Misc
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                keyword:
                  type: string
                  description: keyword
              required:
                - keyword
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      stock_list:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              description: Stock Name
                            symbol:
                              type: string
                              description: "Stock Symbol eg: BABA.US"
                            market:
                              type: string
                              description: "Stock Market eg: US"
                            product:
                              type: string
                              description: "Stock product, for example `ST` or `ETF`"
                            code:
                              type: string
                              description: "Sotck Code eg: BABA"
                            name_cn:
                              type: string
                              description: Stock Chinese Name
                            name_en:
                              type: string
                              description: Stock English Name
                          required:
                            - name
                            - symbol
                            - market
                            - product
                            - code
                            - name_cn
                            - name_en
                        description: stock_list
                    required:
                      - stock_list
              example:
                code: 0
                message: success
                data:
                  stock_list:
                    - name: string
                      symbol: string
                      market: string
                      product: string
                      code: string
                      name_cn: string
                      name_en: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Search Stock
      x-mint:
        href: /en/api-reference/search_stock
        metadata:
          sidebarTitle: Search Stock
  /v1/whaleapi/bind-invite-code:
    post:
      operationId: whale_bind_invite_code
      summary: Bind the invitation code for the specified stage
      tags:
        - Campaigns
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                open_id:
                  type: string
                  description: Open ID
                invite_code:
                  type: string
                  description: Invite Code
                stages:
                  type: array
                  items:
                    type: integer
                    description: 1 REGISTER 2 OPEN 3 DEPOSIT
                  description: Stages
                bound_at:
                  type: integer
                  description: Bound At
              required:
                - open_id
                - invite_code
                - stages
                - bound_at
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                        description: 0 SUCCESS 1 PARTIAL 2 ALL_FAILED
                      results:
                        type: array
                        items:
                          type: object
                          properties:
                            stage:
                              type: integer
                              description: See Request
                            success:
                              type: boolean
                              description: Success
                            code:
                              type: string
                              description: Error Code, See Description
                            message:
                              type: string
                              description: Error Message
                            bound_at:
                              type: integer
                              description: Bound At
                          required:
                            - stage
                            - success
                            - code
                            - message
                            - bound_at
                        description: results
                    required:
                      - status
                      - results
              example:
                code: 0
                message: success
                data:
                  status: string
                  results:
                    - stage: 0
                      success: false
                      code: string
                      message: string
                      bound_at: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        OPA tenants bind invitation codes for designated Long Bridge users during the registration, account opening and deposit stages

        ### Error Codes

        - `400801`: The invitation code does not exist in Longbridge, please check the code
        - `400803`: User identification validation failed, please check and retry
        - `400804`: Unable to resolve mid
        - `400805`: Invalid invite stage value
        - `400806`: The target stage already has an invite relationship with a different code
        - `400807`: The target stage already has an invite relationship with the same code
        - `1901500`: Longbridge internal error, please retry
        - `1901601`: Invalid invitation relationship, you cannot invite yourself
        - `1901602`: Invalid invitation relationship, the inviter and invitee cannot bind to each other
        - `1901603`: The user has already opened an account and cannot be bound to an account opening inviter anymore
        - `1901604`: The user has already made a deposit and cannot be bound to a deposit inviter anymore
      x-mint:
        href: /en/api-reference/whale_bind_invite_code
        metadata:
          sidebarTitle: Bind the invitation code for the specified stage
  /v1/datasets/refs/bank_country_code:
    get:
      operationId: ref_bank_country_code
      summary: Bank / Country Code Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bank / Country Code data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bank / Country Code data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_bank_country_code
        metadata:
          sidebarTitle: Bank / Country Code Data Dictionary
  /v1/datasets/refs/billing_agent_business_type:
    get:
      operationId: ref_billing_agent_business_type
      summary: Agent Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Agent Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Agent Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_agent_business_type
        metadata:
          sidebarTitle: Agent Business Type Data Dictionary
  /v1/datasets/refs/billing_business_type:
    get:
      operationId: ref_billing_business_type
      summary: Business Type (Billing) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type (Billing) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type (Billing) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_business_type
        metadata:
          sidebarTitle: Business Type (Billing) Data Dictionary
  /v1/datasets/refs/billing_business_type_all:
    get:
      operationId: ref_billing_business_type_all
      summary: Business Type (Billing, Incl. All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type (Billing, Incl. All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type (Billing, Incl. All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_business_type_all
        metadata:
          sidebarTitle: Business Type (Billing, Incl. All) Data Dictionary
  /v1/datasets/refs/billing_business_type_code:
    get:
      operationId: ref_billing_business_type_code
      summary: Business Type Code Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type Code data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type Code data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_business_type_code
        metadata:
          sidebarTitle: Business Type Code Data Dictionary
  /v1/datasets/refs/billing_item_names:
    get:
      operationId: ref_billing_item_names
      summary: Charge Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Charge Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Charge Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_item_names
        metadata:
          sidebarTitle: Charge Type Data Dictionary
  /v1/datasets/refs/billing_packages:
    get:
      operationId: ref_billing_packages
      summary: Package Ref Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Package Ref data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Package Ref data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_packages
        metadata:
          sidebarTitle: Package Ref Data Dictionary
  /v1/datasets/refs/billing_scenario_names:
    get:
      operationId: ref_billing_scenario_names
      summary: Scenario Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Scenario Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Scenario Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_scenario_names
        metadata:
          sidebarTitle: Scenario Name Data Dictionary
  /v1/datasets/refs/billing_settlement_channel:
    get:
      operationId: ref_billing_settlement_channel
      summary: Settlement Channel Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Settlement Channel data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Settlement Channel data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_billing_settlement_channel
        metadata:
          sidebarTitle: Settlement Channel Data Dictionary
  /v1/datasets/refs/biz_code_details:
    get:
      operationId: ref_biz_code_details
      summary: Biz Code Details Key Value Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Biz Code Details Key Value data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Biz Code Details Key Value data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_biz_code_details
        metadata:
          sidebarTitle: Biz Code Details Key Value Data Dictionary
  /v1/datasets/refs/biz_code_to_name:
    get:
      operationId: ref_biz_code_to_name
      summary: Biz Code to Name Mapping Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Biz Code to Name Mapping data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Biz Code to Name Mapping data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_biz_code_to_name
        metadata:
          sidebarTitle: Biz Code to Name Mapping Data Dictionary
  /v1/datasets/refs/book_account_features:
    get:
      operationId: ref_book_account_features
      summary: Account Attribute Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Account Attribute data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Account Attribute data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_account_features
        metadata:
          sidebarTitle: Account Attribute Data Dictionary
  /v1/datasets/refs/book_account_type:
    get:
      operationId: ref_book_account_type
      summary: Bookkeeping Account Type (book_account_type) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bookkeeping Account Type (book_account_type) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bookkeeping Account Type (book_account_type) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_account_type
        metadata:
          sidebarTitle: Bookkeeping Account Type (book_account_type) Data Dictionary
  /v1/datasets/refs/book_bill_stat_business_type:
    get:
      operationId: ref_book_bill_stat_business_type
      summary: Business Statement Statistics Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Statement Statistics Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Statement Statistics Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_bill_stat_business_type
        metadata:
          sidebarTitle: Business Statement Statistics Business Type Data Dictionary
  /v1/datasets/refs/book_brokers:
    get:
      operationId: ref_book_brokers
      summary: Broker Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Broker data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Broker data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_brokers
        metadata:
          sidebarTitle: Broker Data Dictionary
  /v1/datasets/refs/book_business_groups:
    get:
      operationId: ref_book_business_groups
      summary: Business Group Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Group data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Group data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_groups
        metadata:
          sidebarTitle: Business Group Data Dictionary
  /v1/datasets/refs/book_business_type:
    get:
      operationId: ref_book_business_type
      summary: Bookkeeping Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bookkeeping Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bookkeeping Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_type
        metadata:
          sidebarTitle: Bookkeeping Business Type Data Dictionary
  /v1/datasets/refs/book_business_type_ccass:
    get:
      operationId: ref_book_business_type_ccass
      summary: Accounting Business Type (CCASS) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Accounting Business Type (CCASS) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Accounting Business Type (CCASS) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_type_ccass
        metadata:
          sidebarTitle: Accounting Business Type (CCASS) Data Dictionary
  /v1/datasets/refs/book_business_type_member_fee:
    get:
      operationId: ref_book_business_type_member_fee
      summary: Bookkeeping Business Type - User Charge Statement Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bookkeeping Business Type - User Charge Statement data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bookkeeping Business Type - User Charge Statement data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_type_member_fee
        metadata:
          sidebarTitle: Bookkeeping Business Type - User Charge Statement Data Dictionary
  /v1/datasets/refs/book_business_types_bilateral:
    get:
      operationId: ref_book_business_types_bilateral
      summary: Business Type (Payment Flow) Mapping (Bookkeeping) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type (Payment Flow) Mapping (Bookkeeping) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type (Payment Flow) Mapping (Bookkeeping) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_types_bilateral
        metadata:
          sidebarTitle: Business Type (Payment Flow) Mapping (Bookkeeping) Data Dictionary
  /v1/datasets/refs/book_business_types_by_source:
    get:
      operationId: ref_book_business_types_by_source
      summary: Bookkeeping Business Type - by Data Source Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bookkeeping Business Type - by Data Source data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bookkeeping Business Type - by Data Source data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_types_by_source
        metadata:
          sidebarTitle: Bookkeeping Business Type - by Data Source Data Dictionary
  /v1/datasets/refs/book_business_types_member_fee:
    get:
      operationId: ref_book_business_types_member_fee
      summary: Business Type (Client Billing Statement) Mapping (Bookkeeping) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type (Client Billing Statement) Mapping (Bookkeeping) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type (Client Billing Statement) Mapping (Bookkeeping) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_business_types_member_fee
        metadata:
          sidebarTitle: Business Type (Client Billing Statement) Mapping (Bookkeeping) Data Dictionary
  /v1/datasets/refs/book_data_source_editable:
    get:
      operationId: ref_book_data_source_editable
      summary: Editable Business Type Data Source Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Editable Business Type Data Source data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Editable Business Type Data Source data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_data_source_editable
        metadata:
          sidebarTitle: Editable Business Type Data Source Data Dictionary
  /v1/datasets/refs/book_data_source_filterable:
    get:
      operationId: ref_book_data_source_filterable
      summary: Filterable Business Type Data Source Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Filterable Business Type Data Source data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Filterable Business Type Data Source data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_data_source_filterable
        metadata:
          sidebarTitle: Filterable Business Type Data Source Data Dictionary
  /v1/datasets/refs/book_entry_account_types:
    get:
      operationId: ref_book_entry_account_types
      summary: Bookkeeping Account Type (book_entry_account_types) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bookkeeping Account Type (book_entry_account_types) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bookkeeping Account Type (book_entry_account_types) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_entry_account_types
        metadata:
          sidebarTitle: Bookkeeping Account Type (book_entry_account_types) Data Dictionary
  /v1/datasets/refs/book_entry_bank_accounts:
    get:
      operationId: ref_book_entry_bank_accounts
      summary: Entry Rule - Bank Account Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Entry Rule - Bank Account data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Entry Rule - Bank Account data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_entry_bank_accounts
        metadata:
          sidebarTitle: Entry Rule - Bank Account Data Dictionary
  /v1/datasets/refs/book_entry_credit_items:
    get:
      operationId: ref_book_entry_credit_items
      summary: Entry Rule Credit Account Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Entry Rule Credit Account data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Entry Rule Credit Account data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_entry_credit_items
        metadata:
          sidebarTitle: Entry Rule Credit Account Data Dictionary
  /v1/datasets/refs/book_entry_data_source:
    get:
      operationId: ref_book_entry_data_source
      summary: Entry Data Source of Entry Rule Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Entry Data Source of Entry Rule data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Entry Data Source of Entry Rule data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_entry_data_source
        metadata:
          sidebarTitle: Entry Data Source of Entry Rule Data Dictionary
  /v1/datasets/refs/book_entry_debit_items:
    get:
      operationId: ref_book_entry_debit_items
      summary: Entry Rule Debit Account Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Entry Rule Debit Account data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Entry Rule Debit Account data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_entry_debit_items
        metadata:
          sidebarTitle: Entry Rule Debit Account Data Dictionary
  /v1/datasets/refs/book_other_bills_business_type:
    get:
      operationId: ref_book_other_bills_business_type
      summary: Other Fee Bill Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Other Fee Bill Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Other Fee Bill Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_other_bills_business_type
        metadata:
          sidebarTitle: Other Fee Bill Business Type Data Dictionary
  /v1/datasets/refs/book_source:
    get:
      operationId: ref_book_source
      summary: Entry Data Source Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Entry Data Source data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Entry Data Source data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_source
        metadata:
          sidebarTitle: Entry Data Source Data Dictionary
  /v1/datasets/refs/book_type_bilateral_member_mapping:
    get:
      operationId: ref_book_type_bilateral_member_mapping
      summary: Business Type Mapping (Client Fee Bill / Payment Transaction) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type Mapping (Client Fee Bill / Payment Transaction) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type Mapping (Client Fee Bill / Payment Transaction) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_book_type_bilateral_member_mapping
        metadata:
          sidebarTitle: Business Type Mapping (Client Fee Bill / Payment Transaction) Data Dictionary
  /v1/datasets/refs/broker_accounts:
    get:
      operationId: ref_broker_accounts
      summary: Broker Account (Standard) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Broker Account (Standard) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Broker Account (Standard) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_broker_accounts
        metadata:
          sidebarTitle: Broker Account (Standard) Data Dictionary
  /v1/datasets/refs/broker_accounts_for_select:
    get:
      operationId: ref_broker_accounts_for_select
      summary: Broker Account (For Select) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Broker Account (For Select) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Broker Account (For Select) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_broker_accounts_for_select
        metadata:
          sidebarTitle: Broker Account (For Select) Data Dictionary
  /v1/datasets/refs/broker_teams_ref:
    get:
      operationId: ref_broker_teams_ref
      summary: Broker Team List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Broker Team List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Broker Team List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_broker_teams_ref
        metadata:
          sidebarTitle: Broker Team List Data Dictionary
  /v1/datasets/refs/cash_notice_bank_region:
    get:
      operationId: ref_cash_notice_bank_region
      summary: Deposit/Withdrawal Notice - Bank Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit/Withdrawal Notice - Bank Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit/Withdrawal Notice - Bank Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_cash_notice_bank_region
        metadata:
          sidebarTitle: Deposit/Withdrawal Notice - Bank Type Data Dictionary
  /v1/datasets/refs/cash_notice_region_bank_name:
    get:
      operationId: ref_cash_notice_region_bank_name
      summary: Deposit/Withdrawal Announcement - Bank Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit/Withdrawal Announcement - Bank Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit/Withdrawal Announcement - Bank Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_cash_notice_region_bank_name
        metadata:
          sidebarTitle: Deposit/Withdrawal Announcement - Bank Name Data Dictionary
  /v1/datasets/refs/clearing_agents_all:
    get:
      operationId: ref_clearing_agents_all
      summary: All Agents (client contracts, broker supplementary orders, agent-billing agent selection) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the All Agents (client contracts, broker supplementary orders, agent-billing agent selection) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available All Agents (client contracts, broker supplementary orders, agent-billing agent selection) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_agents_all
        metadata:
          sidebarTitle: All Agents (client contracts, broker supplementary orders, agent-billing agent selection) Data Dictionary
  /v1/datasets/refs/clearing_config_markets:
    get:
      operationId: ref_clearing_config_markets
      summary: Market (Clearing Config Table Market) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Market (Clearing Config Table Market) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Market (Clearing Config Table Market) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_config_markets
        metadata:
          sidebarTitle: Market (Clearing Config Table Market) Data Dictionary
  /v1/datasets/refs/clearing_custodians:
    get:
      operationId: ref_clearing_custodians
      summary: Service Provider Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Service Provider data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Service Provider data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_custodians
        metadata:
          sidebarTitle: Service Provider Data Dictionary
  /v1/datasets/refs/clearing_file_markets:
    get:
      operationId: ref_clearing_file_markets
      summary: Clearing File Market Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Clearing File Market data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Clearing File Market data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_file_markets
        metadata:
          sidebarTitle: Clearing File Market Data Dictionary
  /v1/datasets/refs/clearing_markets_all:
    get:
      operationId: ref_clearing_markets_all
      summary: Market (Hardcoded All-Market Enumeration) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Market (Hardcoded All-Market Enumeration) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Market (Hardcoded All-Market Enumeration) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_markets_all
        metadata:
          sidebarTitle: Market (Hardcoded All-Market Enumeration) Data Dictionary
  /v1/datasets/refs/clearing_ready_tasks:
    get:
      operationId: ref_clearing_ready_tasks
      summary: Clearing - Pre-Clearing Preparation Task Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Clearing - Pre-Clearing Preparation Task data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Clearing - Pre-Clearing Preparation Task data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_ready_tasks
        metadata:
          sidebarTitle: Clearing - Pre-Clearing Preparation Task Data Dictionary
  /v1/datasets/refs/clearing_settlement_channel_names:
    get:
      operationId: ref_clearing_settlement_channel_names
      summary: Settlement Channel String-to-Name Display Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Settlement Channel String-to-Name Display data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Settlement Channel String-to-Name Display data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_clearing_settlement_channel_names
        metadata:
          sidebarTitle: Settlement Channel String-to-Name Display Data Dictionary
  /v1/datasets/refs/company_account_types:
    get:
      operationId: ref_company_account_types
      summary: Account Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Account Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Account Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_company_account_types
        metadata:
          sidebarTitle: Account Type Data Dictionary
  /v1/datasets/refs/company_bank_account_config:
    get:
      operationId: ref_company_bank_account_config
      summary: Company Type Configuration Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Company Type Configuration data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Company Type Configuration data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_company_bank_account_config
        metadata:
          sidebarTitle: Company Type Configuration Data Dictionary
  /v1/datasets/refs/company_bank_account_full_names:
    get:
      operationId: ref_company_bank_account_full_names
      summary: Company Account List - Full Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Company Account List - Full Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Company Account List - Full Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_company_bank_account_full_names
        metadata:
          sidebarTitle: Company Account List - Full Name Data Dictionary
  /v1/datasets/refs/company_bank_channels:
    get:
      operationId: ref_company_bank_channels
      summary: Company Bank Channel List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Company Bank Channel List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Company Bank Channel List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_company_bank_channels
        metadata:
          sidebarTitle: Company Bank Channel List Data Dictionary
  /v1/datasets/refs/company_bank_names:
    get:
      operationId: ref_company_bank_names
      summary: Bank Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bank Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bank Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_company_bank_names
        metadata:
          sidebarTitle: Bank Name Data Dictionary
  /v1/datasets/refs/corporate_action_fee_code:
    get:
      operationId: ref_corporate_action_fee_code
      summary: Fee Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fee Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fee Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_corporate_action_fee_code
        metadata:
          sidebarTitle: Fee Type Data Dictionary
  /v1/datasets/refs/corporate_action_fee_code_excl_scrip_gst:
    get:
      operationId: ref_corporate_action_fee_code_excl_scrip_gst
      summary: Fee Type (Excluding scrip_fee\gst_fee) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fee Type (Excluding scrip_fee\gst_fee) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fee Type (Excluding scrip_fee\gst_fee) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_corporate_action_fee_code_excl_scrip_gst
        metadata:
          sidebarTitle: Fee Type (Excluding scrip_fee\gst_fee) Data Dictionary
  /v1/datasets/refs/crs_aeoi_id:
    get:
      operationId: ref_crs_aeoi_id
      summary: CRS ID Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the CRS ID data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available CRS ID data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_aeoi_id
        metadata:
          sidebarTitle: CRS ID Data Dictionary
  /v1/datasets/refs/crs_batch:
    get:
      operationId: ref_crs_batch
      summary: CRS Batch Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the CRS Batch data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available CRS Batch data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_batch
        metadata:
          sidebarTitle: CRS Batch Data Dictionary
  /v1/datasets/refs/crs_company_name:
    get:
      operationId: ref_crs_company_name
      summary: CRS Company Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the CRS Company Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available CRS Company Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_company_name
        metadata:
          sidebarTitle: CRS Company Name Data Dictionary
  /v1/datasets/refs/crs_message_id:
    get:
      operationId: ref_crs_message_id
      summary: CRS Message ID Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the CRS Message ID data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available CRS Message ID data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_message_id
        metadata:
          sidebarTitle: CRS Message ID Data Dictionary
  /v1/datasets/refs/crs_return_year:
    get:
      operationId: ref_crs_return_year
      summary: CRS Year Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the CRS Year data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available CRS Year data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_return_year
        metadata:
          sidebarTitle: CRS Year Data Dictionary
  /v1/datasets/refs/crs_tax_jurisdiction:
    get:
      operationId: ref_crs_tax_jurisdiction
      summary: Tax Jurisdiction Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tax Jurisdiction data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tax Jurisdiction data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_crs_tax_jurisdiction
        metadata:
          sidebarTitle: Tax Jurisdiction Data Dictionary
  /v1/datasets/refs/custodian_sub_positions:
    get:
      operationId: ref_custodian_sub_positions
      summary: Custodian Sub-position Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Custodian Sub-position data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Custodian Sub-position data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_custodian_sub_positions
        metadata:
          sidebarTitle: Custodian Sub-position Data Dictionary
  /v1/datasets/refs/deposit_audit_rule_codes:
    get:
      operationId: ref_deposit_audit_rule_codes
      summary: Deposit Hit Rule - Query Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Hit Rule - Query data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Hit Rule - Query data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_audit_rule_codes
        metadata:
          sidebarTitle: Deposit Hit Rule - Query Data Dictionary
  /v1/datasets/refs/deposit_audit_rule_names:
    get:
      operationId: ref_deposit_audit_rule_names
      summary: Deposit Review Rule Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Review Rule Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Review Rule Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_audit_rule_names
        metadata:
          sidebarTitle: Deposit Review Rule Name Data Dictionary
  /v1/datasets/refs/deposit_block_business_types:
    get:
      operationId: ref_deposit_block_business_types
      summary: Deposit Blocking Rule - Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Blocking Rule - Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Blocking Rule - Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_block_business_types
        metadata:
          sidebarTitle: Deposit Blocking Rule - Business Type Data Dictionary
  /v1/datasets/refs/deposit_channel_and_method_with_all:
    get:
      operationId: ref_deposit_channel_and_method_with_all
      summary: Deposit Channel - Channel Name_Deposit Method Name (Including All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Channel - Channel Name_Deposit Method Name (Including All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Channel - Channel Name_Deposit Method Name (Including All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_channel_and_method_with_all
        metadata:
          sidebarTitle: Deposit Channel - Channel Name_Deposit Method Name (Including All) Data Dictionary
  /v1/datasets/refs/deposit_channel_types:
    get:
      operationId: ref_deposit_channel_types
      summary: Deposit Channel Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Channel data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Channel data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_channel_types
        metadata:
          sidebarTitle: Deposit Channel Data Dictionary
  /v1/datasets/refs/deposit_source:
    get:
      operationId: ref_deposit_source
      summary: Deposit Source Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Deposit Source data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Deposit Source data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_deposit_source
        metadata:
          sidebarTitle: Deposit Source Data Dictionary
  /v1/datasets/refs/exchange_from_currency:
    get:
      operationId: ref_exchange_from_currency
      summary: From Currency Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the From Currency data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available From Currency data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_exchange_from_currency
        metadata:
          sidebarTitle: From Currency Data Dictionary
  /v1/datasets/refs/exchange_to_currency:
    get:
      operationId: ref_exchange_to_currency
      summary: Buy Currency Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Buy Currency data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Buy Currency data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_exchange_to_currency
        metadata:
          sidebarTitle: Buy Currency Data Dictionary
  /v1/datasets/refs/forex_channel:
    get:
      operationId: ref_forex_channel
      summary: FX Exchange Channel Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the FX Exchange Channel data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available FX Exchange Channel data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_forex_channel
        metadata:
          sidebarTitle: FX Exchange Channel Data Dictionary
  /v1/datasets/refs/forex_channel_api:
    get:
      operationId: ref_forex_channel_api
      summary: FX Channel (API) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the FX Channel (API) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available FX Channel (API) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_forex_channel_api
        metadata:
          sidebarTitle: FX Channel (API) Data Dictionary
  /v1/datasets/refs/fund_business_type:
    get:
      operationId: ref_fund_business_type
      summary: Business Type (Fund) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Business Type (Fund) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Business Type (Fund) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_business_type
        metadata:
          sidebarTitle: Business Type (Fund) Data Dictionary
  /v1/datasets/refs/fund_channel:
    get:
      operationId: ref_fund_channel
      summary: Fund Channel Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Channel data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Channel data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_channel
        metadata:
          sidebarTitle: Fund Channel Data Dictionary
  /v1/datasets/refs/fund_channel_accounts:
    get:
      operationId: ref_fund_channel_accounts
      summary: Fund Account List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Account List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Account List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_channel_accounts
        metadata:
          sidebarTitle: Fund Account List Data Dictionary
  /v1/datasets/refs/fund_company_channels:
    get:
      operationId: ref_fund_company_channels
      summary: Fund Channel List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Channel List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Channel List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_company_channels
        metadata:
          sidebarTitle: Fund Channel List Data Dictionary
  /v1/datasets/refs/fund_company_names:
    get:
      operationId: ref_fund_company_names
      summary: Fund Channel Name List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Channel Name List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Channel Name List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_company_names
        metadata:
          sidebarTitle: Fund Channel Name List Data Dictionary
  /v1/datasets/refs/fund_counter_ids_excl_private:
    get:
      operationId: ref_fund_counter_ids_excl_private
      summary: Fund Counter ID List (Excluding Private Funds) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Counter ID List (Excluding Private Funds) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Counter ID List (Excluding Private Funds) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_fund_counter_ids_excl_private
        metadata:
          sidebarTitle: Fund Counter ID List (Excluding Private Funds) Data Dictionary
  /v1/datasets/refs/group_companies:
    get:
      operationId: ref_group_companies
      summary: Company List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Company List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Company List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_group_companies
        metadata:
          sidebarTitle: Company List Data Dictionary
  /v1/datasets/refs/idle_cash_product_topics:
    get:
      operationId: ref_idle_cash_product_topics
      summary: Idle Cash Wealth Management Topics Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Idle Cash Wealth Management Topics data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Idle Cash Wealth Management Topics data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_idle_cash_product_topics
        metadata:
          sidebarTitle: Idle Cash Wealth Management Topics Data Dictionary
  /v1/datasets/refs/ifast_business_type:
    get:
      operationId: ref_ifast_business_type
      summary: IFAST Business Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the IFAST Business Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available IFAST Business Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_ifast_business_type
        metadata:
          sidebarTitle: IFAST Business Type Data Dictionary
  /v1/datasets/refs/ipo_channel:
    get:
      operationId: ref_ipo_channel
      summary: IPO Channel Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the IPO Channel data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available IPO Channel data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_ipo_channel
        metadata:
          sidebarTitle: IPO Channel Data Dictionary
  /v1/datasets/refs/member_organizations:
    get:
      operationId: ref_member_organizations
      summary: Institution Name List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Institution Name List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Institution Name List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_member_organizations
        metadata:
          sidebarTitle: Institution Name List Data Dictionary
  /v1/datasets/refs/ob_file_types:
    get:
      operationId: ref_ob_file_types
      summary: OB File Type List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the OB File Type List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available OB File Type List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_ob_file_types
        metadata:
          sidebarTitle: OB File Type List Data Dictionary
  /v1/datasets/refs/offline_deposit_methods:
    get:
      operationId: ref_offline_deposit_methods
      summary: Offline Deposit - Deposit Method Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Offline Deposit - Deposit Method data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Offline Deposit - Deposit Method data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_offline_deposit_methods
        metadata:
          sidebarTitle: Offline Deposit - Deposit Method Data Dictionary
  /v1/datasets/refs/offline_deposit_receiving_bank:
    get:
      operationId: ref_offline_deposit_receiving_bank
      summary: Offline Deposit - Receiving Bank Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Offline Deposit - Receiving Bank data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Offline Deposit - Receiving Bank data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_offline_deposit_receiving_bank
        metadata:
          sidebarTitle: Offline Deposit - Receiving Bank Data Dictionary
  /v1/datasets/refs/operators:
    get:
      operationId: ref_operators
      summary: Operator Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Operator data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Operator data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_operators
        metadata:
          sidebarTitle: Operator Data Dictionary
  /v1/datasets/refs/org_billing_packages:
    get:
      operationId: ref_org_billing_packages
      summary: Available Billing Packages for Institution Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Available Billing Packages for Institution data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Available Billing Packages for Institution data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_org_billing_packages
        metadata:
          sidebarTitle: Available Billing Packages for Institution Data Dictionary
  /v1/datasets/refs/pending_withdraw_bank_names:
    get:
      operationId: ref_pending_withdraw_bank_names
      summary: Pending Withdrawal Order Bank Name Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Pending Withdrawal Order Bank Name data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Pending Withdrawal Order Bank Name data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_pending_withdraw_bank_names
        metadata:
          sidebarTitle: Pending Withdrawal Order Bank Name Data Dictionary
  /v1/datasets/refs/private_fund_counter_ids:
    get:
      operationId: ref_private_fund_counter_ids
      summary: Fund Counter ID List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Counter ID List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Counter ID List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_private_fund_counter_ids
        metadata:
          sidebarTitle: Fund Counter ID List Data Dictionary
  /v1/datasets/refs/private_fund_internal_codes:
    get:
      operationId: ref_private_fund_internal_codes
      summary: Fund Internal Code (Private Fund) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Internal Code (Private Fund) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Internal Code (Private Fund) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_private_fund_internal_codes
        metadata:
          sidebarTitle: Fund Internal Code (Private Fund) Data Dictionary
  /v1/datasets/refs/public_fund_company_channels:
    get:
      operationId: ref_public_fund_company_channels
      summary: Public Fund Channel List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Public Fund Channel List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Public Fund Channel List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_public_fund_company_channels
        metadata:
          sidebarTitle: Public Fund Channel List Data Dictionary
  /v1/datasets/refs/public_fund_internal_codes:
    get:
      operationId: ref_public_fund_internal_codes
      summary: Fund Internal Code (Public Fund) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Fund Internal Code (Public Fund) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Fund Internal Code (Public Fund) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_public_fund_internal_codes
        metadata:
          sidebarTitle: Fund Internal Code (Public Fund) Data Dictionary
  /v1/datasets/refs/receiving_bank_accounts:
    get:
      operationId: ref_receiving_bank_accounts
      summary: Receiving Bank List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Receiving Bank List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Receiving Bank List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_receiving_bank_accounts
        metadata:
          sidebarTitle: Receiving Bank List Data Dictionary
  /v1/datasets/refs/receiving_bank_accounts_with_names:
    get:
      operationId: ref_receiving_bank_accounts_with_names
      summary: Bank Name and Account Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Bank Name and Account data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Bank Name and Account data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_receiving_bank_accounts_with_names
        metadata:
          sidebarTitle: Bank Name and Account Data Dictionary
  /v1/datasets/refs/region_banks:
    get:
      operationId: ref_region_banks
      summary: Regional Bank Information (Standard) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (Standard) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (Standard) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks
        metadata:
          sidebarTitle: Regional Bank Information (Standard) Data Dictionary
  /v1/datasets/refs/region_banks_all:
    get:
      operationId: ref_region_banks_all
      summary: Regional Bank Information (Incl. All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (Incl. All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (Incl. All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_all
        metadata:
          sidebarTitle: Regional Bank Information (Incl. All) Data Dictionary
  /v1/datasets/refs/region_banks_by_name_key:
    get:
      operationId: ref_region_banks_by_name_key
      summary: Regional Bank Information (By Name Key) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (By Name Key) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (By Name Key) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_by_name_key
        metadata:
          sidebarTitle: Regional Bank Information (By Name Key) Data Dictionary
  /v1/datasets/refs/region_banks_code:
    get:
      operationId: ref_region_banks_code
      summary: Regional Bank Information (By Code) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (By Code) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (By Code) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_code
        metadata:
          sidebarTitle: Regional Bank Information (By Code) Data Dictionary
  /v1/datasets/refs/region_banks_for_withdraw_batching:
    get:
      operationId: ref_region_banks_for_withdraw_batching
      summary: Regional Bank Info (For Withdrawal Batching Rules) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Info (For Withdrawal Batching Rules) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Info (For Withdrawal Batching Rules) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_for_withdraw_batching
        metadata:
          sidebarTitle: Regional Bank Info (For Withdrawal Batching Rules) Data Dictionary
  /v1/datasets/refs/region_banks_no_default:
    get:
      operationId: ref_region_banks_no_default
      summary: Regional Bank Information (No Default) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (No Default) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (No Default) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_no_default
        metadata:
          sidebarTitle: Regional Bank Information (No Default) Data Dictionary
  /v1/datasets/refs/region_banks_with_all:
    get:
      operationId: ref_region_banks_with_all
      summary: Regional Bank Information (With 'All' Option) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Regional Bank Information (With 'All' Option) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Regional Bank Information (With 'All' Option) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_region_banks_with_all
        metadata:
          sidebarTitle: Regional Bank Information (With 'All' Option) Data Dictionary
  /v1/datasets/refs/regions:
    get:
      operationId: ref_regions
      summary: Region Information (Standard) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Region Information (Standard) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Region Information (Standard) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_regions
        metadata:
          sidebarTitle: Region Information (Standard) Data Dictionary
  /v1/datasets/refs/regions_cn_name:
    get:
      operationId: ref_regions_cn_name
      summary: Region Information (CN Name) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Region Information (CN Name) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Region Information (CN Name) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_regions_cn_name
        metadata:
          sidebarTitle: Region Information (CN Name) Data Dictionary
  /v1/datasets/refs/report_broker_accounts:
    get:
      operationId: ref_report_broker_accounts
      summary: Broker Account (For Reports) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Broker Account (For Reports) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Broker Account (For Reports) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_report_broker_accounts
        metadata:
          sidebarTitle: Broker Account (For Reports) Data Dictionary
  /v1/datasets/refs/risk_client_groups:
    get:
      operationId: ref_risk_client_groups
      summary: Client Group List (risk-parameter types only) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Client Group List (risk-parameter types only) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Client Group List (risk-parameter types only) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_client_groups
        metadata:
          sidebarTitle: Client Group List (risk-parameter types only) Data Dictionary
  /v1/datasets/refs/risk_limit_codes_client:
    get:
      operationId: ref_risk_limit_codes_client
      summary: Client Limit Code List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Client Limit Code List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Client Limit Code List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_limit_codes_client
        metadata:
          sidebarTitle: Client Limit Code List Data Dictionary
  /v1/datasets/refs/risk_limit_codes_global:
    get:
      operationId: ref_risk_limit_codes_global
      summary: Global Limit Code List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Global Limit Code List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Global Limit Code List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_limit_codes_global
        metadata:
          sidebarTitle: Global Limit Code List Data Dictionary
  /v1/datasets/refs/risk_limit_codes_multi_dimension:
    get:
      operationId: ref_risk_limit_codes_multi_dimension
      summary: Multi-Dimension Limit Code List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Multi-Dimension Limit Code List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Multi-Dimension Limit Code List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_limit_codes_multi_dimension
        metadata:
          sidebarTitle: Multi-Dimension Limit Code List Data Dictionary
  /v1/datasets/refs/risk_limit_codes_stock:
    get:
      operationId: ref_risk_limit_codes_stock
      summary: Stock Limit Code List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Stock Limit Code List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Stock Limit Code List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_limit_codes_stock
        metadata:
          sidebarTitle: Stock Limit Code List Data Dictionary
  /v1/datasets/refs/risk_limit_codes_trader:
    get:
      operationId: ref_risk_limit_codes_trader
      summary: Trader Limit Code List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Trader Limit Code List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Trader Limit Code List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_limit_codes_trader
        metadata:
          sidebarTitle: Trader Limit Code List Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_card:
    get:
      operationId: ref_risk_rule_indicators_card
      summary: Risk Control Rule Indicator List (Bank Card) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Risk Control Rule Indicator List (Bank Card) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Risk Control Rule Indicator List (Bank Card) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_card
        metadata:
          sidebarTitle: Risk Control Rule Indicator List (Bank Card) Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_compliance_withdraw:
    get:
      operationId: ref_risk_rule_indicators_compliance_withdraw
      summary: Compliance Risk Control Rule Indicator List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Compliance Risk Control Rule Indicator List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Compliance Risk Control Rule Indicator List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_compliance_withdraw
        metadata:
          sidebarTitle: Compliance Risk Control Rule Indicator List Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_deposit:
    get:
      operationId: ref_risk_rule_indicators_deposit
      summary: Risk Control Rule Indicator List (Deposit) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Risk Control Rule Indicator List (Deposit) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Risk Control Rule Indicator List (Deposit) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_deposit
        metadata:
          sidebarTitle: Risk Control Rule Indicator List (Deposit) Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_finance_withdraw:
    get:
      operationId: ref_risk_rule_indicators_finance_withdraw
      summary: Finance Risk-Control Rule Indicator List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Finance Risk-Control Rule Indicator List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Finance Risk-Control Rule Indicator List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_finance_withdraw
        metadata:
          sidebarTitle: Finance Risk-Control Rule Indicator List Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_transfer_out:
    get:
      operationId: ref_risk_rule_indicators_transfer_out
      summary: Risk Control Rule Indicator List (Transfer-out) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Risk Control Rule Indicator List (Transfer-out) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Risk Control Rule Indicator List (Transfer-out) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_transfer_out
        metadata:
          sidebarTitle: Risk Control Rule Indicator List (Transfer-out) Data Dictionary
  /v1/datasets/refs/risk_rule_indicators_withdraw:
    get:
      operationId: ref_risk_rule_indicators_withdraw
      summary: Risk Control Rule Indicator List (Withdrawal) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Risk Control Rule Indicator List (Withdrawal) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Risk Control Rule Indicator List (Withdrawal) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_risk_rule_indicators_withdraw
        metadata:
          sidebarTitle: Risk Control Rule Indicator List (Withdrawal) Data Dictionary
  /v1/datasets/refs/settlement_channel_for_deposit_withdraw:
    get:
      operationId: ref_settlement_channel_for_deposit_withdraw
      summary: Settlement Channel (For Deposit/Withdrawal) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Settlement Channel (For Deposit/Withdrawal) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Settlement Channel (For Deposit/Withdrawal) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_settlement_channel_for_deposit_withdraw
        metadata:
          sidebarTitle: Settlement Channel (For Deposit/Withdrawal) Data Dictionary
  /v1/datasets/refs/spot_forex_sources:
    get:
      operationId: ref_spot_forex_sources
      summary: Exchange Source - Spot Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Exchange Source - Spot data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Exchange Source - Spot data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_spot_forex_sources
        metadata:
          sidebarTitle: Exchange Source - Spot Data Dictionary
  /v1/datasets/refs/stock_groups:
    get:
      operationId: ref_stock_groups
      summary: Stock Group Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Stock Group data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Stock Group data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_stock_groups
        metadata:
          sidebarTitle: Stock Group Data Dictionary
  /v1/datasets/refs/tenant_crypto_currencies:
    get:
      operationId: ref_tenant_crypto_currencies
      summary: Tenant Cryptocurrency (Standard) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant Cryptocurrency (Standard) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant Cryptocurrency (Standard) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_crypto_currencies
        metadata:
          sidebarTitle: Tenant Cryptocurrency (Standard) Data Dictionary
  /v1/datasets/refs/tenant_crypto_currencies_with_all:
    get:
      operationId: ref_tenant_crypto_currencies_with_all
      summary: Tenant Cryptocurrency (Incl. All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant Cryptocurrency (Incl. All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant Cryptocurrency (Incl. All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_crypto_currencies_with_all
        metadata:
          sidebarTitle: Tenant Cryptocurrency (Incl. All) Data Dictionary
  /v1/datasets/refs/tenant_currencies:
    get:
      operationId: ref_tenant_currencies
      summary: Tenant All Currencies (All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant All Currencies (All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant All Currencies (All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_currencies
        metadata:
          sidebarTitle: Tenant All Currencies (All) Data Dictionary
  /v1/datasets/refs/tenant_currencies_by_channel:
    get:
      operationId: ref_tenant_currencies_by_channel
      summary: Tenant All Currencies (By Channel) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant All Currencies (By Channel) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant All Currencies (By Channel) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_currencies_by_channel
        metadata:
          sidebarTitle: Tenant All Currencies (By Channel) Data Dictionary
  /v1/datasets/refs/tenant_currencies_for_exchange:
    get:
      operationId: ref_tenant_currencies_for_exchange
      summary: Tenant All Currencies - Exchange Scenario Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant All Currencies - Exchange Scenario data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant All Currencies - Exchange Scenario data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_currencies_for_exchange
        metadata:
          sidebarTitle: Tenant All Currencies - Exchange Scenario Data Dictionary
  /v1/datasets/refs/tenant_currencies_with_all:
    get:
      operationId: ref_tenant_currencies_with_all
      summary: Tenant All Currencies (With All-Currency Parameter) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant All Currencies (With All-Currency Parameter) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant All Currencies (With All-Currency Parameter) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_currencies_with_all
        metadata:
          sidebarTitle: Tenant All Currencies (With All-Currency Parameter) Data Dictionary
  /v1/datasets/refs/tenant_deposit_currencies:
    get:
      operationId: ref_tenant_deposit_currencies
      summary: Tenant Supported Deposit Currencies Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant Supported Deposit Currencies data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant Supported Deposit Currencies data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_deposit_currencies
        metadata:
          sidebarTitle: Tenant Supported Deposit Currencies Data Dictionary
  /v1/datasets/refs/tenant_exchange_currencies:
    get:
      operationId: ref_tenant_exchange_currencies
      summary: Tenant Exchange-supported Currencies Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Tenant Exchange-supported Currencies data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Tenant Exchange-supported Currencies data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_tenant_exchange_currencies
        metadata:
          sidebarTitle: Tenant Exchange-supported Currencies Data Dictionary
  /v1/datasets/refs/transfer_notify_brokers:
    get:
      operationId: ref_transfer_notify_brokers
      summary: Transfer Notification Broker Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Transfer Notification Broker data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Transfer Notification Broker data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_transfer_notify_brokers
        metadata:
          sidebarTitle: Transfer Notification Broker Data Dictionary
  /v1/datasets/refs/underlying_markets:
    get:
      operationId: ref_underlying_markets
      summary: Instrument Market List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Instrument Market List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Instrument Market List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_underlying_markets
        metadata:
          sidebarTitle: Instrument Market List Data Dictionary
  /v1/datasets/refs/va_balance_account_type:
    get:
      operationId: ref_va_balance_account_type
      summary: Virtual Asset Account Position Account Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Virtual Asset Account Position Account Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Virtual Asset Account Position Account Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_va_balance_account_type
        metadata:
          sidebarTitle: Virtual Asset Account Position Account Type Data Dictionary
  /v1/datasets/refs/va_balance_currency:
    get:
      operationId: ref_va_balance_currency
      summary: Virtual Asset Account Position Currency Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Virtual Asset Account Position Currency data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Virtual Asset Account Position Currency data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_va_balance_currency
        metadata:
          sidebarTitle: Virtual Asset Account Position Currency Data Dictionary
  /v1/datasets/refs/withdraw_application_hit_flags:
    get:
      operationId: ref_withdraw_application_hit_flags
      summary: User Withdrawal Application Hit Rule Dynamic Configuration Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the User Withdrawal Application Hit Rule Dynamic Configuration data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available User Withdrawal Application Hit Rule Dynamic Configuration data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_application_hit_flags
        metadata:
          sidebarTitle: User Withdrawal Application Hit Rule Dynamic Configuration Data Dictionary
  /v1/datasets/refs/withdraw_audit_flags:
    get:
      operationId: ref_withdraw_audit_flags
      summary: Withdrawal Hit Rules (Non-Bitwise) (Standard) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Hit Rules (Non-Bitwise) (Standard) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Hit Rules (Non-Bitwise) (Standard) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_audit_flags
        metadata:
          sidebarTitle: Withdrawal Hit Rules (Non-Bitwise) (Standard) Data Dictionary
  /v1/datasets/refs/withdraw_audit_flags_all:
    get:
      operationId: ref_withdraw_audit_flags_all
      summary: Withdrawal Hit Rules (Non-Bitwise) (Incl. All) Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Hit Rules (Non-Bitwise) (Incl. All) data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Hit Rules (Non-Bitwise) (Incl. All) data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_audit_flags_all
        metadata:
          sidebarTitle: Withdrawal Hit Rules (Non-Bitwise) (Incl. All) Data Dictionary
  /v1/datasets/refs/withdraw_company_bank_channels:
    get:
      operationId: ref_withdraw_company_bank_channels
      summary: Withdrawal Rule Company Bank Channel List Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Rule Company Bank Channel List data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Rule Company Bank Channel List data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_company_bank_channels
        metadata:
          sidebarTitle: Withdrawal Rule Company Bank Channel List Data Dictionary
  /v1/datasets/refs/withdraw_company_bank_channels_cascade:
    get:
      operationId: ref_withdraw_company_bank_channels_cascade
      summary: Withdrawal Rule Company Bank Channel List - Cascade Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Rule Company Bank Channel List - Cascade data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Rule Company Bank Channel List - Cascade data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_company_bank_channels_cascade
        metadata:
          sidebarTitle: Withdrawal Rule Company Bank Channel List - Cascade Data Dictionary
  /v1/datasets/refs/withdraw_methods:
    get:
      operationId: ref_withdraw_methods
      summary: Withdrawal Method Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Method data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Method data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_methods
        metadata:
          sidebarTitle: Withdrawal Method Data Dictionary
  /v1/datasets/refs/withdraw_rule_bank_regions:
    get:
      operationId: ref_withdraw_rule_bank_regions
      summary: Withdrawal Rule - Bank Type Data Dictionary
      tags:
        - Misc
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      values:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Key. **Returned value:** Dictionary key to send to related APIs; do not send the display text.
                            value:
                              type: string
                              description: Value. **Returned value:** Display text for the dictionary entry.
                            desc:
                              type: string
                              description: Description. Usually empty.
                            color:
                              type: string
                              description: Color. **Returned value:** Color.
                            pkey:
                              type: string
                              description: Parent Key. Usually empty.
                        description: Dictionary Entries. **Returned value:** List of dictionary entries.
                      message:
                        type: string
                        description: Usually empty. This field is independent of the outer `message` field.
              example:
                code: 0
                message: success
                data:
                  values:
                    - key: "1"
                      value: 示例值 Example
                      desc: ""
                      color: ""
                      pkey: ""
                  message: ""
          description: On success, returns the Withdrawal Rule - Bank Type data-dictionary entries. Each entry contains a `key` for requests and a display `value`.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        Returns the currently available Withdrawal Rule - Bank Type data dictionary.

        ### Using the values

        Read entries from `data.values`. Request parameters normally use an entry's `key`, while `value` is the display label. Values may change with business configuration and should not be hard-coded in clients.
      x-mint:
        href: /en/api-reference/ref_withdraw_rule_bank_regions
        metadata:
          sidebarTitle: Withdrawal Rule - Bank Type Data Dictionary
  /v1/whaleapi/ipo/amend:
    post:
      operationId: amend_ipo_order_by_whale_api
      summary: Amend IPO Order
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                symbol:
                  type: string
                  description: symbol
                sub_qty:
                  type: string
                  description: Subscribe quantity
                method:
                  type: string
                  description: Subscription method
                batch_id:
                  type: string
                  description: Financing batch Id
                order_id:
                  type: string
                  description: Order Id
                account_no:
                  type: string
                  description: account_no
              required:
                - symbol
                - sub_qty
                - method
                - batch_id
                - order_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Order ID
                      name:
                        type: string
                        description: Stock short name
                      symbol:
                        type: string
                        description: Stock code
                      market:
                        type: string
                        description: Stock market
                      currency:
                        type: string
                        description: Stcok currency
                      sub_qty:
                        type: string
                        description: Subscription quantity
                      sub_amount:
                        type: string
                        description: Subscription amount
                      sub_fee:
                        type: string
                        description: Subscription fee
                      withdraw_able:
                        type: string
                        description: Enable withdraw or not
                      lot_win_qty:
                        type: string
                        description: Allotment quantity
                      status:
                        type: string
                        description: Order status
                      financing_amount:
                        type: string
                        description: Financing amount
                      ipo_status:
                        type: string
                        description: IPO status
                      method:
                        type: string
                        description: Subscription method
                      financing_interest:
                        type: string
                        description: Financing interest
                      finance_fee_rate:
                        type: string
                        description: Financing rate
                      ipo_price:
                        type: string
                        description: IPO issue price
                      borrow_amount:
                        type: string
                        description: Margin amount
                      borrow_interest:
                        type: string
                        description: interest rate
                      total_amount:
                        type: string
                        description: Order total amount
                      current_amount:
                        type: string
                        description: Order current amount
                      luck_at:
                        type: string
                        description: Allotment time
                      ipo_date:
                        type: string
                        description: IPO date
                      mart_begin:
                        type: integer
                        description: Grey trading start time
                      mart_end:
                        type: integer
                        description: Grey trading end time
                      modify_enable:
                        type: string
                        description: Order enable amend or not
                      borrow_enable:
                        type: string
                        description: Enable Margin
                    required:
                      - id
                      - name
                      - symbol
                      - market
                      - currency
                      - sub_qty
                      - sub_amount
                      - sub_fee
                      - withdraw_able
                      - lot_win_qty
                      - status
                      - financing_amount
                      - ipo_status
                      - method
                      - financing_interest
                      - finance_fee_rate
                      - ipo_price
                      - borrow_amount
                      - borrow_interest
                      - total_amount
                      - current_amount
                      - luck_at
                      - ipo_date
                      - mart_begin
                      - mart_end
                      - modify_enable
                      - borrow_enable
              example:
                code: 0
                message: success
                data:
                  id: string
                  name: string
                  symbol: string
                  market: string
                  currency: string
                  sub_qty: string
                  sub_amount: string
                  sub_fee: string
                  withdraw_able: string
                  lot_win_qty: string
                  status: string
                  financing_amount: string
                  ipo_status: string
                  method: string
                  financing_interest: string
                  finance_fee_rate: string
                  ipo_price: string
                  borrow_amount: string
                  borrow_interest: string
                  total_amount: string
                  current_amount: string
                  luck_at: string
                  ipo_date: string
                  mart_begin: 0
                  mart_end: 0
                  modify_enable: string
                  borrow_enable: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Amend IPO Order
      x-mint:
        href: /en/api-reference/amend_ipo_order_by_whale_api
        metadata:
          sidebarTitle: Amend IPO Order
  /v1/whaleapi/ipo/buylimit:
    get:
      operationId: fetch_buy_limit_by_whale_api
      summary: Buy Limit
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: symbol
        - name: currency
          in: query
          required: true
          schema:
            type: string
          description: currency
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_amount:
                        type: string
                        description: total buy limit
                      current_amount:
                        type: string
                        description: "Withdrawable amount in the current currency"
                      mmf_amount:
                        type: string
                        description: mmf amount
                      cash_amount:
                        type: string
                        description: cash amount
                    required:
                      - total_amount
                      - current_amount
                      - mmf_amount
                      - cash_amount
              example:
                code: 0
                message: success
                data:
                  total_amount: string
                  current_amount: string
                  mmf_amount: string
                  cash_amount: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Buy Limit
      x-mint:
        href: /en/api-reference/fetch_buy_limit_by_whale_api
        metadata:
          sidebarTitle: Buy Limit
  /v1/whaleapi/ipo/detail:
    get:
      operationId: fetch_ipo_detail
      summary: Get IPO Detail Info
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: "`ticker.region`, for example `700.HK`."
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      symbol:
                        type: string
                        description: symbol
                      market:
                        type: string
                        description: Enum of market
                      state:
                        type: string
                        description: Ipo status
                      apply_start_time:
                        type: integer
                        description: IPO start time
                      cash_end_time:
                        type: integer
                        description: IPO cash end time
                      margin_end_time:
                        type: string
                        description: IPO margin end time
                      charge_time:
                        type: integer
                        description: IPO payment time
                      result_time:
                        type: integer
                        description: IPO allocment time
                      ipo_time:
                        type: string
                        description: IPO time
                      issue_price:
                        type: string
                        description: issue price
                      subscribe_enable:
                        type: string
                        description: Can subscribe or not
                      mart_support:
                        type: boolean
                        description: Support grey trading or not
                      mart_begin_time:
                        type: integer
                        description: grey trading begin time
                      mart_end_time:
                        type: integer
                        description: Grey trading end time
                      pi_cert:
                        type: boolean
                        description: Need PI check or not
                      min_sub_num:
                        type: string
                        description: min subscribe qty
                      total_shares:
                        type: string
                        description: total shares of stock
                      min_price:
                        type: string
                        description: min issue price
                      max_price:
                        type: string
                        description: max issue price
                      prospectus:
                        type: string
                        description: prospectus url
                      sponsor:
                        type: array
                        items:
                          type: string
                        description: sponsor list
                      underwriter:
                        type: array
                        items:
                          type: string
                        description: underwriter list
                    required:
                      - symbol
                      - market
                      - state
                      - apply_start_time
                      - cash_end_time
                      - margin_end_time
                      - charge_time
                      - result_time
                      - ipo_time
                      - issue_price
                      - subscribe_enable
                      - mart_support
                      - mart_begin_time
                      - mart_end_time
                      - pi_cert
                      - min_sub_num
                      - total_shares
                      - min_price
                      - max_price
                      - prospectus
                      - sponsor
                      - underwriter
              example:
                code: 0
                message: success
                data:
                  symbol: string
                  market: string
                  state: string
                  apply_start_time: 0
                  cash_end_time: 0
                  margin_end_time: string
                  charge_time: 0
                  result_time: 0
                  ipo_time: string
                  issue_price: string
                  subscribe_enable: string
                  mart_support: false
                  mart_begin_time: 0
                  mart_end_time: 0
                  pi_cert: false
                  min_sub_num: string
                  total_shares: string
                  min_price: string
                  max_price: string
                  prospectus: string
                  sponsor:
                    - string
                  underwriter:
                    - string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get IPO Detail Info
      x-mint:
        href: /en/api-reference/fetch_ipo_detail
        metadata:
          sidebarTitle: Get IPO Detail Info
  /v1/whaleapi/ipo/list:
    get:
      operationId: fetch_ipo_list
      summary: Get Available IPO List
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        symbol:
                          type: string
                          description: ticker.region
                        apply_start_time:
                          type: integer
                          description: IPO start time
                        cash_end_time:
                          type: integer
                          description: IPO cash end time
                        margin_end_time:
                          type: integer
                          description: IPO margin end time
                        subscribe_enable:
                          type: boolean
                          description: Whether it can be subcribed
                      required:
                        - symbol
                        - apply_start_time
                        - cash_end_time
                        - margin_end_time
                        - subscribe_enable
              example:
                code: 0
                message: success
                data:
                  - symbol: 700.HK
                    apply_start_time: 0
                    cash_end_time: 0
                    margin_end_time: 0
                    subscribe_enable: false
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get Available IPO List
      x-mint:
        href: /en/api-reference/fetch_ipo_list
        metadata:
          sidebarTitle: Get Available IPO List
  /v1/whaleapi/ipo/payable/list:
    get:
      operationId: fetch_ipo_payment_list_whale_api
      summary: Get IPO Payable List
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: ticker.region
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        number:
                          type: integer
                          description: Serial No. Serial No.
                        sub_qty:
                          type: string
                          description: Number of apply quantity
                        sub_amount:
                          type: string
                          description: Payment amount
                        currency:
                          type: string
                          description: currency
                      required:
                        - number
                        - sub_qty
                        - sub_amount
                        - currency
              example:
                code: 0
                message: success
                data:
                  - number: 0
                    sub_qty: string
                    sub_amount: string
                    currency: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get IPO Payable List
      x-mint:
        href: /en/api-reference/fetch_ipo_payment_list_whale_api
        metadata:
          sidebarTitle: Get IPO Payable List
  /v1/whaleapi/ipo/margin/list:
    get:
      operationId: fetch_margin_list_by_whale_api
      summary: Get IPO Margin List
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
          description: Instrument code.
        - name: account_no
          in: query
          required: false
          schema:
            type: string
          description: Account.
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        method:
                          type: string
                          description: Financing identify key
                        handing_fee:
                          type: string
                          description: Subscription fee
                        currency:
                          type: string
                          description: Financing Currency
                        deadline:
                          type: string
                          description: deadline
                        multiple:
                          type: string
                          description: multiple
                        finance_fee_rate:
                          type: string
                          description: Financing fee rate
                        remaining_amount:
                          type: string
                          description: Remain amount of this marigin pool
                        is_enough:
                          type: string
                          description: Amount of marign pool
                        batch_id:
                          type: string
                          description: ID of margin pool
                        is_ended:
                          type: string
                          description: Margin pool has ended or not
                        financing_ratio:
                          type: string
                          description: Maring ratio
                        start_at:
                          type: string
                          description: Maring start time
                        min_cash:
                          type: string
                          description: Min subscribe cash amount
                        display_name:
                          type: string
                          description: Recommend display name
                      required:
                        - method
                        - handing_fee
                        - currency
                        - deadline
                        - multiple
                        - finance_fee_rate
                        - remaining_amount
                        - is_enough
                        - batch_id
                        - is_ended
                        - financing_ratio
                        - start_at
                        - min_cash
                        - display_name
              example:
                code: 0
                message: success
                data:
                  - method: string
                    handing_fee: string
                    currency: string
                    deadline: string
                    multiple: string
                    finance_fee_rate: string
                    remaining_amount: string
                    is_enough: string
                    batch_id: string
                    is_ended: string
                    financing_ratio: string
                    start_at: string
                    min_cash: string
                    display_name: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get IPO Margin List
      x-mint:
        href: /en/api-reference/fetch_margin_list_by_whale_api
        metadata:
          sidebarTitle: Get IPO Margin List
  /v1/whaleapi/ipo/submit:
    post:
      operationId: submit_ipo_order_by_whale_api
      summary: Submit New IPO Order
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                symbol:
                  type: string
                  description: Stock Symbol, in ticker.region format
                sub_qty:
                  type: string
                  description: Subscribe quantity
                method:
                  type: integer
                  description: Subscription method
                batch_id:
                  type: string
                  description: Financing batch Id
                account_no:
                  type: string
                  description: Account Number
              required:
                - symbol
                - sub_qty
                - method
                - batch_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Order ID
                      name:
                        type: string
                        description: Stock short name
                      symbol:
                        type: string
                        description: Stock code
                      market:
                        type: string
                        description: Stock market
                      currency:
                        type: string
                        description: Stcok currency
                      sub_qty:
                        type: string
                        description: Subscription quantity
                      sub_amount:
                        type: string
                        description: Subscription amount
                      handing_fee:
                        type: string
                        description: Subscription fee
                      withdraw_able:
                        type: string
                        description: Enable withdraw or not
                      lot_win_qty:
                        type: string
                        description: Allotment quantity
                      status:
                        type: string
                        description: Order status
                      financing_amount:
                        type: string
                        description: Financing amount
                      ipo_status:
                        type: string
                        description: IPO status
                      method:
                        type: string
                        description: Subscription method
                      financing_interest:
                        type: string
                        description: Financing interest
                      finance_fee_rate:
                        type: string
                        description: Financing rate
                      ipo_price:
                        type: string
                        description: IPO issue price
                      borrow_amount:
                        type: string
                        description: Margin amount
                      borrow_interest:
                        type: string
                        description: interest rate
                      total_amount:
                        type: string
                        description: Order total amount
                      current_amount:
                        type: string
                        description: Order current amount
                      luck_at:
                        type: string
                        description: Allotment time
                      ipo_date:
                        type: string
                        description: IPO date
                      mart_begin:
                        type: integer
                        description: Grey trading start time
                      mart_end:
                        type: integer
                        description: Grey trading end time
                      modify_enable:
                        type: string
                        description: Order enable amend or not
                      borrow_enable:
                        type: string
                        description: Enable Margin
                    required:
                      - id
                      - name
                      - symbol
                      - market
                      - currency
                      - sub_qty
                      - sub_amount
                      - handing_fee
                      - withdraw_able
                      - lot_win_qty
                      - status
                      - financing_amount
                      - ipo_status
                      - method
                      - financing_interest
                      - finance_fee_rate
                      - ipo_price
                      - borrow_amount
                      - borrow_interest
                      - total_amount
                      - current_amount
                      - luck_at
                      - ipo_date
                      - mart_begin
                      - mart_end
                      - modify_enable
                      - borrow_enable
              example:
                code: 0
                message: success
                data:
                  id: string
                  name: string
                  symbol: string
                  market: string
                  currency: string
                  sub_qty: string
                  sub_amount: string
                  handing_fee: string
                  withdraw_able: string
                  lot_win_qty: string
                  status: string
                  financing_amount: string
                  ipo_status: string
                  method: string
                  financing_interest: string
                  finance_fee_rate: string
                  ipo_price: string
                  borrow_amount: string
                  borrow_interest: string
                  total_amount: string
                  current_amount: string
                  luck_at: string
                  ipo_date: string
                  mart_begin: 0
                  mart_end: 0
                  modify_enable: string
                  borrow_enable: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to submit an IPO subscription order. By providing the stock symbol symbol, subscription quantity sub_qty, subscription method method, financing plan ID batch_id, and account number account_no, it completes the submission of the IPO subscription order and returns the complete order details, including subscription amount, handling fee, whether cancellation is allowed, financing information, and grey market trading time.
      x-mint:
        href: /en/api-reference/submit_ipo_order_by_whale_api
        metadata:
          sidebarTitle: Submit New IPO Order
  /v2/whaleapi/ipo/withdraw:
    post:
      operationId: withdraw_order_by_whale_api_v2
      summary: Withdraw IPO Order
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                order_id:
                  type: integer
                  description: Order ID
                account_no:
                  type: string
                  description: account_no
              required:
                - order_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      result:
                        type: boolean
                        description: result
                    required:
                      - result
              example:
                code: 0
                message: success
                data:
                  result: false
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Withdraw IPO Order
      x-mint:
        href: /en/api-reference/withdraw_order_by_whale_api_v2
        metadata:
          sidebarTitle: Withdraw IPO Order
  /v1/whaleapi/ipo/order:
    get:
      operationId: fetch_order_detail_by_whale_api
      summary: Get IPO Order Detail
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: order_id
          in: query
          required: true
          schema:
            type: number
          description: Order ID
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: number
                        description: Order ID
                      name:
                        type: string
                        description: Stock name of the order
                      code:
                        type: string
                        description: Stock code
                      market:
                        type: string
                        description: Stock market
                      sub_qty:
                        type: string
                        description: Subscription quantity
                      sub_amount:
                        type: string
                        description: Subscription amount
                      handing_fee:
                        type: string
                        description: Subscription fee
                      withdraw_able:
                        type: boolean
                        description: Enable withdraw or not
                      lot_win_qty:
                        type: string
                        description: Allotment quantity
                      status:
                        type: string
                        description: IPO status
                      currency:
                        type: string
                        description: Stock Currency
                      financing_amount:
                        type: string
                        description: Financing amount
                      need_to_pay:
                        type: string
                        description: Amount of the order to be paid back
                      ipo_status:
                        type: string
                        description: IPO status
                      method:
                        type: string
                        description: Subscription method
                      financing_interest:
                        type: string
                        description: Financing interest
                      finance_fee_rate:
                        type: string
                        description: Financing fee rate
                      explanation:
                        type: string
                        description: Explanation of the paid back
                      ipo_price:
                        type: string
                        description: IPO issue price
                      borrow_amount:
                        type: string
                        description: Margin amount
                      borrow_interest:
                        type: string
                        description: Interest rate
                      symbol:
                        type: string
                        description: Stock code
                      total_amount:
                        type: string
                        description: Order total amount
                      current_amount:
                        type: string
                        description: Order current amount
                      refund_amount:
                        type: string
                        description: Amount to be refunded
                      luck_at:
                        type: number
                        description: Allotment result time
                      reject_text:
                        type: string
                        description: Reject reason
                      modify_enable:
                        type: boolean
                        description: Enable to amend
                      lucky_fee:
                        type: string
                        description: Allotment quantity fee
                      channel:
                        type: object
                        properties: {}
                        description: Order channel
                      batch_id:
                        type: number
                        description: ID of margin pool
                    required:
                      - id
                      - name
                      - code
                      - market
                      - sub_qty
                      - sub_amount
                      - handing_fee
                      - withdraw_able
                      - lot_win_qty
                      - status
                      - currency
                      - financing_amount
                      - need_to_pay
                      - ipo_status
                      - method
                      - financing_interest
                      - finance_fee_rate
                      - explanation
                      - ipo_price
                      - borrow_amount
                      - borrow_interest
                      - symbol
                      - total_amount
                      - current_amount
                      - refund_amount
                      - luck_at
                      - reject_text
                      - modify_enable
                      - lucky_fee
                      - channel
                      - batch_id
              example:
                code: 0
                message: success
                data:
                  id: 12121321
                  name: Keep
                  code: "02307"
                  market: HK/US
                  sub_qty: "200"
                  sub_amount: string
                  handing_fee: string
                  withdraw_able: false
                  lot_win_qty: string
                  status: string
                  currency: HKD/USD
                  financing_amount: string
                  need_to_pay: string
                  ipo_status: string
                  method: string
                  financing_interest: string
                  finance_fee_rate: string
                  explanation: string
                  ipo_price: string
                  borrow_amount: string
                  borrow_interest: string
                  symbol: string
                  total_amount: string
                  current_amount: string
                  refund_amount: string
                  luck_at: 0
                  reject_text: string
                  modify_enable: false
                  lucky_fee: string
                  channel: {}
                  batch_id: 0
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get IPO Order Detail
      x-mint:
        href: /en/api-reference/fetch_order_detail_by_whale_api
        metadata:
          sidebarTitle: Get IPO Order Detail
  /v1/whaleapi/ipo/order/list:
    get:
      operationId: fetch_order_list_by_whale_api
      summary: Get Order List
      tags:
        - IPO System
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: "`ticker.region`, for example `700.HK`. Returns all when omitted."
        - name: page
          in: query
          required: true
          schema:
            type: string
          description: Page num
        - name: limit
          in: query
          required: true
          schema:
            type: string
          description: Page size
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Securities account number.
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      has_more:
                        type: boolean
                        description: Has more orders or not
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: Order id
                            name:
                              type: string
                              description: Short name of stock
                            code:
                              type: string
                              description: Stock code
                            symbol:
                              type: string
                              description: ticker.region
                            status:
                              type: string
                              description: Order status
                            method:
                              type: string
                              description: Subscription method
                            created_at:
                              type: integer
                              description: Order submit time
                            currency:
                              type: string
                              description: Order currency
                            sub_qty:
                              type: string
                              description: Subscribe qty
                            sub_amount:
                              type: string
                              description: sub_amount
                            cash_amount:
                              type: string
                              description: cash_amount
                            financing_amount:
                              type: string
                              description: financing_amount
                            financing_ratio:
                              type: string
                              description: financing_ratio
                            handing fee:
                              type: object
                              properties: {}
                              description: handing fee
                            lot_win_qty:
                              type: string
                              description: Allocted qty
                          required:
                            - id
                            - name
                            - code
                            - symbol
                            - status
                            - method
                            - created_at
                            - currency
                            - sub_qty
                            - sub_amount
                            - cash_amount
                            - financing_amount
                            - financing_ratio
                            - handing fee
                            - lot_win_qty
                        description: list
                    required:
                      - has_more
                      - list
              example:
                code: 0
                message: success
                data:
                  has_more: false
                  list:
                    - id: string
                      name: string
                      code: string
                      symbol: string
                      status: string
                      method: string
                      created_at: 0
                      currency: string
                      sub_qty: string
                      sub_amount: string
                      cash_amount: string
                      financing_amount: string
                      financing_ratio: string
                      handing fee: {}
                      lot_win_qty: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get Order List
      x-mint:
        href: /en/api-reference/fetch_order_list_by_whale_api
        metadata:
          sidebarTitle: Get Order List
  /v1/whaleapi/portfolio/deposit/apply:
    post:
      operationId: deposit_apply_v2
      summary: Submit  Deposit Application
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                currency:
                  type: string
                  description: Currency
                amount:
                  type: string
                  description: Amount
                reference_no:
                  type: string
                  description: Reference no
                vouchers:
                  type: array
                  items:
                    type: string
                  description: Deposit application url
                card_id:
                  type: number
                  description: User BankCard Id
                deposit_type:
                  type: number
                  description: "Deposit Type option values: [0: normal | 1: open account]"
                deposit_config_id:
                  type: number
                  description: Deposit config Id
                ps:
                  type: string
                  description: The user's note
                account_no:
                  type: string
                  description: Account No
              required:
                - currency
                - amount
                - vouchers
                - card_id
                - deposit_type
                - deposit_config_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                        description: Is submit success
                      id:
                        type: number
                        description: Deposit application id
              example:
                code: 0
                message: success
                data:
                  ok: false
                  id: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Submit  Deposit Application
      x-mint:
        href: /en/api-reference/deposit_apply_v2
        metadata:
          sidebarTitle: Submit  Deposit Application
  /v1/whaleapi/portfolio/deposit/cancel:
    post:
      operationId: deposit_cancel
      summary: Cancel Deposit
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: number
                  description: id
                account_no:
                  type: string
                  description: Account No
              required:
                - id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                        description: Is Success
                      id:
                        type: number
                        description: Affected Id
              example:
                code: 0
                message: success
                data:
                  ok: false
                  id: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Cancel Deposit
      x-mint:
        href: /en/api-reference/deposit_cancel
        metadata:
          sidebarTitle: Cancel Deposit
  /v1/whaleapi/portfolio/deposit/detail:
    get:
      operationId: deposit_detail_v2
      summary: Deposit Detail
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: id
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: number
                    description: code
                  message:
                    type: string
                    description: message
                  datas:
                    type: object
                    properties:
                      id:
                        type: number
                        description: Deposit Detail Id
                      currency:
                        type: string
                        description: Deposit Currency
                      amount:
                        type: string
                        description: Amount
                      order_id:
                        type: string
                        description: Deposit Order Id
                      created_at:
                        type: number
                        description: Create Time
                      state:
                        type: string
                        description: Deposit state name
                      to_bank_id:
                        type: number
                        description: Bank Id
                      to_bank_name:
                        type: string
                        description: Bank Name
                      user_bank_account:
                        type: string
                        description: User Bank Account
                      user_bank_name:
                        type: string
                        description: User Name
                      reference_no:
                        type: string
                        description: Reference No
                      state_code:
                        type: number
                        description: Order State 0-Processing, 1-Accounted, 2-Processing failed, 3-Revoked, 4-Invalidated, 5-Rejected
                      can_cancel:
                        type: boolean
                        description: Can Cancel Order
                      vochers:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              description: Deposit Key
                            img_url:
                              type: string
                              description: Deposit Image Url
                        description: Deposit Guide Url
                      lb_bank_name:
                        type: string
                        description: Bank Name
                      lb_bank_account:
                        type: string
                        description: Bank Account
                      account_at:
                        type: number
                        description: Deposit Account Time
                      receivable_amount:
                        type: string
                        description: Receivable Amount
                      client_to_see_remark:
                        type: boolean
                        description: Reject Reason
                      remark:
                        type: string
                        description: Remark
                      need_modify_field_type:
                        type: number
                        description: Field type to be modifie(0 - none; 1 - Bank Account Name; 2 - Bank Account No; 3 - Bank Account Name And Bank Account No;)
                    description: datas
              example:
                code: 0
                message: string
                datas:
                  id: 0
                  currency: string
                  amount: string
                  order_id: string
                  created_at: 0
                  state: string
                  to_bank_id: 0
                  to_bank_name: string
                  user_bank_account: string
                  user_bank_name: string
                  reference_no: string
                  state_code: 0
                  can_cancel: false
                  vochers:
                    - key: string
                      img_url: string
                  lb_bank_name: string
                  lb_bank_account: string
                  account_at: 0
                  receivable_amount: string
                  client_to_see_remark: false
                  remark: string
                  need_modify_field_type: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Deposit Detail
      x-mint:
        href: /en/api-reference/deposit_detail_v2
        metadata:
          sidebarTitle: Deposit Detail
  /v1/whaleapi/portfolio/deposit/config:
    get:
      operationId: get_deposit_config
      summary: Obtain the deposit information
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: bank_id
          in: query
          required: false
          schema:
            type: string
          description: "Region bank ID. `0` means another bank."
        - name: currency
          in: query
          required: true
          schema:
            type: string
          description: "Currency, for example `USD` or `HKD`. `ALL` means all currencies."
        - name: bank_opeartion_type_id
          in: query
          required: true
          schema:
            type: string
          description: "Bank operation type ID. `0` returns all types; a non-zero value selects a specific bank operation type."
        - name: rule
          in: query
          required: true
          schema:
            type: string
          description: "Rule 0: ordinary obtain | 1: degradation, making the bank didn't seek other Banks, does not contain other degradation rules"
        - name: region
          in: query
          required: true
          schema:
            type: string
          description: Region
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      deposit_configs:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: number
                              description: id
                            company_name:
                              type: string
                              description: Company name
                            currencies:
                              type: array
                              items:
                                type: string
                              description: Support currency
                            region:
                              type: string
                              description: Region
                            bank_name:
                              type: string
                              description: Bank name
                            deposit_channel_name:
                              type: string
                              description: Deposit channel name
                            fps_account:
                              type: string
                              description: fps id
                            weight:
                              type: string
                              description: weight
                            lb_bank_name:
                              type: string
                              description: Receiving bank name
                            lb_account_simplename:
                              type: string
                              description: Receiving bank simple name
                            lb_bank_swift_code:
                              type: string
                              description: Receiving bank swiftcode
                            lb_bank_addr:
                              type: string
                              description: Receiving bank address
                            lb_account_name:
                              type: string
                              description: Receiving bank  account name
                            lb_bank_account:
                              type: string
                              description: Receiving bank account
                            open_lb_bank_account:
                              type: string
                              description: Open  account  receiving bank account
                            lb_bank_account_id:
                              type: number
                              description: Receiving bank  id
                            fee_payer:
                              type: string
                              description: Fee
                            guide_id:
                              type: number
                              description: Guide id
                            bank_operation_id:
                              type: number
                              description: Bank operation id
                            region_bank_id:
                              type: number
                              description: Region bank id
                            guides:
                              type: object
                              properties:
                                currencies:
                                  type: array
                                  items:
                                    type: string
                                  description: Support currency
                                guide_type:
                                  type: string
                                  description: Guide type
                                name:
                                  type: string
                                  description: Guide name
                                config:
                                  type: string
                                  description: Guide config
                              description: Guides
                        description: Deposit config list
                      sub_accounts:
                        type: array
                        description: List of the account's sub-accounts. May be empty.
                        items:
                          type: object
              example:
                code: 0
                message: success
                data:
                  deposit_configs:
                    - id: 0
                      company_name: string
                      currencies:
                        - string
                      region: string
                      bank_name: string
                      deposit_channel_name: string
                      fps_account: string
                      weight: string
                      lb_bank_name: string
                      lb_account_simplename: string
                      lb_bank_swift_code: string
                      lb_bank_addr: string
                      lb_account_name: string
                      lb_bank_account: string
                      open_lb_bank_account: string
                      lb_bank_account_id: 0
                      fee_payer: string
                      guide_id: 0
                      bank_operation_id: 0
                      region_bank_id: 0
                      guides:
                        currencies:
                          - string
                        guide_type: string
                        name: string
                        config: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Obtain the deposit information
      x-mint:
        href: /en/api-reference/get_deposit_config
        metadata:
          sidebarTitle: Obtain the deposit information
  /v1/whaleapi/portfolio/deposit/notify:
    get:
      operationId: notify_record_v4
      summary: Deposit Application List
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: size
          in: query
          required: true
          schema:
            type: string
          description: Page Size
        - name: page
          in: query
          required: true
          schema:
            type: string
          description: Page No
        - name: currencies
          in: query
          required: false
          schema:
            type: string
          description: Currency Query LIst
        - name: states
          in: query
          required: false
          schema:
            type: string
          description: "State Filter : 0-Processing, 1-Accounted, 2-Processing failed, 3-Revoked, 4-Invalidated, 5-Rejected"
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
        - name: from_date
          in: query
          required: false
          schema:
            type: string
          description: begin
        - name: to_date
          in: query
          required: false
          schema:
            type: string
          description: end
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      items:
                        type: object
                        properties:
                          id:
                            type: string
                            description: Order Id
                          created_at:
                            type: string
                            description: Create Time
                          vouchers:
                            type: array
                            items:
                              type: object
                              properties:
                                key:
                                  type: string
                                  description: key
                                img_url:
                                  type: string
                                  description: image url
                            description: Deposit Image Ur
                          currency:
                            type: string
                            description: Currency
                          amount:
                            type: string
                            description: Amount
                          state:
                            type: string
                            description: State name
                          reference_no:
                            type: string
                            description: Reference No
                          state_code:
                            type: string
                            description: State,0-Processing, 1-Accounted, 2-Processing failed, 3-Revoked, 4-Invalidated, 5-Rejected
                          can_cancel:
                            type: boolean
                            description: Can Cancel
                          receivable_amount:
                            type: string
                            description: Receivable Amount
                        description: items
                      total:
                        type: string
                        description: Total Size
              example:
                code: 0
                message: success
                data:
                  items:
                    id: string
                    created_at: string
                    vouchers:
                      - key: string
                        img_url: string
                    currency: string
                    amount: string
                    state: string
                    reference_no: string
                    state_code: string
                    can_cancel: false
                    receivable_amount: string
                  total: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Deposit Application List
      x-mint:
        href: /en/api-reference/notify_record_v4
        metadata:
          sidebarTitle: Deposit Application List
  /v1/whaleapi/portfolio/rollover/detail:
    get:
      operationId: rollover_record_detail_v2
      summary: Transfer in detail
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: id
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: integer
                        description: id
                      order_id:
                        type: string
                        description: order id
                      market:
                        type: string
                        description: market
                      market_symbol:
                        type: string
                        description: market symbol
                      broker:
                        type: string
                        description: broker name
                      broker_id:
                        type: integer
                        description: broker id
                      broker_en:
                        type: string
                        description: broker name in English
                      account:
                        type: string
                        description: account
                      name:
                        type: string
                        description: name
                      code:
                        type: string
                        description: code
                      contact:
                        type: string
                        description: contact
                      contact_phone:
                        type: string
                        description: contact phone
                      contact_email:
                        type: string
                        description: contact email
                      created_at:
                        type: integer
                        description: created timestamp
                      updated_at:
                        type: integer
                        description: updated timestamp
                      state:
                        type: string
                        description: state
                      app_state:
                        type: string
                        description: app state
                      stocks:
                        type: array
                        items:
                          type: object
                          properties:
                            market:
                              type: string
                              description: market
                            name:
                              type: string
                              description: stock name
                            name_en:
                              type: string
                              description: stock name in English
                            code:
                              type: string
                              description: stock code
                            amount:
                              type: string
                              description: amount
                            counter_id:
                              type: string
                              description: counter id
                            currency:
                              type: string
                              description: currency
                        description: stock list
                      user_email:
                        type: string
                        description: user email
                      display_id:
                        type: string
                        description: display id
                      process_at:
                        type: integer
                        description: process timestamp
                      website:
                        type: string
                        description: website
                      is_has_attachment:
                        type: boolean
                        description: is has attachment
                      can_modify:
                        type: integer
                        description: can modify
              example:
                code: 0
                message: success
                data:
                  id: 0
                  order_id: string
                  market: string
                  market_symbol: string
                  broker: string
                  broker_id: 0
                  broker_en: string
                  account: string
                  name: string
                  code: string
                  contact: string
                  contact_phone: string
                  contact_email: string
                  created_at: 0
                  updated_at: 0
                  state: string
                  app_state: string
                  stocks:
                    - market: string
                      name: string
                      name_en: string
                      code: string
                      amount: string
                      counter_id: string
                      currency: string
                  user_email: string
                  display_id: string
                  process_at: 0
                  website: string
                  is_has_attachment: false
                  can_modify: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Transfer in detail
      x-mint:
        href: /en/api-reference/rollover_record_detail_v2
        metadata:
          sidebarTitle: Transfer in detail
  /v1/whaleapi/portfolio/rollover/record:
    get:
      operationId: rollover_record_v3
      summary: Transfer in records
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: market
          in: query
          required: false
          schema:
            type: string
          description: market
        - name: state
          in: query
          required: false
          schema:
            type: integer
          description: state
        - name: start
          in: query
          required: false
          schema:
            type: integer
          description: start timestamp
        - name: end
          in: query
          required: false
          schema:
            type: integer
          description: end timestamp
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: page index
        - name: size
          in: query
          required: false
          schema:
            type: integer
          description: page size
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      records:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              description: id
                            order_id:
                              type: string
                              description: order id
                            market:
                              type: string
                              description: market
                            market_symbol:
                              type: string
                              description: market symbol
                            broker:
                              type: string
                              description: broker name
                            broker_id:
                              type: integer
                              description: broker id
                            broker_en:
                              type: string
                              description: broker name in English
                            account:
                              type: string
                              description: account
                            name:
                              type: string
                              description: name
                            code:
                              type: string
                              description: code
                            contact:
                              type: string
                              description: contact
                            contact_phone:
                              type: string
                              description: contact phone
                            contact_email:
                              type: string
                              description: contact email
                            created_at:
                              type: integer
                              description: created timestamp
                            updated_at:
                              type: integer
                              description: updated timestamp
                            state:
                              type: string
                              description: state
                            app_state:
                              type: string
                              description: app state
                            stocks:
                              type: array
                              items:
                                type: object
                                properties:
                                  market:
                                    type: string
                                    description: market
                                  name:
                                    type: string
                                    description: stock name
                                  name_en:
                                    type: string
                                    description: stock name in English
                                  code:
                                    type: string
                                    description: stock code
                                  amount:
                                    type: string
                                    description: amount
                                  counter_id:
                                    type: string
                                    description: counter id
                                  currency:
                                    type: string
                                    description: currency
                              description: stock list
                            email_direct:
                              type: boolean
                              description: email direct
                            user_email:
                              type: string
                              description: user email
                            process_at:
                              type: integer
                              description: process timestamp
                            website:
                              type: string
                              description: website
                            can_modify:
                              type: integer
                              description: can modify
                        description: records list
                      total:
                        type: integer
                        description: total count
              example:
                code: 0
                message: success
                data:
                  records:
                    - id: 0
                      order_id: string
                      market: string
                      market_symbol: string
                      broker: string
                      broker_id: 0
                      broker_en: string
                      account: string
                      name: string
                      code: string
                      contact: string
                      contact_phone: string
                      contact_email: string
                      created_at: 0
                      updated_at: 0
                      state: string
                      app_state: string
                      stocks:
                        - market: string
                          name: string
                          name_en: string
                          code: string
                          amount: string
                          counter_id: string
                          currency: string
                      email_direct: false
                      user_email: string
                      process_at: 0
                      website: string
                      can_modify: 0
                  total: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Transfer in records
      x-mint:
        href: /en/api-reference/rollover_record_v3
        metadata:
          sidebarTitle: Transfer in records
  /v1/whaleapi/transfer/transfer_out_detail:
    get:
      operationId: transfer_out_detail
      summary: Stock transfer out detail
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: integer
          description: id
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Info
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Stock transfer out detail
      x-mint:
        href: /en/api-reference/transfer_out_detail
        metadata:
          sidebarTitle: Stock transfer out detail
  /v1/whaleapi/transfer/transfer_out_records:
    get:
      operationId: transfer_out_records_v2
      summary: Stock transfer out records
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
        - name: market
          in: query
          required: false
          schema:
            type: string
          description: market
        - name: state
          in: query
          required: false
          schema:
            type: integer
          description: state
        - name: start
          in: query
          required: false
          schema:
            type: integer
          description: start timestamp
        - name: end
          in: query
          required: false
          schema:
            type: integer
          description: end timestamp
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: page index
        - name: size
          in: query
          required: false
          schema:
            type: integer
          description: page size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      records:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: integer
                              description: id
                            broker_id:
                              type: integer
                              description: broker id
                            name_en:
                              type: string
                              description: name in English
                            code:
                              type: string
                              description: broker code
                            contact:
                              type: string
                              description: contact
                            contact_phone:
                              type: string
                              description: contact phone
                            contact_email:
                              type: string
                              description: contact email
                            account:
                              type: string
                              description: account
                            state:
                              type: string
                              description: state
                            stocks:
                              type: array
                              items:
                                type: object
                                properties:
                                  market:
                                    type: string
                                    description: market
                                  name:
                                    type: string
                                    description: stock name
                                  name_en:
                                    type: string
                                    description: stock name in English
                                  code:
                                    type: string
                                    description: stock code
                                  amount:
                                    type: string
                                    description: amount
                                  counter_id:
                                    type: string
                                    description: counter id
                              description: stock list
                            cancel_at:
                              type: integer
                              description: cancel timestamp
                            created_at:
                              type: integer
                              description: created timestamp
                            updated_at:
                              type: integer
                              description: updated timestamp
                            broker:
                              type: string
                              description: broker name
                            name:
                              type: string
                              description: name
                        description: records list
              example:
                code: 0
                message: success
                data:
                  records:
                    - id: 0
                      broker_id: 0
                      name_en: string
                      code: string
                      contact: string
                      contact_phone: string
                      contact_email: string
                      account: string
                      state: string
                      stocks:
                        - market: string
                          name: string
                          name_en: string
                          code: string
                          amount: string
                          counter_id: string
                      cancel_at: 0
                      created_at: 0
                      updated_at: 0
                      broker: string
                      name: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Stock transfer out records
      x-mint:
        href: /en/api-reference/transfer_out_records_v2
        metadata:
          sidebarTitle: Stock transfer out records
  /v2/whaleapi/portfolio/withdraw:
    post:
      operationId: withdraw_apply_v5_v2
      summary: Submit Withdrawal Applylication
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                currency:
                  type: string
                  description: Currency
                amount:
                  type: string
                  description: Amount
                card_id:
                  type: string
                  description: User BankCard Id
                confirm:
                  type: string
                  description: Risk control parameters
                is_pad_funded:
                  type: string
                  description: Is Pad-funded option values:[1 pad funds, do not need to be redeemed (pure CASH case also returns this type) 2 pad funds, to go through the redemption process]
                account_no:
                  type: string
                  description: Account No
                external_order_no:
                  type: string
                  description: External Order No
              required:
                - currency
                - amount
                - card_id
                - is_pad_funded
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                        description: Is submit success\
                      tip_url:
                        type: string
                        description: Risk control URL
                      id:
                        type: string
                        description: Order Id
                    required:
                      - id
              example:
                code: 0
                message: success
                data:
                  ok: false
                  tip_url: string
                  id: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Submit Withdrawal Applylication
      x-mint:
        href: /en/api-reference/withdraw_apply_v5_v2
        metadata:
          sidebarTitle: Submit Withdrawal Applylication
  /v1/whaleapi/portfolio/withdraw/cancel:
    post:
      operationId: withdraw_cancel
      summary: Cancel Withdraw Application
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                order_id:
                  type: string
                  description: Order No
                account_no:
                  type: string
                  description: Account No
              required:
                - order_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                        description: Is Success
                    required:
                      - ok
              example:
                code: 0
                message: success
                data:
                  ok: false
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Cancel Withdraw Application
      x-mint:
        href: /en/api-reference/withdraw_cancel
        metadata:
          sidebarTitle: Cancel Withdraw Application
  /v1/whaleapi/portfolio/withdraw/detail:
    get:
      operationId: withdraw_detail_v5
      summary: Withdraw Detail
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: Order Id
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      id:
                        type: number
                        description: Order Id
                      currency:
                        type: string
                        description: Currency
                      amount:
                        type: string
                        description: Amount
                      order_id:
                        type: string
                        description: Order No
                      name:
                        type: string
                        description: User Name
                      bank:
                        type: string
                        description: Bank Name
                      bank_en:
                        type: string
                        description: Bank Name
                      account:
                        type: string
                        description: User Bank Account
                      swift_code:
                        type: string
                        description: Swift Code
                      created_at:
                        type: number
                        description: Create Time
                      updated_at:
                        type: number
                        description: Last Updated Time
                      state:
                        type: number
                        description: State
                      admin_remark:
                        type: string
                        description: Remark
                      account_channel:
                        type: string
                        description: Account Channel
                      fee:
                        type: string
                        description: Fee
                      settlement_amount:
                        type: string
                        description: Actual receivable amount
                      apply_type:
                        type: string
                        description: Withdraw Apply Type
                      external_order_no:
                        type: string
                        description: External Order No
              example:
                code: 0
                message: success
                data:
                  id: 0
                  currency: string
                  amount: string
                  order_id: string
                  name: string
                  bank: string
                  bank_en: string
                  account: string
                  swift_code: string
                  created_at: 0
                  updated_at: 0
                  state: 0
                  admin_remark: string
                  account_channel: string
                  fee: string
                  settlement_amount: string
                  apply_type: string
                  external_order_no: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Withdraw Detail
      x-mint:
        href: /en/api-reference/withdraw_detail_v5
        metadata:
          sidebarTitle: Withdraw Detail
  /v2/whaleapi/portfolio/withdraw/pre:
    get:
      operationId: withdraw_pre_v3_v2
      summary: Information acquisition before withdrawal v3
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: currency
          in: query
          required: true
          schema:
            type: string
          description: Currency
        - name: card_id
          in: query
          required: false
          schema:
            type: integer
          description: "User bank card ID. May be left blank; when blank, `tip_time` is not returned."
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  cash_amount:
                    type: string
                    description: Cash Amount(cash portion)
                  max_amount:
                    type: string
                    description: "Maximum amount that can be withdrawn (including Balance Pass)."
                  mmf_amount:
                    type: string
                    description: Mmf Amount(If a Balance Pass is used, the corresponding amount in that currency)
                  is_pad_funded:
                    type: string
                    description: Is Pad-fundedoption values:[1 cushion, no need to redeem (this type is also returned in the case of pure CASH) 2 No cushion, to go through the redemption process]
                  cash_list:
                    type: array
                    items:
                      type: object
                      properties:
                        currency:
                          type: string
                          description: Currency
                        amount:
                          type: string
                          description: Amount
                    description: Amount Info List
                  tip_time:
                    type: string
                    description: Estimated time of receipt
                  mmf_time:
                    type: string
                    description: Estimated time of when use Mmf
                  currency_tip:
                    type: boolean
                    description: Whether a withdraw prompt
              example:
                cash_amount: string
                max_amount: string
                mmf_amount: string
                is_pad_funded: string
                cash_list:
                  - currency: string
                    amount: string
                tip_time: string
                mmf_time: string
                currency_tip: false
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Information acquisition before withdrawal v3
      x-mint:
        href: /en/api-reference/withdraw_pre_v3_v2
        metadata:
          sidebarTitle: Information acquisition before withdrawal v3
  /v1/whaleapi/portfolio/withdraw/record:
    get:
      operationId: withdraw_record_v5
      summary: Withdraw Records
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: size
          in: query
          required: false
          schema:
            type: integer
          description: Page Size
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: Page No
        - name: begin
          in: query
          required: false
          schema:
            type: integer
          description: Query Begin Time
        - name: end
          in: query
          required: false
          schema:
            type: integer
          description: Query End Time
        - name: states
          in: query
          required: false
          schema:
            type: integer
          description: Query States
        - name: currencys
          in: query
          required: false
          schema:
            type: string
          description: Currency List
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: number
                    description: code
                  msg:
                    type: string
                    description: msg
                  datas:
                    type: object
                    properties:
                      total:
                        type: number
                        description: Total Size
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: number
                              description: Order Id
                            currency:
                              type: string
                              description: Currency
                            amount:
                              type: string
                              description: Amount
                            order_id:
                              type: string
                              description: Order No
                            created_at:
                              type: number
                              description: Create Time
                            state:
                              type: number
                              description: State
                            bank_account:
                              type: string
                              description: User Bank Account
                            fee:
                              type: string
                              description: Withdraw Fee
                            settlement_amount:
                              type: string
                              description: Actual receivable amount
                        description: list
                    description: datas
              example:
                code: 0
                msg: string
                datas:
                  total: 0
                  list:
                    - id: 0
                      currency: string
                      amount: string
                      order_id: string
                      created_at: 0
                      state: 0
                      bank_account: string
                      fee: string
                      settlement_amount: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Withdraw Records
      x-mint:
        href: /en/api-reference/withdraw_record_v5
        metadata:
          sidebarTitle: Withdraw Records
  /v1/whaleapi/portfolio/withdraw/card:
    post:
      operationId: create_withdraw_card_v2
      summary: Add Bank Card
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bank_id:
                  type: number
                  description: region bank id
                region:
                  type: string
                  description: Region
                bank_en:
                  type: string
                  description: Bank name ,maximum of 30 characters
                name:
                  type: string
                  description: User Name
                name_en:
                  type: string
                  description: User Name En
                account:
                  type: string
                  description: User Bank Account ,maximum of 20 characters
                swift_code:
                  type: string
                  description: swift_code-8 or 11 characters
                account_type:
                  type: string
                  description: Account Type enum:ALL,USD,HKD. Account Type enum:ALL,USD,HKD...
                country:
                  type: string
                  description: Country ,maximum of 50 characters
                address:
                  type: string
                  description: Address maximum of 50 characters
                remark:
                  type: string
                  description: Remark
                bank_code:
                  type: string
                  description: Bank Code
                need_certification:
                  type: boolean
                  description: Need Certification
                fps_id:
                  type: string
                  description: Fps Id
                account_no:
                  type: string
                  description: Account No
              required:
                - bank_id
                - region
                - bank_en
                - name
                - name_en
                - account
                - swift_code
                - account_type
                - country
                - need_certification
                - fps_id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Add Bank Card
      x-mint:
        href: /en/api-reference/create_withdraw_card_v2
        metadata:
          sidebarTitle: Add Bank Card
  /v2/whaleapi/portfolio/withdraw/card:
    delete:
      operationId: delete_withdraw_card_v2
      summary: Delete Bank Card
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
                  description: Bank card id
                account_no:
                  type: string
                  description: The account number which need to delete bank card
              required:
                - id
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Delete Bank Card
      x-mint:
        href: /en/api-reference/delete_withdraw_card_v2
        metadata:
          sidebarTitle: Delete Bank Card
  /v1/whaleapi/portfolio/withdraw/cards:
    get:
      operationId: get_withdraw_card_v2
      summary: Get User Bank Card List
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: withdraw
          in: query
          required: false
          schema:
            type: string
          description: Is Withdraw Card 0,1
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: Currency (USD,HKD,SGD)
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: number
                              description: User Bank Card Id
                            region:
                              type: string
                              description: "Region: `mainland`, `hongkong`, or `abroad`."
                            region_name:
                              type: string
                              description: Region Name
                            bank:
                              type: string
                              description: Bank Name
                            bank_id:
                              type: integer
                              description: bank_id
                            bank_en:
                              type: string
                              description: Bank Name
                            account:
                              type: string
                              description: User Bank Account
                            name:
                              type: string
                              description: User Name
                            icon:
                              type: string
                              description: Bank Icon
                            swift_code:
                              type: string
                              description: Bank Swift Code
                            account_type:
                              type: string
                              description: Account Type
                            country:
                              type: string
                              description: Country
                            address:
                              type: string
                              description: Address
                            remark:
                              type: string
                              description: Remark
                            bank_code:
                              type: string
                              description: Bank Code
                            logo:
                              type: string
                              description: Logo
                            left_background:
                              type: string
                              description: Lower left gradien
                            right_background:
                              type: string
                              description: Gradient to the upper right
                            witness_state:
                              type: number
                              description: "Deposit Witness Status 1: No Witness Required, 2: Witness Pending, 3: Witnessed"
                            support_witness:
                              type: number
                              description: Whether the bank supports Witness
                            need_address:
                              type: number
                              description: Whether need input address 0:no need 1:required
                            "status  ":
                              type: number
                              description: Status0 Not verified 1 In review 2 Verified
                            name_en:
                              type: string
                              description: User name
                            need_swift:
                              type: integer
                              description: If need swift code
                            need_account_type:
                              type: integer
                              description: If need account type
                            can_modify_card_account:
                              type: boolean
                              description: Can nodify bank card account
                            auth_list:
                              type: array
                              items:
                                type: object
                                properties:
                                  flow_id:
                                    type: string
                                    description: Auth flow ID
                                  status:
                                    type: integer
                                    description: State
                                  name:
                                    type: string
                                    description: Name
                                  message:
                                    type: string
                                    description: Error message
                                  custom:
                                    type: string
                                    description: Different flow_id values return different parameters. When flow_id=4, FPSID is returned. When flow_id=3, a list of supported currencies is returned separated by commas. Different flow_id values return different parameters. When flow_id=4, FPSID is returned. When flow_id=3, a list of supported currencies is returned separated by commas
                              description: auth_list
                        description: list
                      last_funding_currency:
                        type: string
                        description: Last deposit currency
                      last_funding_type:
                        type: integer
                        description: Last deposit type
                      last_bank_card_id:
                        type: integer
                        description: Last deposit card id
                    required:
                      - list
                      - last_funding_currency
                      - last_funding_type
                      - last_bank_card_id
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: 0
                      region: string
                      region_name: string
                      bank: string
                      bank_id: 0
                      bank_en: string
                      account: string
                      name: string
                      icon: string
                      swift_code: string
                      account_type: string
                      country: string
                      address: string
                      remark: string
                      bank_code: string
                      logo: string
                      left_background: string
                      right_background: string
                      witness_state: 0
                      support_witness: 0
                      need_address: 0
                      "status  ": 0
                      name_en: string
                      need_swift: 0
                      need_account_type: 0
                      can_modify_card_account: false
                      auth_list:
                        - flow_id: string
                          status: 0
                          name: string
                          message: string
                          custom: string
                  last_funding_currency: string
                  last_funding_type: 0
                  last_bank_card_id: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get User Bank Card List
      x-mint:
        href: /en/api-reference/get_withdraw_card_v2
        metadata:
          sidebarTitle: Get User Bank Card List
  /v1/whaleapi/portfolio/withdraw/banks:
    get:
      operationId: region_bank_v5
      summary: Region Bank
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: currency
          in: query
          required: true
          schema:
            type: string
          description: "Currency. If not provided, defaults to HKD. This parameter is mainly used to obtain the corresponding tags for banking, securities, eDDA, and FPS payments for a particular currency. In Hong Kong, bank lists are sorted by tag count and tag priority; the currency only affects the ordering of the bank list in the Hong Kong region."
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            value:
                              type: array
                              items:
                                type: object
                                properties:
                                  id:
                                    type: number
                                    description: ID
                                  name:
                                    type: string
                                    description: Bank name
                                  name_en:
                                    type: string
                                    description: English bank name
                                  swift_code:
                                    type: string
                                    description: Swift code
                                  icon:
                                    type: string
                                    description: Bank icon url
                                  logo:
                                    type: string
                                    description: Bank logo url
                                  left_background:
                                    type: string
                                    description: Bottom-left gradient color
                                  right_background:
                                    type: string
                                    description: Top-right gradient color
                                  bank_code:
                                    type: string
                                    description: Bank Code
                                  support_witness:
                                    type: string
                                    description: support deposit witness 0:not support 1:support
                                  account_length:
                                    type: string
                                    description: Maximum length limit of bank card
                                  auth_flows:
                                    type: array
                                    items:
                                      type: object
                                      properties:
                                        HKD:
                                          type: object
                                          properties:
                                            Ids:
                                              type: array
                                              items:
                                                type: string
                                              description: authFlowId
                                          required:
                                            - Ids
                                          description: currency HKD
                                        USD:
                                          type: object
                                          properties: {}
                                          description: Currency USD
                                    description: Support auth list
                              description: value
                            region:
                              type: string
                              description: Region
                            region_name:
                              type: string
                              description: Region name
                            bankcode_state:
                              type: number
                              description: "bankcode state, 0: optional |1: required"
                            need_address:
                              type: integer
                              description: need address 0:no need 1:required
                            required_address:
                              type: integer
                              description: Is the address a required field 0:no need 1:required
                            need_swift:
                              type: integer
                              description: if the swift code is required:0:no need 1:required
                            need_account_type:
                              type: integer
                              description: if the account type is required:0:no need 1:required
                            icon:
                              type: string
                              description: icon url
                            tip:
                              type: string
                              description: tip
                            tags:
                              type: string
                              description: tags
                        description: Bank List
                      mainland_card_flag:
                        type: string
                        description: Whether the mainland bank card is bound
                    required:
                      - list
                      - mainland_card_flag
              example:
                code: 0
                message: success
                data:
                  list:
                    - value:
                        - id: 0
                          name: string
                          name_en: string
                          swift_code: string
                          icon: string
                          logo: string
                          left_background: string
                          right_background: string
                          bank_code: string
                          support_witness: string
                          account_length: string
                          auth_flows:
                            - HKD:
                                Ids:
                                  - string
                              USD: {}
                      region: string
                      region_name: string
                      bankcode_state: 0
                      need_address: 0
                      required_address: 0
                      need_swift: 0
                      need_account_type: 0
                      icon: string
                      tip: string
                      tags: string
                  mainland_card_flag: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Region Bank
      x-mint:
        href: /en/api-reference/region_bank_v5
        metadata:
          sidebarTitle: Region Bank
  /v1/whaleapi/portfolio/withdraw/audit_card:
    post:
      operationId: submit_for_review_withdraw_card
      summary: Submit Files For Review Withdraw Card
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: integer
                  description: Card Id
                account_no:
                  type: string
                  description: Account No
                urls:
                  type: array
                  items:
                    type: string
                  description: File Urls
              required:
                - id
                - account_no
                - urls
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      ok:
                        type: boolean
                        description: ok
                      id:
                        type: integer
                        description: id
                    required:
                      - ok
                      - id
              example:
                code: 0
                message: success
                data:
                  ok: false
                  id: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Submit Files For Review Withdraw Card
      x-mint:
        href: /en/api-reference/submit_for_review_withdraw_card
        metadata:
          sidebarTitle: Submit Files For Review Withdraw Card
  /v1/whaleapi/portfolio/withdraw/card/:id:
    put:
      operationId: update_withdraw_card_v2
      summary: Update Bank Card
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bank_id:
                  type: number
                  description: Region Bank Id
                region:
                  type: string
                  description: Region
                bank_en:
                  type: number
                  description: Bank Name
                name:
                  type: string
                  description: User Name
                name_en:
                  type: string
                  description: User English Name
                account:
                  type: string
                  description: User Bank Account
                swift_code:
                  type: string
                  description: Swift Code
                account_type:
                  type: string
                  description: Account Type
                country:
                  type: string
                  description: Country
                address:
                  type: string
                  description: Address
                remark:
                  type: string
                  description: Remark
                bank_code:
                  type: string
                  description: Bank Code
                need_certification:
                  type: boolean
                  description: Need Certfication
                fps_id:
                  type: string
                  description: Fps Id
                account_no:
                  type: string
                  description: Account No
              required:
                - bank_id
                - region
                - bank_en
                - name_en
                - account
                - account_type
                - country
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties: {}
              example:
                code: 0
                message: success
                data: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Update Bank Card
      x-mint:
        href: /en/api-reference/update_withdraw_card_v2
        metadata:
          sidebarTitle: Update Bank Card
  /v1/whaleapi/portfolio/withdraw/card_detail:
    get:
      operationId: withdraw_card_detail_v2
      summary: User Card Detail
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
          description: User Bank Card Id
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: number
                    description: User Bank Card Id
                  region:
                    type: string
                    description: "Region: `mainland`, `hongkong`, or `abroad`."
                  region_name:
                    type: string
                    description: Region Name
                  bank:
                    type: string
                    description: Bank Name
                  bank_en:
                    type: string
                    description: Bank Name
                  account:
                    type: string
                    description: User Bank Account
                  name:
                    type: string
                    description: User Name
                  icon:
                    type: string
                    description: Bank Icon
                  swift_code:
                    type: string
                    description: Bank Swift Code
                  account_type:
                    type: string
                    description: Account Type
                  country:
                    type: string
                    description: Country
                  address:
                    type: string
                    description: Address
                  remark:
                    type: string
                    description: Remark
                  bank_code:
                    type: string
                    description: Bank Code
                  edda_status:
                    type: string
                    description: Edda Auth State
                  bank_sec_status:
                    type: string
                    description: BankSec Auth State
                  out_cash_auth_status:
                    type: string
                    description: Card Audit State
                  edda_enable:
                    type: string
                    description: Whether the bank supports EDDA
                  bank_sec_enable:
                    type: string
                    description: Whether the bank supports BankSec
                  logo:
                    type: string
                    description: Bank Logo
                  left_background:
                    type: string
                    description: Lower left gradien
                  right_background:
                    type: string
                    description: Gradient to the upper right
                  last_funding:
                    type: number
                    description: Is Last Withdraw Card
                  last_funding_type:
                    type: string
                    description: Last Withdraw Type
                  last_funding_currency:
                    type: string
                    description: Last Withdraw Currency
                  witness_state:
                    type: string
                    description: Witness State
                  support_witness:
                    type: string
                    description: Whether the bank supports Witness
                  fps_id:
                    type: string
                    description: Fps Id
                  support_fps:
                    type: string
                    description: Whether the bank supports FPS
                  need_address:
                    type: string
                    description: Whether need input address
                  bank_sec_support_currencies:
                    type: array
                    items:
                      type: string
                    description: Bank Sec Support Currencies
                  status:
                    type: number
                    description: State0 Not verified 1 In review 2 Verified
              example:
                id: 0
                region: string
                region_name: string
                bank: string
                bank_en: string
                account: string
                name: string
                icon: string
                swift_code: string
                account_type: string
                country: string
                address: string
                remark: string
                bank_code: string
                edda_status: string
                bank_sec_status: string
                out_cash_auth_status: string
                edda_enable: string
                bank_sec_enable: string
                logo: string
                left_background: string
                right_background: string
                last_funding: 0
                last_funding_type: string
                last_funding_currency: string
                witness_state: string
                support_witness: string
                fps_id: string
                support_fps: string
                need_address: string
                bank_sec_support_currencies:
                  - string
                status: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: User Card Detail
      x-mint:
        href: /en/api-reference/withdraw_card_detail_v2
        metadata:
          sidebarTitle: User Card Detail
  /v1/whaleapi/portfolio/converts:
    get:
      operationId: convert_history
      summary: Exchange records
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: page
        - name: size
          in: query
          required: false
          schema:
            type: integer
          description: size
        - name: status
          in: query
          required: false
          schema:
            type: string
          description: status
        - name: target_currencys
          in: query
          required: false
          schema:
            type: string
          description: search target currency array
        - name: source_currencys
          in: query
          required: false
          schema:
            type: string
          description: search source currency array
        - name: begin
          in: query
          required: false
          schema:
            type: integer
          description: search  begin time
        - name: end
          in: query
          required: false
          schema:
            type: integer
          description: search  end time
        - name: account_no
          in: query
          required: false
          schema:
            type: string
          description: Account No
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            convert_effective:
                              type: boolean
                              description: "`true`: settled | `false`: succeeded"
                            created_at:
                              type: string
                              description: Created time
                            amount:
                              type: string
                              description: Exchange amount
                            updated_at:
                              type: string
                              description: Updated time
                            order_id:
                              type: string
                              description: Updated time
                            direction:
                              type: object
                              properties: {}
                              description: Exchange from currency
                            estimate_time:
                              type: string
                              description: Estimate time
                            to:
                              type: string
                              description: Exchange target currency
                            state:
                              type: number
                              description: state
                            id:
                              type: number
                              description: id
                            admin_remark:
                              type: string
                              description: Remark
                            from:
                              type: string
                              description: Exchange from currency
                            estimate_amount:
                              type: string
                              description: Estimate amount
                          required:
                            - convert_effective
                            - created_at
                            - amount
                            - updated_at
                            - order_id
                            - direction
                            - estimate_time
                            - to
                            - state
                            - id
                            - admin_remark
                            - from
                            - estimate_amount
                        description: list
                    required:
                      - list
              example:
                code: 0
                message: success
                data:
                  list:
                    - convert_effective: false
                      created_at: string
                      amount: string
                      updated_at: string
                      order_id: string
                      direction: {}
                      estimate_time: string
                      to: string
                      state: 0
                      id: 0
                      admin_remark: string
                      from: string
                      estimate_amount: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Exchange records
      x-mint:
        href: /en/api-reference/convert_history
        metadata:
          sidebarTitle: Exchange records
  /v1/whaleapi/portfolio/forex/getexchangerates:
    get:
      operationId: get_exchange_rates_v2
      summary: Get Exchange
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      universal_exchanges:
                        type: object
                        properties:
                          pspl_sg:
                            type: object
                            properties:
                              spec_exchanges:
                                type: object
                                properties:
                                  am:
                                    type: object
                                    properties:
                                      exchanges:
                                        type: array
                                        items:
                                          type: object
                                          properties:
                                            base_currency:
                                              type: string
                                              description: base_currency
                                            other_currency:
                                              type: string
                                              description: other_currency
                                            bid_rate:
                                              type: number
                                              description: bid_rate
                                            offer_rate:
                                              type: number
                                              description: offer_rate
                                            average_rate:
                                              type: number
                                              description: average_rate
                                        description: exchanges
                                    description: am
                                  am-reference:
                                    type: object
                                    properties:
                                      exchanges:
                                        type: array
                                        items:
                                          type: object
                                          properties:
                                            base_currency:
                                              type: string
                                              description: base_currency
                                            other_currency:
                                              type: string
                                              description: other_currency
                                            bid_rate:
                                              type: number
                                              description: bid_rate
                                            offer_rate:
                                              type: number
                                              description: offer_rate
                                            average_rate:
                                              type: number
                                              description: average_rate
                                        description: exchanges
                                    description: am-reference
                                description: spec_exchanges
                            description: pspl_sg
                        description: universal_exchanges
              example:
                code: 0
                message: success
                data:
                  universal_exchanges:
                    pspl_sg:
                      spec_exchanges:
                        am:
                          exchanges:
                            - base_currency: string
                              other_currency: string
                              bid_rate: 0
                              offer_rate: 0
                              average_rate: 0
                        am-reference:
                          exchanges:
                            - base_currency: string
                              other_currency: string
                              bid_rate: 0
                              offer_rate: 0
                              average_rate: 0
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get Exchange
      x-mint:
        href: /en/api-reference/get_exchange_rates_v2
        metadata:
          sidebarTitle: Get Exchange
  /v1/whaleapi/portfolio/currency/list:
    get:
      operationId: query_all_currency_configs
      summary: Currency List
      tags:
        - Cash Management
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: number
                              description: id
                            currency:
                              type: string
                              description: Currency
                            currency_iso:
                              type: string
                              description: Currency ISO
                            icon:
                              type: string
                              description: Currency Icon
                            logo:
                              type: string
                              description: logo url
                            abbreviation_multi_name:
                              type: string
                              description: Abbreviation Name
                            multi_name:
                              type: string
                              description: Name
                            min_exchange_amount:
                              type: string
                              description: Min Exchange Amount
                            "min_withdrawal_amount ":
                              type: string
                              description: Min Withdraw Amount
                            exchange_rate_precision:
                              type: number
                              description: Exchange Rate Precision
                            amount_precision:
                              type: number
                              description: Amount Precision
                            amount_round_mode:
                              type: string
                              description: "Amount rounding mode: `up` (round up), `down` (round down), or `truncate`."
                            json_config:
                              type: string
                              description: support feature collction
                        description: list
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: 0
                      currency: string
                      currency_iso: string
                      icon: string
                      logo: string
                      abbreviation_multi_name: string
                      multi_name: string
                      min_exchange_amount: string
                      "min_withdrawal_amount ": string
                      exchange_rate_precision: 0
                      amount_precision: 0
                      amount_round_mode: string
                      json_config: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Currency List
      x-mint:
        href: /en/api-reference/query_all_currency_configs
        metadata:
          sidebarTitle: Currency List
  /v2/whaleapi/asset/frozen_cash:
    post:
      operationId: esop_cash_settlement_lock_v2
      summary: Account Cash Balance Freeze
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                currency:
                  type: string
                  description: Currency Eg:HKD
                frozen_amount:
                  type: string
                  description: Frozen Amount
                remark:
                  type: string
                  description: Remark
                biz_code:
                  type: string
                  description: Business Code
                serial_num:
                  type: string
                  description: Serial Number
                request_id:
                  type: string
                  description: Idempotency ID ,keep Global Unique
                require_risk_check:
                  type: boolean
                  description: Need Risk Check
              required:
                - account_no
                - currency
                - frozen_amount
                - biz_code
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      msg:
                        type: string
                        description: msg
                      serial_num:
                        type: string
                        description: Serial Number
                    required:
                      - msg
                      - serial_num
              example:
                code: 0
                message: success
                data:
                  msg: success
                  serial_num: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to freeze cash in a specified securities account. By providing the account number account_no, currency currency, freeze amount frozen_amount, and business code biz_code, it locks the corresponding amount from the available balance and returns a business serial number serial_num. The interface supports a risk control check toggle require_risk_check and idempotency control request_id. Frozen funds must be subsequently processed through the unfreeze or unfreeze-debit interface.
      x-mint:
        href: /en/api-reference/esop_cash_settlement_lock_v2
        metadata:
          sidebarTitle: Account Cash Balance Freeze
  /v2/whaleapi/asset/unfrozen_cash:
    post:
      operationId: esop_cash_settlement_unlock_debit_v2
      summary: Account Cash Balance Unfreeze
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                currency:
                  type: string
                  description: Currency Eg:HKD
                unfrozen_amount:
                  type: string
                  description: Unfrozen Amount
                remark:
                  type: string
                  description: Remark
                biz_code:
                  type: string
                  description: Business Code
                serial_num:
                  type: string
                  description: Serial Number
                request_id:
                  type: string
                  description: Idempotency ID ,keep Global Unique
              required:
                - account_no
                - currency
                - unfrozen_amount
                - biz_code
                - serial_num
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      msg:
                        type: string
                        description: msg
                      serial_num:
                        type: string
                        description: Serial Number
                    required:
                      - msg
                      - serial_num
              example:
                code: 0
                message: success
                data:
                  msg: success
                  serial_num: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to unfreeze previously frozen cash in a specified securities account. By providing the account number account_no, currency currency, unfreeze amount unfrozen_amount, original freeze serial number serial_num, and business code biz_code, it releases the corresponding frozen funds back to the available balance.
      x-mint:
        href: /en/api-reference/esop_cash_settlement_unlock_debit_v2
        metadata:
          sidebarTitle: Account Cash Balance Unfreeze
  /v1/whaleapi/asset/unfrozen_debit:
    post:
      operationId: esop_un_frozen_and_adjust_cash
      summary: Account Cash Balance Unfreeze Debit
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                serial_num:
                  type: string
                  description: Serial Number
                request_id:
                  type: string
                  description: Idempotency ID ,keep Global Unique
                biz_code:
                  type: string
                  description: Business Code
              required:
                - account_no
                - serial_num
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      msg:
                        type: string
                        description: Message
                      serial_num:
                        type: string
                        description: Serial No
                      error_code:
                        type: string
                        description: Error Code
                    required:
                      - msg
                      - error_code
              example:
                code: 0
                message: success
                data:
                  msg: success
                  serial_num: string
                  error_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to unfreeze and directly debit the frozen cash of a specified securities account. By providing the account number account_no and the original freeze serial number serial_num, it deducts the frozen funds directly rather than releasing them back to the available balance, with idempotency guaranteed via request_id. Unlike the unfreeze interface, this interface is suitable for scenarios where frozen funds need to be directly consumed after final business confirmation.
      x-mint:
        href: /en/api-reference/esop_un_frozen_and_adjust_cash
        metadata:
          sidebarTitle: Account Cash Balance Unfreeze Debit
  /v2/whaleapi/asset/temp_adjust:
    post:
      operationId: temporary_adjustment_whale_v2
      summary: Temporary Adjustment
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                security_type:
                  type: string
                  description: security_type
                op_user:
                  type: string
                  description: op_user
                cash_adjusts:
                  type: object
                  properties:
                    currency:
                      type: string
                      description: currency
                    direction:
                      type: string
                      description: "`CREDIT` or `DEBIT`"
                    incurred_amount:
                      type: string
                      description: "Amount"
                    biz_code:
                      type: string
                      description: biz_code
                    remark:
                      type: string
                      description: remark
                    expire_time:
                      type: integer
                      description: expire_time
                    account_no:
                      type: string
                      description: account_no
                  required:
                    - currency
                    - remark
                    - biz_code
                    - incurred_amount
                    - direction
                    - expire_time
                    - account_no
                  description: cash_adjusts
                stock_adjusts:
                  type: object
                  properties:
                    Symbol:
                      type: string
                      description: Symbol
                    direction:
                      type: string
                      description: "`CREDIT` or `DEBIT`"
                    incurred_quantity:
                      type: string
                      description: incurred_quantity
                    biz_code:
                      type: string
                      description: biz_code
                    remark:
                      type: string
                      description: remark
                    currency:
                      type: string
                      description: currency
                    cost_price:
                      type: string
                      description: cost_price
                    expire_time:
                      type: integer
                      description: expire_time
                    position_side:
                      type: string
                      description: LONG, SHORT
                    account_no:
                      type: string
                      description: account_no
                  required:
                    - Symbol
                    - cost_price
                    - currency
                    - remark
                    - biz_code
                    - incurred_quantity
                    - direction
                    - expire_time
                    - position_side
                    - account_no
                  description: stock_adjusts
              required:
                - security_type
                - op_user
                - cash_adjusts
                - stock_adjusts
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      adjust_no:
                        type: string
                        description: adjust_no
                      code:
                        type: string
                        description: code
                      message:
                        type: string
                        description: message
                    required:
                      - adjust_no
                      - message
                      - code
              example:
                code: 0
                message: success
                data:
                  adjust_no: string
                  code: string
                  message: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to perform temporary asset adjustments for a specified securities account, supporting both cash and stock asset types. By providing the security type security_type along with the corresponding cash adjustment details cash_adjusts or stock adjustment details stock_adjusts, it temporarily increases or decreases assets for the account, with an automatic expiration time configurable via expire_time.
      x-mint:
        href: /en/api-reference/temporary_adjustment_whale_v2
        metadata:
          sidebarTitle: Temporary Adjustment
  /v2/whaleapi/asset/adjust_cash:
    post:
      operationId: esop_cash_adjustment_v2
      summary: Account Cash Balance Adjustment
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                direction:
                  type: number
                  description: Adjust Direction
                currency:
                  type: string
                  description: Currency
                amount:
                  type: string
                  description: Adjust Amount
                biz_code:
                  type: string
                  description: Business Code
                remark:
                  type: string
                  description: Adjust Remark
                serial_num:
                  type: string
                  description: Serial Num
                request_id:
                  type: string
                  description: Idempotency ID ,keep Global Unique
                require_risk_check:
                  type: boolean
                  description: Need Risk Check
              required:
                - account_no
                - direction
                - currency
                - amount
                - biz_code
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      msg:
                        type: string
                        description: Message
                      serial_num:
                        type: string
                        description: Serial Number
                      error_code:
                        type: string
                        description: Error Code
                    required:
                      - msg
                      - serial_num
                      - error_code
              example:
                code: 0
                message: success
                data:
                  msg: success
                  serial_num: string
                  error_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to perform a manual cash adjustment for a specified securities account. By providing the account number account_no, adjustment direction direction (credit or debit), currency currency, amount amount, and business code biz_code, it completes the manual increase or decrease of funds and returns a business serial number serial_num. The interface supports a risk control check toggle require_risk_check and idempotency control request_id to prevent duplicate adjustments.
      x-mint:
        href: /en/api-reference/esop_cash_adjustment_v2
        metadata:
          sidebarTitle: Account Cash Balance Adjustment
  /v2/whaleapi/asset/adjust_stock:
    post:
      operationId: esop_stock_adjustment_v2
      summary: Account Position Adjustment
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                direction:
                  type: number
                  description: Adjust Direction
                market:
                  type: string
                  description: Market
                symbol:
                  type: string
                  description: Stock Information
                quantity:
                  type: string
                  description: Adjust Quantity
                biz_code:
                  type: string
                  description: Business Code
                remark:
                  type: string
                  description: Adjust Remark
                request_id:
                  type: string
                  description: Idempotency ID ,keep Global Unique
              required:
                - account_no
                - direction
                - market
                - symbol
                - quantity
                - biz_code
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      msg:
                        type: string
                        description: Message
                      serial_num:
                        type: string
                        description: Serial Number
                      error_code:
                        type: string
                        description: Error Code
                    required:
                      - msg
                      - serial_num
                      - error_code
              example:
                code: 0
                message: success
                data:
                  msg: success
                  serial_num: string
                  error_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to perform a manual stock position adjustment for a specified securities account, currently supporting stock assets only. By providing the account number account_no, adjustment direction direction (credit or debit), market market, stock symbol symbol, quantity quantity, and business code biz_code, it completes the manual increase or decrease of positions and returns a business serial number serial_num. The interface supports idempotency control via request_id.
      x-mint:
        href: /en/api-reference/esop_stock_adjustment_v2
        metadata:
          sidebarTitle: Account Position Adjustment
  /v1/whaleapi/account/open_info:
    get:
      operationId: get_account_info_by_account_no
      summary: Retrieve Account Application Information
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      application_id:
                        type: string
                        description: Account opening application ID
                      client_type:
                        type: string
                        description: Account Open subject type
                      broker_code:
                        type: string
                        description: Broker account
                      account_type:
                        type: string
                        description: Account type
                      account_state:
                        type: object
                        properties: {}
                        description: Account state
                      client_name:
                        type: string
                        description: Client Name
                      contact:
                        type: object
                        properties:
                          phones:
                            type: array
                            items:
                              type: object
                              properties:
                                country_code:
                                  type: string
                                  description: Phone Country Code
                                mobile:
                                  type: string
                                  description: Phone Number
                                contact_type:
                                  type: integer
                                  description: contact_type
                              required:
                                - country_code
                                - mobile
                                - contact_type
                            description: Phone list
                          emails:
                            type: array
                            items:
                              type: object
                              properties:
                                email:
                                  type: string
                                  description: Email
                              required:
                                - email
                            description: Email list
                        required:
                          - phones
                          - emails
                        description: Contact list
                      open_info:
                        type: object
                        properties:
                          base:
                            type: object
                            properties:
                              passport_name:
                                type: string
                                description: Name of master card (Pinyin)
                            required:
                              - passport_name
                            description: base
                        required:
                          - base
                        description: open_info
                    required:
                      - application_id
                      - client_type
                      - broker_code
                      - account_type
                      - account_state
                      - client_name
                      - contact
                      - open_info
              example:
                code: 0
                message: success
                data:
                  application_id: string
                  client_type: string
                  broker_code: string
                  account_type: string
                  account_state: {}
                  client_name: string
                  contact:
                    phones:
                      - country_code: string
                        mobile: string
                        contact_type: 0
                    emails:
                      - email: string
                  open_info:
                    base:
                      passport_name: ZHANG SAN
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the account opening application information for a specified securities account. By providing the account number account_no, it returns the complete account opening details, including the application ID, client type, account type, account status, client name, contact information (phone and email list), as well as the full account opening form data such as document upload info, personal base info, employment info, financial info, compliance info, risk info, identity verification info, and signature info.
      x-mint:
        href: /en/api-reference/get_account_info_by_account_no
        metadata:
          sidebarTitle: Retrieve Account Application Information
  /v1/whaleapi/account/account_risk_and_pi_info:
    get:
      operationId: query_account_risk_and_pi_info
      summary: Query Account Risk And Pi Info
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      pi_info:
                        type: object
                        properties:
                          is_pi:
                            type: boolean
                            description: is_pi
                          pi_expire_time:
                            type: integer
                            description: "Expiration time"
                        description: pi_info
                      rpq_info:
                        type: object
                        properties:
                          risk_level:
                            type: string
                            description: risk_level
                          score:
                            type: integer
                            description: score
                          product:
                            type: string
                            description: product
                        description: rpq_info
              example:
                code: 0
                message: success
                data:
                  pi_info:
                    is_pi: false
                    pi_expire_time: 0
                  rpq_info:
                    risk_level: string
                    score: 0
                    product: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the risk assessment information and Professional Investor (PI) certification details for a specified securities account. By providing the account number account_no, it returns whether the user is a professional investor is_pi, the PI expiration time, as well as the risk assessment level (A1-A5), assessment score, and applicable product type.
      x-mint:
        href: /en/api-reference/query_account_risk_and_pi_info
        metadata:
          sidebarTitle: Query Account Risk And Pi Info
  /v1/whaleapi/member/update_pi_status:
    post:
      operationId: update_member_pi_status
      description: Updates a member's PI (Professional Investor) status.
      summary: Update PI Status
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                member_id:
                  type: integer
                  description: member_id
                pi_status:
                  type: string
                  description: pi_status
              required:
                - member_id
                - pi_status
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      x-mint:
        href: /en/api-reference/update_member_pi_status
        metadata:
          sidebarTitle: Update PI Status
  /v1/whaleapi/account/append_account:
    post:
      operationId: whale_append_account
      summary: Open additional account
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
                additional_account:
                  type: object
                  properties:
                    additional_type:
                      type: string
                      description: Additional Type
                    account_type:
                      type: string
                      description: Account Type, Allowed empty when append sub account
                    account_name:
                      type: string
                      description: Custom Account Name
                  required:
                    - additional_type
                  description: Additional Account Info
              required:
                - account_no
                - additional_account
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      application_id:
                        type: string
                        description: Account Request ID
                      account_type:
                        type: string
                        description: Account Type
                    required:
                      - application_id
                      - account_type
              example:
                code: 0
                message: success
                data:
                  application_id: string
                  account_type: C
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: "This interface is used to open an additional primary or sub-account for a specified securities account. By providing the account number account_no, additional account type (M: Primary Account / S: Sub Account), account type (C: Cash Account / M: Margin Account, optional when opening a sub-account), and a custom account name, it initiates an additional account opening application and returns the application ID application_id and account type."
      x-mint:
        href: /en/api-reference/whale_append_account
        metadata:
          sidebarTitle: Open additional account
  /v1/whaleapi/account/change_account_state:
    post:
      operationId: whale_change_account_state
      summary: Update Account State/Disable Account
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
                to_state:
                  type: string
                  description: "Target account state; changes the account state (for example, to `active`)"
              required:
                - account_no
                - to_state
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: "This interface is used to update the status of a specified securities account. By providing the account number account_no and the target state to_state, the account can be switched to the specified status, supporting three operations: activate (active), suspend (suspend), and disable (disable)."
      x-mint:
        href: /en/api-reference/whale_change_account_state
        metadata:
          sidebarTitle: Update Account State/Disable Account
  /v1/whaleapi/member/change_mobile:
    post:
      operationId: whale_change_mobile
      summary: Change Member Mobile Phone
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                phone:
                  type: object
                  properties:
                    country_code:
                      type: integer
                      description: country_code
                    mobile:
                      type: string
                      description: mobile
                  required:
                    - country_code
                    - mobile
                  description: phone
                new_phone:
                  type: object
                  properties:
                    country_code:
                      type: integer
                      description: country_code
                    mobile:
                      type: string
                      description: mobile
                  required:
                    - country_code
                    - mobile
                  description: new_phone
              required:
                - phone
                - new_phone
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: string
                    description: 0
                  message:
                    type: string
                    description: message
              example:
                code: string
                message: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Change Member Mobile Phone
      x-mint:
        href: /en/api-reference/whale_change_mobile
        metadata:
          sidebarTitle: Change Member Mobile Phone
  /v1/whaleapi/account/close:
    post:
      operationId: whale_close_account
      summary: Close Account
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
              required:
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to cancel a specified securities account. By passing the securities account number (account_no), the account can be canceled. Certain prerequisites must be met before cancellation (such as zero account assets and no pending orders). If these conditions are not met, the corresponding error code (such as 404062, 404092, 404093, etc.) will be returned. **This operation is irreversible; please use it with caution.**
      x-mint:
        href: /en/api-reference/whale_close_account
        metadata:
          sidebarTitle: Close Account
  /v1/whaleapi/account/get:
    post:
      operationId: whale_get_account
      summary: Get Account Status
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account Number
              required:
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      account:
                        type: object
                        properties:
                          account_no:
                            type: string
                            description: account_no
                          state:
                            type: integer
                            description: state
                          disable:
                            type: boolean
                            description: disable
                          account_type:
                            type: string
                            description: account_type
                        required:
                          - account_no
                          - state
                          - disable
                          - account_type
                        description: account
                    required:
                      - account
              example:
                code: 0
                message: success
                data:
                  account:
                    account_no: string
                    state: 0
                    disable: false
                    account_type: M
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the current status of a specified securities account. By providing the account number account_no, it returns the account number, account state , account freeze status , and account type.
      x-mint:
        href: /en/api-reference/whale_get_account
        metadata:
          sidebarTitle: Get Account Status
  /v1/whaleapi/account/contact_info:
    get:
      operationId: whale_get_account_contact
      summary: Get Account Contact Info
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: member_id
          in: query
          required: true
          schema:
            type: string
          description: member ID
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      contact:
                        type: object
                        properties:
                          phones:
                            type: array
                            items:
                              type: object
                              properties:
                                country_code:
                                  type: integer
                                  description: country_code
                                mobile:
                                  type: string
                                  description: mobile
                                defaulted:
                                  type: object
                                  properties: {}
                                  description: defaulted
                                updated_at:
                                  type: integer
                                  description: updated_at
                              required:
                                - country_code
                                - mobile
                                - defaulted
                                - updated_at
                            description: phones
                          emails:
                            type: array
                            items:
                              type: object
                              properties:
                                email:
                                  type: string
                                  description: email
                                defaulted:
                                  type: string
                                  description: defaulted
                                updated_at:
                                  type: integer
                                  description: updated_at
                              required:
                                - email
                                - defaulted
                                - updated_at
                            description: emails
                        required:
                          - phones
                          - emails
                        description: contact
                    required:
                      - contact
              example:
                code: 0
                message: success
                data:
                  contact:
                    phones:
                      - country_code: 0
                        mobile: string
                        defaulted: {}
                        updated_at: 0
                    emails:
                      - email: string
                        defaulted: string
                        updated_at: 0
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the contact information associated with a specified user's account. By providing the user ID member_id, it returns the user's phone number list (nd email list .
      x-mint:
        href: /en/api-reference/whale_get_account_contact
        metadata:
          sidebarTitle: Get Account Contact Info
  /v1/whaleapi/account/trade_permissions:
    get:
      operationId: whale_get_account_permissions
      summary: Get Account Trade Permission
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Response data.
                    properties:
                      permissions:
                        type: array
                        items:
                          type: object
                          properties:
                            product:
                              type: string
                              description: "Account permission type. Common value: `stock`"
                            markets:
                              type: array
                              items:
                                type: string
                                description: "Markets covered by the permission, for example `HK` and `US`"
                              description: markets
                          required:
                            - product
                            - markets
                        description: permissions
                    required:
                      - permissions
              example:
                code: 0
                message: success
                data:
                  permissions:
                    - product: string
                      markets:
                        - string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: "This interface is used to retrieve the trading permissions enabled for a specified securities account. By providing the account number account_no, it returns the list of enabled permissions, where each record includes the permission type (e.g., stock: equities / va: virtual assets) and the list of supported markets (e.g., HK, US)."
      x-mint:
        href: /en/api-reference/whale_get_account_permissions
        metadata:
          sidebarTitle: Get Account Trade Permission
  /v1/whaleapi/account/get_application:
    post:
      operationId: whale_get_application
      summary: get account opening application information
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                application_id:
                  type: string
                  description: application id
              required:
                - application_id
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      application_id:
                        type: string
                        description: application_id
                      member_id:
                        type: string
                        description: member_id
                      account_no:
                        type: string
                        description: account_no
                      client_type:
                        type: integer
                        description: client_type
                      submitted_at:
                        type: string
                        description: "Account-opening application submission time"
                      state:
                        type: integer
                        description: state
                      reason:
                        type: string
                        description: reason
                      open_info:
                        type: object
                        properties:
                          upload:
                            type: object
                            properties:
                              ic_front_assets_id:
                                type: string
                                description: ic_front_assets_id
                              ic_back_assets_id:
                                type: string
                                description: ic_back_assets_id
                              region:
                                type: string
                                description: region
                              card_type:
                                type: integer
                                description: card_type
                              card_country:
                                type: string
                                description: card_country
                              valid_date_end:
                                type: string
                                description: valid_date_end
                              first_name:
                                type: string
                                description: first_name
                              last_name:
                                type: string
                                description: last_name
                              card_id:
                                type: object
                                properties: {}
                                description: card_id
                              ethnicity:
                                type: string
                                description: ethnicity
                            required:
                              - ic_front_assets_id
                              - ic_back_assets_id
                              - region
                              - card_type
                              - card_country
                              - valid_date_end
                              - first_name
                              - last_name
                              - card_id
                            description: upload
                          base:
                            type: object
                            properties:
                              country_code:
                                type: string
                                description: country_code
                              name:
                                type: string
                                description: name
                              first_name:
                                type: string
                                description: first_name
                              last_name:
                                type: string
                                description: last_name
                              passport_name:
                                type: string
                                description: "Name on the primary identity document"
                              passport_no:
                                type: string
                                description: passport_no
                              gender:
                                type: string
                                description: gender
                              education:
                                type: string
                                description: education
                              birth_date:
                                type: string
                                description: birth_date
                              marital_status:
                                type: string
                                description: "Marital status: `single`, `married`, or `other` (for example, divorced or widowed)"
                              residence_country:
                                type: string
                                description: residence_country
                              residence_part:
                                type: string
                                description: residence_part
                              residence_desc:
                                type: string
                                description: residence_desc
                              residence_assets_id:
                                type: string
                                description: residence_assets_id
                            required:
                              - country_code
                              - name
                              - first_name
                              - last_name
                              - passport_name
                              - passport_no
                              - gender
                              - education
                              - birth_date
                              - marital_status
                              - residence_country
                              - residence_part
                              - residence_desc
                              - residence_assets_id
                            description: base
                          employ:
                            type: object
                            properties:
                              type:
                                type: string
                                description: type
                              experience:
                                type: string
                                description: experience
                              business_type:
                                type: string
                                description: business_type
                              occupation:
                                type: string
                                description: occupation
                              company:
                                type: string
                                description: company
                              address_country:
                                type: string
                                description: address_country
                              address_part:
                                type: string
                                description: address_part
                              address_desc:
                                type: string
                                description: address_desc
                              years:
                                type: string
                                description: years
                              email:
                                type: string
                                description: email
                              taxes:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    country:
                                      type: string
                                      description: country
                                    id:
                                      type: string
                                      description: id
                                  required:
                                    - country
                                    - id
                                description: taxes
                            required:
                              - type
                              - experience
                              - business_type
                              - occupation
                              - company
                              - address_country
                              - address_part
                              - address_desc
                              - years
                              - email
                              - taxes
                            description: employ
                          finance:
                            type: object
                            properties:
                              income_level:
                                type: string
                                description: income_level
                              estimated:
                                type: string
                                description: estimated
                              funds_source:
                                type: string
                                description: funds_source
                              funds_source_other:
                                type: string
                                description: "Source of funds"
                              wealth_source:
                                type: string
                                description: wealth_source
                              invest:
                                type: object
                                properties:
                                  purpose:
                                    type: string
                                    description: purpose
                                  risk_tolerance:
                                    type: string
                                    description: risk_tolerance
                                  trade_times:
                                    type: string
                                    description: trade_times
                                  stock:
                                    type: string
                                    description: Stock. Stock.
                                  option:
                                    type: string
                                    description: option
                                  stock_taking:
                                    type: string
                                    description: stock_taking
                                  derivatives_intention:
                                    type: string
                                    description: derivatives_intention
                                  derivatives:
                                    type: boolean
                                    description: "Derivatives trading experience"
                                  bullion:
                                    type: string
                                    description: bullion
                                  structured:
                                    type: string
                                    description: structured
                                required:
                                  - purpose
                                  - risk_tolerance
                                  - trade_times
                                  - stock
                                  - option
                                  - stock_taking
                                  - derivatives_intention
                                  - derivatives
                                  - bullion
                                  - structured
                                description: invest
                            required:
                              - income_level
                              - estimated
                              - funds_source
                              - funds_source_other
                              - wealth_source
                              - invest
                            description: finance
                          risk:
                            type: object
                            properties:
                              confirm:
                                type: boolean
                                description: confirm
                            required:
                              - confirm
                            description: risk
                        required:
                          - upload
                          - base
                          - employ
                          - finance
                          - risk
                        description: open_info
                    required:
                      - application_id
                      - member_id
                      - account_no
                      - client_type
                      - submitted_at
                      - state
                      - reason
                      - open_info
              example:
                code: 0
                message: success
                data:
                  application_id: string
                  member_id: string
                  account_no: string
                  client_type: 0
                  submitted_at: string
                  state: 0
                  reason: string
                  open_info:
                    upload:
                      ic_front_assets_id: string
                      ic_back_assets_id: string
                      region: string
                      card_type: 0
                      card_country: string
                      valid_date_end: string
                      first_name: string
                      last_name: string
                      card_id: {}
                      ethnicity: 汉
                    base:
                      country_code: string
                      name: string
                      first_name: string
                      last_name: string
                      passport_name: string
                      passport_no: string
                      gender: string
                      education: string
                      birth_date: string
                      marital_status: string
                      residence_country: string
                      residence_part: string
                      residence_desc: string
                      residence_assets_id: string
                    employ:
                      type: string
                      experience: string
                      business_type: string
                      occupation: string
                      company: string
                      address_country: string
                      address_part: string
                      address_desc: string
                      years: string
                      email: string
                      taxes:
                        - country: string
                          id: string
                    finance:
                      income_level: string
                      estimated: string
                      funds_source: string
                      funds_source_other: string
                      wealth_source: string
                      invest:
                        purpose: string
                        risk_tolerance: string
                        trade_times: string
                        stock: string
                        option: string
                        stock_taking: string
                        derivatives_intention: string
                        derivatives: false
                        bullion: string
                        structured: string
                    risk:
                      confirm: false
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the detailed information of a specified account opening application. By providing the application ID application_id, it returns the complete application data, including the application ID, user ID, account number, client type, submission time, opening status, rejection reason, and the full account opening form details such as document upload info, personal base info, employment info, financial info, and risk info.
      x-mint:
        href: /en/api-reference/whale_get_application
        metadata:
          sidebarTitle: get account opening application information
  /v1/whaleapi/account/bcan_info:
    get:
      operationId: whale_get_bcan_info_by_account_no
      summary: Get Account Bcan Information
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Response data.
                    properties:
                      ce_no:
                        type: string
                        description: ce_no
                      bcan_no:
                        type: string
                        description: bcan_no
                      is_agree:
                        type: boolean
                        description: is_agree
                      hk_is_agree:
                        type: boolean
                        description: hk_is_agree
                      state:
                        type: integer
                        description: state
                      hk_state:
                        type: integer
                        description: hk_state
                    required:
                      - ce_no
                      - bcan_no
                      - is_agree
                      - hk_is_agree
                      - state
                      - hk_state
              example:
                code: 0
                message: success
                data:
                  ce_no: string
                  bcan_no: string
                  is_agree: false
                  hk_is_agree: false
                  state: 0
                  hk_state: 0
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the BCAN (Broker-to-Client Assigned Number) related information for a specified securities account. By providing the account number account_no, it returns the central number ce_no, BCAN code bcan_no, consent status for China Connect, consent status for HKIDR, and the effective status of both.
      x-mint:
        href: /en/api-reference/whale_get_bcan_info_by_account_no
        metadata:
          sidebarTitle: Get Account Bcan Information
  /v1/whaleapi/member/get_member_by_email:
    post:
      operationId: whale_get_member_by_email
      summary: Get Member Information by Login Email
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  description: Login Email
              required:
                - email
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      member:
                        type: object
                        properties:
                          member_id:
                            type: string
                            description: Member ID
                          email_verified:
                            type: boolean
                            description: Login email verification status
                        required:
                          - member_id
                          - email_verified
                        description: Member info
                    required:
                      - member
              example:
                code: 0
                message: success
                data:
                  member:
                    member_id: "1"
                    email_verified: false
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get Member Information by Login Email
      x-mint:
        href: /en/api-reference/whale_get_member_by_email
        metadata:
          sidebarTitle: Get Member Information by Login Email
  /v1/whaleapi/member/get_member_by_phone:
    post:
      operationId: whale_get_member_by_phone
      summary: Get Member Information by Mobile Phone
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                phone:
                  type: object
                  properties:
                    country_code:
                      type: integer
                      description: country_code
                    mobile:
                      type: string
                      description: "Mobile number"
                  required:
                    - country_code
                    - mobile
                  description: phone
              required:
                - phone
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      member:
                        type: object
                        properties:
                          member_id:
                            type: integer
                            description: member_id
                          trade_password_set:
                            type: boolean
                            description: trade_password_set
                        required:
                          - member_id
                          - trade_password_set
                        description: member
                    required:
                      - member
              example:
                code: 0
                message: success
                data:
                  member:
                    member_id: 0
                    trade_password_set: false
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Get Member Information by Mobile Phone
      x-mint:
        href: /en/api-reference/whale_get_member_by_phone
        metadata:
          sidebarTitle: Get Member Information by Mobile Phone
  /v1/whaleapi/account_requests:
    get:
      operationId: whale_get_requests_by_member_id
      summary: Get Account Request List
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: member_id
          in: query
          required: true
          schema:
            type: string
          description: member ID
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: id
                            state:
                              type: string
                              description: state
                            account_channel:
                              type: string
                              description: account_channel
                          required:
                            - id
                            - state
                            - account_channel
                        description: list
                    required:
                      - list
              example:
                code: 0
                message: success
                data:
                  list:
                    - id: string
                      state: string
                      account_channel: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the list of account opening application records for a specified user. By providing the optional user ID member_id, it returns all account opening application information including the application ID, application state, and account channel.
      x-mint:
        href: /en/api-reference/whale_get_requests_by_member_id
        metadata:
          sidebarTitle: Get Account Request List
  /v2/whaleapi/account/list:
    get:
      operationId: whale_list_account_v2
      summary: Query Account List V2
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_id
          in: query
          required: true
          schema:
            type: string
          description: Pagination ID.
        - name: page_size
          in: query
          required: true
          schema:
            type: string
          description: Number of records per page. Defaults to 10; maximum 500.
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Response data.
                    properties:
                      next_account_id:
                        type: string
                        description: next_account_id
                      list:
                        type: object
                        properties:
                          member_id:
                            type: string
                            description: member_id
                          account_no:
                            type: string
                            description: account_no
                          open_account_time:
                            type: string
                            description: open_account_time
                        required:
                          - member_id
                          - account_no
                          - open_account_time
                        description: list
                    required:
                      - next_account_id
                      - list
              example:
                code: 0
                message: success
                data:
                  next_account_id: string
                  list:
                    member_id: string
                    account_no: string
                    open_account_time: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the account list, supporting cursor-based pagination for batch retrieval of account lists. By providing the pagination ID account_id and the page size page_size (default 10, maximum 500), it returns account list information along with the next page cursor next_account_id.
      x-mint:
        href: /en/api-reference/whale_list_account_v2
        metadata:
          sidebarTitle: Query Account List V2
  /v2/whaleapi/account/submit_application:
    post:
      operationId: whale_open_account_v2
      summary: Submit Application V2
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                member_id:
                  type: string
                  description: member ID
                client_type:
                  type: integer
                  description: client type
                open_info:
                  type: object
                  properties:
                    account_no:
                      type: string
                      description: account number
                    mark:
                      type: string
                      description: account mark
                    broker_code:
                      type: string
                      description: broker code
                    upload:
                      type: object
                      properties:
                        card_country:
                          type: string
                          description: certificate issuing location
                        card_type:
                          type: object
                          properties: {}
                          description: card type
                        card_id:
                          type: string
                          description: card number
                        first_name:
                          type: string
                          description: first name
                        last_name:
                          type: string
                          description: last name
                      required:
                        - card_country
                        - card_type
                        - card_id
                        - first_name
                      description: upload info
                    base:
                      type: object
                      properties:
                        country_code:
                          type: string
                          description: enterprise registration location
                        residence_country:
                          type: string
                          description: residence country
                        residence_part:
                          type: string
                          description: residence part
                        residence_desc:
                          type: string
                          description: residence desc
                      required:
                        - country_code
                        - residence_country
                        - residence_desc
                      description: base info
                    employ:
                      type: object
                      properties:
                        contact:
                          type: object
                          properties:
                            emails:
                              type: array
                              items:
                                type: object
                                properties:
                                  email:
                                    type: string
                                    description: contact email
                                required:
                                  - email
                              description: contact email list
                            phones:
                              type: array
                              items:
                                type: object
                                properties:
                                  country_code:
                                    type: integer
                                    description: country_code
                                  mobile:
                                    type: string
                                    description: mobile
                                  contact_type:
                                    type: integer
                                    description: contact_type
                                  type:
                                    type: integer
                                    description: type
                                required:
                                  - country_code
                                  - mobile
                                  - contact_type
                                  - type
                              description: phones
                          required:
                            - emails
                            - phones
                          description: contact
                        taxes:
                          type: array
                          items:
                            type: object
                            properties:
                              country:
                                type: string
                                description: tax country
                              id:
                                type: string
                                description: tax id
                            required:
                              - country
                          description: taxs info
                      description: employ info
                    finance:
                      type: object
                      properties:
                        account_type:
                          type: string
                          description: account type
                      required:
                        - account_type
                      description: finance info
                    risk:
                      type: object
                      properties:
                        risk_tolerance:
                          type: object
                          properties:
                            audit:
                              type: boolean
                              description: risk assessment status
                            end_at:
                              type: integer
                              description: risk assessment time
                          required:
                            - audit
                            - end_at
                          description: risk assessment info
                      required:
                        - risk_tolerance
                      description: risk info
                  required:
                    - upload
                    - base
                    - finance
                  description: open account form
              required:
                - member_id
                - client_type
                - open_info
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      application_id:
                        type: string
                        description: application_id
                    required:
                      - application_id
              example:
                code: 0
                message: success
                data:
                  application_id: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is an upgraded version of the account opening application submission, supporting both individual and enterprise account opening with a more flexible and comprehensive data structure. By providing the user ID member_id, client type client_type  and account opening form data (including document info, base info, contact info, tax info, account type, and risk assessment info), it creates an account opening application and returns the application ID application_id.
      x-mint:
        href: /en/api-reference/whale_open_account_v2
        metadata:
          sidebarTitle: Submit Application V2
  /v1/whaleapi/account/record_agreement:
    post:
      operationId: whale_record_agreement
      summary: Whale Record Agreement
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: type
                agreements_info:
                  type: array
                  items:
                    type: object
                    properties:
                      key:
                        type: string
                        description: key
                      version:
                        type: string
                        description: version
                      signed:
                        type: string
                        description: signed
                    required:
                      - key
                      - version
                      - signed
                  description: agreements_info
                member_id:
                  type: integer
                  description: member ID
                platform:
                  type: string
                  description: platform
                remark:
                  type: string
                  description: remark
              required:
                - type
                - agreements_info
                - member_id
                - platform
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to synchronize and record the signing status of specified agreements for a user. By providing the agreement type type, agreement information list (including agreement name, version, and signing status), user ID member_id, signing method platform, and remarks, it synchronizes the user's agreement signing status to the platform.
      x-mint:
        href: /en/api-reference/whale_record_agreement
        metadata:
          sidebarTitle: Whale Record Agreement
  /v1/whaleapi/account/update_account_trade_permission:
    post:
      operationId: whale_update_account_trade_permission
      summary: Update Account Trade Permission
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: account_no
                changes:
                  type: array
                  items:
                    type: object
                    properties:
                      permission_type:
                        type: string
                        description: Permission type to update, e.g. `otc.US`, `stock.HK`.
                      permission_status:
                        type: string
                        description: Target permission status, e.g. `close` or `open`.
                    required:
                      - permission_type
                      - permission_status
                  description: changes
              required:
                - account_no
                - changes
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: {}
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: |-
        ### PermissionChange

        # WhaleUpdateAccountTradePermission

        ## Overview

        After a successful account opening, Whale can update one or multiple trade permissions of an account in a single call via `(account_no, [changes...])` — covering account-level permissions, market trading, and trade channels (the risk-disclosure questionnaire category is **not** included in this iteration). This bypasses back-office ticket approval and accelerates the "HK/US trading available right after account opening" client experience.

        Multiple changes share **all-or-nothing** semantics — the entire batch is written atomically: either every change succeeds, or none takes effect. There is no half-applied state.

        ## Request Parameters

        ### Top-level Fields

        | Field | Type | Required | Description |
        |-------|------|----------|-------------|
        | `account_no` | string | Yes | Securities account number, mapped to `accounts.origin_id` |
        | `changes` | PermissionChange[] | Yes | List of permission changes, N ≥ 1 |

        ### PermissionChange

        | Field | Type | Required | Description |
        |-------|------|----------|-------------|
        | `permission_type` | string | Yes | Format: `{product}.{market}`, e.g. `stock.US`; see the table below for valid values |
        | `permission_status` | string | Yes | Only `open` / `close` accepted, case-sensitive |

        ## Valid `permission_type` Values

        > The table below lists the **full set supported at the code level**. The actually available subset is determined by the SaaS Config of `sg_mexc`; the entire `flag.*` group is rejected in this iteration.

        ### Market Codes

        | Code | Meaning |
        |------|---------|
        | `HK` | Hong Kong stocks |
        | `US` | US stocks |
        | `SH` | Shanghai A-shares |
        | `SZ` | Shenzhen A-shares |
        | `SG` | Singapore |
        | `JP` | Japan |
        | `UK` | United Kingdom |
        | `DE` | Germany |
        | `AU` | Australia |

        ### Account-level Permissions `account.*`

        | permission_type | Meaning |
        |-----------------|---------|
        | `account.trade` | Account-level trading switch |
        | `account.exchange` | Currency exchange |
        | `account.rollover` | Position transfer |
        | `account.withdraw` | Withdrawal |
        | `account.deposit` | Deposit |
        | `account.public_funds` | Public mutual funds |
        | `account.private_funds` | Private funds |
        | `account.otc` | OTC market |
        | `account.balance_funds` | Balance funds (cash sweep) |

        ### Market Trading Permissions

        `{MARKET}` is one of the values from "Market Codes" above. For example, `stock.HK` denotes the Hong Kong equity market.

        | permission_type | Meaning |
        |-----------------|---------|
        | `stock.{MARKET}` | Equity market (regular buy/sell) |
        | `options.{MARKET}` | Options |
        | `shortsell.{MARKET}` | Short selling / margin shorting |
        | `ipo.{MARKET}` | IPO subscription |
        | `warrant.{MARKET}` | Warrants / CBBC |
        | `stockgrey.{MARKET}` | Grey market |
        | `otc.{MARKET}` | OTC trading |

        ### Virtual Assets `va.*`

        | permission_type | Meaning |
        |-----------------|---------|
        | `va.OSL` | OSL channel |
        | `va.HAS` | HashKey channel |

        ### Trade Channels `trade.*`

        | permission_type | Meaning |
        |-----------------|---------|
        | `trade.client` | Online trading |
        | `trade.bo` | Back-office (counter) trading |
        | `trade.fix` | FIX channel |
        | `trade.openapi` | Open API |

        ### Not Supported in This Iteration

        Any call referencing the `flag.*` group (`flag.options` / `flag.warrant` / `flag.shortsell` ...) returns an "invalid enum" error and is not persisted.

        ## Response Parameters

        | Field | Type | Description |
        |-------|------|-------------|
        | `failure_index` | int32 | When pre-validation fails fast, this is the index (0-based) of the offending change in the `changes` array. Unset (zero value) in other cases. |

        ### `failure_index` Across Scenarios

        | Scenario | failure_index |
        |----------|---------------|
        | All succeed | Unset (zero value) |
        | Pre-validation failure — invalid enum | Index of the offending change |
        | Pre-validation failure — invalid `permission_status` | Index of the offending change |
        | Pre-validation failure — no actual state change | Index of the offending change |
        | Account-level error — account not found / deregistered | Unset (zero value) |
        | Write-phase failure — DB error, etc. | Unset (zero value) |
      x-mint:
        href: /en/api-reference/whale_update_account_trade_permission
        metadata:
          sidebarTitle: Update Account Trade Permission
  /v2/whaleapi/account/update_application:
    post:
      operationId: whale_update_application_v2
      summary: Update Application V2
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                application_id:
                  type: string
                  description: application id
                member_id:
                  type: string
                  description: member ID
                client_type:
                  type: integer
                  description: client type
                open_info:
                  type: object
                  properties:
                    upload:
                      type: object
                      properties:
                        card_country:
                          type: string
                          description: certificate issuing location
                        card_type:
                          type: integer
                          description: card type
                        card_id:
                          type: string
                          description: card number
                        first_name:
                          type: string
                          description: first name
                        last_name:
                          type: string
                          description: last name
                      required:
                        - card_country
                        - card_type
                        - card_id
                        - last_name
                      description: upload info
                    base:
                      type: object
                      properties:
                        country_code:
                          type: string
                          description: enterprise registration location
                        residence_country:
                          type: string
                          description: residence country
                        residence_part:
                          type: string
                          description: residence part
                        residence_desc:
                          type: string
                          description: residence desc
                      required:
                        - country_code
                        - residence_country
                        - residence_desc
                      description: base info
                    employ:
                      type: object
                      properties:
                        taxes:
                          type: object
                          properties:
                            country:
                              type: string
                              description: tax country
                            id:
                              type: string
                              description: tax id
                          required:
                            - country
                            - id
                          description: taxs info
                      required:
                        - taxes
                      description: employ info
                  required:
                    - upload
                    - base
                    - employ
                  description: open account form
              required:
                - member_id
                - client_type
                - open_info
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: code
                  message:
                    type: string
                    description: message
              example:
                code: 0
                message: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is an upgraded version of the account opening data update, supporting modifications for both individual and enterprise accounts. By providing the application ID application_id, user ID member_id, client type client_type, and the fields to be updated (document info, base info, tax info, etc.), it performs an update on the account opening form data.
      x-mint:
        href: /en/api-reference/whale_update_application_v2
        metadata:
          sidebarTitle: Update Application V2
  /v1/whaleapi/member/change_email:
    post:
      operationId: whale_update_email
      summary: Change Member Login Email
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                member_id:
                  type: string
                  description: Member ID
                new_email:
                  type: string
                  description: New Email
              required:
                - member_id
                - new_email
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: code
                  message:
                    type: string
                    description: message
              example:
                code: 0
                message: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: Change Member Login Email
      x-mint:
        href: /en/api-reference/whale_update_email
        metadata:
          sidebarTitle: Change Member Login Email
  /v1/whaleapi/asset/financial_flows:
    post:
      operationId: esop_financial_flows
      summary: Account Cash Transaction History
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                currency:
                  type: string
                  description: Currency
                financial_flows_direction:
                  type: number
                  description: Transaction Direction
                start_time:
                  type: number
                  description: Start Time
                end_time:
                  type: number
                  description: End Time
                page:
                  type: number
                  description: Page
                size:
                  type: number
                  description: The Number Of  Items Per Page
              required:
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      financial_flows_list:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_name:
                              type: string
                              description: Transaction Name
                            amount:
                              type: string
                              description: Change Amount
                            currency:
                              type: string
                              description: Currency
                            bussiness_time:
                              type: string
                              description: Business Time
                            description:
                              type: string
                              description: Remark
                            direction:
                              type: number
                              description: Transaction Direction
                            serial_num:
                              type: string
                              description: Serial Number
                            bussiness_code:
                              type: string
                              description: Business Code
                          required:
                            - flow_name
                            - amount
                            - currency
                            - bussiness_time
                            - description
                            - direction
                            - serial_num
                            - bussiness_code
                        description: Stock Transaction List
                    required:
                      - financial_flows_list
              example:
                code: 0
                message: success
                data:
                  financial_flows_list:
                    - flow_name: ADR收费
                      amount: "-20.00"
                      currency: HKD
                      bussiness_time: "1678696290"
                      direction: 1
                      serial_num: string
                      bussiness_code: string
                      description: ADR_FEE 01810.HK
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the cash flow records for a specified securities account. By providing the account number account_no, it supports filtering by currency currency, transaction direction financial_flows_direction, and time range, returning a paginated list of matching cash flow records. Each record includes the flow name, amount change, currency, transaction time, and business code.
      x-mint:
        href: /en/api-reference/esop_financial_flows
        metadata:
          sidebarTitle: Account Cash Transaction History
  /v1/whaleapi/asset/financial_frozen_flows:
    get:
      operationId: esop_financial_frozen_flows
      summary: Account Cash Freeze Transaction History
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account NO Eg:L6VQEU00121996
        - name: frozen_status
          in: query
          required: false
          schema:
            type: string
          description: Frozen Status 0 - All 1 - frozee  2 - unfrozee Eg:0
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: Currency Eg:HKD
        - name: start_time
          in: query
          required: false
          schema:
            type: string
          description: Start Time
        - name: end_time
          in: query
          required: false
          schema:
            type: string
          description: End Time
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page
        - name: size
          in: query
          required: false
          schema:
            type: string
          description: The Number Of Page Size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      frozen_flows_list:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_name:
                              type: string
                              description: Transaction Name
                            amount:
                              type: string
                              description: Frozen/UnFrozen Amount
                            currency:
                              type: string
                              description: Currency
                            bussiness_time:
                              type: string
                              description: Business Time
                            serial_num:
                              type: string
                              description: Serial Number
                            frozen_type:
                              type: number
                              description: Frozen Type
                            bussiness_code:
                              type: string
                              description: Business Code
                          required:
                            - flow_name
                            - amount
                            - currency
                            - bussiness_time
                            - serial_num
                            - frozen_type
                            - bussiness_code
                        description: Frozen Transaction History List
                    required:
                      - frozen_flows_list
              example:
                code: 0
                message: success
                data:
                  frozen_flows_list:
                    - flow_name: 股票买入成交
                      amount: "1000"
                      currency: US
                      bussiness_time: "1678694508"
                      serial_num: string
                      frozen_type: 2
                      bussiness_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the cash freeze and unfreeze flow records for a specified securities account. By providing the account number account_no, it supports filtering by currency currency, freeze status frozen_status, and time range, returning a paginated list of matching freeze flow records. Each record includes the freeze type (freeze or unfreeze), amount change, transaction time, and business code.
      x-mint:
        href: /en/api-reference/esop_financial_frozen_flows
        metadata:
          sidebarTitle: Account Cash Freeze Transaction History
  /v1/whaleapi/asset/detail_info:
    post:
      operationId: esop_get_account_asset_info
      summary: Query Account Total Asset
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: account
                currency:
                  type: string
                  description: "currency: HKD, USD"
              required:
                - account_no
                - currency
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Response data.
                    properties:
                      total_asset:
                        type: string
                        description: Total Asset
                      total_cash:
                        type: string
                        description: Total Cash Amount
                      total_mv:
                        type: string
                        description: Total Market Value
                      mv_list:
                        type: array
                        items:
                          type: object
                          properties:
                            currency:
                              type: string
                              description: Currency
                            mv:
                              type: string
                              description: Market Value
                            market:
                              type: string
                              description: Market
                          required:
                            - currency
                            - mv
                            - market
                        description: mv_list
                      margin_info:
                        type: object
                        properties:
                          im:
                            type: string
                            description: im
                          mm:
                            type: string
                            description: mm
                          fm:
                            type: string
                            description: fm
                          mcm:
                            type: string
                            description: mcm
                        required:
                          - im
                          - mcm
                          - fm
                          - mm
                        description: margin_info
                    required:
                      - total_asset
                      - total_cash
                      - total_mv
                      - mv_list
                      - margin_info
              example:
                code: 0
                message: success
                data:
                  total_asset: string
                  total_cash: string
                  total_mv: string
                  mv_list:
                    - currency: string
                      mv: string
                      market: string
                  margin_info:
                    im: string
                    mm: string
                    fm: string
                    mcm: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the total asset overview of a specified securities account. By providing the account number account_no and currency currency, it returns the total assets total_asset, total cash total_cash, and a market value list mv_list broken down by market and currency. Note that the total assets exclude funds, bonds, OTC assets, and in-transit funds.
      x-mint:
        href: /en/api-reference/esop_get_account_asset_info
        metadata:
          sidebarTitle: Query Account Total Asset
  /v1/whaleapi/asset/cash_info:
    get:
      operationId: esop_get_account_cash_info
      summary: Query Account Cash Balance
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Number
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: currency
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_cash:
                        type: string
                        description: total cash amount
                      total_available_cash:
                        type: string
                        description: total available amount
                      total_withdraw_cash:
                        type: string
                        description: total withdraw cash amount
                      cash_list:
                        type: array
                        items:
                          type: object
                          properties:
                            currency:
                              type: string
                              description: currency type
                            cash_amount:
                              type: string
                              description: cash amount
                            available_amount:
                              type: string
                              description: aviable amount
                            withdraw_amount:
                              type: string
                              description: withdrawable amount
                          required:
                            - currency
                            - cash_amount
                            - available_amount
                            - withdraw_amount
                        description: cash lish
                    required:
                      - total_cash
                      - total_available_cash
                      - total_withdraw_cash
                      - cash_list
              example:
                code: 0
                message: success
                data:
                  total_cash: "140131.0000"
                  total_available_cash: "140036.0000"
                  total_withdraw_cash: "140036.0000"
                  cash_list:
                    - currency: USD
                      cash_amount: "10000.0000"
                      available_amount: "10000.0000"
                      withdraw_amount: "10000"
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the cash balance information of a specified securities account. By providing the account number account_no and an optional currency filter currency, it returns the total cash total_cash, total available cash total_available_cash, total withdrawable cash total_withdraw_cash, and a detailed cash list cash_list broken down by currency. Unlike the full cash query interface, this interface supports currency-based filtering.
      x-mint:
        href: /en/api-reference/esop_get_account_cash_info
        metadata:
          sidebarTitle: Query Account Cash Balance
  /v1/whaleapi/asset/stock_info:
    get:
      operationId: esop_get_account_stock_info
      summary: Query Account Position
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Information
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: Currency
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      stock_list:
                        type: array
                        items:
                          type: object
                          properties:
                            symbol:
                              type: string
                              description: security underlying
                            market:
                              type: string
                              description: market
                            currency:
                              type: string
                              description: currency
                            total_quantity:
                              type: string
                              description: total position quantity
                            available_quantity:
                              type: string
                              description: avaiable position quantity
                            last_done:
                              type: string
                              description: Latest price
                            pre_close:
                              type: string
                              description: Yesterday's close
                            average_cost_price:
                              type: string
                              description: average cost price
                            diluted_cost_cost:
                              type: string
                              description: diluted cost price
                            init_quantity:
                              type: string
                              description: init quantity
                            pl_info:
                              type: object
                              properties:
                                currency:
                                  type: string
                                  description: currency
                                amount:
                                  type: string
                                  description: amount
                              description: "[Deprecated] T-1 profit and loss information"
                            buy_amount:
                              type: string
                              description: buy amount
                            sell_amount:
                              type: string
                              description: sell amount
                          required:
                            - symbol
                            - market
                            - currency
                            - total_quantity
                            - available_quantity
                            - last_done
                            - pre_close
                            - average_cost_price
                            - diluted_cost_cost
                            - init_quantity
                            - buy_amount
                            - sell_amount
                        description: position list
                    required:
                      - stock_list
              example:
                code: 0
                message: success
                data:
                  stock_list:
                    - symbol: TSLA.US
                      market: US
                      currency: USD
                      total_quantity: "3000"
                      available_quantity: "3000"
                      last_done: "202.040"
                      pre_close: "214.240"
                      average_cost_price: "0.300"
                      diluted_cost_cost: "0.300"
                      init_quantity: "13"
                      pl_info:
                        currency: string
                        amount: string
                      buy_amount: string
                      sell_amount: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the current stock holding details of a specified securities account. By providing the account number account_no and an optional currency filter currency, it returns detailed information for each holding, including position quantity, cost price, latest price, and today's buy/sell amounts. Note that this interface only returns long stock positions, excluding short positions and other asset types.
      x-mint:
        href: /en/api-reference/esop_get_account_stock_info
        metadata:
          sidebarTitle: Query Account Position
  /v1/whaleapi/asset/ob/cash_info:
    post:
      operationId: esop_get_ob_account_cash_info
      summary: Query OB Account Cash Balance
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_cash:
                        type: string
                        description: Total Cash Amount
                      total_available_cash:
                        type: string
                        description: Total Available Amount
                      total_withdraw_cash:
                        type: object
                        properties: {}
                        description: Total Withdraw Cash Amount
                      total_settled_balance:
                        type: string
                        description: Total Settled Balance
                      total_unsettled_balance:
                        type: string
                        description: Total Unsettled Balance
                      max_finance:
                        type: string
                        description: Max Finance
                      available_finance:
                        type: string
                        description: Available Balance
                      cash_list:
                        type: array
                        items:
                          type: object
                          properties:
                            currency:
                              type: string
                              description: Currency Type
                            cash_amount:
                              type: string
                              description: Cash Amount
                            available_amount:
                              type: string
                              description: Aviable Amount
                            withdraw_amount:
                              type: string
                              description: Withdrawable Amount
                            settled_balance:
                              type: string
                              description: Settled Balance
                            unsettled_balance:
                              type: string
                              description: Unsettled Balance
                          required:
                            - currency
                            - cash_amount
                            - available_amount
                            - withdraw_amount
                            - settled_balance
                            - unsettled_balance
                        description: Cash Lish
                    required:
                      - total_cash
                      - total_available_cash
                      - total_withdraw_cash
                      - total_settled_balance
                      - total_unsettled_balance
                      - max_finance
                      - available_finance
                      - cash_list
              example:
                code: 0
                message: success
                data:
                  total_cash: "140131.0000"
                  total_available_cash: "140036.0000"
                  total_withdraw_cash: {}
                  total_settled_balance: string
                  total_unsettled_balance: string
                  max_finance: string
                  available_finance: string
                  cash_list:
                    - currency: USD
                      cash_amount: "10000.0000"
                      available_amount: "10000.0000"
                      withdraw_amount: "10000"
                      settled_balance: string
                      unsettled_balance: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the detailed cash information for a specified securities account, and is a customized non-generic interface. In addition to the standard cash query results, it returns extended fields such as settled balance settled_balance, unsettled balance unsettled_balance, maximum financing limit max_finance, and available financing limit available_finance.
      x-mint:
        href: /en/api-reference/esop_get_ob_account_cash_info
        metadata:
          sidebarTitle: Query OB Account Cash Balance
  /v1/whaleapi/asset/stock_flows:
    post:
      operationId: esop_stock_flows
      summary: Account Stock Transaction History
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                account_no:
                  type: string
                  description: Account NO Eg:L6VQEU00121996
                stock_flows_direction:
                  type: number
                  description: Transaction Direction
                symbol:
                  type: string
                  description: Stock Information
                market:
                  type: string
                  description: Market
                start_time:
                  type: number
                  description: Start Time
                end_time:
                  type: number
                  description: End Time
                page:
                  type: number
                  description: Page
                size:
                  type: number
                  description: he Number Of Items Per Page
              required:
                - account_no
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      financial_flows_list:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_name:
                              type: string
                              description: Transaction Name
                            quantity:
                              type: string
                              description: Change Quantity
                            market:
                              type: string
                              description: Market
                            symbol:
                              type: string
                              description: Stock Information
                            bussiness_time:
                              type: string
                              description: Business Time
                            description:
                              type: string
                              description: Remark
                            direction:
                              type: number
                              description: Transaction Direction
                            serial_num:
                              type: string
                              description: Serial Number
                            bussiness_code:
                              type: string
                              description: Business Code
                          required:
                            - flow_name
                            - quantity
                            - market
                            - symbol
                            - bussiness_time
                            - description
                            - direction
                            - serial_num
                            - bussiness_code
                        description: Stock Transaction List
                    required:
                      - financial_flows_list
              example:
                code: 0
                message: success
                data:
                  financial_flows_list:
                    - flow_name: 股票买入成交
                      quantity: "1000"
                      market: US
                      symbol: IBM.US
                      bussiness_time: "1678694508"
                      direction: 2
                      serial_num: string
                      bussiness_code: string
                      description: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the stock position change records for a specified securities account. By providing the account number account_no, it supports filtering by stock symbol symbol, market market, transaction direction stock_flows_direction, and time range, returning a paginated list of matching stock flow records. Each record includes the flow name, quantity change, transaction time, and business code.
      x-mint:
        href: /en/api-reference/esop_stock_flows
        metadata:
          sidebarTitle: Account Stock Transaction History
  /v1/whaleapi/asset/stock_frozen_flows:
    get:
      operationId: esop_stock_frozen_flows
      summary: Account Stock Freeze Transaction History
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account NO Eg:L6VQEU00121996
        - name: frozen_status
          in: query
          required: false
          schema:
            type: string
          description: Frozen Status 0 - All 1 - frozee  2 - unfrozee Eg:0
        - name: symbol
          in: query
          required: false
          schema:
            type: string
          description: Stock Code Eg:IBM.US
        - name: market
          in: query
          required: false
          schema:
            type: string
          description: Market Information Eg:US
        - name: start_time
          in: query
          required: false
          schema:
            type: string
          description: Start Time
        - name: end_time
          in: query
          required: false
          schema:
            type: string
          description: End Time
        - name: page
          in: query
          required: false
          schema:
            type: string
          description: Page
        - name: size
          in: query
          required: false
          schema:
            type: string
          description: The Number Of Page Size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      frozen_stock_flows_list:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_name:
                              type: string
                              description: Transaction Name
                            quantity:
                              type: string
                              description: Frozen/UnFrozen Amount
                            market:
                              type: string
                              description: Market
                            symbol:
                              type: string
                              description: Symbol
                            bussiness_time:
                              type: string
                              description: Business Time
                            serial_num:
                              type: string
                              description: Serial Number
                            frozen_type:
                              type: number
                              description: Frozen Type
                            bussiness_code:
                              type: string
                              description: Business Code
                          required:
                            - flow_name
                            - quantity
                            - market
                            - symbol
                            - bussiness_time
                            - serial_num
                            - frozen_type
                            - bussiness_code
                        description: Frozen Stock Transaction List
                    required:
                      - frozen_stock_flows_list
              example:
                code: 0
                message: success
                data:
                  frozen_stock_flows_list:
                    - flow_name: 股票买入成交
                      quantity: "1000"
                      market: US
                      symbol: IBM.US
                      bussiness_time: "1678694508"
                      serial_num: string
                      frozen_type: 2
                      bussiness_code: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the stock freeze and unfreeze flow records for a specified securities account. By providing the account number account_no, it supports filtering by stock symbol symbol, market market, freeze status frozen_status, and time range, returning a paginated list of matching freeze flow records. Each record includes the freeze type (freeze or unfreeze), quantity change, transaction time, and business code.
      x-mint:
        href: /en/api-reference/esop_stock_frozen_flows
        metadata:
          sidebarTitle: Account Stock Freeze Transaction History
  /v1/whaleapi/asset/all_cash:
    get:
      operationId: get_tenant_account_cash
      summary: Get All Users Cash Balance
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
          description: page
        - name: size
          in: query
          required: false
          schema:
            type: integer
          description: size
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      list_details:
                        type: object
                        properties:
                          account_no:
                            type: string
                            description: account_no
                          total_cash:
                            type: object
                            properties:
                              map<string,string>:
                                type: string
                                description: map<string,string>
                            required:
                              - map<string,string>
                            description: total_cash
                        required:
                          - account_no
                          - total_cash
                        description: list_details
                    required:
                      - list_details
              example:
                code: 0
                message: success
                data:
                  list_details:
                    account_no: string
                    total_cash:
                      map<string,string>: string
          description: Success
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the cash balance information for all users on the platform in a paginated manner. By providing pagination parameters page and size, it returns each account and its corresponding multi-currency cash amounts.
      x-mint:
        href: /en/api-reference/get_tenant_account_cash
        metadata:
          sidebarTitle: Get All Users Cash Balance
  /v1/whaleapi/asset/overview:
    get:
      operationId: query_asset_detail_by_account_no
      summary: Query Account Total Asset OverView
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account NO Eg:L6VQEU00121996
        - name: currency
          in: query
          required: true
          schema:
            type: string
          description: "Currency, for example `HKD`."
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    description: Response data.
                    properties:
                      over_view:
                        type: object
                        properties:
                          total_asset:
                            type: string
                            description: total_asset
                          total_cash:
                            type: string
                            description: Total Cash Amount
                          mmf_amount:
                            type: string
                            description: MMF Amount
                          stock_mv:
                            type: string
                            description: Stock Market Value
                          fund_mv:
                            type: string
                            description: Fund Market Value
                          bond_mv:
                            type: string
                            description: Bond Market Value
                          otc_mv:
                            type: string
                            description: Otc Market Value
                          crypto_mv:
                            type: string
                            description: Crypto Market Value
                          currency:
                            type: string
                            description: currency
                        required:
                          - total_asset
                          - total_cash
                          - mmf_amount
                          - stock_mv
                          - fund_mv
                          - bond_mv
                          - otc_mv
                          - crypto_mv
                          - currency
                        description: Asset Over View
                      details:
                        type: object
                        properties:
                          cash_details:
                            type: array
                            items:
                              type: object
                              properties:
                                currency:
                                  type: string
                                  description: Currency
                                settled_balance:
                                  type: string
                                  description: Settled Cash
                                unsettled_balance:
                                  type: string
                                  description: UnSettled Cash
                                outstanding:
                                  type: string
                                  description: Standing Cash
                                frozen_amount:
                                  type: string
                                  description: Frozen Cash
                                available_amount:
                                  type: string
                                  description: AvailAble Cash
                                withdraw_amount:
                                  type: string
                                  description: Withdraw Cash
                              required:
                                - currency
                                - settled_balance
                                - unsettled_balance
                                - outstanding
                                - frozen_amount
                                - available_amount
                                - withdraw_amount
                            description: Cash List
                          stock_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                last_done:
                                  type: string
                                  description: Last Done
                                quantity:
                                  type: string
                                  description: Holding Quantity
                                market_value:
                                  type: string
                                  description: Market Value
                                settled_quantity:
                                  type: string
                                  description: Settled Quantity
                                unsettled_settled_quantity:
                                  type: string
                                  description: UnSettled Quantity
                                symbol_name:
                                  type: string
                                  description: Symbol Name
                              required:
                                - symbol
                                - currency
                                - last_done
                                - quantity
                                - market_value
                                - settled_quantity
                                - unsettled_settled_quantity
                                - symbol_name
                            description: Stock List
                          option_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                last_done:
                                  type: string
                                  description: Last Done
                                quantity:
                                  type: string
                                  description: Holding Quantity
                                market_value:
                                  type: object
                                  properties: {}
                                  description: Market Value
                                settled_quantity:
                                  type: string
                                  description: Settled Quantity
                                unsettled_settled_quantity:
                                  type: string
                                  description: UnSettled Quantity
                                symbol_name:
                                  type: string
                                  description: Symbol Name
                                underlying_symbol:
                                  type: string
                                  description: UnderLying Symbol
                                underlying_symbol_name:
                                  type: string
                                  description: UnderLying Symbol Name
                              required:
                                - symbol
                                - currency
                                - last_done
                                - quantity
                                - market_value
                                - settled_quantity
                                - unsettled_settled_quantity
                                - symbol_name
                                - underlying_symbol
                                - underlying_symbol_name
                            description: Option List
                          fund_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                holding_units:
                                  type: string
                                  description: Holding Units
                                current_net_value:
                                  type: string
                                  description: Net Value
                                market_value:
                                  type: string
                                  description: Market Value
                              required:
                                - symbol
                                - currency
                                - holding_units
                                - current_net_value
                                - market_value
                            description: Fund List
                          bond_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                holding_units:
                                  type: string
                                  description: Holding Units
                                current_net_value:
                                  type: string
                                  description: Net Value
                                market_value:
                                  type: string
                                  description: Market Value
                              required:
                                - symbol
                                - currency
                                - holding_units
                                - current_net_value
                                - market_value
                            description: Bond List
                          otc_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                holding_units:
                                  type: string
                                  description: Holding Units
                                current_net_value:
                                  type: string
                                  description: Net Value
                                market_value:
                                  type: string
                                  description: Market Value
                              required:
                                - symbol
                                - currency
                                - holding_units
                                - current_net_value
                                - market_value
                            description: Otc List
                          va_details:
                            type: array
                            items:
                              type: object
                              properties:
                                symbol:
                                  type: string
                                  description: Symbol
                                currency:
                                  type: string
                                  description: Currency
                                last_done:
                                  type: string
                                  description: Last Done
                                quantity:
                                  type: string
                                  description: Quantity
                                market_value:
                                  type: string
                                  description: Market Value
                              required:
                                - symbol
                                - currency
                                - last_done
                                - quantity
                                - market_value
                            description: Crypto List
                          mmf_details:
                            type: array
                            items:
                              type: object
                              properties:
                                currency:
                                  type: string
                                  description: Currency
                                net_value:
                                  type: string
                                  description: Net Value
                                holding_units:
                                  type: string
                                  description: Holding Units
                                market_value:
                                  type: string
                                  description: Market Value
                              required:
                                - currency
                                - net_value
                                - holding_units
                                - market_value
                            description: MMF List
                        required:
                          - cash_details
                          - stock_details
                          - option_details
                          - fund_details
                          - bond_details
                          - otc_details
                          - va_details
                          - mmf_details
                        description: Asset Detail
                    required:
                      - over_view
                      - details
              example:
                code: 0
                message: success
                data:
                  over_view:
                    total_asset: string
                    total_cash: string
                    mmf_amount: string
                    stock_mv: string
                    fund_mv: string
                    bond_mv: string
                    otc_mv: string
                    crypto_mv: string
                    currency: string
                  details:
                    cash_details:
                      - currency: string
                        settled_balance: string
                        unsettled_balance: string
                        outstanding: string
                        frozen_amount: string
                        available_amount: string
                        withdraw_amount: string
                    stock_details:
                      - symbol: string
                        currency: string
                        last_done: string
                        quantity: string
                        market_value: string
                        settled_quantity: string
                        unsettled_settled_quantity: string
                        symbol_name: string
                    option_details:
                      - symbol: string
                        currency: string
                        last_done: string
                        quantity: string
                        market_value: {}
                        settled_quantity: string
                        unsettled_settled_quantity: string
                        symbol_name: string
                        underlying_symbol: string
                        underlying_symbol_name: string
                    fund_details:
                      - symbol: string
                        currency: string
                        holding_units: string
                        current_net_value: string
                        market_value: string
                    bond_details:
                      - symbol: string
                        currency: string
                        holding_units: string
                        current_net_value: string
                        market_value: string
                    otc_details:
                      - symbol: string
                        currency: string
                        holding_units: string
                        current_net_value: string
                        market_value: string
                    va_details:
                      - symbol: string
                        currency: string
                        last_done: string
                        quantity: string
                        market_value: string
                    mmf_details:
                      - currency: string
                        net_value: string
                        holding_units: string
                        market_value: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the complete asset overview for a specified securities account. By providing the account number account_no and currency currency, it returns the asset summary over_view (including total assets, total cash, money market fund balance, and market values of various asset types) as well as detailed holdings details for each asset category (covering cash, stocks, options, funds, bonds, OTC, virtual assets, and money market funds).
      x-mint:
        href: /en/api-reference/query_asset_detail_by_account_no
        metadata:
          sidebarTitle: Query Account Total Asset OverView
  /v1/whaleapi/asset/hashkey/cash_list:
    get:
      operationId: query_asset_list_by_hashkey
      summary: Query HashKey Cash List
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      asset_list:
                        type: array
                        items:
                          type: object
                          properties:
                            currency:
                              type: string
                              description: Currency
                            total_asset:
                              type: string
                              description: Cash Amount
                            free_asset:
                              type: string
                              description: Available Amount
                            locked_asset:
                              type: string
                              description: FrozenAmount
                          required:
                            - currency
                            - total_asset
                            - free_asset
                            - locked_asset
                        description: Currency List
                    required:
                      - asset_list
              example:
                code: 0
                message: success
                data:
                  asset_list:
                    - currency: BTC
                      total_asset: "100"
                      free_asset: "100"
                      locked_asset: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the cash asset list on the HashKey platform. It returns the total assets total_asset, available assets free_asset, and locked amount locked_asset for each virtual currency, presented by currency dimension.
      x-mint:
        href: /en/api-reference/query_asset_list_by_hashkey
        metadata:
          sidebarTitle: Query HashKey Cash List
  /v1/whaleapi/asset/query_total_cash:
    get:
      operationId: query_total_cash
      summary: Query Total Cash
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      total_cash:
                        type: object
                        properties:
                          currency:
                            type: object
                            properties:
                              settled_cash:
                                type: string
                                description: settled cash
                              available_cash:
                                type: string
                                description: available cash
                            required:
                              - settled_cash
                              - available_cash
                            description: currency
                        required:
                          - currency
                        description: total cash
                    required:
                      - total_cash
              example:
                code: 0
                message: success
                data:
                  total_cash:
                    currency:
                      settled_cash: string
                      available_cash: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the complete cash data across all currencies for the account. Without requiring an account number or currency parameter, it directly returns the settled cash settled_cash and available cash available_cash for each currency, organized by currency as the key. Compared to the account cash query interface, this interface returns full data without currency filtering support.
      x-mint:
        href: /en/api-reference/query_total_cash
        metadata:
          sidebarTitle: Query Total Cash
  /v1/whaleapi/asset/query_total_holdings:
    get:
      operationId: query_total_holdings
      summary: Query Total Holdings
      tags:
        - Account Assets
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      long_stock:
                        type: object
                        properties:
                          symbol:
                            type: object
                            properties:
                              settled_quantity:
                                type: string
                                description: settled quantity
                              available_quantity:
                                type: string
                                description: available quantity
                            required:
                              - settled_quantity
                              - available_quantity
                            description: security underlying
                        required:
                          - symbol
                        description: long stock
                      short_stock:
                        type: object
                        properties:
                          symbol:
                            type: object
                            properties:
                              settled_quantity:
                                type: string
                                description: settled quantity
                              available_quantity:
                                type: string
                                description: available quantity
                            required:
                              - settled_quantity
                              - available_quantity
                            description: security underlying
                        required:
                          - symbol
                        description: short stock
                    required:
                      - long_stock
                      - short_stock
              example:
                code: 0
                message: success
                data:
                  long_stock:
                    symbol:
                      settled_quantity: string
                      available_quantity: string
                  short_stock:
                    symbol:
                      settled_quantity: string
                      available_quantity: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to retrieve the complete stock position data for the account. Without requiring additional parameters, it directly returns all long positions long_stock and short positions short_stock, with each symbol containing settled quantity settled_quantity and available quantity available_quantity, organized by security symbol as the key.
      x-mint:
        href: /en/api-reference/query_total_holdings
        metadata:
          sidebarTitle: Query Total Holdings
  /v1/whaleapi/asset/risk/buy_power:
    get:
      operationId: esop_get_account_buy_power
      summary: Query Buy Power
      tags:
        - Risk Control Compliance
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account Information
        - name: currency
          in: query
          required: false
          schema:
            type: string
          description: Currency
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: integer
                    description: |-
                      `0` indicates success. A non-zero value is a business error code, in which case
                      `data` is usually empty.

                      HTTP status codes use their standard semantics. A successful request normally
                      returns `200`; errors may return `400`, `401`, `403`, `408`, or another applicable
                      status. Interpret the HTTP status together with the business `code`.

                      - `401`: The user is not authenticated.
                      - `403`: The current account is not authorized to use this function. Grant access
                        through User Permission.. Options: `0` = Success
                  message:
                    type: string
                  data:
                    type: object
                    properties:
                      buy_power:
                        type: string
                        description: Buy Power
                      currency:
                        type: string
                        description: currency
                    required:
                      - buy_power
                      - currency
              example:
                code: 0
                message: success
                data:
                  buy_power: string
                  currency: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the current buying power of a specified securities account. By providing the account number account_no and an optional currency currency, it returns the maximum available buying power for the account in the specified currency. The buying power takes into account both the account's cash balance and financing limit.
      x-mint:
        href: /en/api-reference/esop_get_account_buy_power
        metadata:
          sidebarTitle: Query Buy Power
  /v1/whaleapi/asset/query_account_risk_limit_config:
    get:
      operationId: get_account_risk_limit_config
      summary: Query account risk limit config
      tags:
        - Risk Control Compliance
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      parameters:
        - name: account_no
          in: query
          required: true
          schema:
            type: string
          description: Account NO Eg:L6VQEU00121996
        - name: limit_code
          in: query
          required: false
          schema:
            type: string
          description: When omitted, returns all of the client's limits; when provided, returns only the specified limit.
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      limits:
                        type: object
                        properties:
                          CSHB:
                            type: object
                            properties:
                              name:
                                type: string
                                description: Limited Name
                              limit_type:
                                type: string
                                description: Limited Type
                              reject_operator:
                                type: string
                                description: Rejected Operator
                              reject_limit:
                                type: string
                                description: Rejected Limit
                              currency:
                                type: string
                                description: Currency
                            required:
                              - name
                              - limit_type
                              - reject_operator
                              - reject_limit
                              - currency
                            description: Limited Code
                        required:
                          - CSHB
                        description: limits
                    required:
                      - limits
                    description: data
              example:
                data:
                  limits:
                    CSHB:
                      name: 客户单笔卖出下单金额限额
                      limit_type: "2"
                      reject_operator: GreaterThanOrEqual
                      reject_limit: "50000000"
                      currency: USD
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to query the risk limit configuration details for a specified securities account. By providing the account number account_no and an optional limit code limit_code for precise querying, it returns the limit configurations for the account, including the limit name, type (quantity or amount), rejection operator, and rejection threshold.
      x-mint:
        href: /en/api-reference/get_account_risk_limit_config
        metadata:
          sidebarTitle: Query account risk limit config
  /v1/whaleapi/asset/create_account_risk_limit_config:
    post:
      operationId: whale_create_risk_limit_client_config
      summary: Create account risk limit config
      tags:
        - Risk Control Compliance
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to create a risk limit configuration for a specified securities account. By providing the account number account_no, limit code limit_code, currency currency, and rejection threshold reject_limit, it creates the corresponding risk limit rule for the account.
      x-mint:
        href: /en/api-reference/whale_create_risk_limit_client_config
        metadata:
          sidebarTitle: Create account risk limit config
  /v1/whaleapi/asset/update_account_risk_limit_config:
    post:
      operationId: whale_update_risk_limit_client_config
      summary: Update account risk limit config
      tags:
        - Risk Control Compliance
      x-permission-key:
        - admin.general_whale_api
      x-lbonly: false
      x-source: WhaleAPI
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties: {}
              example: string
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: Bad request. Check the business error code in `code` and the error details in `message`.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The user is not authenticated, or the login session has expired.
        "403":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The current account is not authorized to use this feature. Grant the required permission to the account in User Permission.
        "408":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BrokerGatewayErrorEnvelope"
          description: The request timed out. Check the business error code in `code` and the error details in `message`.
      description: This interface is used to update an existing risk limit configuration for a specified securities account. By providing the account number account_no, limit code limit_code, currency currency, and the new rejection threshold reject_limit, it modifies the existing risk limit rule for the account.
      x-mint:
        href: /en/api-reference/whale_update_risk_limit_client_config
        metadata:
          sidebarTitle: Update account risk limit config
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: "ACCESS_TOKEN issued to the broker, sent as `Authorization: Bearer <token>`."
  schemas:
    BrokerGatewayErrorEnvelope:
      type: object
      required:
        - code
        - message
        - data
      properties:
        code:
          type: integer
        message:
          type: string
        data: {}
      description: Error response returned by Broker API Gateway. `code` is the business error code, `message` contains the error details, and `data` is usually empty.
security:
  - bearerAuth: []
