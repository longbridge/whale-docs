---
import { Icon } from "astro-icon/components";
import LucideIcon from "../icon/LucideIcon.astro";

interface Props {
	editUrl?: string;
	/** URL for the "Report an issue" link. When omitted, the link is hidden. */
	reportIssueUrl?: string;
}

const { editUrl, reportIssueUrl } = Astro.props;

const linkClass =
	"group inline-flex items-center gap-2 text-[13px] text-muted-foreground no-underline transition-colors hover:text-foreground";
---

<nav aria-label="Page actions" class="flex flex-col gap-3">
	{
		editUrl && (
			<a href={editUrl} target="_blank" rel="noopener" class={linkClass}>
				<LucideIcon name="pencil" class="h-3.5 w-3.5" aria-hidden="true" />
				<span>Edit page</span>
			</a>
		)
	}
	{
		reportIssueUrl && (
			<a href={reportIssueUrl} target="_blank" rel="noopener" class={linkClass}>
				<Icon name="ph:github-logo" class="h-3.5 w-3.5" aria-hidden="true" />
				<span>Report an issue</span>
			</a>
		)
	}
</nav>
