---
title: Theme demo
description: Every MDX component this theme exports, on one page.
---

## Prose

Regular paragraph text with **bold**, *italic*, `inline code`, and an
[external link](https://example.com) that should pick up an arrow.

## Callouts

<Note>A note, for context the reader can skip.</Note>
<Tip>A tip, for something that makes life easier.</Tip>
<Warning>A warning, for something that bites.</Warning>

## Cards

<CardGroup>
  <Card title="Getting started" icon="rocket" href="/">Install and configure.</Card>
  <Card title="Reference" icon="code" href="/">Every prop, every token.</Card>
</CardGroup>

## Accordions

<AccordionGroup>
  <Accordion title="First section" open={true}>Open by default.</Accordion>
  <Accordion title="Second section">Collapsed by default.</Accordion>
</AccordionGroup>

## Steps

<Steps>
  <Step>Install the theme.</Step>
  <Step>Add the CSS entry with your `@source` roots.</Step>
  <Step>Render a page.</Step>
</Steps>

## Code

```ts
export function greet(name: string): string {
  return `Hello, ${name}`;
}
```

## Tables

| Token | Purpose |
| --- | --- |
| `--nb-primary` | Brand colour |
| `--nb-background` | Page surface |
